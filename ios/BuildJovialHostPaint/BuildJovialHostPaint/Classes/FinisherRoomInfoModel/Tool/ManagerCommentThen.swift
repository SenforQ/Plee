
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let show_resumePath:String = "SOqualityKE"
fileprivate let appEvaluatePath:String = "something address aspect fatherR_KEY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let kStatementTitle:[UInt8] = [0x86,0x9a,0x96,0x9e,0x90,0x81,0x8a,0x83,0x9c,0x91,0x90,0x9a,0x96,0x9d,0x94,0x81,0x8a,0x87,0x90,0x84,0x80,0x90,0x86,0x81,0x96,0x94,0x99,0x99,0x8a,0x9b,0x9a,0x81,0x9c,0x93,0x9c,0x96,0x94,0x81,0x9c,0x9a,0x9b]

private func holdCoat(shadow num: UInt8) -> UInt8 {
    return num ^ 213
}

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let user_harassmentUrl:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x4e,0x45,0x45,0x52,0x43,0x53,0x54,0x41,0x4f,0x4c,0x46,0x5f,0x45,0x56,0x49,0x43,0x45,0x52]

/*: "isCalling" :*/
fileprivate let dataDefinitionMsg:[Character] = ["i","s","C","a","l","l","i","n","g"]
 
/*: "SOCKET 连接成功." :*/
fileprivate let kSubjectStr:String = "SOCwelcome"
fileprivate let noti_translationValue:String = "\u{529f}."

/*: "SOCKET 连接失败, error: :*/
fileprivate let notiBuryWorkText:[Character] = ["S","O","C","K","E","T"," ","连","\u{63a5}","失","败",","," ","e"]
fileprivate let noti_smallUrl:[Character] = ["r","r","o","r",":"]

/*: "SOCKET 失败 :*/
fileprivate let showLowSubstancePath:[Character] = ["S","O","C","K","E","T"," ","失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let userOccurId:[Character] = ["次",","," ","切","换","过","S","o","c","k","e","t","域","名","重","\u{65b0}","尝","试"]
fileprivate let appSizeWithKey:String = "，\u{5730}址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let userSecureStr:String = "SOgivenK"
fileprivate let app_missReaderValue:String = "闭, cdiscount document produce"
fileprivate let const_yieldText:[Character] = ["o","d","e",":"]

/*: , reason: :*/
fileprivate let app_shotMessage:[Character] = [","," ","r","e","a"]
fileprivate let show_chancePath:[Character] = ["s","o","n",":"]

/*: /?sid= :*/
fileprivate let dataAwayName:String = "there whole/?sid="

/*: &uid= :*/
fileprivate let dataMaterialFormat:String = "&uid=acquisition foundation"

/*: &platform=iphone&version= :*/
fileprivate let noti_medalNorthData:[Character] = ["&","p","l","a","t","f","o","r","m","=","i"]
fileprivate let dataProcessingValue:[Character] = ["p"]
fileprivate let user_originalText:String = "superior chemicalhone&v"

/*: &packageId= :*/
fileprivate let kRequireMessage:String = "turn mix gesture lots&pack"

/*: "1v1server. :*/
fileprivate let showWeekFormat:[Character] = ["1","v","1","s","e"]
fileprivate let main_theoryMsg:String = "rver.conversation margin"

/*: .com:7766" :*/
fileprivate let userDistantMsg:String = ".com:file attention tune judgment stay"
fileprivate let userIncludeUrl:String = "selectedselected66"

/*: "43.129.185.194:7767" :*/
fileprivate let appSomeoneWireMessage:[Character] = ["4","3",".","1","2","9",".","1","8","5",".","1","9","4",":","7","7","6","7"]

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let appOfKey:[UInt8] = [0x42,0x3e,0x32,0x3a,0x34,0x43,0xf,0xd6,0xac,0x80,0xd6,0xaa,0x8b,0x61,0x54,0x50,0x52,0x57,0x50,0x51,0x58,0x5b,0x58,0x63,0x68,0x32,0x57,0x50,0x5d,0x56,0x54,0x53,0xd6,0x8a,0x80,0xd4,0x7f,0x9b,0xde,0xab,0x7b,0xd5,0x85,0x9c,0xd6,0xac,0x80,0xd4,0x74,0xa2,0xd8,0x86,0x9c,0x1d]

fileprivate func mysteryAll(his num: UInt8) -> UInt8 {
    let value = Int(num) - 239
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cmd" :*/
fileprivate let dataMatterAlwaysUrl:[Character] = ["c","m","d"]

/*: "ping" :*/
fileprivate let mainNatureBridgeStr:String = "pireason"

/*: "callHeartbeat" :*/
fileprivate let dataSaleUrl:[Character] = ["c","a","l","l","H","e"]
fileprivate let noti_itemData:String = "generate"
fileprivate let k_digUrl:String = "tapetbeat"

/*: "recievedDate" :*/
fileprivate let mainQualityId:String = "recaree"
fileprivate let noti_borrowName:String = "acquire format examplevedDate"

/*: "liveHeartBeat" :*/
fileprivate let kFirstCoatUrl:String = "lspeakve"
fileprivate let appAliveKey:String = "description statisticalBeat"

/*: "🔗 关闭 socket" :*/
fileprivate let showReplacementFormat:String = "\u{0001f517} 关闭 s"
fileprivate let appQueenOvalStr:[Character] = ["o","c","k","e","t"]

/*: "The user logs out" :*/
fileprivate let k_electronicBoyTalkName:String = "The upublicly environment"
fileprivate let dataAddedNumbBreakId:String = "sufficient disabled type shadow interactions out"

/*: "SOCKET  :*/
fileprivate let mainTurnMessage:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let noti_usIndependenceStr:String = "\u{91cd}连. 重试次数"
fileprivate let kTowardMsg:String = "golf"

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let data_mAlbumMsg:[Character] = ["S","O","C","K","E","T"," ","连","\u{63a5}","正","常"," ","\u{4e0d}","\u{9700}","要","重"]
fileprivate let k_minMessage:String = "新连shake."

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let notiTransferPath:[Character] = ["S"]
fileprivate let main_hideName:[Character] = ["O","C","K","E","T"," ","\u{6b63}","在","连","接"," ","不","\u{9700}","要","重","新","连","\u{63a5}","."]

/*: "&resume=1" :*/
fileprivate let appJawEmptyUrl:[Character] = ["&","r","e"]
fileprivate let notiBrakePatentTitle:[Character] = ["s","u","m","e","=","1"]

/*: "&lang= :*/
fileprivate let kFemaleAssignReportData:String = "&lang=mm signal fatal ex class"

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let show_independenceMessage:[Character] = ["S","O","C","K","E","T"," ","\u{5f00}","始","连"]
fileprivate let noti_anotherDecreaseText:String = "limited, 地址："

/*: "onNetDisabled" :*/
fileprivate let kDynamicsBenchWhiteMessage:String = "super column space before chooseonNetDis"
fileprivate let k_immuneTitle:[Character] = ["a","b","l","e","d"]

/*: "errno" :*/
fileprivate let noti_gumValue:String = "errtexto"

/*: "data" :*/
fileprivate let const_ovalOurTransactionTitle:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let kBlueStr:String = "sock"
fileprivate let showGroupKey:String = "保\u{6d3b}cm"
fileprivate let user_replyStr:[Character] = ["d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let data_branchMsg:[UInt8] = [0x2e,0x74,0x61,0x65,0x62,0x74,0x72,0x61,0x65,0x48,0x6c,0x6c,0x61,0x63,0x20,0x3a,0x64,0x6d,0x63,0xbb,0xb4,0xe6,0x9d,0xbf,0xe4,0x63,0x74,0x72,0x74,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let constDragSoulUrl:[UInt8] = [0x12,0xe,0x2,0xa,0x4,0x13,0xdf,0x2b,0x28,0x35,0x24,0xa3,0x7e,0x5c,0xa5,0x73,0x7a,0x22,0x2c,0x23,0xf9,0xdf,0x2b,0x28,0x35,0x24,0x7,0x24,0x20,0x31,0x33,0x1,0x24,0x20,0x33,0xed]

fileprivate func versionStroke(presentation num: UInt8) -> UInt8 {
    let value = Int(num) - 191
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onP" :*/
fileprivate let notiTelevisionMessage:String = "onPtumble intro time law exception"

/*: "msg" :*/
fileprivate let k_commonUrl:String = "msvariation"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let userReinStr:[Character] = ["\u{0001f517}"," ","处","理","\u{6d88}","息","：","c","m","d","N","a"]
fileprivate let user_lipName:String = "carve better recover soulme:"

/*: , errNo: :*/
fileprivate let mainKingName:String = ", erpresent global"

/*: , msg: :*/
fileprivate let kEffectLaunchFormat:[Character] = [","," ","m","s","g",":"]

/*: , data: :*/
fileprivate let constChemicalMessage:String = "satisfy"
fileprivate let notiHarassmentMessage:String = "cut maximum ex data:"

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let mainListenMsg:String = "SOdecisionKE"
fileprivate let show_givenInstallPath:String = "异\u{5e38}\u{6d88}息：cm"
fileprivate let user_tenderName:[Character] = ["d","N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let constBlackEditionMsg:[Character] = ["o","n","R","e","c"]
fileprivate let const_argumentKey:String = "eivethumb"

/*: "onVideoMatch" :*/
fileprivate let kSelectionData:[Character] = ["o","n","V","i"]
fileprivate let appMinuteLimitMixData:String = "deoMkit input refuse filter extension"

/*: "onRequestCall" :*/
fileprivate let const_contentUrl:String = "onReqmini so edge component"
fileprivate let dataActItsPath:String = "Callresent weak nature fundamental wrap"

/*: "onCancelCall" :*/
fileprivate let notiReserveMsg:[Character] = ["o","n","C","a","n","c","e","l","C","a"]
fileprivate let app_transferDestroyWaveKey:[Character] = ["l","l"]

/*: "onRefuseCall" :*/
fileprivate let dataWarnFormat:String = "reading luck should word eventonRe"
fileprivate let show_whiteMineUrl:String = "camera"

/*: "onAcceptCall" :*/
fileprivate let constDarkData:String = "onAcccurrency info"
fileprivate let user_reduceFatherNineTitle:String = "eptCalllack far let remove"

/*: "onStopCall" :*/
fileprivate let appDevoteData:String = "again voteonStop"
fileprivate let kRocketStr:String = "big operationCall"

/*: "freeCallTimes" :*/
fileprivate let notiAlongsideId:[Character] = ["f","r","e","e","C","a","l","l","T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let const_warningText:String = "honey"
fileprivate let show_channelSumValue:String = "team bean our before positivefCoin"

/*: "onMissCoin" :*/
fileprivate let kEffName:[Character] = ["o","n","M"]
fileprivate let user_pitchFormat:[Character] = ["i","s","s","C","o","i","n"]

/*: "acceptCall" :*/
fileprivate let showKingMessage:[Character] = ["a","c","c","e","p","t","C"]
fileprivate let userSeemMessage:String = "successfull"

/*: "stopCall" :*/
fileprivate let noti_capacityTitle:String = "ststatep"

/*: "requestCall" :*/
fileprivate let show_coordinateFloorMessage:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "[Invite you to a video call...]" :*/
fileprivate let main_grossData:[UInt8] = [0x3,0x11,0x36,0x2e,0x31,0x2c,0x3d,0x78,0x21,0x37,0x2d,0x78,0x2c,0x37,0x78,0x39,0x78,0x2e,0x31,0x3c,0x3d,0x37,0x78,0x3b,0x39,0x34,0x34,0x76,0x76,0x76,0x5]

private func betweenPrevious(cover num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "type" :*/
fileprivate let const_universalKey:String = "tyfollowinge"

/*: "[Invite you to a voice call...]" :*/
fileprivate let appFormatValue:[UInt8] = [0x5d,0x2e,0x2e,0x2e,0x6c,0x6c,0x61,0x63,0x20,0x65,0x63,0x69,0x6f,0x76,0x20,0x61,0x20,0x6f,0x74,0x20,0x75,0x6f,0x79,0x20,0x65,0x74,0x69,0x76,0x6e,0x49,0x5b]

/*: "uid" :*/
fileprivate let app_eyeRecoverTurnPath:String = "uiprevious"

/*: "nickname" :*/
fileprivate let app_halfCenterFormat:String = "NI"
fileprivate let const_wayContent:[Character] = ["c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let show_receiverFormat:[Character] = ["h","e","a","d","P"]
fileprivate let dataQuickUrl:String = "seem"

/*: "onCallResume" :*/
fileprivate let constAlreadyTitle:String = "onCaltogether nervous security produce"
fileprivate let k_differenceId:String = "umhair"

/*: "cancelCall" :*/
fileprivate let show_elementStr:String = "CANCE"
fileprivate let user_animalMessage:String = "lCalldown accomplish blind info"

/*: "refuseCall" :*/
fileprivate let data_boundaryEachKey:String = "refuseCstick alive issue more mobile"
fileprivate let user_houseTitle:[Character] = ["a","l","l"]

/*: "onCallMotivation" :*/
fileprivate let k_shallData:String = "onCallater problem"
fileprivate let const_tingId:String = "ivatitelln"

/*: "onLiveWarning" :*/
fileprivate let userCosStr:String = "receive forth ovalonLiv"
fileprivate let noti_sharedUrl:String = "center"

/*: "reason" :*/
fileprivate let const_recordingData:String = "reasyearn"

/*: "time" :*/
fileprivate let showWarnStr:String = "timmatch"

/*: "onStopLive" :*/
fileprivate let notiReasonData:[Character] = ["o","n","S","t","o","p"]
fileprivate let mainJudgeKey:String = "plus active our togetherLive"

/*: "onSendProgramGift" :*/
fileprivate let constBlueName:String = "safe queen clock clickonSend"
fileprivate let noti_capacityStr:String = "pass foundation plant themamGi"
fileprivate let user_errTitle:String = "relatedt"

/*: "onGameFs" :*/
fileprivate let mainReceiveTitle:String = "onGavertical take voice tool pop"

/*: "broadStatus" :*/
fileprivate let mainCellName:[Character] = ["b","r","o","a","d","S","t","a"]
fileprivate let userTimingMsg:String = "ttrades"

/*: "onCallChatMsg" :*/
fileprivate let k_calculatePath:[Character] = ["o","n","C","a"]
fileprivate let show_minName:String = "as shot route twin presentationllCha"

/*: "onNotificationPopupWin" :*/
fileprivate let notiShareContent:[Character] = ["o","n","N","o","t"]
fileprivate let data_fundamentalId:[Character] = ["i","f","i","c","a","t","i","o","n"]
fileprivate let data_oneId:String = "PopupWinbefore fit find copy"

/*: "onUserBlock" :*/
fileprivate let constSuiteData:String = "onUscell fragment explain"

/*: "isRecharge" :*/
fileprivate let user_runningTitle:String = "isRestart ask resent opening option"
fileprivate let showSuccessfullyData:String = "downe"

/*: "source" :*/
fileprivate let userRouteYieldSeriesMsg:[Character] = ["s","o","u","r","c","e"]

/*: "invitePop" :*/
fileprivate let k_badTitle:[Character] = ["i","n","v","i","t","e"]
fileprivate let showOvalValue:String = "Popbasic context computer"

/*: "videoPop" :*/
fileprivate let data_boyTitle:String = "videoPopair array our shadow"
fileprivate let main_alongsideId:String = "still"

/*: "coin" :*/
fileprivate let constActFormat:[Character] = ["c","o","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ManagerCommentThen.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let show_selectTitle = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let mainGroupMsg = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let constClosedUrl = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let main_scaleCaptureMessage = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let noti_profileAddressStr = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let dataFaceBeautyStr = (show_resumePath.replacingOccurrences(of: "quality", with: "C") + "T_ADD" + String(appEvaluatePath.suffix(5)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let show_backgroundFeatureValue = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let data_postName = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let userConversationUrl = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let kServerId = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let showBlockText = NSNotification.Name(rawValue: String(bytes: kStatementTitle.map{holdCoat(shadow: $0)}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let app_rankUrl = NSNotification.Name(rawValue: String(bytes: user_harassmentUrl.reversed(), encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol BetiseObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func beforeRelationHolder(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol CurrencyMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func doingerFill(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func linkData(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol ExistingObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func beautyEndLibrary(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func doHighlight(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func labelToday(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func drownData(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol AnyoneMaterialPartyDelegate: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func bitCollection(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func acceptObserve(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func nativeData(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class ManagerCommentThen: NSObject {
    //: enum SocketRetryType: String {
    enum SocketPromise: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currFisherVC: AnyObject?
    var currFisherVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: RepresentLanguageManager.shared.direction == .rightToLeft ? 0 : dataModeBlockStr,
        //: y: StatusBarHeight+190
        y: constSoundTabStr + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: RepresentLanguageManager.shared.direction == .rightToLeft ? 0 : dataModeBlockStr,
        //: y: StatusBarHeight+190
        y: constSoundTabStr + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: BetiseObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: CurrencyMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: ExistingObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: AnyoneMaterialPartyDelegate? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    
    @objc dynamic var isFisher = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = ManagerCommentThen()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(shrinkQuantityObserve(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(labBe),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(dataDefinitionMsg)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (BrowKingfisherCompatible.shared as! BrowKingfisherCompatible).appearanceTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isFisher")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (BrowKingfisherCompatible.shared as! BrowKingfisherCompatible).appearanceTimer()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension ManagerCommentThen: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        GraphicCanvasThen.mnemonics(msg: (kSubjectStr.replacingOccurrences(of: "welcome", with: "KE") + "T 连接成" + noti_translationValue))
        //: sendPing()
        factorJust()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: app_networkKey,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: dataFaceBeautyStr)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        drownDeadline()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        GraphicCanvasThen.mnemonics(msg: (String(notiBuryWorkText) + String(noti_smallUrl)) + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = RealmReactiveCompatible.share.reachability?.connection, status != .unavailable else {
        guard let status = RealmReactiveCompatible.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == main_scaleCaptureMessage {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = getFocusRetry(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            GraphicCanvasThen.mnemonics(msg: (String(showLowSubstancePath)) + "\(main_scaleCaptureMessage)" + (String(userOccurId) + appSizeWithKey) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.reconnectToVariant(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.answerMessage(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        GraphicCanvasThen.mnemonics(msg: (userSecureStr.replacingOccurrences(of: "given", with: "C") + "ET 已关" + String(app_missReaderValue.prefix(4)) + String(const_yieldText)) + "\(code)" + (String(app_shotMessage) + String(show_chancePath)) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.dayOfRemembrance(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == kServerId {
            //: return
            return
        }
        //: if let status = RealmReactiveCompatible.share.reachability?.connection, status != .unavailable {
        if let status = RealmReactiveCompatible.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.reconnectToVariant(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func getFocusRetry(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = RealmReactiveCompatible.share.appConfigMode.ws
        self.linkAddrArr = RealmReactiveCompatible.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = account()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: dataFaceBeautyStr)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = RealmReactiveCompatible.share.loginUid
        let uid = RealmReactiveCompatible.share.loginUid
        //: let sid = RealmReactiveCompatible.share.loginSessionId
        let sid = RealmReactiveCompatible.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(dataAwayName.suffix(6))) + "\(sid)" + (String(dataMaterialFormat.prefix(5))) + "\(uid)" + (String(noti_medalNorthData) + String(dataProcessingValue) + String(user_originalText.suffix(6)) + "ersion=") + "\(data_malePath)" + (String(kRequireMessage.suffix(5)) + "ageId=") + "\(user_workName)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func account() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return showSceneColorText ? [(String(showWeekFormat) + String(main_theoryMsg.prefix(5))) + "\(mainRequestMsg)" + (String(userDistantMsg.prefix(5)) + userIncludeUrl.replacingOccurrences(of: "selected", with: "7"))] : [(String(appSomeoneWireMessage))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension ManagerCommentThen {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func shrinkQuantityObserve(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            GraphicCanvasThen.mnemonics(msg: String(bytes: appOfKey.map{mysteryAll(his: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.dayOfRemembrance(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.reconnectToVariant(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func labBe() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.reconnectToVariant(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.quantitativeRelation()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension ManagerCommentThen {
    /// socket保活心跳包
    //: private func sendPing() {
    private func factorJust() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: show_selectTitle, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.telephoneReceiverPhoneMessage(info: [(String(dataMatterAlwaysUrl)): (mainNatureBridgeStr.replacingOccurrences(of: "reason", with: "ng"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func attitude() {
        //: endTalkingPing()
        ample()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: mainGroupMsg, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isFisher || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currFisherVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currFisherVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isFisher || (self.currentPositionController()?.isKind(of: CrosswaysViewController.self) == false && self.currentPositionController()?.isKind(of: ErrorPartyDelegate.self) == false) && (self.currFisherVC?.isKind(of: CrosswaysViewController.self) == false && self.currFisherVC?.isKind(of: ErrorPartyDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.telephoneReceiverPhoneMessage(info: [(String(dataMatterAlwaysUrl)): (String(dataSaleUrl) + noti_itemData.replacingOccurrences(of: "generate", with: "a") + k_digUrl.replacingOccurrences(of: "tape", with: "r"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func ample() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func quantitativeRelation() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(mainQualityId.replacingOccurrences(of: "care", with: "ci") + String(noti_borrowName.suffix(7)))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < notiDisplayValue {
                //: RealmReactiveCompatible.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                RealmReactiveCompatible.share.reportRecieveMessage(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension ManagerCommentThen {
    /// 开启直播心跳包
    //: func startLivePing() {
    func feedback() {
        //: endLivePing()
        behindMiniPing()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: constClosedUrl, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.telephoneReceiverPhoneMessage(info: [(String(dataMatterAlwaysUrl)): (kFirstCoatUrl.replacingOccurrences(of: "speak", with: "i") + "Heart" + String(appAliveKey.suffix(4)))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func behindMiniPing() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension ManagerCommentThen {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func drownDeadline() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = FoolishnessThen()
        //: let keyWindow = CurrencyDefineReactiveCompatible.getWindow()
        let keyWindow = CurrencyDefineReactiveCompatible.stripElement()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isFisher
        self.containerView?.isHidden = !self.isFisher
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func containerBubble() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard SoundCombinedReactiveCompatible.buildShared().isLive == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension ManagerCommentThen {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func dayOfRemembrance(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (showReplacementFormat + String(appQueenOvalStr)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: kServerId, reason: (String(k_electronicBoyTalkName.prefix(5)) + "ser log" + String(dataAddedNumbBreakId.suffix(5))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        ample()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            containerBubble()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func counternational() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = ManagerCommentThen.shared.getFocusRetry(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.reconnectToVariant(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func reconnectToVariant(timeInterval: TimeInterval, type: SocketPromise) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            GraphicCanvasThen.mnemonics(msg: (String(mainTurnMessage)) + "\(type.rawValue)" + (noti_usIndependenceStr + kTowardMsg.replacingOccurrences(of: "golf", with: "：")) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.actualStr(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func actualStr(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                GraphicCanvasThen.mnemonics(msg: (String(data_mAlbumMsg) + k_minMessage.replacingOccurrences(of: "shake", with: "接")))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                GraphicCanvasThen.mnemonics(msg: (String(notiTransferPath) + String(main_hideName)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isFisher {
        if self.isFisher {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(appJawEmptyUrl) + String(notiBrakePatentTitle)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(kFemaleAssignReportData.prefix(6))) + "\(CoverAddrTool.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        dayOfRemembrance(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = noti_profileAddressStr
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        GraphicCanvasThen.mnemonics(msg: (String(show_independenceMessage) + noti_anotherDecreaseText.replacingOccurrences(of: "limited", with: "接")) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func telephoneReceiverPhoneMessage(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if RealmReactiveCompatible.share.reachability?.connection == nil || RealmReactiveCompatible.share.reachability?.connection == .unavailable {
        if RealmReactiveCompatible.share.reachability?.connection == nil || RealmReactiveCompatible.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(String(dataMatterAlwaysUrl)): (String(kDynamicsBenchWhiteMessage.suffix(8)) + String(k_immuneTitle)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (noti_gumValue.replacingOccurrences(of: "text", with: "n")): "\(userConversationUrl)",
                                       //: "data": [:]]
                                       (String(const_ovalOurTransactionTitle)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            answerMessage(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(dataMatterAlwaysUrl))] as? String == (mainNatureBridgeStr.replacingOccurrences(of: "reason", with: "ng")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                GraphicCanvasThen.mnemonics(msg: (kBlueStr.uppercased() + "ET \u{8fde}接" + showGroupKey + String(user_replyStr)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(String(dataMatterAlwaysUrl))] as? String == (String(dataSaleUrl) + noti_itemData.replacingOccurrences(of: "generate", with: "a") + k_digUrl.replacingOccurrences(of: "tape", with: "r")) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                GraphicCanvasThen.mnemonics(msg: String(bytes: data_branchMsg.reversed(), encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(String(dataMatterAlwaysUrl))] as? String == (kFirstCoatUrl.replacingOccurrences(of: "speak", with: "i") + "Heart" + String(appAliveKey.suffix(4))) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                GraphicCanvasThen.mnemonics(msg: String(bytes: constDragSoulUrl.map{versionStroke(presentation: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(String(dataMatterAlwaysUrl))] as? String == (mainNatureBridgeStr.replacingOccurrences(of: "reason", with: "ng")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(String(dataMatterAlwaysUrl)): (String(notiTelevisionMessage.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (noti_gumValue.replacingOccurrences(of: "text", with: "n")): "\(show_backgroundFeatureValue)",
                                           //: "data": [:]]
                                           (String(const_ovalOurTransactionTitle)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                answerMessage(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.reconnectToVariant(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func answerMessage(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(String(dataMatterAlwaysUrl))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(noti_gumValue.replacingOccurrences(of: "text", with: "n"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(k_commonUrl.replacingOccurrences(of: "variation", with: "g"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(const_ovalOurTransactionTitle))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(userReinStr) + String(user_lipName.suffix(3))) + "\(cmdName)" + (String(mainKingName.prefix(4)) + "rNo:") + "\(errorNo)" + (String(kEffectLaunchFormat)) + "\(msg)" + (constChemicalMessage.replacingOccurrences(of: "satisfy", with: ",") + String(notiHarassmentMessage.suffix(6))) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        nearSub(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            GraphicCanvasThen.mnemonics(msg: (mainListenMsg.replacingOccurrences(of: "decision", with: "C") + "T \u{63a5}收" + show_givenInstallPath + String(user_tenderName)) + "\(cmdName)" + (String(mainKingName.prefix(4)) + "rNo:") + "\(errorNo)" + (String(kEffectLaunchFormat)) + "\(msg)" + (constChemicalMessage.replacingOccurrences(of: "satisfy", with: ",") + String(notiHarassmentMessage.suffix(6))) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: RealmReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                RealmReactiveCompatible.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !RealmReactiveCompatible.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !RealmReactiveCompatible.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                EquallyThen.shared.asAlive()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.beforeRelationHolder(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(constBlackEditionMsg) + const_argumentKey.replacingOccurrences(of: "thumb", with: "d") + "Gift") { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: show_movieStr, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(kSelectionData) + String(appMinuteLimitMixData.prefix(4)) + "atch") { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.doingerFill(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(const_contentUrl.prefix(5)) + "uest" + String(dataActItsPath.prefix(4))) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.linkData(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(notiReserveMsg) + String(app_transferDestroyWaveKey)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(dataWarnFormat.suffix(4)) + "fuseCal" + show_whiteMineUrl.replacingOccurrences(of: "camera", with: "l")) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(constDarkData.prefix(5)) + String(user_reduceFatherNineTitle.prefix(7))) { // 接收方发送acceptCall回执
            //: self.isFisher = true
            self.isFisher = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            attitude()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.acceptObserve(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(appDevoteData.suffix(6)) + String(kRocketStr.suffix(4))) { // 接收方发送stopCall回执
            //: self.isFisher = false
            self.isFisher = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            ample()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(notiAlongsideId))] as? Int {
                //: RealmReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
                RealmReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: RealmReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            RealmReactiveCompatible.share.loginUserMode.mf_coin = json[(String(const_ovalOurTransactionTitle))][(const_warningText.replacingOccurrences(of: "honey", with: "m") + String(show_channelSumValue.suffix(5)))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            term(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: noti_routeFormat, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (BrowKingfisherCompatible.shared as! BrowKingfisherCompatible).mpOwn()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(kEffName) + String(user_pitchFormat)) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.drownData(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(showKingMessage) + userSeemMessage.replacingOccurrences(of: "successful", with: "al")) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isFisher = true
            self.isFisher = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            attitude()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.beautyEndLibrary(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (noti_capacityTitle.replacingOccurrences(of: "state", with: "o") + "Call") { // 接收方/拨打方 挂断了通话
            //: self.isFisher = false
            self.isFisher = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            ample()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.doHighlight(data: data)
            // 更新余额
            //: RealmReactiveCompatible.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            RealmReactiveCompatible.share.loginUserMode.mf_coin = json[(String(const_ovalOurTransactionTitle))][(const_warningText.replacingOccurrences(of: "honey", with: "m") + String(show_channelSumValue.suffix(5)))].stringValue
            
            if let freeCallTimes = data["freeCallTimes"] as? Int {
                RealmReactiveCompatible.share.loginUserMode.freeCallTimes = freeCallTimes
            }

            //: inviteCallPopViewEndShow(data: data)
            term(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: noti_routeFormat, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (BrowKingfisherCompatible.shared as! BrowKingfisherCompatible).mpOwn()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (String(show_coordinateFloorMessage)) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: showBlockText, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: RealmReactiveCompatible.share.recieve1v1TalkRequest(info: data)
                RealmReactiveCompatible.share.reportRecieveMessage(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(mainQualityId.replacingOccurrences(of: "care", with: "ci") + String(noti_borrowName.suffix(7)))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: main_grossData.map{betweenPrevious(cover: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(const_ovalOurTransactionTitle))][(const_universalKey.replacingOccurrences(of: "following", with: "p"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: appFormatValue.reversed(), encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                TranslateThen.attribute(uid: String(json[(String(const_ovalOurTransactionTitle))][(app_eyeRecoverTurnPath.replacingOccurrences(of: "previous", with: "d"))].intValue),
                                            //: title: data["nickname"] as? String,
                                            title: data[(app_halfCenterFormat.lowercased() + String(const_wayContent))] as? String,
                                            //: body: body,
                                            body: body,
                                            //: imageUrl: data["headPic"] as? String)
                                            imageUrl: data[(String(show_receiverFormat) + dataQuickUrl.replacingOccurrences(of: "seem", with: "ic"))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                TranslateThen.shared.militaryInstallation()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(constAlreadyTitle.prefix(5)) + "lRes" + k_differenceId.replacingOccurrences(of: "hair", with: "e")) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isFisher = true
            self.isFisher = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            attitude()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(constAlreadyTitle.prefix(5)) + "lRes" + k_differenceId.replacingOccurrences(of: "hair", with: "e"))] = "1"
            //: RealmReactiveCompatible.share.recieve1v1TalkResumeRequest(info: resumeData)
            RealmReactiveCompatible.share.act(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (BrowKingfisherCompatible.shared as! BrowKingfisherCompatible).appearanceTimer()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (show_elementStr.lowercased() + String(user_animalMessage.prefix(5))) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isFisher = false
            self.isFisher = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            ample()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.labelToday(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                TranslateThen.lustiness(identifier: String(json[(String(const_ovalOurTransactionTitle))][(app_eyeRecoverTurnPath.replacingOccurrences(of: "previous", with: "d"))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                TranslateThen.shared.version()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(data_boundaryEachKey.prefix(7)) + String(user_houseTitle)) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isFisher = false
            self.isFisher = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            ample()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.bitCollection(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(k_shallData.prefix(5)) + "lMot" + const_tingId.replacingOccurrences(of: "tell", with: "o")) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if RealmReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.nativeData?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(userCosStr.suffix(5)) + "eWarnin" + noti_sharedUrl.replacingOccurrences(of: "center", with: "g")) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            SoundCombinedReactiveCompatible.burnTime(content: msg, reason: json[(String(const_ovalOurTransactionTitle))][(const_recordingData.replacingOccurrences(of: "year", with: "o"))].stringValue, time: json[(String(const_ovalOurTransactionTitle))][(showWarnStr.replacingOccurrences(of: "match", with: "e"))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(notiReasonData) + String(mainJudgeKey.suffix(4))) { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SoundCombinedReactiveCompatible.buildShared().loadEvery()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(constBlueName.suffix(6)) + "Progr" + String(noti_capacityStr.suffix(4)) + user_errTitle.replacingOccurrences(of: "related", with: "f")) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.readingDownMsg(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(mainReceiveTitle.prefix(4)) + "meFs") { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(mainCellName) + userTimingMsg.replacingOccurrences(of: "trade", with: "u"))] as? Int ?? -1
            //: if RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: app_rankUrl, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: show_movieStr, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: app_rankUrl, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(k_calculatePath) + String(show_minName.suffix(5)) + "tMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            MultiPossibilityDanmuManager.the().focusAppear(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(notiShareContent) + String(data_fundamentalId) + String(data_oneId.prefix(8))) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            EquallyThen.shared.hearing(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(constSuiteData.prefix(4)) + "erBlock") { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            SoundCombinedReactiveCompatible.buildShared().loadEvery()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: user_beginMsg, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension ManagerCommentThen {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func nearSub(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (String(show_coordinateFloorMessage)) || cmdName == (String(const_contentUrl.prefix(5)) + "uest" + String(dataActItsPath.prefix(4))) {
            //: if TalkingLiveManager.shared().isLive == true {
            if SoundCombinedReactiveCompatible.buildShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                noti_beautyValue = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                SoundCombinedReactiveCompatible.stopCompletion(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                SoundCombinedReactiveCompatible.buildShared().loadEvery()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard noti_beautyValue == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (show_elementStr.lowercased() + String(user_animalMessage.prefix(5))) || cmdName == (String(notiReserveMsg) + String(app_transferDestroyWaveKey)) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(data_boundaryEachKey.prefix(7)) + String(user_houseTitle)) || cmdName == (String(dataWarnFormat.suffix(4)) + "fuseCal" + show_whiteMineUrl.replacingOccurrences(of: "camera", with: "l")) ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (noti_capacityTitle.replacingOccurrences(of: "state", with: "o") + "Call") || cmdName == (String(appDevoteData.suffix(6)) + String(kRocketStr.suffix(4)))
        {
            //: NEED_RESTORE_LIVE = false
            noti_beautyValue = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = ViewThen(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                SoundCombinedReactiveCompatible.buildShared().partnerCollectionChoice()
            }
            //: alert.show()
            alert.allShow()
        }
    }

    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func term(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        OperateRunningRatingManager.shared.apply()
        //: guard RealmReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else {
        guard RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(user_runningTitle.prefix(4)) + "char" + showSuccessfullyData.replacingOccurrences(of: "down", with: "g"))] as? Bool == false {
            //: BrowReactiveCompatible.share.func_pushToDiscountRechargeView()
            BrowReactiveCompatible.share.setView()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(String(userRouteYieldSeriesMsg))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (String(k_badTitle) + String(showOvalValue.prefix(3))) || source == (String(data_boyTitle.prefix(7)) + main_alongsideId.replacingOccurrences(of: "still", with: "p")) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(String(constActFormat))] as? Int) ?? 0 < 60 {
            //: BrowReactiveCompatible.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            BrowReactiveCompatible.share.progressEvent(webViewType: .RechargeHalfPage)
        }
    }
}
