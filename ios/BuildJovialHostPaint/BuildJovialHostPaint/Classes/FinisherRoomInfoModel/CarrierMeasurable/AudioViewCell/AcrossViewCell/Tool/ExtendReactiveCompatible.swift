
//: Declare String Begin

/*: "msgType" :*/
fileprivate let constUnlessData:[Character] = ["m","s","g","T","y","p","e"]

/*: "tips" :*/
fileprivate let noti_distanceTitle:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let mainColorKey:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let user_piFormat:String = "quality channel demonstrate statisticalmsgInf"
fileprivate let main_historyId:String = "down"

/*: "user" :*/
fileprivate let userCalculatePath:String = "usecamera"

/*: "uid" :*/
fileprivate let dataLetterName:String = "uigum"

/*: "nickname" :*/
fileprivate let main_trustSeriesName:[UInt8] = [0x65,0x6d,0x61,0x6e,0x6b,0x63,0x69,0x6e]

/*: "headPic" :*/
fileprivate let main_signatureName:[UInt8] = [0xd8,0xd5,0xd1,0xd4,0xe0,0xd9,0xd3]

private func makePackApplication(walk num: UInt8) -> UInt8 {
    return num ^ 176
}

/*: "age" :*/
fileprivate let mainUniversalHomeFormat:[UInt8] = [0x65,0x67,0x61]

/*: "gift" :*/
fileprivate let app_assistPath:String = "GIFT"

/*: "isGroup" :*/
fileprivate let userHelpEnjoyUrl:[Character] = ["i"]
fileprivate let show_stopWireTitle:String = "sGroupdoc don"

/*: "WantGift" :*/
fileprivate let appSureKey:String = "WantGifchild example peer"
fileprivate let noti_magnitudeValue:[Character] = ["t"]

/*: "txt" :*/
fileprivate let app_dynamicsData:String = "mentiont"

/*: "img" :*/
fileprivate let main_analysisId:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let data_transformTitle:String = "vidprovoke"

/*: "audio" :*/
fileprivate let main_mustValue:String = "AUDIO"

/*: "messageType" :*/
fileprivate let notiCookValue:String = "margumentsa"
fileprivate let main_burnStr:String = "prepresent"

/*: "contentType" :*/
fileprivate let show_coordinatorStr:String = "copolicy"
fileprivate let dataPitchText:String = "capable"

/*: "ImgMsg" :*/
fileprivate let constSlimBrightKey:String = "attach gapImgMsg"

/*: "imageUri" :*/
fileprivate let show_definitionId:[Character] = ["i","m","a","g","e","U","r","i"]

/*: "type" :*/
fileprivate let k_directValue:String = "saype"

/*: "pic" :*/
fileprivate let mainConversationValue:String = "pithere"

/*: "isBurn" :*/
fileprivate let user_downMessage:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let data_oddStr:[Character] = ["g","i","f","t","I","d"]

/*: "params" :*/
fileprivate let appRefMsg:String = "patooams"

/*: "audioLength" :*/
fileprivate let mainDeleteMinStr:String = "audmaker"
fileprivate let userSlightPastOrKey:String = "tdrag"

/*: "len" :*/
fileprivate let noti_requireData:[Character] = ["l","e","n"]

/*: "audioSandbox" :*/
fileprivate let user_argumentMessage:[Character] = ["a","u","d","i","o","S","a","n"]
fileprivate let main_featureUrl:String = "downerx"

/*: "senderId" :*/
fileprivate let data_selectionKey:String = "senpunishr"
fileprivate let dataUniformPath:String = "device say answer smallId"

/*: "AudioMsg" :*/
fileprivate let constPhotoFormat:String = "manual moment make waitAudi"

/*: "content" :*/
fileprivate let app_statusFormat:[Character] = ["c","o","n","t","e"]
fileprivate let showMediumContent:[Character] = ["n","t"]

/*: "TxtMsg" :*/
fileprivate let constMakeFormat:[Character] = ["T"]
fileprivate let app_decreaseData:[Character] = ["x","t","M","s","g"]

/*: "message" :*/
fileprivate let k_dramaMessage:String = "mtranslatesage"

/*: "quoteMsgId" :*/
fileprivate let show_blindValue:[Character] = ["q","u","o","t","e","M"]
fileprivate let mainMeetingDistributionMessage:String = "sgIdmedal violation angle"

/*: "quoteMsgInfo" :*/
fileprivate let mainRichFormat:String = "service lay skin mergequoteMs"
fileprivate let user_anotherDefinitionName:String = "mine hand theregInfo"

/*: "rYMsgType" :*/
fileprivate let userZoneId:String = "rYMss hypothesis"
fileprivate let constCookieDefineValue:String = "warn province flexible cosgType"

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let show_userText:[Character] = ["G","J",":","S","e","s"]
fileprivate let user_challengeUrl:String = "s:Autoboard already ear address recording"
fileprivate let noti_bigKey:[Character] = ["t","C","u","s","t","o","m"]

/*: "changeDirection" :*/
fileprivate let k_defineUrl:String = "CHAN"
fileprivate let showUniversalMessage:String = "ecappropriateo"
fileprivate let constInputFormat:[Character] = ["n"]

/*: "system" :*/
fileprivate let dataUnknownMsg:[Character] = ["s","y","s","t","e"]
fileprivate let k_meetValue:[Character] = ["m"]

/*: "title" :*/
fileprivate let data_extraThenUrl:String = "titlwalk"

/*: "icon" :*/
fileprivate let notiAddressText:String = "maleon"

/*: "all" :*/
fileprivate let main_limitedData:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let kStripBasicKey:[Character] = ["i","c","o","n"]
fileprivate let constLastContent:String = "core file_cha"

/*: "status" :*/
fileprivate let dataFarMsg:[Character] = ["s","t","a","t","u","s"]

/*: "msgIncome" :*/
fileprivate let main_builderStatusPath:[Character] = ["m","s","g","I","n","c","o","m","e"]

/*: "msgId" :*/
fileprivate let app_mirrorStr:String = "list natural give imsgId"

/*: "AirExpressionConvertible" :*/
fileprivate let mainBeautifulFormat:String = "TMsgTrpath commit under"
fileprivate let appPopFormat:String = "ANSL"
fileprivate let mainBoldId:[Character] = ["a","t","e","S","t","a","t","u","s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let show_trackOutsideData:String = "TMsgTremain accomplish revenue wed"
fileprivate let mainVisitorTitle:String = "tedContweek behind enter one"
fileprivate let show_ownerPath:[Character] = ["e","n","t"]

/*: "formatType" :*/
fileprivate let userCombineTitle:[Character] = ["f","o","r","m","a","t","T","y","p","e"]

/*: "plain" :*/
fileprivate let notiCapabilityName:[Character] = ["p","l","a","i","n"]

/*: "imageData" :*/
fileprivate let appPanelMessage:String = "export cooperative utilizeimageData"

/*: "VideoMsg" :*/
fileprivate let showResultErrPickUrl:String = "Videyet possible when"

/*: "match" :*/
fileprivate let data_togetherMeetingAskText:[Character] = ["m","a","t","c","h"]

/*: "userIntro" :*/
fileprivate let data_adjustmentTitle:String = "userIntcut installation"
fileprivate let constPadKey:[Character] = ["r","o"]

/*: "targetId" :*/
fileprivate let mainTingName:[Character] = ["t","a","r"]
fileprivate let k_femaleName:String = "getIditem small keep install disabled"

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let user_lapNervousThreePath:[Character] = ["y","y","y"]
fileprivate let show_balanceKey:String = "y-MMbase max"
fileprivate let showLetterStr:String = "complaint commit statistical:mm:ss"

/*: "sendTime" :*/
fileprivate let userStandMsg:String = "sendTrid span hypothesis ta province"
fileprivate let userChildArtPath:String = "resme"

/*: "isRead" :*/
fileprivate let data_rainUrl:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let dataTimeId:String = "abadi"
fileprivate let kContextUrl:[Character] = ["o","D","a","t","a"]

/*: "audioUri" :*/
fileprivate let user_rankSightValue:String = "listen each normallyaudioU"
fileprivate let data_showId:String = "stacki"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ExtendReactiveCompatible.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum DrawPlayRelatableTarget: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - ExtendReactiveCompatible

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class ExtendReactiveCompatible: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func connectTime() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> EveryBitThen? {
    public class func packet(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> EveryBitThen? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = equalAbout(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: DepictionViewManager.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            DepictionViewManager.shared.drogueContact(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> EveryBitThen? {
    public class func equalAbout(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> EveryBitThen? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(constUnlessData))] = (String(noti_distanceTitle))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(mainColorKey)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = loadUp(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = TrivialityThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ProLipModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? WitnesserModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(user_piFormat.suffix(6)) + main_historyId.replacingOccurrences(of: "down", with: "o"))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(userCalculatePath.replacingOccurrences(of: "camera", with: "r"))] = [
            //: "uid": RealmReactiveCompatible.share.loginUserMode.userID,
            (dataLetterName.replacingOccurrences(of: "gum", with: "d")): RealmReactiveCompatible.share.loginUserMode.userID,
            //: "nickname": RealmReactiveCompatible.share.loginUserMode.nickname,
            String(bytes: main_trustSeriesName.reversed(), encoding: .utf8)!: RealmReactiveCompatible.share.loginUserMode.nickname,
            //: "headPic": RealmReactiveCompatible.share.loginUserMode.headPic,
            String(bytes: main_signatureName.map{makePackApplication(walk: $0)}, encoding: .utf8)!: RealmReactiveCompatible.share.loginUserMode.headPic,
            //: "age": RealmReactiveCompatible.share.loginUserMode.age,
            String(bytes: mainUniversalHomeFormat.reversed(), encoding: .utf8)!: RealmReactiveCompatible.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(mainColorKey)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = loadUp(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(constUnlessData))] as? String == (app_assistPath.lowercased()) {
            //: var data: EveryBitThen
            var data: EveryBitThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(userHelpEnjoyUrl) + String(show_stopWireTitle.prefix(6)))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = RemainCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = GeneralCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = GeneralCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ProLipModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(constUnlessData))] as? String == (String(appSureKey.prefix(7)) + String(noti_magnitudeValue)) {
            //: let data = CompositionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = CompositionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ProLipModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(constUnlessData))] as? String == (app_dynamicsData.replacingOccurrences(of: "mention", with: "tx")) {
            //: var data: EveryBitThen
            var data: EveryBitThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(userHelpEnjoyUrl) + String(show_stopWireTitle.prefix(6)))] as? String == "1" { // 群聊
                //: data = CollectionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = CollectionCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = ExternalPinCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ExternalPinCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ProLipModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(constUnlessData))] as? String == (String(main_analysisId)) {
            //: let data = ManufacturerGraphReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ManufacturerGraphReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ProLipModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(constUnlessData))] as? String == (data_transformTitle.replacingOccurrences(of: "provoke", with: "eo")) {
            //: let data = RepresentReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = RepresentReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ProLipModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(constUnlessData))] as? String == (main_mustValue.lowercased()) {
            //: let data = HumanCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = HumanCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ProLipModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(app_assistPath.lowercased())] != nil && (msgInfo[(notiCookValue.replacingOccurrences(of: "argument", with: "es") + "geTy" + main_burnStr.replacingOccurrences(of: "represent", with: "e"))] as? Int) == 4 {
            //: let data = ExternalPinCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ExternalPinCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? ProLipModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: EveryBitThen) -> EveryBitThen? {
    public class func alongsideFail(withData cellData: EveryBitThen) -> EveryBitThen? {
        // 图片
        //: if let reCellData = cellData as? ManufacturerGraphReactiveCompatible {
        if let reCellData = cellData as? ManufacturerGraphReactiveCompatible {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(CurrencyDefineReactiveCompatible.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(CurrencyDefineReactiveCompatible.bringInPopularMetronymicIdentity())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(show_coordinatorStr.replacingOccurrences(of: "policy", with: "nt") + "entTyp" + dataPitchText.replacingOccurrences(of: "capable", with: "e"))] = (String(constSlimBrightKey.suffix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(constUnlessData))] = (String(main_analysisId))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(show_definitionId))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(user_piFormat.suffix(6)) + main_historyId.replacingOccurrences(of: "down", with: "o"))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(k_directValue.replacingOccurrences(of: "say", with: "ty"))] = (String(constSlimBrightKey.suffix(6)))
            //: params["pic"] = filePath
            params[(mainConversationValue.replacingOccurrences(of: "there", with: "c"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(user_downMessage))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(user_downMessage))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(data_oddStr))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(data_oddStr))] = "0"
            }

            //: dic["params"] = params
            dic[(appRefMsg.replacingOccurrences(of: "too", with: "r"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = equalAbout(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? RepresentReactiveCompatible {
        if let reCellData = cellData as? RepresentReactiveCompatible {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? HumanCellData {
        if let reCellData = cellData as? HumanCellData {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = DotInfoManager.messageHolder(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(mainDeleteMinStr.replacingOccurrences(of: "maker", with: "i") + "oLeng" + userSlightPastOrKey.replacingOccurrences(of: "drag", with: "h"))] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(String(noti_requireData))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(user_argumentMessage) + main_featureUrl.replacingOccurrences(of: "owner", with: "bo"))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(user_argumentMessage) + main_featureUrl.replacingOccurrences(of: "owner", with: "bo"))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(constUnlessData))] = (main_mustValue.lowercased())
            //: msgInfo["senderId"] = RealmReactiveCompatible.share.loginUserMode.userID
            msgInfo[(data_selectionKey.replacingOccurrences(of: "punish", with: "de") + String(dataUniformPath.suffix(2)))] = RealmReactiveCompatible.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(user_piFormat.suffix(6)) + main_historyId.replacingOccurrences(of: "down", with: "o"))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(k_directValue.replacingOccurrences(of: "say", with: "ty"))] = (String(constPhotoFormat.suffix(4)) + "oMsg")
            //: dic["params"] = params
            dic[(appRefMsg.replacingOccurrences(of: "too", with: "r"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = equalAbout(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? ExternalPinCellData {
        if let reCellData = cellData as? ExternalPinCellData {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(String(app_statusFormat) + String(showMediumContent))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(constUnlessData))] = (app_dynamicsData.replacingOccurrences(of: "mention", with: "tx"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(user_piFormat.suffix(6)) + main_historyId.replacingOccurrences(of: "down", with: "o"))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(k_directValue.replacingOccurrences(of: "say", with: "ty"))] = (String(constMakeFormat) + String(app_decreaseData))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(k_dramaMessage.replacingOccurrences(of: "translate", with: "es"))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.outgrowth() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(show_blindValue) + String(mainMeetingDistributionMessage.prefix(4)))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(mainRichFormat.suffix(7)) + String(user_anotherDefinitionName.suffix(5)))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(appRefMsg.replacingOccurrences(of: "too", with: "r"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = equalAbout(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> EveryBitThen? {
    public class func usIncomeSet(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> EveryBitThen? {
        //: var cellData: EveryBitThen?
        var cellData: EveryBitThen?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(mainColorKey))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(user_piFormat.suffix(6)) + main_historyId.replacingOccurrences(of: "down", with: "o"))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(userCalculatePath.replacingOccurrences(of: "camera", with: "r"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? ProLipModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == RealmReactiveCompatible.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == RealmReactiveCompatible.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == RealmReactiveCompatible.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == RealmReactiveCompatible.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(userZoneId.prefix(4)) + String(constCookieDefineValue.suffix(5)))] as? String == (String(show_userText) + String(user_challengeUrl.prefix(6)) + "Greet:Tx" + String(noti_bigKey)) {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(k_defineUrl.lowercased() + "geDir" + showUniversalMessage.replacingOccurrences(of: "appropriate", with: "ti") + String(constInputFormat))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = rear(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(constUnlessData))] as? String == (String(dataUnknownMsg) + String(k_meetValue)) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = ProLipModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? WitnesserModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = TrivialityThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(String(app_statusFormat) + String(showMediumContent))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = ManufacturerGraphReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ManufacturerGraphReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = RepresentReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = RepresentReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = RemainCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = GeneralCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = GeneralCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = viaCorner(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(mainColorKey))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(user_piFormat.suffix(6)) + main_historyId.replacingOccurrences(of: "down", with: "o"))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? AnteriorModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = CompositionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CompositionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = HumanCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = HumanCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? HumanCellData {
            if let audioCellData = cellData as? HumanCellData {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = modelEqualElement(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = VoiceFamilyMsgTable.cere(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = VoiceFamilyMsgTable.detectFlush(truncateJaw(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = RepresentThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(data_extraThenUrl.replacingOccurrences(of: "walk", with: "e"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(String(app_statusFormat) + String(showMediumContent))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(main_analysisId))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = OrbitThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = CollectionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = CollectionCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = ExternalPinCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ExternalPinCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? DistantModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(notiAddressText.replacingOccurrences(of: "male", with: "ic"))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: RealmReactiveCompatible.share.loginUserMode.headPic ?? ""
                        RealmReactiveCompatible.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = AnteriorModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(String(app_statusFormat) + String(showMediumContent))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(mainColorKey))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = VerticalAnalogDigitalConverterModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(main_limitedData))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(kStripBasicKey) + String(constLastContent.suffix(4)) + "ts_pcr")
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.connectCrop(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(String(dataFarMsg))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(main_builderStatusPath))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(notiCookValue.replacingOccurrences(of: "argument", with: "es") + "geTy" + main_burnStr.replacingOccurrences(of: "represent", with: "e"))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(app_mirrorStr.suffix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(mainBeautifulFormat.prefix(6)) + appPopFormat.lowercased() + String(mainBoldId))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = AirExpressionConvertible(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(show_trackOutsideData.prefix(5)) + "ransla" + String(mainVisitorTitle.prefix(7)) + String(show_ownerPath))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = AppearanceModel.fillWithPyeDogDict()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = AppearanceModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = SessionModel.front()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = SessionModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = MeetingModel.range()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func rear(_ extraDic: [String: Any]) -> DrawPlayRelatableTarget {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(mainColorKey))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(user_piFormat.suffix(6)) + main_historyId.replacingOccurrences(of: "down", with: "o"))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? ProLipModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(userCombineTitle))] as? String == (String(notiCapabilityName)) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(show_definitionId))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(String(appPanelMessage.suffix(9)))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(showResultErrPickUrl.prefix(4)) + "oMsg") {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = personhood(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(String(data_togetherMeetingAskText))] as? Int) == 1 || (msgInfo?[(String(data_togetherMeetingAskText))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(constUnlessData))] as? String == (String(noti_distanceTitle)) || (dic?[(notiCookValue.replacingOccurrences(of: "argument", with: "es") + "geTy" + main_burnStr.replacingOccurrences(of: "represent", with: "e"))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(userCombineTitle))] as? String == "mf") && ((dic?[(notiCookValue.replacingOccurrences(of: "argument", with: "es") + "geTy" + main_burnStr.replacingOccurrences(of: "represent", with: "e"))] as? Int) == 15 || (dic?[(notiCookValue.replacingOccurrences(of: "argument", with: "es") + "geTy" + main_burnStr.replacingOccurrences(of: "represent", with: "e"))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(constUnlessData))] as? String == (String(data_adjustmentTitle.prefix(7)) + String(constPadKey)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(constUnlessData))] as? String == (String(appSureKey.prefix(7)) + String(noti_magnitudeValue)) || (msgInfo?[(notiCookValue.replacingOccurrences(of: "argument", with: "es") + "geTy" + main_burnStr.replacingOccurrences(of: "represent", with: "e"))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func truncateJaw(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.connectCrop(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(mainColorKey))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(user_piFormat.suffix(6)) + main_historyId.replacingOccurrences(of: "down", with: "o"))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(userCalculatePath.replacingOccurrences(of: "camera", with: "r"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(data_selectionKey.replacingOccurrences(of: "punish", with: "de") + String(dataUniformPath.suffix(2)))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == RealmReactiveCompatible.share.loginUserMode.userID {
        if senderId == RealmReactiveCompatible.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(data_selectionKey.replacingOccurrences(of: "punish", with: "de") + String(dataUniformPath.suffix(2)))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(mainTingName) + String(k_femaleName.prefix(5)))] = message.userID ?? (user?[(String(mainTingName) + String(k_femaleName.prefix(5)))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(data_selectionKey.replacingOccurrences(of: "punish", with: "de") + String(dataUniformPath.suffix(2)))] = "\(msgInfo?[(dataLetterName.replacingOccurrences(of: "gum", with: "d"))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(mainTingName) + String(k_femaleName.prefix(5)))] = "\(msgInfo?[(dataLetterName.replacingOccurrences(of: "gum", with: "d"))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(app_mirrorStr.suffix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(app_mirrorStr.suffix(5)))] = "\(msgInfo?[(String(app_mirrorStr.suffix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.connectCrop(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(app_mirrorStr.suffix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(app_mirrorStr.suffix(5)))] = "\(customDic[(String(app_mirrorStr.suffix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(app_mirrorStr.suffix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(user_argumentMessage) + main_featureUrl.replacingOccurrences(of: "owner", with: "bo"))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(user_argumentMessage) + main_featureUrl.replacingOccurrences(of: "owner", with: "bo"))] = "\(msgInfo?[(String(user_argumentMessage) + main_featureUrl.replacingOccurrences(of: "owner", with: "bo"))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((mainDeleteMinStr.replacingOccurrences(of: "maker", with: "i") + "oLeng" + userSlightPastOrKey.replacingOccurrences(of: "drag", with: "h"))) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(mainDeleteMinStr.replacingOccurrences(of: "maker", with: "i") + "oLeng" + userSlightPastOrKey.replacingOccurrences(of: "drag", with: "h"))] = "\(msgInfo?[(mainDeleteMinStr.replacingOccurrences(of: "maker", with: "i") + "oLeng" + userSlightPastOrKey.replacingOccurrences(of: "drag", with: "h"))] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(user_lapNervousThreePath) + String(show_balanceKey.prefix(4)) + "-dd HH" + String(showLetterStr.suffix(6)))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(userStandMsg.prefix(5)) + userChildArtPath.replacingOccurrences(of: "res", with: "i"))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == RealmReactiveCompatible.share.loginUserMode.userID ? "1" : "0"
        info?[(String(data_rainUrl))] = senderId == RealmReactiveCompatible.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func modelEqualElement(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = truncateJaw(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(app_mirrorStr.suffix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func viaCorner(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func loadUp(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func personhood(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(constUnlessData))] as? String, msgType == (main_mustValue.lowercased()) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(show_coordinatorStr.replacingOccurrences(of: "policy", with: "nt") + "entTyp" + dataPitchText.replacingOccurrences(of: "capable", with: "e"))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(constPhotoFormat.suffix(4)) + "oMsg")
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(dataTimeId.replacingOccurrences(of: "bad", with: "ud") + String(kContextUrl))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(user_rankSightValue.suffix(6)) + data_showId.replacingOccurrences(of: "stack", with: "r"))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
