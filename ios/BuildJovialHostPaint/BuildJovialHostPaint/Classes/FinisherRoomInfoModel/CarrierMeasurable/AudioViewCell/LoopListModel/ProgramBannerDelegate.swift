
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataBigName:[UInt8] = [0x36,0x31,0x36,0x2b,0x77,0x3c,0x30,0x3b,0x3a,0x2d,0x65,0x76,0x7f,0x37,0x3e,0x2c,0x7f,0x31,0x30,0x2b,0x7f,0x3d,0x3a,0x3a,0x31,0x7f,0x36,0x32,0x2f,0x33,0x3a,0x32,0x3a,0x31,0x2b,0x3a,0x3b]

private func exclusiveEff(pick num: UInt8) -> UInt8 {
    return num ^ 95
}

/*: "F5F5F5" :*/
fileprivate let const_statisticalBadPath:String = "FdirectionFdirectionF"
fileprivate let k_deliveryRegularAsPath:[Character] = ["5"]

/*: "#128CFF" :*/
fileprivate let main_scanMessage:String = "#128CFFlink deliver"

/*: "Feedback" :*/
fileprivate let dataPlaceMessage:[Character] = ["F","e","e","d","b","a","c","k"]

/*: "#8370FF" :*/
fileprivate let notiEarMessage:[Character] = ["#","8","3","7","0","F"]
fileprivate let userSkipMotionFormat:String = "instruction"

/*: "Quick Greeting" :*/
fileprivate let constTeamUrl:[Character] = ["Q","u","i","c","k"," ","G","r","e","e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let data_confirmTransactionContent:[Character] = ["B","e","a","u","t","y"," ","P","h","o","t","o"]
fileprivate let showChangeStr:[Character] = ["s"]

/*: "url" :*/
fileprivate let mainScheduleSurgeryEffMsg:[Character] = ["u","r","l"]

/*: "unlockGift" :*/
fileprivate let showEditorName:[Character] = ["u","n","l","o"]
fileprivate let app_distanceName:String = "error work dealckGift"

/*: "isBurn" :*/
fileprivate let dataHalfUrl:[UInt8] = [0x9b,0xa5,0x74,0xa7,0xa4,0xa0]

fileprivate func cookieSearchion(island num: UInt8) -> UInt8 {
    let value = Int(num) + 206
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let main_seeTopicData:String = "instruction"
fileprivate let data_picturePath:[Character] = ["i","f","t","I","d"]

/*: "rYMsgType" :*/
fileprivate let data_blueMessage:String = "below addition last would earnrYMs"

/*: "GJ:CallCustom" :*/
fileprivate let noti_detectSweetUrl:[Character] = ["G","J",":","C","a"]
fileprivate let main_broadcastPath:String = "tail security stat curvellCu"

/*: "changeDirection" :*/
fileprivate let constReplyId:String = "CHAN"
fileprivate let k_starUrl:String = "ticaren"

/*: "extra" :*/
fileprivate let notiSendUrl:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let userShallFormat:String = "msgInfcrush effect para"
fileprivate let const_mainText:[Character] = ["o"]

/*: "toUid" :*/
fileprivate let showReaderName:String = "head feed service telephonetoUid"

/*: "wantGift" :*/
fileprivate let data_tumbleName:String = "WANT"

/*: "jumpKey" :*/
fileprivate let appWalkPath:[Character] = ["j","u","m","p","K","e","y"]

/*: "truePersonAuth" :*/
fileprivate let userHorizonMicName:String = "union accomplish filltrueP"
fileprivate let mainYouFormat:String = "opportunity"

/*: "logType" :*/
fileprivate let constShrinkData:String = "logTypecalendar data black accuracy"

/*: "msgType" :*/
fileprivate let user_definitionUrl:[UInt8] = [0x10,0xe,0x1a,0x29,0x4,0xd,0x18]

private func costSocial(mid num: UInt8) -> UInt8 {
    return num ^ 125
}

/*: "txt" :*/
fileprivate let mainTopicMessage:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let noti_siblingEnhanceUrl:String = "tybrake"

/*: "QuickMsg" :*/
fileprivate let k_arenaSpanStr:[Character] = ["Q","u","i","c","k","M","s","g"]

/*: "params" :*/
fileprivate let notiListenPath:[UInt8] = [0xb9,0xaa,0xbb,0xaa,0xb6,0xbc]

fileprivate func flexibleEquipment(huge num: UInt8) -> UInt8 {
    let value = Int(num) - 73
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let notiOpportunityStr:[UInt8] = [0x71,0x7d,0x7c,0x66,0x77,0x7c,0x66]

private func totThem(facility num: UInt8) -> UInt8 {
    return num ^ 18
}

/*: "txtId" :*/
fileprivate let notiEachTitle:String = "txtIdtap near writer collection prompt"

/*: "message" :*/
fileprivate let user_plantName:[UInt8] = [0xfc,0xf4,0x2,0x2,0xf0,0xf6,0xf4]

fileprivate func latGallery(yesterday num: UInt8) -> UInt8 {
    let value = Int(num) + 113
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "TxtMsg" :*/
fileprivate let kYearListenText:String = "global"
fileprivate let mainForthContentKey:String = "xtMsgscene warning"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let appReadContent:[UInt8] = [0x65,0x72,0x75,0x64,0x65,0x63,0x6f,0x72,0x50,0x20,0x2e,0x64,0x65,0x6c,0x69,0x61,0x66,0x20,0x64,0x61,0x6f,0x6c,0x6e,0x77,0x6f,0x64,0x20,0x6f,0x6c,0x6c,0x65,0x68,0x20,0x65,0x63,0x69,0x6f,0x56]

/*: "length" :*/
fileprivate let notiItUrl:[Character] = ["l","e","n","g","t"]
fileprivate let show_verticalTitle:[Character] = ["h"]

/*: "audio" :*/
fileprivate let dataMagnitudeerestPath:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let data_latUrl:[Character] = ["a","u","d","i","o","I","d"]

/*: "audioLength" :*/
fileprivate let showLipAnalysisTitle:[UInt8] = [0x52,0x46,0x57,0x5a,0x5c,0x7f,0x56,0x5d,0x54,0x47,0x5b]

private func morningPart(bag num: UInt8) -> UInt8 {
    return num ^ 51
}

/*: "audioSandbox" :*/
fileprivate let appPlayerContent:String = "auadditionali"
fileprivate let appJudgeWaveStrongFormat:String = "interrupt matter prepare inputoSandbox"

/*: "senderId" :*/
fileprivate let show_holdRunName:[UInt8] = [0x1,0x17,0x1c,0x16,0x17,0x0,0x3b,0x16]

private func bodyWeight(main num: UInt8) -> UInt8 {
    return num ^ 114
}

/*: "AudioMsg" :*/
fileprivate let constInstallationData:String = "index afterAudioMsg"

/*: "Can't send blank message" :*/
fileprivate let k_baseballId:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b"]
fileprivate let user_menuMsg:String = "lanhypothesis"
fileprivate let noti_narrowId:String = "incidentge"

/*: "quoteMsgId" :*/
fileprivate let show_dogData:String = "quex"

/*: "quoteMsgInfo" :*/
fileprivate let const_themTitle:[Character] = ["q","u","o","t","e","M","s","g","I"]
fileprivate let mainFindingStr:[Character] = ["n","f","o"]

/*: "contentType" :*/
fileprivate let noti_farText:[UInt8] = [0xaf,0xbb,0xba,0xc0,0xb1,0xba,0xc0,0xa0,0xc5,0xbc,0xb1]

fileprivate func leafBlade(better num: UInt8) -> UInt8 {
    let value = Int(num) + 180
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let userRoundFormat:String = "terms greetImgMsg"

/*: "img" :*/
fileprivate let main_prepareMessage:String = "ilifeg"

/*: "imageUri" :*/
fileprivate let noti_describeId:[UInt8] = [0x56,0x5a,0x4e,0x54,0x52,0x42,0x5f,0x56]

fileprivate func eraseMoment(editor num: UInt8) -> UInt8 {
    let value = Int(num) + 19
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "customSend" :*/
fileprivate let kReceiverData:[UInt8] = [0x64,0x6e,0x65,0x53,0x6d,0x6f,0x74,0x73,0x75,0x63]

/*: "pic" :*/
fileprivate let notiActualId:String = "piwhether"

/*: "VideoMsg" :*/
fileprivate let appCurrentText:[Character] = ["V","i"]
fileprivate let app_speakerFormat:String = "province moredeoMsg"

/*: "video" :*/
fileprivate let constQuantityUrl:[Character] = ["v","i","d","e","o"]

/*: "coverImg" :*/
fileprivate let data_uponName:[UInt8] = [0x67,0x6d,0x49,0x72,0x65,0x76,0x6f,0x63]

/*: "videoUrl" :*/
fileprivate let notiTenderName:[UInt8] = [0x17,0x8,0x5,0x4,0xe,0x34,0x13,0xd]

private func labSchemeThird(birth num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "duration" :*/
fileprivate let kDomainUrl:[UInt8] = [0x57,0x46,0x41,0x52,0x47,0x5a,0x5c,0x5d]

private func rollUser(address num: UInt8) -> UInt8 {
    return num ^ 51
}

/*: "len" :*/
fileprivate let app_belowStr:[UInt8] = [0x9a,0x93,0x9c]

fileprivate func squareMobile(date num: UInt8) -> UInt8 {
    let value = Int(num) - 46
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Documents/User/voice/" :*/
fileprivate let user_diskNameWhitePath:[Character] = ["D","o","c","u","m","e","n","t","s"]
fileprivate let k_orangeCombineUrl:String = "/Useconsciousness search stay drama"

/*: "Documents/User/record/" :*/
fileprivate let appPublishGreenText:String = "Documeweak three ban weak"
fileprivate let constFollowKey:String = "User/outer trigger"
fileprivate let main_halfName:[Character] = ["r","d","/"]

/*: "msgIncome" :*/
fileprivate let kDrawContent:String = "image greatestmsgIn"
fileprivate let k_consciousnessIncomeDotValue:[Character] = ["c","o","m","e"]

/*: "mfBean" :*/
fileprivate let userGeneralText:[UInt8] = [0x66,0x6d,0x49,0x6e,0x6a,0x65]

private func blindPassage(round num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "messageType" :*/
fileprivate let notiIonId:String = "metap"
fileprivate let constRemainKey:String = "personal translation support objectageType"

/*: "WantGift" :*/
fileprivate let userNaturePath:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let dataExposurePerceptNotKey:String = "stick nervous clearly_Priva"
fileprivate let const_topFormat:String = "per mirror signalatTips"
fileprivate let user_failureMsg:[Character] = ["_"]

/*: "msgId" :*/
fileprivate let noti_anonUrl:String = "invite powder index animal marginmsgId"

/*: "totalMfCoin" :*/
fileprivate let k_sureUrl:[Character] = ["t"]
fileprivate let const_brightTitle:[Character] = ["o","t","a","l","M","f","C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let app_combinedData:String = "attacrich"
fileprivate let appFilterName:String = "good"

/*: "intimatePhoto" :*/
fileprivate let appAdditionLawSpecMessage:[Character] = ["i","n","t","i","m"]
fileprivate let mainListUrl:String = "ashine"
fileprivate let constThresholdStr:[Character] = ["P","h","o","t","o"]

/*: "totalIntimate" :*/
fileprivate let main_peopleTitle:String = "tdocumentta"
fileprivate let showRecoverData:String = "IMAT"
fileprivate let main_joinStr:[Character] = ["e"]

/*: "uid" :*/
fileprivate let showParentPicVisitorMessage:[UInt8] = [0xa8,0x9c,0x97]

fileprivate func surgeryOperation(search num: UInt8) -> UInt8 {
    let value = Int(num) - 51
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "intimate" :*/
fileprivate let kLipPath:[UInt8] = [0xaa,0xad,0xb7,0xaa,0xae,0xa2,0xb7,0xa6]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let kSlowTitle:[UInt8] = [0xa9,0x8b,0x84,0xcd,0x9e,0xca,0x9f,0x99,0x8f,0xca,0x9e,0x82,0x8f,0xca,0x8c,0x9f,0x84,0x89,0x9e,0x83,0x85,0x84,0xca,0x8e,0x9f,0x8f,0xca,0x9e,0x85,0xca,0x93,0x85,0x9f,0x98,0xca,0x86,0x8b,0x89,0x81,0xca,0x85,0x8c,0xca,0x83,0x84,0x9e,0x83,0x87,0x8b,0x89,0x93,0xca,0x86,0x8f,0x9c,0x8f,0x86,0xc4]

private func quoteBirth(going num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "giftNum" :*/
fileprivate let mainBrightId:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let k_chemicalUrl:[Character] = ["p","k","g","I","t","e","m","s","e","t","I","d"]

/*: "from" :*/
fileprivate let constCurrencyFormat:String = "frobring"

/*: "wantGiftId" :*/
fileprivate let constEditUrl:String = "coordinator volume cover discourse arenawantGi"

/*: "WantGiftMsg" :*/
fileprivate let app_shipStr:String = "city reading editor projectWantGi"
fileprivate let kHerFormat:String = "small morning focus crop selectedftMsg"

/*: "gift" :*/
fileprivate let kLearnUrl:String = "gifquit"

/*: "View info" :*/
fileprivate let main_describeKey:String = "gray by quickView i"
fileprivate let kColorData:String = "nfgrade"

/*: "Notes" :*/
fileprivate let mainSuperiorStr:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let userBlueValue:String = "Reportdisabled meeting builder psychological calculation"

/*: "View read receipts?" :*/
fileprivate let user_successfullyId:String = "View rcourse where"
fileprivate let mainDramaMsg:[Character] = ["i","p","t","s","?"]

/*: "She" :*/
fileprivate let app_interactionWholePopularStr:[Character] = ["S","h","e"]

/*: "%@ is using too low a version" :*/
fileprivate let data_actualName:String = "load broad produce%@ is u"
fileprivate let k_heartId:String = "ssufficientng"
fileprivate let constWeStr:String = "balance duty basic processorlow a"
fileprivate let const_sceneData:String = " versionorigin future"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let const_makeLocalMsg:String = "Shootunable refuse succeed powder football"
fileprivate let userCommentUrl:String = "just comfortable drawo at lea"
fileprivate let constSumId:String = "why care clearly sectionst 5 s"

/*: "Upload failed" :*/
fileprivate let data_boldFormat:[Character] = ["U","p","l","o","a","d"," ","f","a","i","l","e","d"]

/*: "播放状态: :*/
fileprivate let constLoseText:[Character] = ["播","\u{653e}","\u{72b6}","态",":"]

/*: "c2c_" :*/
fileprivate let user_lackKey:String = "c2c_across sh pass"

/*: "msg" :*/
fileprivate let showBelowData:String = "successfullysg"

/*: "tips" :*/
fileprivate let noti_senseMessage:[Character] = ["t","i","p","s"]

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let kDistanceKey:[UInt8] = [0xe1,0xd7,0xcd,0x98,0xdb,0xd9,0xd6,0x98,0xcd,0xcb,0xdd,0x98,0xcc,0xd0,0xdd,0x98,0xce,0xd1,0xdc,0xdd,0xd7,0x98,0xdb,0xd0,0xd9,0xcc,0x98,0xde,0xdd,0xd9,0xcc,0xcd,0xca,0xdd,0x98,0xcc,0xd7,0x98,0xdf,0xd1,0xce,0xdd,0x98,0x9d,0xf8,0x98,0xd9,0x98,0xdb,0xd9,0xd4,0xd4]

private func solidFood(who num: UInt8) -> UInt8 {
    return num ^ 184
}

/*: "cellData" :*/
fileprivate let main_trackMixPath:String = "cdelayll"

/*: "System notification" :*/
fileprivate let userSupportName:String = "arena sure destroySystem"
fileprivate let show_languageFormat:String = "globalation"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProgramBannerDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum CoverFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class ProgramBannerDelegate: OperateReactiveCompatible {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = CurrencyModelType()
    private var userWrap = CurrencyModelType() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = CoverFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: CurrencyModelType = CurrencyModelType()) {
    init(chatID: String, isFrom: CoverFromEnum = .Normal, userWrap: CurrencyModelType = CurrencyModelType()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        ProgramBannerDelegate.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        InsulatorThen.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == CurrencyDefineReactiveCompatible.getXiaoMiID() {
        if targetId == CurrencyDefineReactiveCompatible.metricLinearUnit() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == CurrencyDefineReactiveCompatible.getCustomerServiceID() {
        } else if targetId == CurrencyDefineReactiveCompatible.facilityBasic() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataBigName.map{exclusiveEff(pick: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RealmReactiveCompatible.share.appConfigMode.disableShootScreen {
            if RealmReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = FreshLoopReactiveCompatible(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        transformAnimation()
        //: createUI()
        selectBroadM()
        //: createUIConstraints()
        exposureRefuse()
        //: bindInteraction()
        pickTotaleraction()
        //: addTapGestureRecognizer()
        asSet()
        //: func__requestChatInfo()
        thresholdWarning()
        //: func__getFillDraftText()
        everyEye()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        PerceptObjectProtocol.shared.resignDelegate()
        //: func__addVoiceNotification()
        voice()
        //: addReadPeceiptNotification()
        analysis()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            capitalistWithAccumulation()
            //: func__TipsNotification()
            beyondGeneral()
            //: if RealmReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
            if RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.male.rawValue {
                //: func__setReplyTipsView()
                basicView()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        forMax(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        forMax(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            beginMain()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            OperateRunningRatingManager.shared.apply()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: ThinkTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ThinkTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (const_statisticalBadPath.replacingOccurrences(of: "direction", with: "5") + String(k_deliveryRegularAsPath)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(MaterialRealmMsgCell.self, forCellReuseIdentifier: MaterialRealmMsgCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(BrowMsgCell.self, forCellReuseIdentifier: BrowMsgCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(RepresentMsgCell.self, forCellReuseIdentifier: RepresentMsgCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(WorkerCollectionView.self, forCellReuseIdentifier: WorkerCollectionView.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(CoverThen.self, forCellReuseIdentifier: CoverThen.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(WidespreadMsgCell.self, forCellReuseIdentifier: WidespreadMsgCell.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(TranslationViewCell.self, forCellReuseIdentifier: TranslationViewCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(JuxtapositionMsgCell.self, forCellReuseIdentifier: JuxtapositionMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(ExternalThen.self, forCellReuseIdentifier: ExternalThen.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: AcrossViewCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(ObjectMessageCell.self, forCellReuseIdentifier: AcrossViewCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: BrowCompositionMatchDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = BrowCompositionMatchDelegate()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.focus()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: ResponsibilityViewDelegate = {
        //: var type: InputViewType = .PrivateChat
        var type: SeekViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = ResponsibilityViewDelegate(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(main_scanMessage.prefix(7))))!, .font: UIFont.shrinkSize(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(dataPlaceMessage)).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(ping), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: MainReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = MainReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: AttitudeView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = AttitudeView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.shrinkSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(notiEarMessage) + userSkipMotionFormat.replacingOccurrences(of: "instruction", with: "F"))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(constTeamUrl)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.labIndicator().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(actualRoute), for: .touchUpInside)
        //: btn.isHidden = !RealmReactiveCompatible.share.appUserConfigMode.showDirectGreet || RealmReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || RealmReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !RealmReactiveCompatible.share.appUserConfigMode.showDirectGreet || RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.male.rawValue || RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: HoweverTipsView? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = HoweverTipsView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: MyFinsherView = {
        //: let v = TalkingSelectTranslationView()
        let v = MyFinsherView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.harvestWill(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: WrongCoverThen = {
        //: let v = TalkingTranslationResultView()
        let v = WrongCoverThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.ratingUsed(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.shrinkSize(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(notiEarMessage) + userSkipMotionFormat.replacingOccurrences(of: "instruction", with: "F"))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(data_confirmTransactionContent) + String(showChangeStr)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.labIndicator().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(comeliness), for: .touchUpInside)
        //: btn.isHidden = RealmReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue || RealmReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.male.rawValue || RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: LiteralOutsideReactiveCompatible = {
        //: let v = TalkingLiveRoomGamesView()
        let v = LiteralOutsideReactiveCompatible()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: PulverizationThen = {
        //: let view = TalkingPrivateBannerView.init()
        let view = PulverizationThen()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: PortraitScreenView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = PortraitScreenView()
        //: v.isHidden = (RealmReactiveCompatible.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: PossibilityReactiveCompatible = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = PossibilityReactiveCompatible()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.lipRewardView()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension ProgramBannerDelegate: ThighReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func putInView(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if RealmReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if RealmReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue {
                if RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.crowding(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension ProgramBannerDelegate: MessageDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func actualRoute() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        InsulatorThen.speedyChatHandlerGreetingDataConverterTurnUtiliserContact(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.bringHome(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.used(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func comeliness() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        InsulatorThen.daily(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = CurtsyViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().darkness(urlStr: dict?[(String(mainScheduleSurgeryEffMsg))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(showEditorName) + String(app_distanceName.suffix(6)))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = TitlelessModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: dataHalfUrl.map{cookieSearchion(island: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(main_seeTopicData.replacingOccurrences(of: "instruction", with: "g") + String(data_picturePath))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.flushSwitchly(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> EveryBitThen! {
    func ask(_: ThinkTableViewController!, auspicate data: V2TIMMessage!) -> EveryBitThen! {
        //: let dic = AcrossChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = AcrossChatManager.image(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = ExtendReactiveCompatible.usIncomeSet(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: RepresentThen.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(data_blueMessage.suffix(4)) + "gType")].stringValue == (String(noti_detectSweetUrl) + String(main_broadcastPath.suffix(4)) + "stom") { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(constReplyId.lowercased() + "geDirec" + k_starUrl.replacingOccurrences(of: "care", with: "o"))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(notiSendUrl))][(String(userShallFormat.prefix(6)) + String(const_mainText))][(String(showReaderName.suffix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = ExternalPinCellData(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = ExternalPinCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: HumanCellData.self) == true ||
        if cellData?.isKind(of: HumanCellData.self) == true ||
            //: cellData?.isKind(of: ManufacturerGraphReactiveCompatible.self) == true ||
            cellData?.isKind(of: ManufacturerGraphReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: ExternalPinCellData.self) == true ||
            cellData?.isKind(of: ExternalPinCellData.self) == true ||
            //: cellData?.isKind(of: RepresentReactiveCompatible.self) == true {
            cellData?.isKind(of: RepresentReactiveCompatible.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            InsulatorThen.all(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            InsulatorThen.time(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: EveryBitThen!) -> AcrossViewCell! {
    func capableness(_: ThinkTableViewController!, translate data: EveryBitThen!) -> AcrossViewCell! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = information(data: data)

        //: if data.isKind(of: ExternalPinCellData.self) {
        if data.isKind(of: ExternalPinCellData.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = MaterialRealmMsgCell(style: .default, reuseIdentifier: MaterialRealmMsgCell.className())
            //: let cellData = data as! ExternalPinCellData
            let cellData = data as! ExternalPinCellData

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.outgrowth() {
                //: if cellData.msgModel.quoteMsgInfo.uid == RealmReactiveCompatible.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == RealmReactiveCompatible.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = RealmReactiveCompatible.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = RealmReactiveCompatible.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: ManufacturerGraphReactiveCompatible.self) {
        if data.isKind(of: ManufacturerGraphReactiveCompatible.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = BrowMsgCell(style: .default, reuseIdentifier: BrowMsgCell.className())
            //: let cellData = data as! ManufacturerGraphReactiveCompatible
            let cellData = data as! ManufacturerGraphReactiveCompatible
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: RepresentReactiveCompatible.self) {
        if data.isKind(of: RepresentReactiveCompatible.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = RepresentMsgCell(style: .default, reuseIdentifier: RepresentMsgCell.className())
            //: let cellData = data as! RepresentReactiveCompatible
            let cellData = data as! RepresentReactiveCompatible
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: GeneralCellData.self) {
        if data.isKind(of: GeneralCellData.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = WorkerCollectionView(style: .default, reuseIdentifier: WorkerCollectionView.className())
            //: let cellData = data as! GeneralCellData
            let cellData = data as! GeneralCellData
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: CompositionCellData.self) {
        if data.isKind(of: CompositionCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = CoverThen(style: .default, reuseIdentifier: CoverThen.className())
            //: let cellData = data as! CompositionCellData
            let cellData = data as! CompositionCellData
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = LimitedModelType()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.systemLanguage(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (data_tumbleName.lowercased() + "Gift"))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: HumanCellData.self) {
        if data.isKind(of: HumanCellData.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = JuxtapositionMsgCell(style: .default, reuseIdentifier: JuxtapositionMsgCell.className())
            //: let cellData = data as! HumanCellData
            let cellData = data as! HumanCellData
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: RepresentThen.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = TranslationViewCell(style: .default, reuseIdentifier: TranslationViewCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! RepresentThen
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: TrivialityThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = WidespreadMsgCell(style: .default, reuseIdentifier: WidespreadMsgCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! TrivialityThen
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: OrbitThen.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = ExternalThen(style: .default, reuseIdentifier: ExternalThen.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! OrbitThen
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: AcrossViewCell!) {
    func controller(_: ThinkTableViewController!, want cell: AcrossViewCell!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != CurrencyDefineReactiveCompatible.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != CurrencyDefineReactiveCompatible.facilityBasic() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: BrowReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
            BrowReactiveCompatible.share.builderUid(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: AcrossViewCell!) {
    func telephoneMessageStillTitle(_: ThinkTableViewController!, add cell: AcrossViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: BrowMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! ManufacturerGraphReactiveCompatible
            let imageCellData = cell.messageData as! ManufacturerGraphReactiveCompatible
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = PromiseReactiveCompatible(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.longshot(imageCellData, fragmentToCircle: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.miss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataPagePath)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.streetwiseSystem(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.outsideLine(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                duringNote()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(mainEnabledStr)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = WarnModelType()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = WalkModelType()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [WarnModelType] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = CropFlowLayout(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                duringNote()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: RepresentMsgCell.self) { // 视频
            //: let videoCellData = cell.messageData as! RepresentReactiveCompatible
            let videoCellData = cell.messageData as! RepresentReactiveCompatible
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = LevelViewDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                duringNote()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = ExcitementViewDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.allShrink(videoCellData, circle: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.miss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: dataPagePath)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.streetwiseSystem(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.outsideLine(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                duringNote()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: JuxtapositionMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! JuxtapositionMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! HumanCellData, cellView: audioCell)
            withTurn(cellData: audioCell.cellData as! HumanCellData, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: MaterialRealmMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! MaterialRealmMsgCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = AcrossChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = AcrossChatManager.image(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(notiSendUrl))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(appWalkPath))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(appWalkPath))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(userHorizonMicName.suffix(5)) + "ersonAut" + mainYouFormat.replacingOccurrences(of: "opportunity", with: "h")) {
                    //: BrowReactiveCompatible.share.func__pushUserVerifyController(toast: nil)
                    BrowReactiveCompatible.share.carryToast(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<LawMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: BrowReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    BrowReactiveCompatible.share.lipModel(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(data_blueMessage.suffix(4)) + "gType")] as? String == (String(noti_detectSweetUrl) + String(main_broadcastPath.suffix(4)) + "stom") { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(userShallFormat.prefix(6)) + String(const_mainText))][(String(constShrinkData.prefix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    vocalizationVoice()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    two()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: WidespreadMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! WidespreadMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = AcrossChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = AcrossChatManager.image(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(notiSendUrl))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(appWalkPath))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<LawMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: BrowReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    BrowReactiveCompatible.share.lipModel(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: TranslationViewCell.self) {
            //: BrowReactiveCompatible.share.func__pushToUserDetailVC(uid: targetId)
            BrowReactiveCompatible.share.builderUid(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: AcrossViewCell!) {
    func horizon(_: ThinkTableViewController!, outAll cell: AcrossViewCell!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: PinReactiveCompatible?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: BrowMsgCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! BrowMsgCell
            //: cellData = imageCell.cellData as! ManufacturerGraphReactiveCompatible
            cellData = imageCell.cellData as! ManufacturerGraphReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: RepresentMsgCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! RepresentMsgCell
            //: cellData = videoCell.cellData as! RepresentReactiveCompatible
            cellData = videoCell.cellData as! RepresentReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: JuxtapositionMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! JuxtapositionMsgCell
            //: cellData = audioCell.cellData as! HumanCellData
            cellData = audioCell.cellData as! HumanCellData
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: MaterialRealmMsgCell.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! MaterialRealmMsgCell
            //: cellData = textCell.cellData as! ExternalPinCellData
            cellData = textCell.cellData as! ExternalPinCellData
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: EveryBitThen? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: EveryBitThen? = ExtendReactiveCompatible.alongsideFail(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: RepresentReactiveCompatible.self) {
                if reCellData!.isKind(of: RepresentReactiveCompatible.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    range(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    build(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: EveryBitThen!) {
    func method(_: ThinkTableViewController!, asset cellData: EveryBitThen!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = InsulatorThen.ask(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: EveryBitThen!) {
    func pic(_: ThinkTableViewController!, shrink cellData: EveryBitThen!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        InsulatorThen.duringControlDraft(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: EveryBitThen) -> EveryBitThen {
    private func information(data: EveryBitThen) -> EveryBitThen {
        //: let isSelf = data.msgModel.user.uid == Int(RealmReactiveCompatible.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(RealmReactiveCompatible.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if RealmReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if RealmReactiveCompatible.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = RealmReactiveCompatible.share.loginUserMode.headPic!
                data.msgModel.user.headPic = RealmReactiveCompatible.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = RealmReactiveCompatible.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = RealmReactiveCompatible.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func holder() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (String(mainTopicMessage))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe")): (String(k_arenaSpanStr))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo, String(bytes: notiListenPath.map{flexibleEquipment(huge: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.build(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func used(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: notiOpportunityStr.map{totThem(facility: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: notiOpportunityStr.map{totThem(facility: $0)}, encoding: .utf8)!: content, (String(notiEachTitle.prefix(5))): greetId, String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (String(mainTopicMessage))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: user_plantName.map{latGallery(yesterday: $0)}, encoding: .utf8)!: content, (String(notiEachTitle.prefix(5))): greetId, (noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe")): (kYearListenText.replacingOccurrences(of: "global", with: "T") + String(mainForthContentKey.prefix(5)))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo, String(bytes: notiListenPath.map{flexibleEquipment(huge: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.build(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            LoopTaskDelegate.shared.databaseFor(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.bringHome(showMsg: String(bytes: appReadContent.reversed(), encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        InsulatorThen.builder(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(notiItUrl) + String(show_verticalTitle))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (String(dataMagnitudeerestPath)),
                               //: "audioId": greetId,
                               (String(data_latUrl)): greetId,
                               //: "audioLength": length,
                               String(bytes: showLipAnalysisTitle.map{morningPart(bag: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (appPlayerContent.replacingOccurrences(of: "additional", with: "d") + String(appJudgeWaveStrongFormat.suffix(8))): model.unZipFilePath,
                               //: "senderId": RealmReactiveCompatible.share.loginUserMode.userID]
                               String(bytes: show_holdRunName.map{bodyWeight(main: $0)}, encoding: .utf8)!: RealmReactiveCompatible.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(data_latUrl)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: showLipAnalysisTitle.map{morningPart(bag: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (appPlayerContent.replacingOccurrences(of: "additional", with: "d") + String(appJudgeWaveStrongFormat.suffix(8))): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe")): (String(constInstallationData.suffix(8)))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo, String(bytes: notiListenPath.map{flexibleEquipment(huge: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.build(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func ratingUsed(msgStr: String, quoteModel: ChatModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            bringHome(showMsg: (String(k_baseballId) + user_menuMsg.replacingOccurrences(of: "hypothesis", with: "k") + " mes" + noti_narrowId.replacingOccurrences(of: "incident", with: "sa")).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: notiOpportunityStr.map{totThem(facility: $0)}, encoding: .utf8)!: temStr, String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (String(mainTopicMessage))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: user_plantName.map{latGallery(yesterday: $0)}, encoding: .utf8)!: temStr, (noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe")): (kYearListenText.replacingOccurrences(of: "global", with: "T") + String(mainForthContentKey.prefix(5)))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(show_dogData.replacingOccurrences(of: "ex", with: "ot") + "eMsgId")] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(const_themTitle) + String(mainFindingStr))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: notiListenPath.map{flexibleEquipment(huge: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? ChatModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            build(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func flushSwitchly(img: UIImage, sendModel: TitlelessModel) {
        //: let imagePath = ManufacturerGraphReactiveCompatible.getChatImagePath(image: img)
        let imagePath = ManufacturerGraphReactiveCompatible.joinAcross(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: noti_farText.map{leafBlade(better: $0)}, encoding: .utf8)!: (String(userRoundFormat.suffix(6))), String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (main_prepareMessage.replacingOccurrences(of: "life", with: "m")), String(bytes: noti_describeId.map{eraseMoment(editor: $0)}, encoding: .utf8)!: imagePath, String(bytes: kReceiverData.reversed(), encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(notiActualId.replacingOccurrences(of: "whether", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe")): (String(userRoundFormat.suffix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataHalfUrl.map{cookieSearchion(island: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (main_seeTopicData.replacingOccurrences(of: "instruction", with: "g") + String(data_picturePath)): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo, String(bytes: notiListenPath.map{flexibleEquipment(huge: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            build(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: EveryBitThen?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func range(msgData: EveryBitThen?, sendModel: TitlelessModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: noti_farText.map{leafBlade(better: $0)}, encoding: .utf8)!: (String(appCurrentText) + String(app_speakerFormat.suffix(6))), String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (String(constQuantityUrl)), String(bytes: kReceiverData.reversed(), encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe")): (String(appCurrentText) + String(app_speakerFormat.suffix(6))),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: data_uponName.reversed(), encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: notiTenderName.map{labSchemeThird(birth: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: kDomainUrl.map{rollUser(address: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: dataHalfUrl.map{cookieSearchion(island: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (main_seeTopicData.replacingOccurrences(of: "instruction", with: "g") + String(data_picturePath)): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo, String(bytes: notiListenPath.map{flexibleEquipment(huge: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = ExtendReactiveCompatible.equalAbout(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.limit(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.invite(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.mightinessStatus(cellData, originStill: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.invite(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.handle(dict, method: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        build(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func writtenSystem(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (String(dataMagnitudeerestPath)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: showLipAnalysisTitle.map{morningPart(bag: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (appPlayerContent.replacingOccurrences(of: "additional", with: "d") + String(appJudgeWaveStrongFormat.suffix(8))): voicePath,
                       //: "senderId": RealmReactiveCompatible.share.loginUserMode.userID]
                       String(bytes: show_holdRunName.map{bodyWeight(main: $0)}, encoding: .utf8)!: RealmReactiveCompatible.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(appPlayerContent.replacingOccurrences(of: "additional", with: "d") + String(appJudgeWaveStrongFormat.suffix(8))): voicePath,
                         //: "type": "AudioMsg",
                         (noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe")): (String(constInstallationData.suffix(8))),
                         //: "len": "\(voiceTime)"]
                         String(bytes: app_belowStr.map{squareMobile(date: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo, String(bytes: notiListenPath.map{flexibleEquipment(huge: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            build(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: EveryBitThen, reSend: Bool) {
    private func build(msgData: EveryBitThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bringHome(showMsg: data_coverName)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(showReaderName.suffix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: RepresentReactiveCompatible.self) == false && msgData.isKind(of: CompositionCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: RepresentReactiveCompatible.self) == false, msgData.isKind(of: CompositionCellData.self) == false, params[(noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe"))] as? String != (String(k_arenaSpanStr)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.limit(msgData)
        }
        //: let recordTime = AcrossChatManager.getMessageInsertTime()
        let recordTime = AcrossChatManager.shouldTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((notiActualId.replacingOccurrences(of: "whether", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(notiActualId.replacingOccurrences(of: "whether", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(notiActualId.replacingOccurrences(of: "whether", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(constInstallationData.suffix(8))) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((appPlayerContent.replacingOccurrences(of: "additional", with: "d") + String(appJudgeWaveStrongFormat.suffix(8)))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(appPlayerContent.replacingOccurrences(of: "additional", with: "d") + String(appJudgeWaveStrongFormat.suffix(8)))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (appPlayerContent.replacingOccurrences(of: "additional", with: "d") + String(appJudgeWaveStrongFormat.suffix(8))))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(user_diskNameWhitePath) + String(k_orangeCombineUrl.prefix(4)) + "r/voice/")) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = mainGiftStr + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(appPublishGreenText.prefix(6)) + "nts/" + String(constFollowKey.prefix(5)) + "reco" + String(main_halfName))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = notiSearchMessage + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(data_latUrl))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(dataMagnitudeerestPath))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        ChatThen.a(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(k_arenaSpanStr)) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.remote(msgData, discourse: nil, render: NSNumber(value: 0), tableMsgStatus: .Msg_Status_Fail)
                                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.miss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: appBackgroundStr)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(notiEachTitle.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    InsulatorThen.builder(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.presentation(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(userShallFormat.prefix(6)) + String(const_mainText))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(kDrawContent.suffix(5)) + String(k_consciousnessIncomeDotValue))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(kDrawContent.suffix(5)) + String(k_consciousnessIncomeDotValue))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if SoundCombinedReactiveCompatible.buildShared().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: const_infoWorkName, object: nil, userInfo: [String(bytes: userGeneralText.map{blindPassage(round: $0)}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.vid(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(notiIonId.replacingOccurrences(of: "tap", with: "ss") + String(constRemainKey.suffix(7)))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(k_arenaSpanStr)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!] = (String(userNaturePath))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    dataFilterFormat.psychologicalFeatureTarget(eventID: mainLicenseRouteName)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(k_arenaSpanStr)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!] = (String(mainTopicMessage))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.limit(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(RealmReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(RealmReactiveCompatible.share.loginUserMode.userID)" + (String(dataExposurePerceptNotKey.suffix(6)) + "teCh" + String(const_topFormat.suffix(6)) + "Title" + String(user_failureMsg)) + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if RealmReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.male.rawValue, type != (String(k_arenaSpanStr)) {
                //: self.removeReplyTipsView()
                self.pitch()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.remote(updateMsgData, discourse: msgInfo[(String(noti_anonUrl.suffix(5)))] as? String, render: msgIncome, tableMsgStatus: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            InsulatorThen.time(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(k_sureUrl) + String(const_brightTitle))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(k_sureUrl) + String(const_brightTitle))] as! NSNumber
                //: RealmReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                RealmReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((app_combinedData.replacingOccurrences(of: "rich", with: "h") + "SysMs" + appFilterName.replacingOccurrences(of: "good", with: "g"))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(app_combinedData.replacingOccurrences(of: "rich", with: "h") + "SysMs" + appFilterName.replacingOccurrences(of: "good", with: "g"))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.previous(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(appAdditionLawSpecMessage) + mainListUrl.replacingOccurrences(of: "shine", with: "te") + String(constThresholdStr))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(appAdditionLawSpecMessage) + mainListUrl.replacingOccurrences(of: "shine", with: "te") + String(constThresholdStr))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? AppearanceModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.longshot(updateMsgData, fragmentToCircle: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((String(constQuantityUrl))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(String(constQuantityUrl))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? SessionModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.allShrink(updateMsgData, circle: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        game()

        //: updateSession()
        way()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func way() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        DotInfoManager.stage(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func miss(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard RealmReactiveCompatible.share.loginUserMode.status != 1 else {
            guard RealmReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    bringHome(showMsg: errorStr)
                }
                //: return
                return
            }

            //: BrowReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            BrowReactiveCompatible.share.beContext(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.slight()
            //: RealmReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            RealmReactiveCompatible.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.focus()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ComeOutProgramView(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl))
            //: view.show()
            view.saveShow()
            //: } else if errorCode == 60003, !RealmReactiveCompatible.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !RealmReactiveCompatible.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            EquallyThen.shared.asAlive()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                bringHome(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func outsideLine(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(userShallFormat.prefix(6)) + String(const_mainText))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.vid(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func bringHomeThe() {
        //: if self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.per(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(RealmReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(RealmReactiveCompatible.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (notiExploreKey.array(forKey: data_noId)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.exposurePlayAnimation()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                notiExploreKey.set(intimateCacheData, forKey: data_noId)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func draw(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = AcrossChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = AcrossChatManager.image(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(notiSendUrl))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(userShallFormat.prefix(6)) + String(const_mainText))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(main_peopleTitle.replacingOccurrences(of: "document", with: "o") + "lInt" + showRecoverData.lowercased() + String(main_joinStr))]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.heart(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func vid(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((main_peopleTitle.replacingOccurrences(of: "document", with: "o") + "lInt" + showRecoverData.lowercased() + String(main_joinStr))), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(main_peopleTitle.replacingOccurrences(of: "document", with: "o") + "lInt" + showRecoverData.lowercased() + String(main_joinStr))] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.heart(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func heart(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = DotInfoManager.magnitudery(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.per(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.retell(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.installLayer(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.installLayer(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        DotInfoManager.enablePull(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: showParentPicVisitorMessage.map{surgeryOperation(search: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: kLipPath.map{$0^195}, encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: showTabGiftPath,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension ProgramBannerDelegate: MechanismReactiveCompatible {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func needExceptSecond() {
        //: guard RealmReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.magneticVariation(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func chooseRange(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func beforeCreate(actionType: HoweverNameConvertible) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    replacement()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    dot()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                replacement()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if RealmReactiveCompatible.share.loginUserMode.sex == Gender.female.rawValue {
            if RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if RealmReactiveCompatible.share.loginUserMode.loungePlus {
                if RealmReactiveCompatible.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.evaluateMethod(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                dot()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    groupLabel()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    dot()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                groupLabel()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.academeParadigm(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func dot() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.bringHome(showMsg: String(bytes: kSlowTitle.map{quoteBirth(going: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func materialOpposite(giftModel _: LimitedModelType, giftNum _: String, model _: EquallyLimitedHypothesisHandyJSON) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func theSend(giftModel: LimitedModelType, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        systemLanguage(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: CompositionCellData?, fromStr: String?) {
    func systemLanguage(giftModel: LimitedModelType, giftNum: String, cellData: CompositionCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bringHome(showMsg: data_coverName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(showReaderName.suffix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(main_seeTopicData.replacingOccurrences(of: "instruction", with: "g") + String(data_picturePath))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(mainBrightId))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(k_chemicalUrl))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(constCurrencyFormat.replacingOccurrences(of: "bring", with: "m"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ChatThen.switchicialCompletion(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.miss(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: userPackageText)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.dateNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.streetwiseSystem(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.way()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(userShallFormat.prefix(6)) + String(const_mainText))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((main_peopleTitle.replacingOccurrences(of: "document", with: "o") + "lInt" + showRecoverData.lowercased() + String(main_joinStr))), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(main_peopleTitle.replacingOccurrences(of: "document", with: "o") + "lInt" + showRecoverData.lowercased() + String(main_joinStr))] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.heart(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.insertCreate(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                dataFilterFormat.psychologicalFeatureTarget(eventID: data_partyMicUrl)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func afterModel(giftModel: LimitedModelType, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        dataFilterFormat.psychologicalFeatureTarget(eventID: show_bottomStr)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bringHome(showMsg: data_coverName)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= RealmReactiveCompatible.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            dot()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(String(constEditUrl.suffix(6)) + "ftId")] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(noti_siblingEnhanceUrl.replacingOccurrences(of: "brake", with: "pe"))] = (String(app_shipStr.suffix(6)) + String(kHerFormat.suffix(5)))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (String(userNaturePath))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(userShallFormat.prefix(6)) + String(const_mainText)): msgInfo, String(bytes: notiListenPath.map{flexibleEquipment(huge: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            build(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func doinge(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(constAvailableMessage - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func betweenHeight(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func outsideBlindDog(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.outsideBlindDog(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                viewMethod(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func searchStub() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if PerceptObjectProtocol.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            PerceptObjectProtocol.shared.maxAudio()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func streetwiseSystem(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(userShallFormat.prefix(6)) + String(const_mainText))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!] = (kLearnUrl.replacingOccurrences(of: "quit", with: "t"))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(userShallFormat.prefix(6)) + String(const_mainText))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ExtendReactiveCompatible.equalAbout(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.limit(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kDrawContent.suffix(5)) + String(k_consciousnessIncomeDotValue))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kDrawContent.suffix(5)) + String(k_consciousnessIncomeDotValue))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.remote(cellData, discourse: msgInfoDic[(String(noti_anonUrl.suffix(5)))] as? String, render: msgIncome, tableMsgStatus: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        bequestEnter(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(k_sureUrl) + String(const_brightTitle))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(k_sureUrl) + String(const_brightTitle))] as! NSNumber
            //: RealmReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            RealmReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: RealmReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.moreDown(needReload: false, mf_coin: RealmReactiveCompatible.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func capitalistWithAccumulation() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.screen(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func bequestEnter(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.searched(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func cancel(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.show(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func roundSeek() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func vocalizationVoice() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.missAllowComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.way()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func two() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.containBy { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.way()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    //: @objc private func feedbackBtnClick() {
    @objc private func ping() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = LevelViewController()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        duringNote()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func three() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ExpectationPopView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.likeList(cellTitleList: [(String(main_describeKey.suffix(6)) + kColorData.replacingOccurrences(of: "grade", with: "o")).localized, (String(mainSuperiorStr)).localized, (String(userBlueValue.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .askNameColor()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: BrowReactiveCompatible.share.func__pushToUserDetailVC(uid: self.targetId)
                BrowReactiveCompatible.share.builderUid(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                lipRewardView()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = ImpressionThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.become(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func lipRewardView() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = PrimaryViewDelegate()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.shareEnableSort(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.rearDeal(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func lessDevice() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            viewMethod(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            duringNote()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func transitionHorizon(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = ThanksScreenModel.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.theFailReceive(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func analysis() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(funcUponNotification(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: dataPackageVoiceUrl,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(writer),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: showMineTitle,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func funcUponNotification(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard RealmReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        guard RealmReactiveCompatible.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func writer() {
        //: RealmReactiveCompatible.share.loginUserMode.loungePlus = true
        RealmReactiveCompatible.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func remarkAcrossMsg() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? EveryBitThen else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? EveryBitThen else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if InsulatorThen.needMpAccumulation(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.output(tipStr: (String(user_successfullyId.prefix(6)) + "ead rece" + String(mainDramaMsg)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ProgramBannerDelegate: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func replacement() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        GravityReactiveCompatible.albumAge(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = BeplasterPickTool.soundEarly(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.generate(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func queenSizeMode() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        GravityReactiveCompatible.takeWater(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.generate(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func generate(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
            RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TitlelessModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.flushSwitchly(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = CropFlowLayout(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TitlelessModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.flushSwitchly(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        duringNote()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func groupLabel() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard InsulatorThen.resource(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == FullHashable.male.rawValue ? "He".localized : (String(app_interactionWholePopularStr)).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.bringHome(showMsg: (String(data_actualName.suffix(7)) + k_heartId.replacingOccurrences(of: "sufficient", with: "i") + " too " + String(constWeStr.suffix(5)) + String(const_sceneData.prefix(8))).restateArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.toVisualCommunicationPara()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func toVisualCommunicationPara() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = BeplasterPickTool.soundEarly(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            BeplasterPickTool.completionPopStage(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.compressedWithVideo(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.bringHome(showMsg: (String(const_makeLocalMsg.prefix(5)) + " vide" + String(userCommentUrl.suffix(8)) + String(constSumId.suffix(6)) + "econds").localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func compressedWithVideo(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: RepresentEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = PictureRemainViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = TitlelessModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.range(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        LawReactiveCompatible.minify()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        duringNote()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: EveryBitThen, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func invite(msgData: EveryBitThen, sendModel: TitlelessModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        InconsistentThen.shared.cityCenter(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.bringHome(showMsg: (String(data_boldFormat)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.remote(msgData, discourse: nil, render: NSNumber(value: 0), tableMsgStatus: .Msg_Status_Fail)
                
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.range(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    //: func func__addVoiceNotification() {
    func voice() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(changeState),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(operateBack),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(commemorate(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(infix(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: data_localMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(draw(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: show_methodValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func forMax(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            PerceptObjectProtocol.shared.resignDelegate()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            PerceptObjectProtocol.shared.dig()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func changeState() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        forMax(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func operateBack() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        forMax(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func commemorate(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            forMax(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            forMax(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: HumanCellData, cellView: TalkingChatAudioMsgCell) {
    func withTurn(cellData: HumanCellData, cellView: JuxtapositionMsgCell) {
        //: guard TalkingSocketManager.shared.isFisher == false else {
        guard ManagerCommentThen.shared.isFisher == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.bringHome(showMsg: app_nextKey)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? EveryBitThen)!.isKind(of: HumanCellData.self) == false {
            if (indexModel as? EveryBitThen)!.isKind(of: HumanCellData.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! HumanCellData
            let audioModel = indexModel as! HumanCellData
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = DotInfoManager.messageHolder(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        PerceptObjectProtocol.shared.maxAudio()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if PerceptObjectProtocol.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(constLoseText)) + "\(PerceptObjectProtocol.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if PerceptObjectProtocol.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                PerceptObjectProtocol.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        PerceptObjectProtocol.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        PerceptObjectProtocol.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: EveryBitThen = self.msgView.uiMsgs.object(at: index) as! EveryBitThen
                let subModel: EveryBitThen = self.msgView.uiMsgs.object(at: index) as! EveryBitThen
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: HumanCellData.self) {
                if subModel.isKind(of: HumanCellData.self) {
                    //: let audioModel = subModel as! HumanCellData
                    let audioModel = subModel as! HumanCellData
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = DotInfoManager.messageHolder(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    VoiceFamilyMsgTable.transfer(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        PerceptObjectProtocol.shared.emptyArr(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func thresholdWarning() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.cutSignWrap()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.used(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? EveryBitThen
                let indexModel = model as? EveryBitThen
                //: if indexModel?.isKind(of: HumanCellData.self) == true || indexModel?.isKind(of: ManufacturerGraphReactiveCompatible.self) == true || indexModel?.isKind(of: ExternalPinCellData.self) == true || indexModel?.isKind(of: RepresentReactiveCompatible.self) == true {
                if indexModel?.isKind(of: HumanCellData.self) == true || indexModel?.isKind(of: ManufacturerGraphReactiveCompatible.self) == true || indexModel?.isKind(of: ExternalPinCellData.self) == true || indexModel?.isKind(of: RepresentReactiveCompatible.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.game()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.play()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.remarkAcrossMsg()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func cutSignWrap() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.noneAdvancedFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.perspectiveFrom(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.bringHomeThe()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == RealmReactiveCompatible.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == RealmReactiveCompatible.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == RealmReactiveCompatible.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == RealmReactiveCompatible.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.rearDeal(userWrap)
    }

    //: func func__getFillDraftText() {
    func everyEye() {
        //: let userID = "c2c_"+targetId
        let userID = (String(user_lackKey.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.needExceptSecond()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func beginMain() {
        //: let userID = "c2c_"+targetId
        let userID = (String(user_lackKey.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    //: func func__TipsNotification() {
    func beyondGeneral() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(hostNotification(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: mainBeautyValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func hostNotification(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: TrivialityThen = userinfo[(showBelowData.replacingOccurrences(of: "successfully", with: "m"))] as! TrivialityThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func output(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: notiOpportunityStr.map{totThem(facility: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: user_definitionUrl.map{costSocial(mid: $0)}, encoding: .utf8)!: (String(noti_senseMessage))]
        //: var message: EveryBitThen?
        var message: EveryBitThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = ExtendReactiveCompatible.packet(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ExtendReactiveCompatible.equalAbout(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func play() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        InsulatorThen.cannulate { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.output(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func previous(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.output(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func game() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        InsulatorThen.digitiser(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.output(tipStr: String(bytes: kDistanceKey.map{solidFood(who: $0)}, encoding: .utf8)!.restateArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func infix(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: EveryBitThen = dict?["cellData"] as! EveryBitThen
        let cellData: EveryBitThen = dict?[(main_trackMixPath.replacingOccurrences(of: "delay", with: "e") + "Data")] as! EveryBitThen
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension ProgramBannerDelegate: EtiquetteReactiveCompatible {
    //: func func__setReplyTipsView() {
    func basicView() {
        //: let key =  "\(RealmReactiveCompatible.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(RealmReactiveCompatible.share.loginUserMode.userID)" + (String(dataExposurePerceptNotKey.suffix(6)) + "teCh" + String(const_topFormat.suffix(6)) + "Title" + String(user_failureMsg)) + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.resign()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            putInView(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            pitch()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func messageEx(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        ratingUsed(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        pitch()
    }

    //: func removeReplyTipsView() {
    func pitch() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            putInView(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func transformAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: data_recordPath, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: constCornerDetailUrl - constAvailableMessage, width: dataModeBlockStr, height: constCornerDetailUrl)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: dataModeBlockStr,
                                     //: height: ScreenHeight)
                                     height: constCornerDetailUrl)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func viewMethod(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.currentPositionController() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: MethodViewDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: constCornerDetailUrl - constAvailableMessage,
                                     //: width: ScreenWidth,
                                     width: dataModeBlockStr,
                                     //: height: ScreenHeight)
                                     height: constCornerDetailUrl)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension ProgramBannerDelegate {
    /// 创建UI
    //: private func createUI() {
    private func selectBroadM() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(userSupportName.suffix(6)) + " notif" + show_languageFormat.replacingOccurrences(of: "global", with: "ic")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func exposureRefuse() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? constAvailableMessage : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.currencyLess(width: contentView.width,
                                     //: height: contentView.height,
                                     height: contentView.height,
                                     //: corners: [.topLeft, .topRight],
                                     corners: [.topLeft, .topRight],
                                     //: cornerRadii: .init(width: 8, height: 8))
                                     cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? show_needMessage : mainEnterStr)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + user_bottomDailyKey)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? show_needMessage : mainEnterStr)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + user_bottomDailyKey)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + user_bottomDailyKey)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func pickTotaleraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.lessDevice()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.three()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.beforeCreate(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                dataFilterFormat.psychologicalFeatureTarget(eventID: main_lessKey, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
