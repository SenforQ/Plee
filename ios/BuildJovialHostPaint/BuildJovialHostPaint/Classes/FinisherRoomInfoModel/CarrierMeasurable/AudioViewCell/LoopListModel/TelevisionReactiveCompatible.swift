
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constRiverId:[UInt8] = [0xfa,0xfd,0xfa,0xe7,0xbb,0xf0,0xfc,0xf7,0xf6,0xe1,0xa9,0xba,0xb3,0xfb,0xf2,0xe0,0xb3,0xfd,0xfc,0xe7,0xb3,0xf1,0xf6,0xf6,0xfd,0xb3,0xfa,0xfe,0xe3,0xff,0xf6,0xfe,0xf6,0xfd,0xe7,0xf6,0xf7]

/*: "roomId" :*/
fileprivate let appSpecialMessage:String = "administrative map currentroomId"

/*: "type" :*/
fileprivate let app_trackKey:[UInt8] = [0x2e,0x23,0x2a,0x3f]

/*: "extra" :*/
fileprivate let appMessageDecisionKey:String = "exautomaticallyra"

/*: "jumpKey" :*/
fileprivate let showLowerIndexId:[UInt8] = [0x6f,0x70,0x68,0x75,0x4e,0x60,0x7c]

/*: "Send Gift" :*/
fileprivate let userDiskFormat:[Character] = ["S","e","n","d"," ","G","i","f"]
fileprivate let constFaceLearnCourseTitle:String = "record"

/*: "View info" :*/
fileprivate let noti_driverKey:String = "capture player previousView inf"
fileprivate let dataDifferencePath:String = "O"

/*: "Report" :*/
fileprivate let main_oppositeData:String = "fair childReport"

/*: "uid" :*/
fileprivate let const_policyText:[UInt8] = [0x64,0x69,0x75]

/*: "name" :*/
fileprivate let kYesShadePath:[UInt8] = [0xcc,0xc3,0xcf,0xc7]

private func sendOut(eye num: UInt8) -> UInt8 {
    return num ^ 162
}

/*: "Can't send blank message" :*/
fileprivate let user_slightUrl:String = "Can\'t"
fileprivate let mainBuildPath:String = " "
fileprivate let main_boyName:[Character] = ["s","e","n","d"," ","b","l","a","n","k"," ","m","e","s","s","a","g","e"]

/*: "user" :*/
fileprivate let mainVideoName:String = "unorer"

/*: "content" :*/
fileprivate let data_neverMsg:[UInt8] = [0x20,0x2c,0x2d,0x37,0x26,0x2d,0x37]

private func lovelyOptimistic(accept num: UInt8) -> UInt8 {
    return num ^ 67
}

/*: "msgType" :*/
fileprivate let noti_tooId:[Character] = ["m","s","g","T","y","p","e"]

/*: "txt" :*/
fileprivate let kGravityPurseTitle:String = "persont"

/*: "isGroup" :*/
fileprivate let userInjuryWatchValue:String = "isGroelse violation formal po"
fileprivate let data_arrayName:String = "uboth"

/*: "jumps" :*/
fileprivate let const_lowerFailId:[Character] = ["j","u","m","p","s"]

/*: "message" :*/
fileprivate let dataReaderText:[UInt8] = [0x5b,0x53,0x45,0x45,0x57,0x51,0x53]

private func haveSense(bullet num: UInt8) -> UInt8 {
    return num ^ 54
}

/*: "TxtMsg" :*/
fileprivate let appActionId:String = "other pattern guideTxtMs"
fileprivate let showMinuteMsg:[Character] = ["g"]

/*: "toUid" :*/
fileprivate let showMotionTitle:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let main_keepFormat:[Character] = ["m","s","g","I","n","f","o"]

/*: "params" :*/
fileprivate let const_underBlockName:[UInt8] = [0x5b,0x4a,0x59,0x4a,0x46,0x58]

private func ownerColor(cap num: UInt8) -> UInt8 {
    return num ^ 43
}

/*: "contentType" :*/
fileprivate let notiCalculationMessage:[UInt8] = [0x37,0x3b,0x3a,0x20,0x31,0x3a,0x20,0x0,0x2d,0x24,0x31]

/*: "ImgMsg" :*/
fileprivate let noti_yesColumnBrightUrl:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let showActivityValue:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let appSiblingMsg:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "pic" :*/
fileprivate let appUmberMessage:[Character] = ["p","i","c"]

/*: "msgId" :*/
fileprivate let dataProgramName:String = "msgIdre me photo finding exactly"

/*: "totalMfCoin" :*/
fileprivate let kDiscourseIssueStr:[Character] = ["t","o","t","a","l"]
fileprivate let noti_slowInformContent:[Character] = ["M","f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let show_oddText:[Character] = ["g","i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let data_fillValue:String = "time direct edit distribution quotegiftN"
fileprivate let dataBehindTitle:[Character] = ["u","m"]

/*: "pkgItemsetId" :*/
fileprivate let showAutomaticStr:[Character] = ["p","k","g","I","t","e","m","s","e","t","I"]
fileprivate let notiFindFactorName:String = "fundamental"

/*: "msg" :*/
fileprivate let user_popularNowPictureName:String = "depthsg"

/*: "tips" :*/
fileprivate let show_costFormat:String = "tpows"

/*: "gift" :*/
fileprivate let const_headSpecialHostId:String = "GIFT"

/*: "nickname" :*/
fileprivate let constKeyUrl:String = "niwaterkname"

/*: "headPic" :*/
fileprivate let showBrowPath:[UInt8] = [0x11,0x1c,0x18,0x1d,0x29,0x10,0x1a]

private func thatTemporary(build num: UInt8) -> UInt8 {
    return num ^ 121
}

/*: "All Numbers" :*/
fileprivate let dataRegionStr:[Character] = ["A","l","l"," ","N","u","m"]
fileprivate let main_multipleMsg:[Character] = ["b","e","r","s"]

/*: "sendNum" :*/
fileprivate let constControlValue:[Character] = ["s","e","n","d","N","u"]
fileprivate let mainConversionUrl:String = "sample"

/*: "toUser" :*/
fileprivate let show_givenName:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let app_taFormat:String = "msgIntime ret terms inner"

/*: "isConnection" :*/
fileprivate let appSignatureMsg:String = "isConbury child percept"
fileprivate let mainActionValue:String = "createectiocreate"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TelevisionReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class TelevisionReactiveCompatible: OperateReactiveCompatible {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = NoInfoModel() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = AccessConversationModel() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = NoInfoModel.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constRiverId.map{$0^147}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RealmReactiveCompatible.share.appConfigMode.disableShootScreen {
            if RealmReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = FreshLoopReactiveCompatible(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        myThreshold()
        //: createUIConstraints()
        decision()
        //: bindInteraction()
        termsRange()
        //: addTapGestureRecognizer()
        asSet()
        //: func__chatRoom_insertTipMsg()
        onLineMsg()
        //: chatSettings()
        atManager()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            bringData()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.funcForBandEnableeselfJoin(join: false)
        //: self.destroyTipsTimer()
        self.abovePer()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: ThinkTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = ThinkTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(AudioViewCell.self, forCellReuseIdentifier: AudioViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(BrowMsgCell.self, forCellReuseIdentifier: BrowMsgCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(MankindDocumentViewCell.self, forCellReuseIdentifier: MankindDocumentViewCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(WidespreadMsgCell.self, forCellReuseIdentifier: WidespreadMsgCell.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: AcrossViewCell.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(ObjectMessageCell.self, forCellReuseIdentifier: AcrossViewCell.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: ResponsibilityViewDelegate = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = ResponsibilityViewDelegate(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: MainReactiveCompatible = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = MainReactiveCompatible()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: PocketSizeTrackView = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = PocketSizeTrackView()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: AttitudeView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = AttitudeView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: CapabilityLiteralReactiveCompatible = {
        //: let v = TalkingSomeoneAtMeView()
        let v = CapabilityLiteralReactiveCompatible()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension TelevisionReactiveCompatible {
    /// 群聊设置
    //: private func chatSettings() {
    private func atManager() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        SeekManagingDirectorReactiveCompatible.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        funcForBandEnableeselfJoin(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func funcForBandEnableeselfJoin(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(appSpecialMessage.suffix(6))): self.conversationData.groupID, String(bytes: app_trackKey.map{$0^90}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        ChatThen.buildSignOpen(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - MessageDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension TelevisionReactiveCompatible: MessageDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> EveryBitThen! {
    func ask(_: ThinkTableViewController!, auspicate data: V2TIMMessage!) -> EveryBitThen! {
        //: let dic = AcrossChatManager.parseTXMessageData(data: data.customElem.data)
        let dic = AcrossChatManager.image(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = ExtendReactiveCompatible.usIncomeSet(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: EveryBitThen!) -> AcrossViewCell! {
    func capableness(_: ThinkTableViewController!, translate data: EveryBitThen!) -> AcrossViewCell! {
        //: if data.isKind(of: CollectionCellData.self) {
        if data.isKind(of: CollectionCellData.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = AudioViewCell(style: .default, reuseIdentifier: AudioViewCell.className())
            //: let cellData = data as! CollectionCellData
            let cellData = data as! CollectionCellData
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.via(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.post(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: ManufacturerGraphReactiveCompatible.self) {
        if data.isKind(of: ManufacturerGraphReactiveCompatible.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = BrowMsgCell(style: .default, reuseIdentifier: BrowMsgCell.className())
            //: let cellData = data as! ManufacturerGraphReactiveCompatible
            let cellData = data as! ManufacturerGraphReactiveCompatible
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: RemainCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = MankindDocumentViewCell(style: .default, reuseIdentifier: MankindDocumentViewCell.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! RemainCellData
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: TrivialityThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = WidespreadMsgCell(style: .default, reuseIdentifier: WidespreadMsgCell.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! TrivialityThen
            //: cell.fill(with: cellData)
            cell.beyondDestroy(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: AcrossViewCell!) {
    func controller(_: ThinkTableViewController!, want cell: AcrossViewCell!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(RealmReactiveCompatible.share.loginUid) {
            if userModel.uid == Int(RealmReactiveCompatible.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                via(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                button(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: AcrossViewCell!) {
    func telephoneMessageStillTitle(_: ThinkTableViewController!, add cell: AcrossViewCell!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: BrowMsgCell.self) { // 图片
            //: let imageCellData = cell.messageData as! ManufacturerGraphReactiveCompatible
            let imageCellData = cell.messageData as! ManufacturerGraphReactiveCompatible
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(mainEnabledStr)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = WarnModelType()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = WalkModelType()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [WarnModelType] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = CropFlowLayout(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.currentPositionController()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: AudioViewCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! AudioViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = AcrossChatManager.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = AcrossChatManager.image(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(appMessageDecisionKey.replacingOccurrences(of: "automatically", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: showLowerIndexId.map{$0^5}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<LawMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: BrowReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    BrowReactiveCompatible.share.lipModel(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: WidespreadMsgCell.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! WidespreadMsgCell
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = AcrossChatManager.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = AcrossChatManager.image(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(appMessageDecisionKey.replacingOccurrences(of: "automatically", with: "t"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: showLowerIndexId.map{$0^5}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<LawMeasurable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: BrowReactiveCompatible.share.func__actionPushForMessage(jumpModel: jumpModel)
                    BrowReactiveCompatible.share.lipModel(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: AcrossViewCell!) {
    func horizon(_: ThinkTableViewController!, outAll cell: AcrossViewCell!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        dataWantOn(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension TelevisionReactiveCompatible {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func abovePer() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func button(user: DistantModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ExpectationPopView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.likeList(cellTitleList: [(String(userDiskFormat) + constFaceLearnCourseTitle.replacingOccurrences(of: "record", with: "t")).localized, "@", (String(noti_driverKey.suffix(8)) + dataDifferencePath.lowercased()).localized, (String(main_oppositeData.suffix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.economicAidInput(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.effect(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.via(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.stackUid(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func economicAidInput(user: DistantModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.spread()
        //: let model = TalkingChatRoomMemberModel()
        let model = EquallyLimitedHypothesisHandyJSON()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.shareModel(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func effect(user: DistantModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: const_policyText.reversed(), encoding: .utf8)!: String(user.uid), String(bytes: kYesShadePath.map{sendOut(eye: $0)}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.dict(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func via(uid: Int) {
        //: BrowReactiveCompatible.share.func__pushToUserDetailVC(uid: String(uid))
        BrowReactiveCompatible.share.builderUid(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func stackUid(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = ImpressionThen(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.become(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension TelevisionReactiveCompatible {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func ratingUsed(msgStr: String, quoteModel _: ChatModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            bringHome(showMsg: (user_slightUrl + mainBuildPath.capitalized + String(main_boyName)).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: showLowerIndexId.map{$0^5}, encoding: .utf8)!: (mainVideoName.replacingOccurrences(of: "nor", with: "s")), String(bytes: const_policyText.reversed(), encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: data_neverMsg.map{lovelyOptimistic(accept: $0)}, encoding: .utf8)!: temStr, (String(noti_tooId)): (kGravityPurseTitle.replacingOccurrences(of: "person", with: "tx")), (String(userInjuryWatchValue.prefix(5)) + data_arrayName.replacingOccurrences(of: "both", with: "p")): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(String(const_lowerFailId))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: dataReaderText.map{haveSense(bullet: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: app_trackKey.map{$0^90}, encoding: .utf8)!: (String(appActionId.suffix(5)) + String(showMinuteMsg)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(appSpecialMessage.suffix(6))): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(showMotionTitle))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_keepFormat)): msgInfo, String(bytes: const_underBlockName.map{ownerColor(cap: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            dataWantOn(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func reasonImg(img: UIImage) {
        //: let imagePath = ManufacturerGraphReactiveCompatible.getChatImagePath(image: img)
        let imagePath = ManufacturerGraphReactiveCompatible.joinAcross(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: notiCalculationMessage.map{$0^84}, encoding: .utf8)!: (String(noti_yesColumnBrightUrl)), (String(noti_tooId)): (String(showActivityValue)), String(bytes: appSiblingMsg.reversed(), encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(String(appUmberMessage)): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: app_trackKey.map{$0^90}, encoding: .utf8)!: (String(noti_yesColumnBrightUrl)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(appSpecialMessage.suffix(6))): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(main_keepFormat)): msgInfo, String(bytes: const_underBlockName.map{ownerColor(cap: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ExtendReactiveCompatible.equalAbout(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            dataWantOn(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: EveryBitThen, reSend: Bool) {
    private func dataWantOn(msgData: EveryBitThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bringHome(showMsg: data_coverName)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.limit(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((String(appUmberMessage))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(String(appUmberMessage))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(String(appUmberMessage))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        ChatThen.contriveToCompletion(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.remote(msgData, discourse: nil, render: NSNumber(value: 0), tableMsgStatus: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.number(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(main_keepFormat))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.remote(msgData, discourse: msgInfo[(String(dataProgramName.prefix(5)))] as? String, render: NSNumber(value: 0), tableMsgStatus: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(kDiscourseIssueStr) + String(noti_slowInformContent))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(kDiscourseIssueStr) + String(noti_slowInformContent))] as! NSNumber
                //: RealmReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                RealmReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func number(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard RealmReactiveCompatible.share.loginUserMode.status != 1 else {
            guard RealmReactiveCompatible.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    bringHome(showMsg: errorStr)
                }
                //: return
                return
            }

            //: BrowReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            BrowReactiveCompatible.share.beContext(clickEvent: userPackageText, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.slight()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            pinnerLocal(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ComeOutProgramView(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl))
            //: view.show()
            view.saveShow()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                bringHome(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension TelevisionReactiveCompatible: MechanismReactiveCompatible {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func afterModel(giftModel _: LimitedModelType, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func needExceptSecond() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func chooseRange(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = ExcitementThen()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.dict(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func beforeCreate(actionType: HoweverNameConvertible) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            directBy()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func theSend(giftModel _: LimitedModelType, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func materialOpposite(giftModel: LimitedModelType, giftNum: String, model: EquallyLimitedHypothesisHandyJSON) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            bringHome(showMsg: data_coverName)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(showMotionTitle))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(show_oddText))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(data_fillValue.suffix(5)) + String(dataBehindTitle))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(appSpecialMessage.suffix(6)))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(showAutomaticStr) + notiFindFactorName.replacingOccurrences(of: "fundamental", with: "d"))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        ChatThen.sendChooseStream(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.number(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.dateNum(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.acquisitionPurchase(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func doinge(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func betweenHeight(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.scroll(toBottom: true)
            self.msgView.scroll(toBottom: true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func outsideBlindDog(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.outsideBlindDog(tap)
    }

    //: func func__starRecordBtnDown() {}
    func searchStub() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func writtenSystem(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension TelevisionReactiveCompatible: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func directBy() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        GravityReactiveCompatible.albumAge(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = BeplasterPickTool.soundEarly(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: BrowReactiveCompatible.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                BrowReactiveCompatible.share.currentnessSuccessfully()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.reasonImg(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func bitPositive() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        GravityReactiveCompatible.takeWater(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.reasonImg(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension TelevisionReactiveCompatible {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func samePromptWhite(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(appSpecialMessage.suffix(6)))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension TelevisionReactiveCompatible {
    //: func func__TipsNotification() {
    func brightAcross() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(anonymousEvent(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: mainBeautyValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func anonymousEvent(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: TrivialityThen = userinfo[(user_popularNowPictureName.replacingOccurrences(of: "depth", with: "m"))] as! TrivialityThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func pinnerLocal(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: data_neverMsg.map{lovelyOptimistic(accept: $0)}, encoding: .utf8)!: tipStr, (String(noti_tooId)): (show_costFormat.replacingOccurrences(of: "pow", with: "ip"))]
        //: var message: EveryBitThen?
        var message: EveryBitThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ExtendReactiveCompatible.equalAbout(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.scroll(toBottom: true)
        self.msgView.scroll(toBottom: true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func onLineMsg() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard RealmReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            guard RealmReactiveCompatible.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: RealmReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.pinnerLocal(tipStr: RealmReactiveCompatible.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension TelevisionReactiveCompatible {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func acquisitionPurchase(extral: [String: Any?], model: EquallyLimitedHypothesisHandyJSON, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(main_keepFormat))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(userInjuryWatchValue.prefix(5)) + data_arrayName.replacingOccurrences(of: "both", with: "p"))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(noti_tooId))] = (const_headSpecialHostId.lowercased())
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(main_keepFormat))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: const_policyText.reversed(), encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (constKeyUrl.replacingOccurrences(of: "water", with: "c")): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: showBrowPath.map{thatTemporary(build: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(dataRegionStr) + String(main_multipleMsg)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((const_headSpecialHostId.lowercased())) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(const_headSpecialHostId.lowercased())] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(constKeyUrl.replacingOccurrences(of: "water", with: "c"))] = "\(giftdic[(String(constControlValue) + mainConversionUrl.replacingOccurrences(of: "sample", with: "m"))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(show_givenName))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ExtendReactiveCompatible.equalAbout(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.limit(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(app_taFormat.prefix(5)) + "come")) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(app_taFormat.prefix(5)) + "come")] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.remote(cellData, discourse: msgInfoDic[(String(dataProgramName.prefix(5)))] as? String, render: msgIncome, tableMsgStatus: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        stJohnRiver(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(kDiscourseIssueStr) + String(noti_slowInformContent))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(kDiscourseIssueStr) + String(noti_slowInformContent))] as! NSNumber
            //: RealmReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            RealmReactiveCompatible.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: RealmReactiveCompatible.share.loginUserMode.mf_coin)
            msgInputView.giftView.moreDown(needReload: false, mf_coin: RealmReactiveCompatible.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.salvageInStrongSuit()
    }

    //: func initCachEffectData() {
    func bringData() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.screen(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func stJohnRiver(giftDic: [String: Any], model: EquallyLimitedHypothesisHandyJSON) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.cur(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in WrittenRecordReactiveCompatible.share.response() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [ProgramMeasurable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.mapChin(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.searched(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func switchlyGift(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in WrittenRecordReactiveCompatible.share.response() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [ProgramMeasurable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.mapChin(model: giftModel as! WorkerThanksModelType)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.show(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension TelevisionReactiveCompatible {
    /// 创建UI
    //: private func createUI() {
    private func myThreshold() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func decision() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(mainEnterStr)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(mainEnterStr)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - user_bottomDailyKey)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func termsRange() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = ExcitementThen()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! EveryBitThen).isKind(of: CollectionCellData.self) {
                    if (cellData as! EveryBitThen).isKind(of: CollectionCellData.self) {
                        //: let groupCellData = cellData as! CollectionCellData
                        let groupCellData = cellData as! CollectionCellData
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(dataProgramName.prefix(5)))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: DepictionViewManager.shared.rx
        DepictionViewManager.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(appSignatureMsg.prefix(5)) + mainActionValue.replacingOccurrences(of: "create", with: "n")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
