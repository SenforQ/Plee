
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appCurveNatureTooKey:[UInt8] = [0x5,0x2,0x5,0x18,0x44,0xf,0x3,0x8,0x9,0x1e,0x56,0x45,0x4c,0x4,0xd,0x1f,0x4c,0x2,0x3,0x18,0x4c,0xe,0x9,0x9,0x2,0x4c,0x5,0x1,0x1c,0x0,0x9,0x1,0x9,0x2,0x18,0x9,0x8]

private func peopleMove(jump num: UInt8) -> UInt8 {
    return num ^ 108
}

/*: "male_invite_call_mask" :*/
fileprivate let user_temporaryUrl:String = "until through stage placemale_inv"
fileprivate let constNoPath:String = "ite_canext identity law"

/*: "get json error" :*/
fileprivate let appWhoGreenId:String = "gship"
fileprivate let data_advertisingStr:[Character] = ["t"," ","j","s","o","n"," ","e","r","r","o","r"]

/*: "#FF2348" :*/
fileprivate let main_monthKey:[Character] = ["#","F","F","2","3","4","8"]

/*: "She invites you to call" :*/
fileprivate let mainWindowKey:String = "return prime purse city cutShe i"
fileprivate let app_personName:String = "nvrefte"
fileprivate let noti_elementRequireText:String = "s you alive dimension mini camera produce"

/*: "btn_video_drop_nor" :*/
fileprivate let mainSystemDisagreeMessage:String = "figure strike commendation screenbtn_v"
fileprivate let k_voteData:String = "rop_norseek spring list prompt average"

/*: "male_invite_call_nor" :*/
fileprivate let const_circuitMessage:String = "male_m false any"
fileprivate let const_writingKey:String = "admin think scan contacte_ca"
fileprivate let user_destroyContent:String = "ll_norsort protection now"

/*: "transform.scale" :*/
fileprivate let main_systemPopPath:String = "TRANSFO"
fileprivate let const_adviceText:String = "happy far unlessrm.scale"

/*: "animation" :*/
fileprivate let app_firstFormat:String = "animatiflagn"

/*: "icon_Chat_freetimes" :*/
fileprivate let mainUnableName:[Character] = ["i","c","o","n","_","C"]
fileprivate let userDecideHoneyMessage:String = "hat_heritage yellow"
fileprivate let show_hostPath:String = "tilitees"

/*: "limit" :*/
fileprivate let k_fitFactorPath:[UInt8] = [0xe,0xb,0xf,0xb,0x16]

private func controlLeave(method num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "uid" :*/
fileprivate let showPaintBroadName:[Character] = ["u","i","d"]

/*: "invitePop" :*/
fileprivate let k_mText:String = "INVITE"
fileprivate let appFindVisitorMessage:String = "Popking save pro trade"

/*: "headPic" :*/
fileprivate let app_zzId:String = "plan plan live general employmentheadP"
fileprivate let appHeavyValue:String = "event"

/*: "videoUrl" :*/
fileprivate let dataLightText:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "videoPop" :*/
fileprivate let data_utilizeId:[Character] = ["v","i","d","e","o","P","o"]
fileprivate let appBounceSignalPieceTitle:String = "illegal"

/*: "yyyy-MM-dd" :*/
fileprivate let userLimitedId:[Character] = ["y","y","y","y","-","M","M","-","d"]
fileprivate let userPaintValue:String = "key"

/*: "today" :*/
fileprivate let app_guidanceId:String = "TODAY"

/*: "times" :*/
fileprivate let noti_angleName:[Character] = ["t","i","m","e","s"]

/*: "ShowMaleInviteCallViewDateKey_ :*/
fileprivate let kSongEliteName:String = "ShowMagreen strip"
fileprivate let k_wingMsg:String = "leInvviolation superior success analyze platform"
fileprivate let k_telephoneUrl:String = "sodium day variationllView"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RestrictionThen.swift
//  BuildJovialHostPaint
//
//  Created by DouXiu on 2025/8/21.
//

//: import UIKit
import UIKit

//: class MaleInviteCallView: UIView {
class RestrictionThen: UIView {
    //: var dismissBlock: (() -> Void)?
    var dismissBlock: (() -> Void)?
    //: private var popView: TalkingPopView?
    private var popView: StanceExcitementPopView?
    /// 计时器
    //: private var timeoutTimer: Timer?
    private var timeoutTimer: Timer?
    /// 拨打中背景音乐
    //: private var bgmPlayer: TalkingCallBGMPlayer?
    private var bgmPlayer: GraphSoundReactiveCompatible?
    /// 视频封面
    //: private var myVideoUrl = ""
    private var myVideoUrl = ""
    /// 通话头像
    //: private var callHeadPic = ""
    private var callHeadPic = ""
    /// 用户不在线时，是否换个用户拨打
    //: private var needCallAgain = false
    private var needCallAgain = false

    //: private override init(frame: CGRect) {
    override private init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: self.frame = UIScreen.main.bounds
        self.frame = UIScreen.main.bounds
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appCurveNatureTooKey.map{peopleMove(jump: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy Load

    //: private lazy var bgImgView: UIImageView = {
    private lazy var bgImgView: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.image = UIImage.placeHolderImage(sex: Gender.female.rawValue)
        v.image = UIImage.iii(sex: FullHashable.female.rawValue)
        //: v.contentMode = .scaleAspectFill
        v.contentMode = .scaleAspectFill
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: v.layer.cornerRadius = 16
        v.layer.cornerRadius = 16
        //: v.layer.masksToBounds = true
        v.layer.masksToBounds = true
        //: self.addSubview(v)
        self.addSubview(v)
        //: return v
        return v
        //: }()
    }()

    // 视频播放器
    //: private lazy var videoPlayer: TalkingVideoPlayerManager = {
    private lazy var videoPlayer: AmplePlayListener = {
        //: let player = TalkingVideoPlayerManager()
        let player = AmplePlayListener()
        //: player.setMute(bEnable: true)
        player.appShould(bEnable: true)
        //: return player
        return player
        //: }()
    }()

    //: private lazy var maskTopImgV: UIImageView = {
    private lazy var maskTopImgV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: let img = UIImage.BundleImageNamed(name: "male_invite_call_mask")
        let img = UIImage.adjust(name: (String(user_temporaryUrl.suffix(8)) + String(constNoPath.prefix(6)) + "ll_mask"))
        //: v.image = UIImage(cgImage: img.cgImage!, scale: img.scale, orientation: .down)
        if let cgimage = img.cgImage {
            v.image = UIImage(cgImage: cgimage, scale: img.scale, orientation: .down)
        }
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: bgImgView.addSubview(v)
        bgImgView.addSubview(v)
        //: return v
        return v
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill
        //: bgImgView.addSubview(player)
        bgImgView.addSubview(player)
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: .Live_countdown_bg)
        let url = PortraitJackEffectTool.default.responsibility(type: .Live_countdown_bg)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: player.videoItem = videoItem
                player.videoItem = videoItem
                //: player.startAnimation()
                player.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (appWhoGreenId.replacingOccurrences(of: "ship", with: "e") + String(data_advertisingStr)))
        }
        //: return player
        return player
        //: }()
    }()

    //: private lazy var svgaNumLab: UILabel = {
    private lazy var svgaNumLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.font = UIFont.pingfangMediumFont(fontSize: 20)
        lab.font = UIFont.telescopic(fontSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.textColor = UIColor(hex: "#FF2348")
        lab.textColor = UIColor(hex: (String(main_monthKey)))
        //: bgImgView.addSubview(lab)
        bgImgView.addSubview(lab)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var titleLab: UILabel = {
    private lazy var titleLab: UILabel = {
        //: let label = UILabel()
        let label = UILabel()
        //: label.text = "She invites you to call".localized
        label.text = (String(mainWindowKey.suffix(5)) + app_personName.replacingOccurrences(of: "ref", with: "i") + String(noti_elementRequireText.prefix(6)) + "to call").localized
        //: label.font = UIFont.pingfangMediumFont(fontSize: 18)
        label.font = UIFont.telescopic(fontSize: 18)
        //: label.textColor = .white
        label.textColor = .white
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: bgImgView.addSubview(label)
        bgImgView.addSubview(label)
        //: return label
        return label
        //: }()
    }()

    //: private lazy var maskBottomImgV: UIImageView = {
    private lazy var maskBottomImgV: UIImageView = {
        //: let v = UIImageView()
        let v = UIImageView()
        //: v.image = UIImage.BundleImageNamed(name: "male_invite_call_mask")
        v.image = UIImage.adjust(name: (String(user_temporaryUrl.suffix(8)) + String(constNoPath.prefix(6)) + "ll_mask"))
        //: v.isUserInteractionEnabled = true
        v.isUserInteractionEnabled = true
        //: bgImgView.addSubview(v)
        bgImgView.addSubview(v)
        //: return v
        return v
        //: }()
    }()

    // 挂断/取消 按钮
    //: private lazy var hangupBtn: UIButton = {
    private lazy var hangupBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_video_drop_nor"), for: .normal)
        btn.setImage(UIImage.adjust(name: (String(mainSystemDisagreeMessage.suffix(5)) + "ideo_d" + String(k_voteData.prefix(7)))), for: .normal)
        //: btn.addTarget(self, action: #selector(hangupButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(countOrigin), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: bgImgView.addSubview(btn)
        bgImgView.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    // 接听按钮
    //: private lazy var acceptBtn: UIButton = {
    private lazy var acceptBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "male_invite_call_nor"), for: .normal)
        btn.setImage(UIImage.adjust(name: (String(const_circuitMessage.prefix(5)) + "invit" + String(const_writingKey.suffix(4)) + String(user_destroyContent.prefix(6)))), for: .normal)
        //: btn.addTarget(self, action: #selector(acceptButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(descriptionDown), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        // 添加关键帧动画
        //: let keyAnimation = CAKeyframeAnimation()
        let keyAnimation = CAKeyframeAnimation()
        //: keyAnimation.keyPath = "transform.scale"
        keyAnimation.keyPath = (main_systemPopPath.lowercased() + String(const_adviceText.suffix(8)))
        //: keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
        keyAnimation.values = [NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1)),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 84/70), y: actualWidth(w: 84/70))),
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: actualWidth(w: 84 / 70), y: actualWidth(w: 84 / 70))),
                               //: NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
                               NSValue(cgAffineTransform: CGAffineTransform(scaleX: 1, y: 1))]
        //: keyAnimation.duration = 1.2
        keyAnimation.duration = 1.2
        //: keyAnimation.repeatCount = MAXFLOAT
        keyAnimation.repeatCount = MAXFLOAT
        //: keyAnimation.isRemovedOnCompletion = false
        keyAnimation.isRemovedOnCompletion = false
        //: keyAnimation.fillMode = .both
        keyAnimation.fillMode = .both
        //: keyAnimation.calculationMode = .cubicPaced
        keyAnimation.calculationMode = .cubicPaced
        //: btn.layer.add(keyAnimation, forKey: "animation")
        btn.layer.add(keyAnimation, forKey: (app_firstFormat.replacingOccurrences(of: "flag", with: "o")))
        //: bgImgView.addSubview(btn)
        bgImgView.addSubview(btn)
        //: return btn
        return btn
        //: }()
    }()

    // 免费通话图标
    //: private lazy var freeImg: UIImageView = {
    private lazy var freeImg: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_Chat_freetimes")
        img.image = UIImage.adjust(name: (String(mainUnableName) + String(userDecideHoneyMessage.prefix(4)) + "free" + show_hostPath.replacingOccurrences(of: "lite", with: "m")))
        //: img.isHidden = true
        img.isHidden = true
        //: bgImgView.addSubview(img)
        bgImgView.addSubview(img)
        //: return img
        return img
        //: }()
    }()

    // 通话控件
    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: BrowCompositionMatchDelegate = {
        //: let m = TalkingCallMenuManager()
        let m = BrowCompositionMatchDelegate()
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Load Data

//: extension MaleInviteCallView {
extension RestrictionThen {
    /// 加载视频匹配用户数据
    //: private func loadMatchData(needRefreshUI: Bool) {
    private func accost(needRefreshUI: Bool) {
        //: TalkingQuickVideoManager.req_getMatchV4List(params: ["limit":20]) { succeed, result, errorModel in
        BrowThen.dialogUsed(params: [String(bytes: k_fitFactorPath.map{controlLeave(method: $0)}, encoding: .utf8)!: 20]) { succeed, result, _ in
            //: guard succeed == true, let result = result as? [[String: Any]], !result.isEmpty else {
            guard succeed == true, let result = result as? [[String: Any]], !result.isEmpty else {
                //: self.dismiss()
                self.tableCount()
                //: return
                return
            }
            // 随机选择一个字典
            //: guard let randomDict = result.randomElement() else {
            guard let randomDict = result.randomElement() else {
                //: self.dismiss()
                self.tableCount()
                //: return
                return
            }
            //: self.videoCallManager.uid = randomDict["uid"] as? String ?? ""
            self.videoCallManager.uid = randomDict[(String(showPaintBroadName))] as? String ?? ""
            //: self.videoCallManager.source = "invitePop"
            self.videoCallManager.source = (k_mText.lowercased() + String(appFindVisitorMessage.prefix(3)))

            //: if needRefreshUI == true {
            if needRefreshUI == true { // 更新当前用户信息，刷新UI
                //: self.callHeadPic = randomDict["headPic"] as? String ?? ""
                self.callHeadPic = randomDict[(String(app_zzId.suffix(5)) + appHeavyValue.replacingOccurrences(of: "event", with: "ic"))] as? String ?? ""
                //: self.myVideoUrl = randomDict["videoUrl"] as? String ?? ""
                self.myVideoUrl = randomDict[(String(dataLightText))] as? String ?? ""
                //: self.refreshUI()
                self.stepError()

                //: } else {
            } else { // 直接拨打
                //: self.callVideo()
                self.positiveDistance()
            }
        }
    }
}

// MARK: - Event

//: extension MaleInviteCallView {
extension RestrictionThen {
    /// 展示弹窗
    /// - Parameters:
    ///   - uid: 用户ID
    ///   - videoUrl: 视频封面
    ///   - headPic: 头像
    //: func show(uid: String, videoUrl: String, headPic: String) {
    func priceLibrary(uid: String, videoUrl: String, headPic: String) {
        // 记录一次展示次数
        //: MaleInviteCallView.writeMaleInviteCallViewTimes()
        RestrictionThen.step()

        //: if !uid.isEmpty {
        if !uid.isEmpty { // 直接展示，没有再取匹配池
            //: videoCallManager.uid = uid
            videoCallManager.uid = uid
            //: videoCallManager.source = "videoPop"
            videoCallManager.source = (String(data_utilizeId) + appBounceSignalPieceTitle.replacingOccurrences(of: "illegal", with: "p"))
            //: callHeadPic = headPic
            callHeadPic = headPic
            //: myVideoUrl = videoUrl
            myVideoUrl = videoUrl
            //: needCallAgain = true
            needCallAgain = true
            //: refreshUI()
            stepError()

            //: } else {
        } else {
            // 直接取匹配池
            //: loadMatchData(needRefreshUI: true)
            accost(needRefreshUI: true)
        }
    }

    /// 拨打视频通话
    //: private func callVideo() {
    private func positiveDistance() {
        //: videoCallManager.checkAndCallVideo()
        videoCallManager.containBy()
        //: videoCallManager.onReqestCallErrorBlock = { [weak self] errNo in
        videoCallManager.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self, needCallAgain == true, errNo == 250822 else {
            guard let self = self, needCallAgain == true, errNo == 250_822 else {
                //: self?.dismiss()
                self?.tableCount()
                //: return
                return
            }
            //: self.needCallAgain = false
            self.needCallAgain = false
            //: self.loadMatchData(needRefreshUI: false)
            self.accost(needRefreshUI: false)
        }
    }

    /// 取消通话按钮点击事件
    //: @objc private func hangupButtonClick() {
    @objc private func countOrigin() {
        //: dismiss()
        tableCount()
    }

    /// 接听按钮点击事件
    //: @objc private func acceptButtonClick() {
    @objc private func descriptionDown() {
        //: self.callVideo()
        self.positiveDistance()
    }

    //: @objc private func dismiss() {
    @objc private func tableCount() {
        //: dismissBlock?()
        dismissBlock?()
        //: popView?.dismissView()
        popView?.destination()
        //: popView = nil
        popView = nil
        //: timeoutTimer?.invalidate()
        timeoutTimer?.invalidate()
        //: timeoutTimer = nil
        timeoutTimer = nil
        //: stopBGM()
        con()
    }
    
    func closeView() {
        tableCount()
    }
}

// MARK: - 控制每日展示的Key

//: extension MaleInviteCallView {
extension RestrictionThen {
    /// 检测是否可以展示邀请视频
    /// - Returns: 检测结果
    //: class func checkShowMaleInviteCallView() -> Bool {
    class func backBy() -> Bool {
        //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
        let today = NSDate.boldStatus(date: Date(), dateFormat: (String(userLimitedId) + userPaintValue.replacingOccurrences(of: "key", with: "d")))
        //: let dict = Defaults.dictionary(forKey: MaleInviteCallView.getMaleInviteCallViewKey())
        let dict = notiExploreKey.dictionary(forKey: RestrictionThen.since())
        //: let storedDate = dict?["today"] as? String
        let storedDate = dict?[(app_guidanceId.lowercased())] as? String
        //: var times = dict?["times"] as? Int ?? 0
        var times = dict?[(String(noti_angleName))] as? Int ?? 0
        //: if dict == nil || storedDate != today {
        if dict == nil || storedDate != today {
            //: times = 0
            times = 0
        }
        //: if times >= RealmReactiveCompatible.share.appUserConfigMode.inviteTimes {
        if times >= RealmReactiveCompatible.share.appUserConfigMode.inviteTimes {
            //: return false
            return false
        }
        //: return true
        return true
    }

    /// 消耗当日一次弹窗额度
    //: class private func writeMaleInviteCallViewTimes() {
    private class func step() {
        //: let key = MaleInviteCallView.getMaleInviteCallViewKey()
        let key = RestrictionThen.since()
        //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
        let today = NSDate.boldStatus(date: Date(), dateFormat: (String(userLimitedId) + userPaintValue.replacingOccurrences(of: "key", with: "d")))
        //: var dict = Defaults.dictionary(forKey: key) ?? [:]
        var dict = notiExploreKey.dictionary(forKey: key) ?? [:]
        //: let storedDate = dict["today"] as? String
        let storedDate = dict[(app_guidanceId.lowercased())] as? String
        //: var times = dict["times"] as? Int ?? 0
        var times = dict[(String(noti_angleName))] as? Int ?? 0
        //: if storedDate != today {
        if storedDate != today {
            //: times = 0
            times = 0
        }
        //: dict["today"] = today
        dict[(app_guidanceId.lowercased())] = today
        //: dict["times"] = times + 1
        dict[(String(noti_angleName))] = times + 1
        //: Defaults.set(dict, forKey: key)
        notiExploreKey.set(dict, forKey: key)
    }

    /// 展示邀请视频弹窗的Key
    //: class private func getMaleInviteCallViewKey() -> String {
    private class func since() -> String {
        //: return "ShowMaleInviteCallViewDateKey_\(RealmReactiveCompatible.share.loginUserMode.userID)"
        return (String(kSongEliteName.prefix(6)) + String(k_wingMsg.prefix(5)) + "iteCa" + String(k_telephoneUrl.suffix(6)) + "DateKey_") + "\(RealmReactiveCompatible.share.loginUserMode.userID)"
    }
}

// MARK: - 通话音乐

//: extension MaleInviteCallView {
extension RestrictionThen {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMAndStartPlayer() {
    private func submit() {
        //: self.bgmPlayer = TalkingCallBGMPlayer.callBGMPlayer(type: .invitePop)
        self.bgmPlayer = GraphSoundReactiveCompatible.getableCos(type: .invitePop)
        //: self.bgmPlayer?.play()
        self.bgmPlayer?.quotePlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func con() {
        //: guard self.bgmPlayer != nil else { return }
        guard self.bgmPlayer != nil else { return }
        //: self.bgmPlayer?.stop()
        self.bgmPlayer?.referee()
        //: self.bgmPlayer = nil
        self.bgmPlayer = nil
    }
}

// MARK: - EntrywayReactiveCompatible【视频播放器】

//: extension MaleInviteCallView: TalkingVideoPlayerDelegate {
extension RestrictionThen: EntrywayReactiveCompatible {
    //: func func__playerStatus(player: TalkingVideoPlayerManager, status: VideoPlayerStatus) {
    func statusWill(player _: AmplePlayListener, status: PatternPlayerStatus) {
        //: if status == .Playing {
        if status == .Playing {
            //: self.videoPlayer.setRenderMode(renderMode: .FILL_SCREEN)
            self.videoPlayer.spaceMode(renderMode: .FILL_SCREEN)
        }
    }

    //: func func__playerDuration(player: TalkingVideoPlayerManager, duration: Int, currentTime: Int) {}
    func tabStep(player _: AmplePlayListener, duration _: Int, currentTime _: Int) {}

    //: func func__playerProgress(player: TalkingVideoPlayerManager, progress: CGFloat) {}
    func writeChoice(player _: AmplePlayListener, progress _: CGFloat) {}
}

// MARK: - Layout

//: extension MaleInviteCallView {
extension RestrictionThen {
    /// 刷新UI
    //: private func refreshUI() {
    private func stepError() {
        //: if self.popView == nil {
        if self.popView == nil {
            //: self.popView = TalkingPopView.init(frame: UIScreen.main.bounds)
            self.popView = StanceExcitementPopView(frame: UIScreen.main.bounds)
            //: self.popView?.initWithView(view: self)
            self.popView?.ane(view: self)
            //: self.popView?.showInView(view: CurrencyDefineReactiveCompatible.getWindow())
            self.popView?.footnote(view: CurrencyDefineReactiveCompatible.stripElement())
            //: self.popView?.isRemoveTapGes = true
            self.popView?.isRemoveTapGes = true
        }
        //: setupSubViewsConstraint()
        belowShould()
        //: if timeoutTimer == nil {
        if timeoutTimer == nil {
            //: var countdown = 30
            var countdown = 30
            //: timeoutTimer = Timer.scheduledTimer(withTimeInterval: 1, repeats: true, block: { [weak self] _ in
            timeoutTimer = Timer.scheduledTimer(withTimeInterval: 1, repeats: true, block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.svgaNumLab.text = "\(countdown)"
                self.svgaNumLab.text = "\(countdown)"
                //: countdown -= 1
                countdown -= 1
                //: if countdown <= 0 {
                if countdown <= 0 {
                    //: self.dismiss()
                    self.tableCount()
                }
                //: })
            })
            //: timeoutTimer?.fire()
            timeoutTimer?.fire()
        }
        //: setUpBGMAndStartPlayer()
        submit()
        //: bgImgView.setUrlImage(urlStr: callHeadPic)
        bgImgView.noneAdvancedFinish(urlStr: callHeadPic)

        //: if !myVideoUrl.isEmpty {
        if !myVideoUrl.isEmpty {
            //: videoPlayer.delegate = self
            videoPlayer.delegate = self
            //: videoPlayer.playerWithUrlAndVideoView(url: self.myVideoUrl, view: self.bgImgView)
            videoPlayer.midnight(url: self.myVideoUrl, view: self.bgImgView)
            //: } else {
        } else {
            //: videoPlayer.stopPlay()
            videoPlayer.indicator()
            //: videoPlayer.removeVideoWidget()
            videoPlayer.bot()
        }

        //: hangupBtn.isHidden = false
        hangupBtn.isHidden = false
        //: acceptBtn.isHidden = false
        acceptBtn.isHidden = false
        //: freeImg.isHidden = !(RealmReactiveCompatible.share.loginUserMode.freeCallTimes > 0)
        freeImg.isHidden = !(RealmReactiveCompatible.share.loginUserMode.freeCallTimes > 0)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func belowShould() {
        //: bgImgView.snp.remakeConstraints { make in
        bgImgView.snp.remakeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 279), height: actualWidth(w: 346)))
            make.size.equalTo(CGSize(width: actualWidth(w: 279), height: actualWidth(w: 346)))
        }

        //: maskTopImgV.snp.remakeConstraints { make in
        maskTopImgV.snp.remakeConstraints { make in
            //: make.leading.trailing.top.equalToSuperview()
            make.leading.trailing.top.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 70))
            make.height.equalTo(actualWidth(w: 70))
        }

        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.top.equalTo(actualWidth(w: 20))
            make.leading.top.equalTo(actualWidth(w: 20))
            //: make.width.height.equalTo(40)
            make.width.height.equalTo(40)
        }

        //: svgaNumLab.snp.makeConstraints { make in
        svgaNumLab.snp.makeConstraints { make in
            //: make.center.width.height.equalTo(svgaPlayer)
            make.center.width.height.equalTo(svgaPlayer)
        }

        //: titleLab.snp.remakeConstraints { make in
        titleLab.snp.remakeConstraints { make in
            //: make.centerY.equalTo(svgaPlayer)
            make.centerY.equalTo(svgaPlayer)
            //: make.leading.equalTo(svgaPlayer.snp.trailing).offset(actualWidth(w: 10))
            make.leading.equalTo(svgaPlayer.snp.trailing).offset(actualWidth(w: 10))
            //: make.trailing.equalTo(-actualWidth(w: 20))
            make.trailing.equalTo(-actualWidth(w: 20))
        }

        //: maskBottomImgV.snp.remakeConstraints { make in
        maskBottomImgV.snp.remakeConstraints { make in
            //: make.leading.trailing.bottom.equalToSuperview()
            make.leading.trailing.bottom.equalToSuperview()
            //: make.height.equalTo(maskTopImgV)
            make.height.equalTo(maskTopImgV)
        }

        //: hangupBtn.snp.remakeConstraints { make in
        hangupBtn.snp.remakeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 20))
            make.leading.equalTo(actualWidth(w: 20))
            //: make.bottom.equalTo(-actualWidth(w: 40))
            make.bottom.equalTo(-actualWidth(w: 40))
            //: make.size.equalTo(actualWidth(w: 56))
            make.size.equalTo(actualWidth(w: 56))
        }

        //: acceptBtn.snp.remakeConstraints { make in
        acceptBtn.snp.remakeConstraints { make in
            //: make.trailing.equalTo(-actualWidth(w: 20))
            make.trailing.equalTo(-actualWidth(w: 20))
            //: make.centerY.equalTo(hangupBtn)
            make.centerY.equalTo(hangupBtn)
            //: make.size.equalTo(actualWidth(w: 70))
            make.size.equalTo(actualWidth(w: 70))
        }

        //: freeImg.snp.makeConstraints { make in
        freeImg.snp.makeConstraints { make in
            //: make.trailing.equalTo(acceptBtn).offset(actualWidth(w: 2))
            make.trailing.equalTo(acceptBtn).offset(actualWidth(w: 2))
            //: make.top.equalTo(acceptBtn).offset(actualWidth(w: -8))
            make.top.equalTo(acceptBtn).offset(actualWidth(w: -8))
            //: make.size.equalTo(CGSize(width: actualWidth(w: 41), height: actualWidth(w: 28)))
            make.size.equalTo(CGSize(width: actualWidth(w: 41), height: actualWidth(w: 28)))
        }
    }
}
