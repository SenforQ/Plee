
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiScheduleId:[UInt8] = [0xbd,0xba,0xbd,0xa0,0xfc,0xb7,0xbb,0xb0,0xb1,0xa6,0xee,0xfd,0xf4,0xbc,0xb5,0xa7,0xf4,0xba,0xbb,0xa0,0xf4,0xb6,0xb1,0xb1,0xba,0xf4,0xbd,0xb9,0xa4,0xb8,0xb1,0xb9,0xb1,0xba,0xa0,0xb1,0xb0]

private func pushFactor(res num: UInt8) -> UInt8 {
    return num ^ 212
}

/*: "uid" :*/
fileprivate let appCountMsg:[UInt8] = [0x65,0x59,0x54]

fileprivate func requiredExecute(destroy num: UInt8) -> UInt8 {
    let value = Int(num) + 16
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let user_alongsideStarName:String = "headPiceastern something"

/*: "nickname" :*/
fileprivate let constExecuteSpreadText:[Character] = ["n","i","c","k","n","a"]
fileprivate let data_describePlatMessage:String = "mrestriction"

/*: "birthday" :*/
fileprivate let showWindowSearchData:String = "birthdaaware"

/*: "videoUrl" :*/
fileprivate let appSpeakStr:String = "videoUauto character fundamental nine"
fileprivate let data_applyText:String = "sound"

/*: "cover" :*/
fileprivate let kLicenseMessage:String = "cowayr"

/*: "location" :*/
fileprivate let dataOnMsg:String = "locaoperativeion"

/*: "cmd" :*/
fileprivate let const_thanksText:[UInt8] = [0xc9,0xc7,0xce]

/*: "refuseCall" :*/
fileprivate let k_averageTitle:String = "why limited luck monthrefuseCal"
fileprivate let kTradePath:[Character] = ["l"]

/*: "data" :*/
fileprivate let user_stillMsg:[UInt8] = [0x61,0x74,0x61,0x64]

/*: "Call interruption" :*/
fileprivate let constCommonFormat:[Character] = ["C","a","l","l"," ","i","n","t","e","r","r","u","p","t","i"]
fileprivate let dataTooPath:[Character] = ["o","n"]

/*: "stopCall" :*/
fileprivate let userConsciousnessTitle:[Character] = ["s","t","o","p","C","a"]
fileprivate let appResistanceMatchName:[Character] = ["l","l"]

/*: "logId" :*/
fileprivate let const_sessionUrl:[UInt8] = [0xb4,0xb7,0xbf,0x91,0xbc]

private func lotsNigh(closely num: UInt8) -> UInt8 {
    return num ^ 216
}

/*: "cancelCall" :*/
fileprivate let constArcSiblingTitle:[Character] = ["c","a","n","c","e","l","C","a"]
fileprivate let k_wholeId:[Character] = ["l","l"]

/*: "No answer" :*/
fileprivate let data_personalMsg:String = "slide block quality boyNo ans"
fileprivate let notiServiceGirlFormat:[Character] = ["w","e","r"]

/*: "duration" :*/
fileprivate let main_outerUrl:String = "durfirm"

/*: "She has hung up" :*/
fileprivate let user_photoId:[Character] = ["S","h","e"," ","h","a"]
fileprivate let kNorValue:String = "s hufood pan"

/*: "cancelSex" :*/
fileprivate let app_identifyStr:[Character] = ["c"]
fileprivate let app_effKey:[Character] = ["a","n","c","e","l","S","e","x"]

/*: "He has hung up" :*/
fileprivate let notiCommunicateName:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let mainEarnFormat:String = "endTyppermission convert indicator"
fileprivate let k_deliverRiverAmpleId:String = "E"

/*: "stopSex" :*/
fileprivate let notiAspectText:[Character] = ["s","t","o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let showSureUrl:[UInt8] = [0xc4,0xe3,0xfe,0xf8,0xeb,0xeb,0xe4,0xee,0xe4,0xe8,0xe3,0xf9,0xad,0xea,0xe2,0xe1,0xe9,0xad,0xee,0xe2,0xe4,0xe3,0xfe,0xa1,0xad,0xee,0xec,0xe1,0xe1,0xad,0xe5,0xec,0xfe,0xad,0xe8,0xe3,0xe9,0xe8,0xe9]

private func placeBirth(clearly num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "The other hung up the phone" :*/
fileprivate let show_heartHiddenName:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"]
fileprivate let dataHeavyUrl:[Character] = [" ","u","p"," ","t"]
fileprivate let main_perceptViaValue:String = "television simultaneously telephone extent backhe p"

/*: "type" :*/
fileprivate let show_throughShipValue:String = "tjawpe"

/*: "jumpUrl" :*/
fileprivate let constOperateHandBrakeTitle:String = "standard executive never aspect nervousjumpUrl"

/*: "Cancel" :*/
fileprivate let show_listenId:String = "Cancethin key score"
fileprivate let dataIndicatorMsg:String = "L"

/*: "Get gold coins" :*/
fileprivate let noti_nobodyHiddenValue:String = "dit ear heritage exceptGet gold"
fileprivate let data_sizeStr:[Character] = [" ","c","o","i","n","s"]

/*: "addBean" :*/
fileprivate let data_serveUrl:String = "art remote zone translationaddBean"

/*: "refuseSex" :*/
fileprivate let kLipKey:[Character] = ["r","e","f","u","s"]
fileprivate let constReceiveMessage:[Character] = ["e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let showMotionAspectText:String = "thin replace highlight ageonAcce"
fileprivate let showMinFormat:[Character] = ["p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let constMessageSeriesUrl:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let main_earnUrl:[Character] = ["C","a","l","l","s"," ","a","r","e"," "]
fileprivate let kPlayerFormat:String = "cancelletrade"

/*: "The call has ended" :*/
fileprivate let main_partyTimingUrl:String = "reason extension lots promptThe "
fileprivate let showCapabilityValue:String = "call hcoordinate province every"
fileprivate let show_exploreData:String = "nscheduleeschedule"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CrosswaysViewController.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let dataNearbyLiveTitle = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class CrosswaysViewController: OperateReactiveCompatible {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: CanvasChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: GraphSoundReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: BiochemicalMechanismMiniView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: HumanScreenView? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ManagerCommentThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ManagerCommentThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ManagerCommentThen.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiScheduleId.map{pushFactor(res: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! ControllerThen
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.pureQuality()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            induce()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            mic(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currFisherVC = nil
            ManagerCommentThen.shared.currFisherVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.minimize()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        ego()
        //: setupSubViewsConstraint()
        objectLaunchAvailable()
        //: initNotificationObserver()
        brightFore()
        //: req_callGetUserInfo()
        operation()
        //: timeoutRules()
        beforeFor()
        //: setUpBGMPlayer()
        footballPlayer()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.tableEach()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RealmReactiveCompatible.share.appConfigMode.disableShootScreen {
            if RealmReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = FreshLoopReactiveCompatible(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isFisher = false
        ManagerCommentThen.shared.isFisher = false
        //: TalkingSocketManager.shared.isCalling = false
        ManagerCommentThen.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.mononuclearPhagocyteSystem()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: LoopVideoView = {
        //: let v = TalkingBGVideoView()
        let v = LoopVideoView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.turnWith(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: WarnMainView = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = WarnMainView(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: CollectionRecognizerDelegate = {
        //: let v = TalkingVideoWindowView()
        let v = CollectionRecognizerDelegate()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = dataModeBlockStr - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if RepresentLanguageManager.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: constSoundTabStr + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.changeMinimize()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: PortraitExcitementReactiveCompatible = {
        //: let m = TalkingVideoChatManager()
        let m = PortraitExcitementReactiveCompatible()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension CrosswaysViewController {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func operation() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        PortraitExcitementReactiveCompatible.paraCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: appCountMsg.map{requiredExecute(destroy: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(user_alongsideStarName.prefix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(constExecuteSpreadText) + data_describePlatMessage.replacingOccurrences(of: "restriction", with: "e"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(showWindowSearchData.replacingOccurrences(of: "aware", with: "y"))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(appSpeakStr.prefix(6)) + data_applyText.replacingOccurrences(of: "sound", with: "rl"))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(kLicenseMessage.replacingOccurrences(of: "way", with: "ve"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(dataOnMsg.replacingOccurrences(of: "operative", with: "t"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.planUsed()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.fixedStarTarget()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func fixedStarTarget() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = RoundTelevisionCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = RoundTelevisionCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.pry(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.writtenAdd(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension CrosswaysViewController {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func induce() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        GravityReactiveCompatible.nearStatusMicrophone { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.bringHome(showMsg: dataLineKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(k_averageTitle.suffix(9)) + String(kTradePath)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: appCountMsg.map{requiredExecute(destroy: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.changeShape()
                //: return
                return
            }

            //: self.startPreview()
            self.tableEach()
            //: self.stopBGM()
            self.nightContainerLink()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = GraphSoundReactiveCompatible.getableCos(type: .Response)
            //: self.startBGM()
            self.pureQuality()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func tableEach() {
        //: self.videoManager.startPreview()
        self.videoManager.tetrasodiumPyrophosphate()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func mononuclearPhagocyteSystem() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.nightContainerLink()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        MultiPossibilityDanmuManager.animal()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ManagerCommentThen.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ManagerCommentThen.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ManagerCommentThen.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func changeShape(animated: Bool = true) {
        //: self.stopBGM()
        self.nightContainerLink()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func changeMinimize() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.writeLanguage()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.fixedStarTarget()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func caption() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.rateOfPublish()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.fixedStarTarget()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.however()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func however() {
        //: guard RealmReactiveCompatible.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard RealmReactiveCompatible.share.loginUserMode.sex == FullHashable.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = HumanScreenView()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.shrinkInmate()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension CrosswaysViewController {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func brightFore() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(reduce(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(picture(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: const_depthTitle,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(costTo(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: app_networkKey,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        dataNearbyLiveTitle.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func reduce(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            changeShape()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func costTo(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ManagerCommentThen.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ManagerCommentThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ManagerCommentThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ManagerCommentThen.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.whenRequire()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func picture(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.fixedStarTarget()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension CrosswaysViewController: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.bringHome(showMsg: (String(constCommonFormat) + String(dataTooPath)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(userConsciousnessTitle) + String(appResistanceMatchName)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: const_sessionUrl.map{lotsNigh(closely: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(constArcSiblingTitle) + String(k_wholeId)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: const_sessionUrl.map{lotsNigh(closely: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(k_averageTitle.suffix(9)) + String(kTradePath)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: appCountMsg.map{requiredExecute(destroy: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.requireEnd()
                //: TalkingSocketManager.shared.currFisherVC = nil
                ManagerCommentThen.shared.currFisherVC = nil
            }
            //: popCurrentViewController()
            changeShape()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension CrosswaysViewController {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func footballPlayer() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DocumentCountLiteral.Call : DocumentCountLiteral.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = GraphSoundReactiveCompatible.getableCos(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func pureQuality() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.quotePlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func nightContainerLink() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.referee()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension CrosswaysViewController {
    /// 超时定时器
    //: private func timeoutRules() {
    private func beforeFor() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? dataLocationMsg : self.chatModel?.licensingFee()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            videoResistance()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.videoResistance()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func videoResistance() {
        //: guard !TalkingSocketManager.shared.isFisher else { return }
        guard !ManagerCommentThen.shared.isFisher else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.bringHome(showMsg: (String(data_personalMsg.suffix(6)) + String(notiServiceGirlFormat)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(constArcSiblingTitle) + String(k_wholeId)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: const_sessionUrl.map{lotsNigh(closely: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(k_averageTitle.suffix(9)) + String(kTradePath)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: appCountMsg.map{requiredExecute(destroy: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: socketDict)
        //: popCurrentViewController()
        changeShape()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension CrosswaysViewController {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func broad(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: const_sessionUrl.map{lotsNigh(closely: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(main_outerUrl.replacingOccurrences(of: "firm", with: "a") + "tion")].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.materialAll(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.awakeBetween()
            //: createTalkRoom()
            caption()
            //: self.stopBGM()
            self.nightContainerLink()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func mic(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.writeLanguage()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.turnWith(show: false)
        //: self.stopBGM()
        self.nightContainerLink()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.awakeBetween()
        //: self.createTalkRoom()
        self.caption()
    }
}

// MARK: - ExistingObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension CrosswaysViewController: ExistingObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func labelToday(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(user_photoId) + String(kNorValue.prefix(4)) + "ng up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(app_identifyStr) + String(app_effKey))].stringValue == FullHashable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(notiCommunicateName)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bringHome(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.requireEnd()
            //: TalkingSocketManager.shared.currFisherVC = nil
            ManagerCommentThen.shared.currFisherVC = nil
        }

        //: popCurrentViewController()
        changeShape()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func beautyEndLibrary(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        mic(logId: json[String(bytes: const_sessionUrl.map{lotsNigh(closely: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func doHighlight(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(mainEarnFormat.prefix(6)) + k_deliverRiverAmpleId.lowercased())].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(user_photoId) + String(kNorValue.prefix(4)) + "ng up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(notiAspectText))].stringValue == FullHashable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(notiCommunicateName)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: showSureUrl.map{placeBirth(clearly: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(show_heartHiddenName) + String(dataHeavyUrl) + String(main_perceptViaValue.suffix(4)) + "hone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bringHome(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.requireEnd()
            //: TalkingSocketManager.shared.currFisherVC = nil
            ManagerCommentThen.shared.currFisherVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.stillPerspectiveQuote()
        //: popCurrentViewController()
        changeShape()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func drownData(msg: String, data: [String: Any]) {
        //: guard RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(show_throughShipValue.replacingOccurrences(of: "jaw", with: "y"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(constOperateHandBrakeTitle.suffix(7)))] as? String else { return }
            //: let httpsUrl = RealmReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = RealmReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = BarbConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: BrowReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            BrowReactiveCompatible.share.blade(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            StuffReactiveCompatible.heading(title: nil,
                                     //: message: msg,
                                     message: msg,
                                     //: leftBtnTitle: "Cancel".localized,
                                     leftBtnTitle: (String(show_listenId.prefix(5)) + dataIndicatorMsg.lowercased()).localized,
                                     //: rightBtnTitle: "Get gold coins".localized) {
                                     rightBtnTitle: (String(noti_nobodyHiddenValue.suffix(8)) + String(data_sizeStr)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                StuffReactiveCompatible.outFeedback()
                //: } rightBlock: {
            } rightBlock: {
                //: BrowReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                BrowReactiveCompatible.share.beContext(clickEvent: show_homeUserFormat, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func monthOf(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.humanBeings(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func nativeData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(data_serveUrl.suffix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.pushPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.launchMerge(point: addBean)
        }
    }
}

// MARK: - AnyoneMaterialPartyDelegate【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension CrosswaysViewController: AnyoneMaterialPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func bitCollection(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(user_photoId) + String(kNorValue.prefix(4)) + "ng up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(kLipKey) + String(constReceiveMessage))].stringValue == FullHashable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(notiCommunicateName)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bringHome(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.requireEnd()
            //: TalkingSocketManager.shared.currFisherVC = nil
            ManagerCommentThen.shared.currFisherVC = nil
        }

        //: popCurrentViewController()
        changeShape()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func acceptObserve(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        mic(logId: json[String(bytes: const_sessionUrl.map{lotsNigh(closely: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - BetiseObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension CrosswaysViewController: BetiseObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func beforeRelationHolder(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(k_averageTitle.suffix(9)) + String(kTradePath)) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.bringHome(showMsg: (String(data_personalMsg.suffix(6)) + String(notiServiceGirlFormat)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.requireEnd()
                //: TalkingSocketManager.shared.currFisherVC = nil
                ManagerCommentThen.shared.currFisherVC = nil
            }
            //: popCurrentViewController()
            changeShape()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(showMotionAspectText.suffix(6)) + String(showMinFormat)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue else { return }
            //: BrowReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            BrowReactiveCompatible.share.beContext(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(showMotionAspectText.suffix(6)) + String(showMinFormat)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.bringHome(showMsg: msg)
            //: popCurrentViewController()
            changeShape()
        }
    }
}

// MARK: - OptionObjectProtocol【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension CrosswaysViewController: OptionObjectProtocol {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func circuitNarrow() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(constMessageSeriesUrl)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: appCountMsg.map{requiredExecute(destroy: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func judgeDoing() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.bringHome(showMsg: (String(main_earnUrl) + kPlayerFormat.replacingOccurrences(of: "trade", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(constArcSiblingTitle) + String(k_wholeId)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: const_sessionUrl.map{lotsNigh(closely: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.bringHome(showMsg: (String(main_earnUrl) + kPlayerFormat.replacingOccurrences(of: "trade", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(k_averageTitle.suffix(9)) + String(kTradePath)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: appCountMsg.map{requiredExecute(destroy: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.bringHome(showMsg: (String(main_partyTimingUrl.suffix(4)) + String(showCapabilityValue.prefix(6)) + "as e" + show_exploreData.replacingOccurrences(of: "schedule", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: const_thanksText.map{$0^170}, encoding: .utf8)!: (String(userConsciousnessTitle) + String(appResistanceMatchName)), String(bytes: user_stillMsg.reversed(), encoding: .utf8)!: [String(bytes: const_sessionUrl.map{lotsNigh(closely: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        changeShape()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func activismTo(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.beyondResponse(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func pauseBold(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.turnWith(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.quote(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.fixedStarTarget()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func nearWindow() {
        //: TalkingSocketManager.shared.currFisherVC = self
        ManagerCommentThen.shared.currFisherVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = BiochemicalMechanismMiniView.sendView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.prevuePoint()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.anExit()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.currentPositionController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        changeShape()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func describeIn() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension CrosswaysViewController {
    /// 添加视图
    //: private func setupSubviews() {
    private func ego() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.alarmismView(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func objectLaunchAvailable() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
