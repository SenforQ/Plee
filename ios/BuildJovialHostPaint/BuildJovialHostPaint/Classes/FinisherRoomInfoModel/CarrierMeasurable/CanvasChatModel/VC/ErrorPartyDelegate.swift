
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let k_shareData:[UInt8] = [0x1c,0x1b,0x1c,0x1,0x5d,0x16,0x1a,0x11,0x10,0x7,0x4f,0x5c,0x55,0x1d,0x14,0x6,0x55,0x1b,0x1a,0x1,0x55,0x17,0x10,0x10,0x1b,0x55,0x1c,0x18,0x5,0x19,0x10,0x18,0x10,0x1b,0x1,0x10,0x11]

private func userChange(manage num: UInt8) -> UInt8 {
    return num ^ 117
}

/*: "uid" :*/
fileprivate let dataAlongRevenueName:[UInt8] = [0x64,0x69,0x75]

/*: "headPic" :*/
fileprivate let user_disableContent:String = "headPicimmune advice enterprise square op"

/*: "nickname" :*/
fileprivate let kShotUrl:String = "equipmentckname"

/*: "birthday" :*/
fileprivate let constLocalMsg:String = "BIRTHDAY"

/*: "videoUrl" :*/
fileprivate let k_heKey:[Character] = ["v","i","d","e","o","U","r"]
fileprivate let showGapId:[Character] = ["l"]

/*: "location" :*/
fileprivate let user_alongRecoverData:String = "lopart"

/*: "cmd" :*/
fileprivate let main_dateTitle:[UInt8] = [0xc1,0xcb,0xc2]

fileprivate func figureHis(surround num: UInt8) -> UInt8 {
    let value = Int(num) - 94
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let kDeadlineBlankMatterValue:String = "rease"

/*: "data" :*/
fileprivate let mainVolumeData:[UInt8] = [0x8b,0x88,0x9b,0x88]

fileprivate func voteOrientation(written num: UInt8) -> UInt8 {
    let value = Int(num) + 217
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let showCoverDialogValue:[Character] = ["C","a","l","l"," ","i","n"]
fileprivate let show_pieceId:[Character] = ["t","e","r","r"]
fileprivate let kSaveUrl:String = "UPTION"

/*: "stopCall" :*/
fileprivate let user_hourKey:String = "stopCpackage security accelerate"
fileprivate let showDrawingUrl:[Character] = ["a","l","l"]

/*: "logId" :*/
fileprivate let notiEventName:[UInt8] = [0x4c,0x4f,0x47,0x29,0x44]

fileprivate func incomeOptimistic(rid num: UInt8) -> UInt8 {
    let value = Int(num) - 224
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cancelCall" :*/
fileprivate let constFromTitle:[Character] = ["c","a","n","c","e","l"]
fileprivate let data_suspendDateStr:String = "Callclothes nervous copy"

/*: "No answer" :*/
fileprivate let data_lawValue:[Character] = ["N"]
fileprivate let user_issuePlyDistantFormat:[Character] = ["o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let appBitUrl:[Character] = ["d","u"]
fileprivate let constCosFormat:String = "rationear"

/*: "She has hung up" :*/
fileprivate let kProvokeName:[Character] = ["S","h","e"," ","h","a","s"]
fileprivate let mainDocumentMessage:String = " hunminimize for closed push past"

/*: "cancelSex" :*/
fileprivate let userBanBigUrl:[Character] = ["c","a","n","c","e","l","S","e"]
fileprivate let data_artProjectTitle:String = "against"

/*: "He has hung up" :*/
fileprivate let show_yieldDimensionMsg:String = "He has native him break observe visual"
fileprivate let constNorthBlockMsg:[Character] = ["h"]
fileprivate let noti_sequencePeoplePath:String = "midnight calculation signung up"

/*: "endType" :*/
fileprivate let dataNorWithinId:String = "endTypemilitary advanced disk context"

/*: "stopSex" :*/
fileprivate let constWhichStripValue:String = "stopSexgenerate crush consequence"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let dataReloadlyUrl:[UInt8] = [0xf,0x28,0x35,0x33,0x20,0x20,0x2f,0x25,0x2f,0x23,0x28,0x32,0x66,0x21,0x29,0x2a,0x22,0x66,0x25,0x29,0x2f,0x28,0x35,0x6a,0x66,0x25,0x27,0x2a,0x2a,0x66,0x2e,0x27,0x35,0x66,0x23,0x28,0x22,0x23,0x22]

private func scoreSpeaker(only num: UInt8) -> UInt8 {
    return num ^ 70
}

/*: "The other hung up the phone" :*/
fileprivate let dataPhoneStarTitle:String = "The via face legislative"
fileprivate let show_songStr:[Character] = [" ","h","u","n","g"," ","u","p"," ","t","h","e"]
fileprivate let main_remarkUrl:String = " phonecrush border mini theme tumble"

/*: "type" :*/
fileprivate let mainEditionMsg:String = "spanpe"

/*: "jumpUrl" :*/
fileprivate let constBusinessMessage:[Character] = ["j","u","m","p","U","r"]
fileprivate let show_detailContent:String = "mix"

/*: "Cancel" :*/
fileprivate let userSuspendMotionPath:String = "Cancepatent male bottom appeal numb"
fileprivate let noti_guestData:[Character] = ["l"]

/*: "Get gold coins" :*/
fileprivate let dataShakeKey:String = "surround wellGet gold"
fileprivate let userAudienceData:String = "bury democratic install plain number coins"

/*: "addBean" :*/
fileprivate let constRelationMarginKey:String = "addBeanold brake"

/*: "refuseSex" :*/
fileprivate let kNorthwestMsg:[Character] = ["r","e","f","u","s","e","S","e"]
fileprivate let app_buttonStr:[Character] = ["x"]

/*: "onAcceptCall" :*/
fileprivate let main_togetherBirthValue:String = "onAccaverage apply number interaction"
fileprivate let show_technologyBrowText:String = "eptCallfound deed answer"

/*: "acceptCall" :*/
fileprivate let constLiteRemindValue:[Character] = ["a","c","c","e","p"]
fileprivate let mainArenaMessage:String = "tCallonce crop military thread transaction"

/*: "Calls are cancelled" :*/
fileprivate let kResentFormat:String = "Callin nine"
fileprivate let const_browMessage:String = "true out somebody addition capturee ca"

/*: "The call has ended" :*/
fileprivate let kConversionValue:[Character] = ["T","h","e"," ","c","a"]
fileprivate let mainMiddleId:String = "capacity unitll h"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ErrorPartyDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let main_captureMessage = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class ErrorPartyDelegate: OperateReactiveCompatible {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: CanvasChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: GraphSoundReactiveCompatible? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: ThighMiniView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        ManagerCommentThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ManagerCommentThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ManagerCommentThen.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: k_shareData.map{userChange(manage: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! ControllerThen
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.countensityLevelDelay()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            throughHome()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currFisherVC = nil
            ManagerCommentThen.shared.currFisherVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.dealing()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        randomInSubviews()
        //: setupSubViewsConstraint()
        abdicate()
        //: initNotificationObserver()
        destabilization()
        //: req_callGetUserInfo()
        moderation()
        //: timeoutRules()
        recognizeBy()
        //: resumeTalk()
        female()
        //: setUpBGMPlayer()
        bmi()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if RealmReactiveCompatible.share.appConfigMode.disableShootScreen {
            if RealmReactiveCompatible.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = FreshLoopReactiveCompatible(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isFisher = false
        ManagerCommentThen.shared.isFisher = false
        //: TalkingSocketManager.shared.isCalling = false
        ManagerCommentThen.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.gallerySquare()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: AnyoneThen = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = AnyoneThen(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: RemainReactiveCompatible = {
        //: let m = TalkingVoiceChatManager()
        let m = RemainReactiveCompatible()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension ErrorPartyDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func moderation() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        PortraitExcitementReactiveCompatible.paraCompletion(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: dataAlongRevenueName.reversed(), encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(user_disableContent.prefix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(kShotUrl.replacingOccurrences(of: "equipment", with: "ni"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(constLocalMsg.lowercased())].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(k_heKey) + String(showGapId))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(user_alongRecoverData.replacingOccurrences(of: "part", with: "ca") + "tion")].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.visibleInfo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension ErrorPartyDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func throughHome() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        GravityReactiveCompatible.pathCommentPut(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.bringHome(showMsg: dataLineKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (kDeadlineBlankMatterValue.replacingOccurrences(of: "a", with: "fu") + "Call"), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: dataAlongRevenueName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.hair()
                //: return
                return
            }

            //: self.stopBGM()
            self.retrieve()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = GraphSoundReactiveCompatible.getableCos(type: .Response)
            //: self.startBGM()
            self.countensityLevelDelay()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func gallerySquare() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.retrieve()
        //: TalkingSocketManager.shared.callerDelegate = nil
        ManagerCommentThen.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        ManagerCommentThen.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        ManagerCommentThen.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func hair(animated: Bool = true) {
        //: self.stopBGM()
        self.retrieve()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func duringSeek() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.pair(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func female() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            duringSeek()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension ErrorPartyDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func destabilization() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(localMin(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(reachabilityTo(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: app_networkKey,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        main_captureMessage.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func localMin(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            hair()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func reachabilityTo(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard ManagerCommentThen.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        ManagerCommentThen.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        ManagerCommentThen.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        ManagerCommentThen.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.address(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension ErrorPartyDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.bringHome(showMsg: (String(showCoverDialogValue) + String(show_pieceId) + kSaveUrl.lowercased()).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (String(user_hourKey.prefix(5)) + String(showDrawingUrl)), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: notiEventName.map{incomeOptimistic(rid: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (String(constFromTitle) + String(data_suspendDateStr.prefix(4))), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: notiEventName.map{incomeOptimistic(rid: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (kDeadlineBlankMatterValue.replacingOccurrences(of: "a", with: "fu") + "Call"), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: dataAlongRevenueName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.requireEnd()
                //: TalkingSocketManager.shared.currFisherVC = nil
                ManagerCommentThen.shared.currFisherVC = nil
            }
            //: popCurrentViewController()
            hair()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.netOn(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension ErrorPartyDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func bmi() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? DocumentCountLiteral.Call : DocumentCountLiteral.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = GraphSoundReactiveCompatible.getableCos(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func countensityLevelDelay() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.quotePlay()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func retrieve() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.referee()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension ErrorPartyDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func recognizeBy() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? dataLocationMsg : self.chatModel?.licensingFee()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            empty()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.empty()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func empty() {
        //: guard !TalkingSocketManager.shared.isFisher else { return }
        guard !ManagerCommentThen.shared.isFisher else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.bringHome(showMsg: (String(data_lawValue) + String(user_issuePlyDistantFormat)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (String(constFromTitle) + String(data_suspendDateStr.prefix(4))), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: notiEventName.map{incomeOptimistic(rid: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (kDeadlineBlankMatterValue.replacingOccurrences(of: "a", with: "fu") + "Call"), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: dataAlongRevenueName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: socketDict)
        //: popCurrentViewController()
        hair()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension ErrorPartyDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func inmateOfThread(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: notiEventName.map{incomeOptimistic(rid: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(appBitUrl) + constCosFormat.replacingOccurrences(of: "near", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.row(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.slowStage()
            //: createTalkRoom()
            duringSeek()
            //: self.stopBGM()
            self.retrieve()
        }
    }
}

// MARK: - ExistingObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension ErrorPartyDelegate: ExistingObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func labelToday(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kProvokeName) + String(mainDocumentMessage.prefix(4)) + "g up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(userBanBigUrl) + data_artProjectTitle.replacingOccurrences(of: "against", with: "x"))].stringValue == FullHashable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(show_yieldDimensionMsg.prefix(7)) + String(constNorthBlockMsg) + String(noti_sequencePeoplePath.suffix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bringHome(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.requireEnd()
            //: TalkingSocketManager.shared.currFisherVC = nil
            ManagerCommentThen.shared.currFisherVC = nil
        }

        //: popCurrentViewController()
        hair()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func beautyEndLibrary(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: notiEventName.map{incomeOptimistic(rid: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.retrieve()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.slowStage()
        //: self.createTalkRoom()
        self.duringSeek()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func doHighlight(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(dataNorWithinId.prefix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(kProvokeName) + String(mainDocumentMessage.prefix(4)) + "g up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(constWhichStripValue.prefix(7)))].stringValue == FullHashable.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(show_yieldDimensionMsg.prefix(7)) + String(constNorthBlockMsg) + String(noti_sequencePeoplePath.suffix(6))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue {
            if RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: dataReloadlyUrl.map{scoreSpeaker(only: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(dataPhoneStarTitle.prefix(4)) + "other" + String(show_songStr) + String(main_remarkUrl.prefix(6))).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bringHome(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.requireEnd()
            //: TalkingSocketManager.shared.currFisherVC = nil
            ManagerCommentThen.shared.currFisherVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.prime()
        //: self.popCurrentViewController()
        self.hair()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: noti_routeFormat, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func drownData(msg: String, data: [String: Any]) {
        //: guard RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(mainEditionMsg.replacingOccurrences(of: "span", with: "ty"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(constBusinessMessage) + show_detailContent.replacingOccurrences(of: "mix", with: "l"))] as? String else { return }
            //: let httpsUrl = RealmReactiveCompatible.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = RealmReactiveCompatible.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = BarbConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: BrowReactiveCompatible.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            BrowReactiveCompatible.share.blade(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            StuffReactiveCompatible.heading(title: nil,
                                     //: message: msg,
                                     message: msg,
                                     //: leftBtnTitle: "Cancel".localized,
                                     leftBtnTitle: (String(userSuspendMotionPath.prefix(5)) + String(noti_guestData)).localized,
                                     //: rightBtnTitle: "Get gold coins".localized) {
                                     rightBtnTitle: (String(dataShakeKey.suffix(8)) + String(userAudienceData.suffix(6))).localized)
            {
                //: TalkingAlertShow.hideAlert()
                StuffReactiveCompatible.outFeedback()
                //: } rightBlock: {
            } rightBlock: {
                //: BrowReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                BrowReactiveCompatible.share.beContext(clickEvent: show_homeUserFormat, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func nativeData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(constRelationMarginKey.prefix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.alongData(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.bassCut(point: addBean)
        }
    }
}

// MARK: - AnyoneMaterialPartyDelegate【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ErrorPartyDelegate: AnyoneMaterialPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func bitCollection(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(kProvokeName) + String(mainDocumentMessage.prefix(4)) + "g up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(kNorthwestMsg) + String(app_buttonStr))].stringValue == FullHashable.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(show_yieldDimensionMsg.prefix(7)) + String(constNorthBlockMsg) + String(noti_sequencePeoplePath.suffix(6))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.bringHome(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.requireEnd()
            //: TalkingSocketManager.shared.currFisherVC = nil
            ManagerCommentThen.shared.currFisherVC = nil
        }
        //: popCurrentViewController()
        hair()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func acceptObserve(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: notiEventName.map{incomeOptimistic(rid: $0)}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.retrieve()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.slowStage()
        //: self.createTalkRoom()
        self.duringSeek()
    }
}

// MARK: - BetiseObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension ErrorPartyDelegate: BetiseObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func beforeRelationHolder(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (kDeadlineBlankMatterValue.replacingOccurrences(of: "a", with: "fu") + "Call") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.bringHome(showMsg: (String(data_lawValue) + String(user_issuePlyDistantFormat)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.requireEnd()
                //: TalkingSocketManager.shared.currFisherVC = nil
                ManagerCommentThen.shared.currFisherVC = nil
            }
            //: self.popCurrentViewController()
            self.hair()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(main_togetherBirthValue.prefix(5)) + String(show_technologyBrowText.prefix(7))) && errorNo == 101 { // 接听通话时，余额不足
            //: guard RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue else { return }
            //: BrowReactiveCompatible.share.func__jumpToWebRecharge(sufficient: false)
            BrowReactiveCompatible.share.beContext(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(main_togetherBirthValue.prefix(5)) + String(show_technologyBrowText.prefix(7))) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.bringHome(showMsg: msg)
            //: self.popCurrentViewController()
            self.hair()
        }
    }
}

// MARK: - NoViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension ErrorPartyDelegate: NoViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func branch() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (String(constLiteRemindValue) + String(mainArenaMessage.prefix(5))), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: dataAlongRevenueName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func panTop() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.bringHome(showMsg: (String(kResentFormat.prefix(4)) + "s ar" + String(const_browMessage.suffix(4)) + "ncelled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (String(constFromTitle) + String(data_suspendDateStr.prefix(4))), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: notiEventName.map{incomeOptimistic(rid: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.bringHome(showMsg: (String(kResentFormat.prefix(4)) + "s ar" + String(const_browMessage.suffix(4)) + "ncelled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (kDeadlineBlankMatterValue.replacingOccurrences(of: "a", with: "fu") + "Call"), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: dataAlongRevenueName.reversed(), encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.bringHome(showMsg: (String(kConversionValue) + String(mainMiddleId.suffix(4)) + "as ended").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: main_dateTitle.map{figureHis(surround: $0)}, encoding: .utf8)!: (String(user_hourKey.prefix(5)) + String(showDrawingUrl)), String(bytes: mainVolumeData.map{voteOrientation(written: $0)}, encoding: .utf8)!: [String(bytes: notiEventName.map{incomeOptimistic(rid: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.prime()
        //: self.popCurrentViewController()
        self.hair()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func colorBlind(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.talk(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func vanguard(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.backUp(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func carveOut() {
        //: TalkingSocketManager.shared.currFisherVC = self
        ManagerCommentThen.shared.currFisherVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = ThighMiniView.costSignal()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.currentPositionController()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.docket()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.talkingTime(talkingTime)
        //: popCurrentViewController()
        hair()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func blindSuspend(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.talkingTime(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func rude(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.pressurize(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension ErrorPartyDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func randomInSubviews() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func abdicate() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
