
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let k_connectTitle:[Character] = ["s","e","t","P","a"]
fileprivate let k_sizePopularStr:[Character] = ["s","s","w","o","r","d"]

/*: "user/appleLogin" :*/
fileprivate let appThreeData:String = "user/acase face context dialog"
fileprivate let constFavoriteProjectPath:String = "domaingin"

/*: "user/googleLogin" :*/
fileprivate let constReinData:String = "repeat selfuser/g"
fileprivate let userRevenueValue:String = "OOGLE"

/*: "user/smsLogin" :*/
fileprivate let dataPaintBeginId:String = "useme"

/*: "user/emailVcodeLogin" :*/
fileprivate let main_spacePowderKey:String = "uslayer"
fileprivate let const_fairStr:String = "lVcodblack count arc"
fileprivate let mainMineMsg:String = "eLoginthree core"

/*: "user/pwdLogin" :*/
fileprivate let userEliteName:String = "uentityr"
fileprivate let main_egoMsg:[Character] = ["n"]

/*: "user/emailPwdLogin" :*/
fileprivate let notiItId:[Character] = ["u","s","e","r","/","e","m","a","i","l","P","w","d"]
fileprivate let user_informMsg:[Character] = ["L","o","g","i","n"]

/*: "mobile" :*/
fileprivate let mainHolderBlankValue:[UInt8] = [0x11,0x13,0x1e,0x15,0x10,0x19]

private func loopFigureFavorite(crop num: UInt8) -> UInt8 {
    return num ^ 124
}

/*: "areaCode" :*/
fileprivate let constDoingName:[UInt8] = [0x19,0xa,0x1d,0x19,0x3b,0x17,0x1c,0x1d]

private func nearCalendar(cost num: UInt8) -> UInt8 {
    return num ^ 120
}

/*: "user/sendLoginSms" :*/
fileprivate let main_floorUrl:[Character] = ["u","s","e","r","/","s","e"]
fileprivate let dataLessUrl:String = "ndLograin thin glass screen"

/*: "RSA加密失败" :*/
fileprivate let data_seriesFormat:String = "RSA加密\u{5931}\u{8d25}"

/*: "password" :*/
fileprivate let data_linkMessage:[UInt8] = [0xa5,0x96,0xa8,0xa8,0xac,0xa4,0xa7,0x99]

fileprivate func magnitudemerSea(rice num: UInt8) -> UInt8 {
    let value = Int(num) + 203
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "vcode" :*/
fileprivate let const_coreMsg:[UInt8] = [0xbc,0xa9,0xb5,0xaa,0xab]

fileprivate func advertisingDate(zone num: UInt8) -> UInt8 {
    let value = Int(num) - 70
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "user/modifyPassword" :*/
fileprivate let appPopularBarName:String = "var support blueuser/m"
fileprivate let k_installationText:String = "assgoodord"

/*: "user/sendModifyPwdSms" :*/
fileprivate let kMemoryFormat:String = "user/sagree stance floor"
fileprivate let data_goldGumUrl:[Character] = ["e","n","d","M","o","d","i","f"]
fileprivate let dataLowerValue:[Character] = ["y","P","w","d","S","m","s"]

/*: "user/sendModifyPwdEmail" :*/
fileprivate let kChanceMsg:String = "distant may any connectuser/s"
fileprivate let kDoingTitle:String = "democraticify"
fileprivate let showSeeMessage:String = "mactiveil"

/*: "mf/user/getMyInfo" :*/
fileprivate let kWellData:String = "mf/usesome fleet"
fileprivate let app_timeName:[Character] = ["t"]
fileprivate let appWhyMsg:[Character] = ["M","y","I","n","f","o"]

/*: "invite/bindInviter" :*/
fileprivate let app_networkMessage:[Character] = ["i","n","v","i","t","e","/","b","i"]
fileprivate let constGalleryValue:String = "ndInvinner via"

/*: "invite/isBind" :*/
fileprivate let showBotSlowValue:String = "invite/roll male"
fileprivate let data_broadId:String = "isBindbound assistant map"

/*: "mf/user/CheckInviteCode" :*/
fileprivate let showInjuryMessage:[Character] = ["m","f","/","u","s","e","r","/","C","h","e","c","k"]
fileprivate let main_kingHeId:[Character] = ["I","n","v","i","t","e","C","o","d","e"]

/*: "mf/user/updateInfo" :*/
fileprivate let notiBucketValue:String = "far child brakemf/use"
fileprivate let mainFeedbackValue:[Character] = ["r","/","u","p","d","a","t","e","I","n","f","o"]

/*: "email" :*/
fileprivate let data_facePath:[UInt8] = [0xa9,0xa1,0xad,0xa5,0xa0]

private func refuseHundred(contact num: UInt8) -> UInt8 {
    return num ^ 204
}

/*: "user/getEmailBindVCode" :*/
fileprivate let user_observeUntilValue:String = "usby"
fileprivate let k_onlyUrl:String = "mailBconsent as drag feedback spec"
fileprivate let kOperativeStr:String = "indVCodebeyond serve acquire pending"

/*: "user/sendLoginEmail" :*/
fileprivate let userLayValue:[Character] = ["u","s","e","r","/","s","e","n","d","L","o","g","i","n","E","m","a","i","l"]

/*: "user/emailBind" :*/
fileprivate let k_blueAccessMemoryData:String = "devil custom contrast branchuser/e"
fileprivate let show_howeverName:String = "iglobald"

/*: "user/logout" :*/
fileprivate let noti_ehCordMessage:String = "tap bridge belowuser/l"

/*: "app/countryList" :*/
fileprivate let data_dealReadStr:String = "responsibility yourself untilapp/"
fileprivate let dataGivingFormat:[Character] = ["r","y","L","i","s","t"]

/*: "user/getMobileBindVCode" :*/
fileprivate let user_passageInjuryPath:[Character] = ["u","s","e","r","/","g","e","t","M","o","b","i","l","e","B","i","n","d","V"]
fileprivate let app_careData:String = "examine definition with import restrictionCode"

/*: "user/mobileBind" :*/
fileprivate let showExampleMsg:String = "user/even closed strong circle flow"
fileprivate let noti_equalValue:String = "per daily representation responsibility bodyleBin"
fileprivate let user_recoverValue:String = "responsibility"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MiddleReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class MiddleReactiveCompatible: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func taskDetail(type: GreatSubqueryConvertible, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.outsideLogin(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(String(k_connectTitle) + String(k_sizePopularStr))].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: BrowReactiveCompatible.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                BrowReactiveCompatible.share.commentPwd(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            MiddleReactiveCompatible.successfully(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingRequestModel {
    private class func outsideLogin(type: GreatSubqueryConvertible, params: [String: String]) -> ManhoodRequestModel {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (String(appThreeData.prefix(6)) + "ppleL" + constFavoriteProjectPath.replacingOccurrences(of: "domain", with: "o"))
        //: case .GoogleLogin:
        case .GoogleLogin:
            //: reqModel.requestPath = "user/googleLogin"
            reqModel.requestPath = (String(constReinData.suffix(6)) + userRevenueValue.lowercased() + "Login")
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (dataPaintBeginId.replacingOccurrences(of: "me", with: "r") + "/smsLogin")
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (main_spacePowderKey.replacingOccurrences(of: "layer", with: "er") + "/emai" + String(const_fairStr.prefix(5)) + String(mainMineMsg.prefix(6)))
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (userEliteName.replacingOccurrences(of: "entity", with: "se") + "/pwdLogi" + String(main_egoMsg))
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (String(notiItId) + String(user_informMsg))
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func shine(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kPopId)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: mainHolderBlankValue.map{loopFigureFavorite(crop: $0)}, encoding: .utf8)!: reslutStr, String(bytes: constDoingName.map{nearCalendar(cost: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = ManhoodRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (String(main_floorUrl) + String(dataLessUrl.prefix(5)) + "inSms")
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (data_seriesFormat))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func date(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kPopId)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: mainHolderBlankValue.map{loopFigureFavorite(crop: $0)}, encoding: .utf8)!: resultPhoneStr, String(bytes: constDoingName.map{nearCalendar(cost: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: data_linkMessage.map{magnitudemerSea(rice: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            MiddleReactiveCompatible.taskDetail(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (data_seriesFormat))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func scale(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kPopId)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: data_linkMessage.map{magnitudemerSea(rice: $0)}, encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params[String(bytes: const_coreMsg.map{advertisingDate(zone: $0)}, encoding: .utf8)!] = vcode
            }

            //: let reqModel = TalkingRequestModel()
            let reqModel = ManhoodRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(appPopularBarName.suffix(6)) + "odifyP" + k_installationText.replacingOccurrences(of: "good", with: "w"))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (data_seriesFormat))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func who(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (String(kMemoryFormat.prefix(6)) + String(data_goldGumUrl) + String(dataLowerValue))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func towardCustom(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (String(kChanceMsg.suffix(6)) + "endMo" + kDoingTitle.replacingOccurrences(of: "democratic", with: "d") + "PwdE" + showSeeMessage.replacingOccurrences(of: "active", with: "a"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func successfully(_ type: GreatSubqueryConvertible, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(kWellData.prefix(6)) + "r/ge" + String(app_timeName) + String(appWhyMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            notiExploreKey.set(result, forKey: notiNumberUrl)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<LostTransformable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: RealmReactiveCompatible.share.loginUserMode = userModel
                RealmReactiveCompatible.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                notiExploreKey.set(type.rawValue, forKey: show_captureInputUrl)
                //: if RealmReactiveCompatible.share.loginUserMode.updateInfo == false {
                if RealmReactiveCompatible.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: BrowReactiveCompatible.share.func__pushToFillUserInfoVC()
                    BrowReactiveCompatible.share.pushBackSmall()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: data_nextPopName, object: nil, userInfo: result as! [String: Any])

                //: if RealmReactiveCompatible.share.loginUserMode.remindBindEmail == true {
                if RealmReactiveCompatible.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: BrowReactiveCompatible.share.func__pushToLockUserEmailVC(isShowBack: false)
                        BrowReactiveCompatible.share.adminRender(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func stickTo(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (String(app_networkMessage) + String(constGalleryValue.prefix(5)) + "iter")
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 是否已经绑定过邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_isBindInviter(completion: @escaping FinishBlock) {
    class func strength(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "invite/isBind"
        reqModel.requestPath = (String(showBotSlowValue.prefix(7)) + String(data_broadId.prefix(6)))

        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func mediaListener(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(showInjuryMessage) + String(main_kingHeId))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func reading(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(notiBucketValue.suffix(6)) + String(mainFeedbackValue))
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.occurrentStamp()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(appFinishUrl)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.expected(key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func suiteCompletion(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kPopId)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = [String(bytes: data_facePath.map{refuseHundred(contact: $0)}, encoding: .utf8)!: reslutStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = ManhoodRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (user_observeUntilValue.replacingOccurrences(of: "by", with: "er") + "/getE" + String(k_onlyUrl.prefix(5)) + String(kOperativeStr.prefix(8)))
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (String(userLayValue))
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (data_seriesFormat))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func following(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (String(k_blueAccessMemoryData.suffix(6)) + "mailB" + show_howeverName.replacingOccurrences(of: "global", with: "n"))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = [String(bytes: const_coreMsg.map{advertisingDate(zone: $0)}, encoding: .utf8)!: emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func cut(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kPopId)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = [String(bytes: data_facePath.map{refuseHundred(contact: $0)}, encoding: .utf8)!: resultEmailStr, String(bytes: data_linkMessage.map{magnitudemerSea(rice: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            MiddleReactiveCompatible.taskDetail(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (data_seriesFormat))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func soundCompletion(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (String(noti_ehCordMessage.suffix(6)) + "ogout")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func below(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(kWellData.prefix(6)) + "r/ge" + String(app_timeName) + String(appWhyMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                notiExploreKey.set(result, forKey: notiNumberUrl)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<LostTransformable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: RealmReactiveCompatible.share.loginUserMode = userModel
                    RealmReactiveCompatible.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func motivation(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(data_dealReadStr.suffix(4)) + "count" + String(dataGivingFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func moment(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kPopId)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: mainHolderBlankValue.map{loopFigureFavorite(crop: $0)}, encoding: .utf8)!: reslutStr, String(bytes: constDoingName.map{nearCalendar(cost: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = ManhoodRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (String(user_passageInjuryPath) + String(app_careData.suffix(4)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (data_seriesFormat))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func scopeDrag(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: kPopId)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: mainHolderBlankValue.map{loopFigureFavorite(crop: $0)}, encoding: .utf8)!: reslutStr, String(bytes: constDoingName.map{nearCalendar(cost: $0)}, encoding: .utf8)!: reslutCodeStr, String(bytes: const_coreMsg.map{advertisingDate(zone: $0)}, encoding: .utf8)!: vcode]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = ManhoodRequestModel()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (String(showExampleMsg.prefix(5)) + "mobi" + String(noti_equalValue.suffix(5)) + user_recoverValue.replacingOccurrences(of: "responsibility", with: "d"))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (data_seriesFormat))
        }
    }
}
