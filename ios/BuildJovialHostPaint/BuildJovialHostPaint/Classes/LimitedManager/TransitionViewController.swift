
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let data_labKey:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let k_faceContent:[Character] = ["L","o","a","d",".",".","."]

/*: "Skip" :*/
fileprivate let showPrivacyMarkText:String = "become indicator additional driver televisionSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let notiLiteralPath:[UInt8] = [0xc4,0xf8,0xf8,0xfc,0xd8,0xe3,0xe3,0xe0,0xa2,0xc2,0xed,0xf8,0xe5,0xfa,0xe9,0xd8,0xe3,0xc6,0xff,0xa4,0xab,0xfe,0xe9,0xef,0xe4,0xed,0xfe,0xeb,0xe9,0xab,0xa5]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let user_devoteMessage:[UInt8] = [0xe2,0xfc,0xfb,0xf1,0xfa,0xe2,0xbb,0xfa,0xfb,0xc5,0xf4,0xf2,0xf0,0xc6,0xfd,0xfa,0xe2,0xb3,0xb3,0xfa,0xfb,0xc5,0xf4,0xf2,0xf0,0xc6,0xfd,0xfa,0xe2,0xbd,0xbc,0xae]

private func attractiveLip(greet num: UInt8) -> UInt8 {
    return num ^ 149
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let show_arcMsg:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e","S","h","o"]
fileprivate let show_airValue:String = "w): girl via lay"

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let showPerspectiveData:[UInt8] = [0x86,0x98,0x9f,0x95,0x9e,0x86,0xdf,0x9e,0x9f,0xa1,0x90,0x96,0x94,0xb9,0x98,0x95,0x94,0xd7,0xd7,0x9e,0x9f,0xa1,0x90,0x96,0x94,0xb9,0x98,0x95,0x94,0xd9,0xd8,0xca]

private func stillMode(search num: UInt8) -> UInt8 {
    return num ^ 241
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let noti_devoteMessage:[Character] = ["j","s","E","v","e","n","t","(","o"]
fileprivate let constBranchKey:[Character] = ["n","P","a","g"]
fileprivate let noti_shapeStr:[Character] = ["e","H","i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let appErrFormat:String = "TITLE"

/*: "canGoBack" :*/
fileprivate let showPerceptUrl:String = "occur modify siblingcanGoBack"

/*: "PHPSESSID= :*/
fileprivate let noti_drawName:[Character] = ["P","H","P","S","E","S"]
fileprivate let show_barTitle:String = "SID=operate letter complete hearing active"

/*: ";uid= :*/
fileprivate let user_conCollectionStr:String = "suspend ceremony care kind;uid="

/*: "Cookie" :*/
fileprivate let appActiveFormat:[Character] = ["C","o","o","k","i"]
fileprivate let showBearImagePostData:[Character] = ["e"]

/*: "nav_back_black_nor" :*/
fileprivate let noti_putLotsFormat:[Character] = ["n","a","v","_","b","a","c","k","_","b","l","a","c","k","_","n","o"]
fileprivate let appFailureId:[Character] = ["r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let app_traceValue:[Character] = ["i","c","o","_","i"]
fileprivate let constEarlyWithinFormat:String = "nproperlyimaproperly"
fileprivate let const_speakName:String = "cancel suit notice warningt_sm"

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let show_carveWarningInnerKey:[UInt8] = [0x2d,0x26,0x2a,0x3c,0x24,0x2c,0x27,0x3d,0x67,0x2e,0x2c,0x3d,0xc,0x25,0x2c,0x24,0x2c,0x27,0x3d,0x3a,0xb,0x30,0x1d,0x28,0x2e,0x7,0x28,0x24,0x2c,0x61,0x6e,0x2b,0x26,0x2d,0x30,0x6e,0x60,0x12,0x79,0x14,0x67,0x3a,0x3d,0x30,0x25,0x2c,0x67,0x2b,0x28,0x2a,0x22,0x2e,0x3b,0x26,0x3c,0x27,0x2d,0x74,0x6e,0x3b,0x2e,0x2b,0x28,0x61,0x79,0x65,0x79,0x65,0x79,0x65,0x79,0x60,0x6e]

private func writerAge(statement num: UInt8) -> UInt8 {
    return num ^ 73
}

/*: "Alert" :*/
fileprivate let dataInfoSourceName:[Character] = ["A","l","e","r","t"]

/*: "Cancle" :*/
fileprivate let dataTotalName:String = "Cancleskin admin per need cycle"

/*: "Done" :*/
fileprivate let k_centerLengthData:String = "Donestrike recover prime us"

/*: "js call method name =  :*/
fileprivate let userOurStr:String = "js caadmin medication publish guidance"
fileprivate let constAnyoneValue:String = "to paperll m"
fileprivate let constRemoteValue:String = "transform script member everyone name = "

/*: , message =  :*/
fileprivate let user_totData:String = ", "
fileprivate let appTapFullValue:String = "MESSA"

/*: "http://" :*/
fileprivate let appSuccessStr:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let userCaptureStr:String = "https://fun schedule super psychological against"

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let app_somethingFormat:[UInt8] = [0x1e,0x9,0x1a,0xc8,0xb,0x17,0x17,0x13,0x11,0xd,0xf6,0x9,0x15,0xd,0x1b,0xc8,0xe5,0xc8,0xc,0x17,0xb,0x1d,0x15,0xd,0x16,0x1c,0xd6,0xb,0x17,0x17,0x13,0x11,0xd,0xd6,0x1b,0x18,0x14,0x11,0x1c,0xd0,0xcf,0xe3,0xc8,0xcf,0xd1,0xd6,0x15,0x9,0x18,0xd0,0xe,0x1d,0x16,0xb,0x1c,0x11,0x17,0x16,0xd0,0xb,0x17,0x17,0x13,0x11,0xd,0xd1,0xc8,0x23,0xc8,0x1a,0xd,0x1c,0x1d,0x1a,0x16,0xc8,0xb,0x17,0x17,0x13,0x11,0xd,0xd6,0x1b,0x18,0x14,0x11,0x1c,0xd0,0xcf,0xe5,0xcf,0xd1,0x3,0xd8,0x5,0xc8,0x25,0xc8,0xd1,0xe3,0xb2]

fileprivate func pressOut(found num: UInt8) -> UInt8 {
    let value = Int(num) - 168
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid= :*/
fileprivate let mainToneDetailPath:[Character] = ["u","i","d","="]

/*: ;domain= :*/
fileprivate let constPopKey:String = "finding"
fileprivate let showSiblingAnimalMsg:[Character] = ["d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let dataTelevisionMessage:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let userFigureBotId:String = "signcumen"
fileprivate let k_fundamentalUrl:String = "vehicle minit.co"

/*: ;domin= :*/
fileprivate let user_scriptStr:[Character] = [";","d","o","m","i","n"]
fileprivate let dataSpendId:String = "="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let show_productZoneMsg:[UInt8] = [0x65,0x6a,0x2c,0x24,0x6f,0x63,0x63,0x67,0x65,0x69,0x42,0x6d,0x61,0x69,0x7f,0x22,0x65,0x62,0x68,0x69,0x74,0x43,0x6a,0x24,0x2b,0x5c,0x44,0x5c,0x5f,0x49,0x5f,0x5f,0x45,0x48,0x2b,0x25,0x2c,0x31,0x31,0x2c,0x21,0x3d,0x25,0x2c,0x77,0x2c,0x68,0x63,0x6f,0x79,0x61,0x69,0x62,0x78,0x22,0x6f,0x63,0x63,0x67,0x65,0x69,0x31,0x2b]

/*: "h5Domain =  :*/
fileprivate let kFailData:String = "h5Domqueen distribution tun piece parent"

/*: "getAllCookies =  :*/
fileprivate let user_animaFormat:[Character] = ["g","e","t","A","l","l","C","o","o","k","i"]
fileprivate let constBassData:String = "es = advanced fire"

/*: "PHPSESSID" :*/
fileprivate let notiUsName:[Character] = ["P","H"]
fileprivate let notiCalculateName:String = "PclosedSSID"

/*: "uid" :*/
fileprivate let user_horseId:String = "uroundd"

/*: "appCookies =  :*/
fileprivate let noti_successfullyStr:String = "appCoclosely dig acquire driver"
fileprivate let const_replyContent:[Character] = ["o","k","i"]
fileprivate let appBothText:[Character] = ["e","s"," ","="," "]

/*: "UID" :*/
fileprivate let kSatisfyText:String = "uid"

/*: "navigator.userAgent" :*/
fileprivate let appDistanceData:[Character] = ["n","a","v","i","g","a","t","o"]
fileprivate let dataWhyScanPath:String = "r.useproject enter robot should"

/*: "xunwu_iphone" :*/
fileprivate let noti_capableReplacementKey:[Character] = ["x","u","n","w","u","_","i","p"]
fileprivate let show_frontBalanceValue:String = "horandome"

/*: " xunwu_device_id/ :*/
fileprivate let constHumanTaMsg:[Character] = [" ","x","u","n","w","u","_","d","e","v"]
fileprivate let user_marginValue:String = "ice_id/decision commit history"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let user_alongSoId:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"," ","x","u","n","w","u"]
fileprivate let data_chooseName:String = "_vertogether violation why"
fileprivate let showDotPath:String = "sion/awake install blank"

/*:  xunwu_package/ :*/
fileprivate let kEveryId:String = " xunwucircuit reason bean widespread"
fileprivate let dataMyUrl:String = "_ptape mark perspective"
fileprivate let appUnderSuitMsg:String = "ackage/action on consciousness boundary"

/*:  xunwu_bundleId/ :*/
fileprivate let dataExceptionMessage:[Character] = [" ","x","u","n","w"]
fileprivate let constSeemMessage:String = "u_bunoffer clear after evaluate"

/*:   webkit/1" :*/
fileprivate let main_bringName:String = "achievement map visible split translate  webkit/"
fileprivate let kMemoryInsertListenMsg:String = "1"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TransitionViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class TransitionViewController: CombinedReactiveCompatible {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: BarbConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: ObjectDefaultsSerializable?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: ObjectDefaultsSerializable?, webConfig: BarbConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: data_labKey.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        anBalanceShow()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        pictureTarget()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(k_faceContent))
        //: addNotification()
        currencyUntil()
        //: func__checkConfig()
        publishMinimum()
        //: func__setUpWebView()
        originalRestore()
        //: func__registerObserver()
        scopeAlongsideNo()
        //: func__beginStartRequest()
        keyTake()
        //: func_checkAppleIAP()
        changeRound()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: BrowReactiveCompatible.share.func__pushToWebVC(webViewType: .DiscountPopup)
                BrowReactiveCompatible.share.pathNext(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: kFrameValue, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            plumage(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.labIndicator(), .font: UIFont.shrinkSize(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(showPrivacyMarkText.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension TransitionViewController {
    /// 添加通知
    //: private func addNotification() {
    private func currencyUntil() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(mpWith),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: notiWarnFormat,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(anBalanceShow),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func mpWith() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: notiLiteralPath.map{$0^140}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func anBalanceShow() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: user_devoteMessage.map{attractiveLip(greet: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(show_arcMsg) + String(show_airValue.prefix(4))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func pictureTarget() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: showPerspectiveData.map{stillMode(search: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(noti_devoteMessage) + String(constBranchKey) + String(noti_shapeStr)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension TransitionViewController {
    //: func func__registerObserver() {
    func scopeAlongsideNo() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (appErrFormat.lowercased()))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(showPerceptUrl.suffix(9))))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(chart), name: noti_routeFormat, object: nil)
    }

    //: func func__setUpWebView() {
    func originalRestore() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        originMin()
        //: func__addBridgeMethod()
        afterCount()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.smallColor()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            LawReactiveCompatible.noticeDemonstrate(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(constSoundTabStr)
            }
        }
    }

    //: func func__beginStartRequest() {
    func keyTake() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.visibleList()
            }
            //: } else {
        } else {
            //: func__startRequest()
            visibleList()
        }
    }

    //: func func__startRequest() {
    func visibleList() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = RealmReactiveCompatible.share.loginUid
        let uid = RealmReactiveCompatible.share.loginUid
        //: let ssid = RealmReactiveCompatible.share.loginSessionId
        let ssid = RealmReactiveCompatible.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(noti_drawName) + String(show_barTitle.prefix(4))) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(user_conCollectionStr.suffix(5))) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(appActiveFormat) + String(showBearImagePostData)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        insertColor()
        //: func__initRightButton()
        strongMark()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func changeRound() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            FalseFruitLiteralLandAgentReactiveCompatible.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.quitScale(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            FalseFruitLiteralLandAgentReactiveCompatible.shared.umber()
        }
    }

    //: func func__initLeftButton() {
    func clockWatching() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.adjust(name: (String(noti_putLotsFormat) + String(appFailureId))), style: .plain, target: self, action: #selector(enableer(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func strongMark() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(ObjectDefaultsSerializable.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.adjust(name: (String(app_traceValue) + constEarlyWithinFormat.replacingOccurrences(of: "properly", with: "t") + "e_lis" + String(const_speakName.suffix(4)))), style: .plain, target: self, action: #selector(tidewaterRiverSet))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func enableer(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        incomeStage()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.currentPositionController() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: TransitionViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! TransitionViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! TransitionViewController).anBalanceShow()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func publishMinimum() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func refill(sucess: Bool) {
        //: ProgressHUD.dismiss()
        LawReactiveCompatible.minify()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func insertColor() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: show_carveWarningInnerKey.map{writerAge(statement: $0)}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func cognizantListen(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: BrowReactiveCompatible.share.func__pushToWebVC(urlStr: url)
            BrowReactiveCompatible.share.blade(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func warningPrepare(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func chart() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: noti_routeFormat, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func tidewaterRiverSet() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = MethodView(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl))
        //: view.show()
        view.fun()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension TransitionViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            LawReactiveCompatible.noticeDemonstrate(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            refill(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        clockWatching()
        //: func__clearJSBgColor()
        insertColor()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            refill(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            refill(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(dataInfoSourceName)), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(dataTotalName.prefix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(k_centerLengthData.prefix(4))).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension TransitionViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(userOurStr.prefix(5)) + String(constAnyoneValue.suffix(4)) + "ethod" + String(constRemoteValue.suffix(8))) + "\(message.name)" + (user_totData.capitalized + appTapFullValue.lowercased() + "ge = ") + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.wing(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func afterCount() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in LimitedTransformable.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func incomeStage() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in LimitedTransformable.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension TransitionViewController {
    //: func func__addUserScript() {
    func firstIndex() {
        //: var htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(appSuccessStr))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(appSuccessStr)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(userCaptureStr.prefix(8)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(userCaptureStr.prefix(8))), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = RealmReactiveCompatible.share.loginUserMode.userID
        let tempUid: String = RealmReactiveCompatible.share.loginUserMode.userID
        //: let tempSessionID: String = RealmReactiveCompatible.share.loginSessionId
        let tempSessionID: String = RealmReactiveCompatible.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: app_somethingFormat.map{pressOut(found: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(mainToneDetailPath)) + "\(tempUid)" + (constPopKey.replacingOccurrences(of: "finding", with: ";") + String(showSiblingAnimalMsg)) + "\(htmlDomain)" + (String(dataTelevisionMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((userFigureBotId.replacingOccurrences(of: "sign", with: "do") + String(k_fundamentalUrl.suffix(4)) + "okie=\'") + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(noti_drawName) + String(show_barTitle.prefix(4))) + "\(tempSessionID)" + (String(user_scriptStr) + dataSpendId.capitalized) + "\(htmlDomain)" + (String(dataTelevisionMessage))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((userFigureBotId.replacingOccurrences(of: "sign", with: "do") + String(k_fundamentalUrl.suffix(4)) + "okie=\'") + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: show_productZoneMsg.map{$0^12}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(mainToneDetailPath)) + "\(tempUid)" + (constPopKey.replacingOccurrences(of: "finding", with: ";") + String(showSiblingAnimalMsg)) + "\(htmlDomain)" + (String(dataTelevisionMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((userFigureBotId.replacingOccurrences(of: "sign", with: "do") + String(k_fundamentalUrl.suffix(4)) + "okie=\'") + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(mainToneDetailPath)) + "\(tempSessionID)" + (constPopKey.replacingOccurrences(of: "finding", with: ";") + String(showSiblingAnimalMsg)) + "\(htmlDomain)" + (String(dataTelevisionMessage))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((userFigureBotId.replacingOccurrences(of: "sign", with: "do") + String(k_fundamentalUrl.suffix(4)) + "okie=\'") + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func plumage(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
            let htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(kFailData.prefix(5)) + "ain = ") + "\(htmlDomain)")
            //: let userID: String = RealmReactiveCompatible.share.loginUserMode.userID
            let userID: String = RealmReactiveCompatible.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(user_animaFormat) + String(constBassData.prefix(5))) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(notiUsName) + notiCalculateName.replacingOccurrences(of: "closed", with: "SE")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (user_horseId.replacingOccurrences(of: "round", with: "i")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.keyTake()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.superior(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func superior(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
                let htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(kFailData.prefix(5)) + "ain = ") + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: CoverAddrTool.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(noti_successfullyStr.prefix(5)) + String(const_replyContent) + String(appBothText)) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (kSatisfyText.uppercased()), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (user_horseId.replacingOccurrences(of: "round", with: "i"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(notiUsName) + notiCalculateName.replacingOccurrences(of: "closed", with: "SE")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(notiUsName) + notiCalculateName.replacingOccurrences(of: "closed", with: "SE"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.keyTake()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func originMin() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((String(appDistanceData) + String(dataWhyScanPath.prefix(5)) + "rAgent")) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (String(appDistanceData) + String(dataWhyScanPath.prefix(5)) + "rAgent"))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(noti_capableReplacementKey) + show_frontBalanceValue.replacingOccurrences(of: "random", with: "n"))) {
                //: let str = getUserAgentString()
                let str = deviceShow()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.cropBlock { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(constHumanTaMsg) + String(user_marginValue.prefix(7))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func deviceShow() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(user_alongSoId) + String(data_chooseName.prefix(4)) + String(showDotPath.prefix(5))) + "\(data_malePath)" + (String(kEveryId.prefix(6)) + String(dataMyUrl.prefix(2)) + String(appUnderSuitMsg.prefix(7))) + "\(user_workName)" + (String(dataExceptionMessage) + String(constSeemMessage.prefix(5)) + "dleId/") + "\(main_informationId)" + (String(main_bringName.suffix(9)) + kMemoryInsertListenMsg.capitalized)
    }
}
