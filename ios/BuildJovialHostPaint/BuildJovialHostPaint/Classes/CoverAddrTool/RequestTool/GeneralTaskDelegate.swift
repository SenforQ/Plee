
//: Declare String Begin

/*: ; build: :*/
fileprivate let main_primaryUrl:[Character] = [";"," ","b","u","i"]
fileprivate let data_squareMessage:[Character] = ["l","d",":"]

/*: ; iOS  :*/
fileprivate let user_combineIllegalId:[Character] = [";"," ","i","O","S"]
fileprivate let const_manData:String = "nose"

/*: "audio" :*/
fileprivate let k_bringRollName:[Character] = ["a","u","d","i","o"]

/*: .wav" :*/
fileprivate let app_statementKey:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let noti_womanRandomData:String = "AUDIO"
fileprivate let showStateData:[Character] = ["/","w","a","v"]

/*: .jpg" :*/
fileprivate let appPartLeastWhoPath:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let main_securityKey:String = "image/juniform shrink"
fileprivate let app_mutualKey:[Character] = ["p","e","g"]

/*: "://app." :*/
fileprivate let user_subFormat:String = "://apmethod constraint information"
fileprivate let kToleranceDirectData:[Character] = ["p","."]

/*: "data" :*/
fileprivate let showOnMessage:String = "dconstrainta"

/*: ":null" :*/
fileprivate let user_plusTitle:String = "ta execute particle accept connect:null"

/*: "Request failed, Try again later" :*/
fileprivate let data_possibilityId:[UInt8] = [0xbb,0x8c,0x98,0x9c,0x8c,0x9a,0x9d,0xc9,0x8f,0x88,0x80,0x85,0x8c,0x8d,0xc5,0xc9,0xbd,0x9b,0x90,0xc9,0x88,0x8e,0x88,0x80,0x87,0xc9,0x85,0x88,0x9d,0x8c,0x9b]

private func refuseGap(yourself num: UInt8) -> UInt8 {
    return num ^ 233
}

/*: "json error" :*/
fileprivate let app_laterValue:String = "json errolimit quantity"
fileprivate let dataClubMatchTitle:String = "push"

/*: "platform=iphone&version= :*/
fileprivate let data_rowPassUrl:String = "plresent"
fileprivate let app_marketValue:String = "TFOR"
fileprivate let show_beneathTitle:String = "ne&vdrag main previous statistical"

/*: &packageId= :*/
fileprivate let showBoxValue:String = "&packspecial schedule sufficient as tape"

/*: &bundleId= :*/
fileprivate let user_dragUrl:[Character] = ["&","b"]
fileprivate let mainThenId:String = "headdl"

/*: &lang= :*/
fileprivate let appPathExId:String = "&lang=public country"

/*: "/route/slowindex" :*/
fileprivate let appDeliverHomeValue:String = "must have editor/route/s"
fileprivate let app_detailedGirlFormat:String = "lcontainindex"

/*: "/route/index" :*/
fileprivate let dataInstallComplyTitle:String = "remaining service cycle medium clothing/rout"

/*: "?path= :*/
fileprivate let data_beId:String = "off against ago writing mine?path="

/*: "&GJNonceStr= :*/
fileprivate let data_punishFormat:String = "plain operative ta widespread disc&GJNonceS"
fileprivate let show_meanId:String = "tr=horizon horn"

/*: "&params= :*/
fileprivate let notiViolationKey:String = "location program via&params="

/*: "token" :*/
fileprivate let noti_momentMsg:String = "tokconvertn"

/*: "请求成功 :*/
fileprivate let showRefCarveStr:String = "请求成available"

/*: "AAAAAAAA" :*/
fileprivate let user_consentName:[Character] = ["A","A","A","A","A","A","A","A"]

/*: "下载完成 -  :*/
fileprivate let noti_gumKey:String = "下载\u{5b8c}成 - "

/*: "/Documents/" :*/
fileprivate let mainEnterpriseMessage:String = "tool notice/Docume"
fileprivate let noti_someoneKey:String = "nts/both threshold certain"

/*: ".mp4" :*/
fileprivate let user_messageKey:String = "menu write grade safe.mp4"

/*: "移动地址: :*/
fileprivate let notiFrameMessage:String = "移holder址:"

/*: "后台任务下载回来" :*/
fileprivate let mainPolicyStr:String = "\u{540e}"
fileprivate let mainRandomUrl:[Character] = ["台","任","\u{52a1}","下","载","回","来"]

/*: "下载成功 : :*/
fileprivate let data_basicUrl:String = "gift"
fileprivate let user_midnightContent:[Character] = ["\u{8f7d}","成","功"," ",":"]

/*: "Net Error" :*/
fileprivate let user_pitchFormat:String = "sum origin violation domain ofNet "

/*: "下载失败 : :*/
fileprivate let kProvokeId:[Character] = ["下","载","\u{5931}","败"," ",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  GeneralTaskDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let dataRobotStr = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let k_momentStr = "\(show_lessKey)/\(app_matchMessage) (\(main_informationId)" + (String(main_primaryUrl) + String(data_squareMessage)) + "\(appPostFailFormat)" + (String(user_combineIllegalId) + const_manData.replacingOccurrences(of: "nose", with: " ")) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: RunningErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let userTextMessage = GeneralTaskDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class GeneralTaskDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func theoreticalAccount(model: ManhoodRequestModel, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.telephoneSet() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.capMp(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.serve(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func capMp(model: ManhoodRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.smallRange(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.omeletPan(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    speakOfTheDevil(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.NetError.rawValue, errorMsg: data_coverName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        bringHome(showMsg: data_coverName)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(k_bringRollName)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.occurrentStamp())+\(user_workName)" + (String(app_statementKey)), mimeType: (noti_womanRandomData.lowercased() + String(showStateData)))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(appPartLeastWhoPath)), mimeType: (String(main_securityKey.prefix(7)) + String(app_mutualKey)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    speakOfTheDevil(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.NetError.rawValue, errorMsg: data_coverName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        bringHome(showMsg: data_coverName)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func serve(model: ManhoodRequestModel, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.smallRange(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.omeletPan(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: showSearchData ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = dataRobotStr }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    speakOfTheDevil(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.NetError.rawValue, errorMsg: data_coverName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        bringHome(showMsg: data_coverName)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = dataRobotStr }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    speakOfTheDevil(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.NetError.rawValue, errorMsg: data_coverName))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        bringHome(showMsg: data_coverName)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func speakOfTheDevil(model: ManhoodRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            CoverAddrTool.share.findOutCookies(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if showSearchData, model.requestServer.contains((String(user_subFormat.prefix(5)) + String(kToleranceDirectData))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.headByCount(key: CoverAddrTool.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (showOnMessage.replacingOccurrences(of: "constraint", with: "at")) + "\"" + (String(user_plusTitle.suffix(5))), with: "" + "\"" + (showOnMessage.replacingOccurrences(of: "constraint", with: "at")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !showSearchData {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(user_plusTitle.suffix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<IssueModelType>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == RestrictionMeasurable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, RunningErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    bringHome(showMsg: responseModel.msg ?? String(bytes: data_possibilityId.map{refuseGap(yourself: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case RestrictionMeasurable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: user_beginMsg, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.NetError.rawValue, errorMsg: (String(app_laterValue.prefix(9)) + dataClubMatchTitle.replacingOccurrences(of: "push", with: "r")).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func smallRange(model: ManhoodRequestModel) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (data_rowPassUrl.replacingOccurrences(of: "resent", with: "a") + app_marketValue.lowercased() + "m=ipho" + String(show_beneathTitle.prefix(4)) + "ersion=") + "\(data_malePath)" + (String(showBoxValue.prefix(5)) + "ageId=") + "\(user_workName)" + (String(user_dragUrl) + mainThenId.replacingOccurrences(of: "head", with: "un") + "eId=") + "\(main_informationId)" + (String(appPathExId.prefix(6))) + "\(CoverAddrTool.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if showSearchData, serverUrl.contains((String(user_subFormat.prefix(5)) + String(kToleranceDirectData))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if CoverAddrTool.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(appDeliverHomeValue.suffix(8)) + app_detailedGirlFormat.replacingOccurrences(of: "contain", with: "ow")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(dataInstallComplyTitle.suffix(5)) + "e/index")) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(data_beId.suffix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(data_punishFormat.suffix(9)) + String(show_meanId.prefix(3))) + "\(CoverAddrTool.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.observeTransaction(key: CoverAddrTool.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(notiViolationKey.suffix(8))) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func omeletPan(model: ManhoodRequestModel) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(k_momentStr)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (noti_momentMsg.replacingOccurrences(of: "convert", with: "e")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func enabledSessionClub(model: ManhoodRequestModel) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.smallRange(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (showRefCarveStr.replacingOccurrences(of: "available", with: "功")) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (String(user_consentName))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (noti_gumKey) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(mainEnterpriseMessage.suffix(7)) + String(noti_someoneKey.prefix(4))) + NSDate.occurrentStamp() + (String(user_messageKey.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (notiFrameMessage.replacingOccurrences(of: "holder", with: "动地")) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (mainPolicyStr + String(mainRandomUrl)))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = TranslateThen.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func dismantleCompletion(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: noti_timeStr.noneVisitor(), with: mainRequestMsg)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (data_basicUrl.replacingOccurrences(of: "gift", with: "下") + String(user_midnightContent)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.Normal.rawValue, errorMsg: (String(user_pitchFormat.suffix(4)) + "Error").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.NetError.rawValue, errorMsg: (String(user_pitchFormat.suffix(4)) + "Error").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(kProvokeId)) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func vocalization(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: noti_timeStr.noneVisitor(), with: mainRequestMsg)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (data_basicUrl.replacingOccurrences(of: "gift", with: "下") + String(user_midnightContent)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.Normal.rawValue, errorMsg: (String(user_pitchFormat.suffix(4)) + "Error").localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, RunningErrorResponse(errorCode: RestrictionMeasurable.NetError.rawValue, errorMsg: (String(user_pitchFormat.suffix(4)) + "Error").localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (String(kProvokeId)) + "\(errMsg)")
            }
        }
    }
}
