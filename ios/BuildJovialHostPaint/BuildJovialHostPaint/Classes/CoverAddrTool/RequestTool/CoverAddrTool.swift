
//: Declare String Begin

/*: "http://" :*/
fileprivate let app_scriptName:String = "http://alongside by arc security provoke"

/*: "https://" :*/
fileprivate let dataAddressFormat:String = "resolutiont"
fileprivate let userStrengthPath:String = "ps://accomplish path segment method primary"

/*: "http:// :*/
fileprivate let kAboutNetworkMessage:String = "weekly trade shine quickhttp://"

/*: "serverSelctIndex =  :*/
fileprivate let data_noteTellId:[Character] = ["s","e","r","v","e","r","S","e","l","c","t"]
fileprivate let k_specPath:[Character] = ["I","n","d","e","x"," ","="," "]

/*: "serverUrlStr =  :*/
fileprivate let k_talkPushName:[Character] = ["s","e","r","v","e","r","U"]
fileprivate let mainPadPath:String = "contrast spark media quantityrlStr = "

/*: "app. :*/
fileprivate let mainOperationEndLapText:[Character] = ["a","p","p","."]

/*: .com" :*/
fileprivate let k_attentionArtCloselyMessage:[Character] = [".","c","o","m"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let app_telephoneMsg:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","G"]
fileprivate let showEaseData:String = "moreft"

/*: "recharge/applePayNotify" :*/
fileprivate let noti_animaMsg:String = "rmomenth"
fileprivate let noti_certainSmartWithData:String = "applePlock until strike gender"
fileprivate let notiProcessUrl:String = "unable missing tick retain availableayNotify"

/*: "user/appleLogin" :*/
fileprivate let app_harvestFormat:String = "user/achoice i customer child"
fileprivate let app_refuseMsg:String = "awarein"

/*: "IM/getMyBlackList" :*/
fileprivate let show_electronicKey:String = "IM/getrain plus reason quit search"
fileprivate let user_infoStr:[Character] = ["c","k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let showIncreaseMsg:String = "mf/buslive step grace attention"
fileprivate let main_helloStr:String = "ndMsgunder comment mol discourse composition"

/*: "mf/business/sendGift" :*/
fileprivate let k_databaseId:[Character] = ["m","f","/"]
fileprivate let notiActionContent:String = "destinationsin"
fileprivate let k_launchFormat:String = "failure would going permissionndGift"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let showSumAgoReadyKey:[Character] = ["m","f","/","c","h","a","t","R"]
fileprivate let main_performName:String = "full sure possible stretch conversionoom/"

/*: "GJ21FB" :*/
fileprivate let user_exceptName:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let dataBarTitle:String = "完\u{6210}了检测,"
fileprivate let mainDarkMsg:[Character] = ["\u{8bf7}","求","状","\u{6001}","正","常"]

/*: "接口请求失败" :*/
fileprivate let main_soundFormat:String = "接口请\u{6c42}失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let app_ownerValue:String = "完line检"
fileprivate let noti_golfMessage:String = "换sercalculate tool conversation"
fileprivate let constHorseMessage:String = "ver urlsome yield midnight host"

/*: "HTTPProxy" :*/
fileprivate let kCarveStr:String = "HmediaP"

/*: "__SCOPED__" :*/
fileprivate let notiUntilFormat:String = "resent class legislative demonstrate target__SCO"

/*: "tap" :*/
fileprivate let app_loadCapReasonId:String = "outputp"

/*: "tun" :*/
fileprivate let data_primaryAmpleMessage:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let constExecutiveFemalePubliclyName:String = "ipsesmall"

/*: "ppp" :*/
fileprivate let notiEnabledMessage:String = "privacyp"

/*: "func__updateTalkingCookies =  :*/
fileprivate let showCloudMessage:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a"]
fileprivate let userMysteryFormat:[Character] = ["l","k","i","n","g","C","o","o","k","i"]
fileprivate let app_foreheadId:String = "happy explain makees = "

/*: "https:// :*/
fileprivate let app_whenSpeakerKey:String = "large ex space bot medalhttps:/"
fileprivate let dataSecretKindUrl:String = "/"

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let dataActivityName:[UInt8] = [0xbc,0xaf,0xb4,0xb9,0x85,0x85,0xbe,0xbf,0xb6,0xbf,0xae,0xbf,0x89,0xbb,0xb7,0xbf,0x99,0xb5,0xb5,0xb1,0xb3,0xbf,0xa9,0x8d,0xb3,0xae,0xb2,0x8f,0xa8,0xb6,0xfa,0xe7,0xfa]

private func surgeryDevoteTail(row num: UInt8) -> UInt8 {
    return num ^ 218
}

/*: "UID" :*/
fileprivate let k_sendBroadId:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let app_threadFormat:String = "PHPlapElaplapI"
fileprivate let notiMiniName:[Character] = ["D"]

/*: "func__saveAllCookies =  :*/
fileprivate let dataPubliclyPath:[Character] = ["f","u","n","c","_","_","s","a"]
fileprivate let dataNumbermerMessage:[Character] = ["v"]
fileprivate let noti_fatalStr:String = "eAllCreader equal big soon regular"

/*: "func__saveAllCookies  error" :*/
fileprivate let user_contextData:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o","k","i","e","s"," "," ","e","r"]
fileprivate let main_technicalFormat:String = "ROR"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  CoverAddrTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum CurrencyTableConvertible: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class CoverAddrTool: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = CoverAddrTool()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        privatenessLimited()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(app_scriptName.prefix(7)))) || newValue.hasPrefix((dataAddressFormat.replacingOccurrences(of: "resolution", with: "ht") + String(userStrengthPath.prefix(5)))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if showSceneColorText && !showSearchData {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(kAboutNetworkMessage.suffix(7))) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(kAboutNetworkMessage.suffix(7))) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    findOutCookies(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func privatenessLimited() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = notiExploreKey.integer(forKey: constFeatureMsg)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (String(data_noteTellId) + String(k_specPath)) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = statGet()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(app_scriptName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if showSceneColorText && !showSearchData {
            //: serverPrefix = "http://"
            serverPrefix = (String(app_scriptName.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (String(k_talkPushName) + String(mainPadPath.suffix(8))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func statGet() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func dismissHistory(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(mainOperationEndLapText)) + "\(mainRequestMsg)" + (String(k_attentionArtCloselyMessage)), (String(mainOperationEndLapText)) + "\(mainRequestMsg)" + (String(k_attentionArtCloselyMessage))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(app_telephoneMsg) + showEaseData.replacingOccurrences(of: "more", with: "i")),
         //: "recharge/applePayNotify",
         (noti_animaMsg.replacingOccurrences(of: "moment", with: "ec") + "arge/" + String(noti_certainSmartWithData.prefix(6)) + String(notiProcessUrl.suffix(8))),
         //: "user/appleLogin"] + slowPathArr1
         (String(app_harvestFormat.prefix(6)) + "ppleL" + app_refuseMsg.replacingOccurrences(of: "aware", with: "og"))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(show_electronicKey.prefix(6)) + "MyBla" + String(user_infoStr)),
         //: "mf/business/sendMsg",
         (String(showIncreaseMsg.prefix(6)) + "iness/se" + String(main_helloStr.prefix(5))),
         //: "mf/business/sendGift",
         (String(k_databaseId) + notiActionContent.replacingOccurrences(of: "destination", with: "bu") + "ess/se" + String(k_launchFormat.suffix(6))),
         //: "mf/chatRoom/sendMsg"]
         (String(showSumAgoReadyKey) + String(main_performName.suffix(4)) + "sendMsg")]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = CurrencyDefineReactiveCompatible.getSystemLangCode()
        let lang = CurrencyDefineReactiveCompatible.afterPinChild()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .warningAccount(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(user_exceptName)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension CoverAddrTool {
    //: func checkServerUrl() {
    func goodPart() {
        //: if RealmReactiveCompatible.share.networkStatus != .Unavailable {
        if RealmReactiveCompatible.share.networkStatus != .Unavailable {
            //: func_requestHost()
            miniWithHost()
            //: } else {
        } else {
            //: func_starCodeTime()
            thatTime()
        }
    }

    //: func func_starCodeTime() {
    func thatTime() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.miniWithHost()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func quitShape() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func miniWithHost() {
        //: if RealmReactiveCompatible.share.networkStatus == .Unavailable {
        if RealmReactiveCompatible.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        quitShape()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ManufacturerAppReactiveCompatible.shared { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (dataBarTitle + String(mainDarkMsg)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (main_soundFormat))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.thatTime()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (app_ownerValue.replacingOccurrences(of: "line", with: "成了") + "\u{6d4b},需要\u{66f4}" + String(noti_golfMessage.prefix(4)) + String(constHorseMessage.prefix(7))))
                    //: self.func__changeHostUrl()
                    self.halfPlayer()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func halfPlayer() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        quitShape()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        notiExploreKey.set(serverSelctIndex, forKey: constFeatureMsg)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(app_scriptName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if showSceneColorText, !showSearchData {
            //: serverPrefix = "http://"
            serverPrefix = (String(app_scriptName.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension CoverAddrTool {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func unlessDevice() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(kCarveStr.replacingOccurrences(of: "media", with: "TT") + "Proxy")] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func buryInService() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(notiUntilFormat.suffix(5)) + "PED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((app_loadCapReasonId.replacingOccurrences(of: "output", with: "ta"))) || keyStr.contains((String(data_primaryAmpleMessage))) || keyStr.contains((constExecutiveFemalePubliclyName.replacingOccurrences(of: "small", with: "c"))) || keyStr.contains((notiEnabledMessage.replacingOccurrences(of: "privacy", with: "pp"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension CoverAddrTool {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func findOutCookies(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(showCloudMessage) + String(userMysteryFormat) + String(app_foreheadId.suffix(5))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = canvassShow()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
        var urlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(app_scriptName.prefix(7))), with: (dataAddressFormat.replacingOccurrences(of: "resolution", with: "ht") + String(userStrengthPath.prefix(5))))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((dataAddressFormat.replacingOccurrences(of: "resolution", with: "ht") + String(userStrengthPath.prefix(5)))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(app_whenSpeakerKey.suffix(7)) + dataSecretKindUrl.capitalized) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            nightMenu(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        cookiesEveryLastRelieveFunc()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func nightMenu(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: dataActivityName.map{surgeryDevoteTail(row: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func canvassShow() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(app_scriptName.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if showSceneColorText, !showSearchData {
            //: serverPrefix = "https://"
            serverPrefix = (dataAddressFormat.replacingOccurrences(of: "resolution", with: "ht") + String(userStrengthPath.prefix(5)))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(mainOperationEndLapText)) + "\(mainRequestMsg)" + (String(k_attentionArtCloselyMessage)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func ideality(note _: Notification) {
        //: if RealmReactiveCompatible.share.loginUid.isEmptyString {
        if RealmReactiveCompatible.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            presentationVersion()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            angleDown()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func presentationVersion() {
        //: var htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
        var htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_sendBroadId)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(k_sendBroadId))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_sendBroadId)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(k_sendBroadId))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(k_sendBroadId))), h5Cookie.keys.contains((String(k_sendBroadId))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(k_sendBroadId))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(k_sendBroadId))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))), h5Cookie.keys.contains((app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func angleDown() {
        //: let htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
        let htmlDomain: String = RealmReactiveCompatible.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == RealmReactiveCompatible.share.loginUserMode.userID {
            if cookie.name == (String(k_sendBroadId)), cookie.value == RealmReactiveCompatible.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(k_sendBroadId)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(k_sendBroadId))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(k_sendBroadId))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(k_sendBroadId))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(app_threadFormat.replacingOccurrences(of: "lap", with: "S") + String(notiMiniName))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func cookiesEveryLastRelieveFunc() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(dataPubliclyPath) + String(dataNumbermerMessage) + String(noti_fatalStr.prefix(5)) + "ookies = ") + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            notiExploreKey.set(cookieData, forKey: kBlockStr)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(user_contextData) + main_technicalFormat.lowercased()))
        }
    }

    //: func func__setAppCookies() {
    func generalOff() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = notiExploreKey.object(forKey: kBlockStr)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func remindGlobal() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        notiExploreKey.removeObject(forKey: kBlockStr)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
