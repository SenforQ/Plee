
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let appBalanceFormat:[UInt8] = [0x75,0x64,0x64,0x78,0x71,0x6b,0x7d,0x75,0x64,0x6b,0x64,0x75,0x6d,0x6b,0x67,0x61,0x77,0x77,0x71,0x71,0x70,0x6b,0x7a,0x7b,0x60,0x7d,0x72,0x7d,0x77,0x75,0x60,0x7d,0x7b,0x7a]

private func exZone(shrink num: UInt8) -> UInt8 {
    return num ^ 52
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let noti_characterAgencyContent:[UInt8] = [0xbd,0xac,0xac,0xb0,0xb9,0xa3,0xb5,0xbd,0xac,0xa3,0xaf,0xa9,0xbe,0xaf,0xbf,0xae,0xb5,0xbe,0xb9,0xa3,0xaf,0xa9,0xbf,0xbf,0xb9,0xb9,0xb8,0xa3,0xb2,0xb3,0xa8,0xb5,0xba,0xb5,0xbf,0xbd,0xa8,0xb5,0xb3,0xb2]

private func hereCookie(manage num: UInt8) -> UInt8 {
    return num ^ 252
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let dataPullPossibleMessage:String = "point jump scene mode randommf/re"
fileprivate let noti_mailId:String = "CHARG"
fileprivate let k_blockSayStr:String = "wayte"
fileprivate let user_stageCaptureName:String = "Applpitch image"

/*: "productId" :*/
fileprivate let mainSafetyValue:String = "paboutoduct"
fileprivate let app_publishSubUrl:[Character] = ["I","d"]

/*: "source" :*/
fileprivate let dataPossibleTitle:String = "shellource"

/*: "IAP充值创建订单失败: :*/
fileprivate let user_calculateId:String = "IAP充\u{503c}\u{521b}"
fileprivate let user_lawMsg:[Character] = ["建","订","\u{5355}","失","败",":"]

/*: "orderNum" :*/
fileprivate let constPanelUrl:String = "orderNumhundred hair"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let dataRouteMedalId:String = "label pointmf/r"
fileprivate let constBringValue:String = "together stringrge/"
fileprivate let mainLiteMessage:String = "Notifychance apply gum yet appeal"

/*: "IAP充值校验失败: :*/
fileprivate let notiFemaleStr:[Character] = ["I","A","P","充","值","校","验","\u{5931}","败",":"]

/*: "transactionId" :*/
fileprivate let appClickValueMessage:String = "transaunlessi"
fileprivate let app_birdName:String = "cookie shallonId"

/*: "reportMoney" :*/
fileprivate let kAddedToId:[Character] = ["r","e","p","o","r","t","M","o","n"]
fileprivate let dataBroadName:String = "hung"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let notiPlainData:[Character] = ["m","f","/","A","u","t","o","S","u","b","/","A","p","p","l","e","C","r","e","a","t","e","O","r"]
fileprivate let data_becomeEquallyKey:[Character] = ["d","e","r"]

/*: "IAP订阅创建订单失败: :*/
fileprivate let user_pressureMessage:[Character] = ["I","A","P","\u{8ba2}","阅","创","建","订","单","失","败",":"]

/*: "orderId" :*/
fileprivate let data_alwaysName:[UInt8] = [0xa5,0xb8,0xae,0xaf,0xb8,0x83,0xae]

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let mainBackgroundFormat:String = "mf/Aslide new highlight"
fileprivate let noti_devoteUrl:String = "/Applany widespread stumble"
fileprivate let userCordMessage:String = "slows"

/*: "IAP订阅校验失败: :*/
fileprivate let constChartPath:[Character] = ["I","A","P","订","阅","校","验","\u{5931}","败"]
fileprivate let dataFlexibleValue:String = "player"

/*: "App" :*/
fileprivate let mainForthValue:String = "base print movie action demonstrateApp"

/*: "OrderTransactionInfo_ :*/
fileprivate let kTalkUrl:[Character] = ["O","r","d","e","r","T","r","a","n","s","a","c","t","i","o","n","I","n","f"]
fileprivate let kDocumentMsg:[Character] = ["o","_"]

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let show_centerValue:[UInt8] = [0x5f,0x65,0x62,0x69,0x72,0x63,0x73,0x62,0x75,0x53,0x5f,0x6f,0x66,0x6e,0x49,0x6e,0x6f,0x69,0x74,0x63,0x61,0x73,0x6e,0x61,0x72,0x54,0x72,0x65,0x64,0x72,0x4f]

/*: "verifyData" :*/
fileprivate let notiTotalValue:[UInt8] = [0x14,0x7,0x10,0xb,0x4,0x1b,0x26,0x3,0x16,0x3]

private func taskGiving(domain num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let main_foreLaterData:String = "superior umber shall before rawIAP查询内"
fileprivate let main_oppositionText:String = "购\u{5546}品失败, p"
fileprivate let constLengthGoingPath:String = "roducapture"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let constRecoverKey:String = "possible adjust fast givingIAP查询"
fileprivate let kRequestFormat:String = "失败, prround female"
fileprivate let show_possessionKey:String = "memberduct"
fileprivate let userCapableName:String = "t\u{4e3a}0."

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let notiMiniCreateerValue:String = "IAP\u{67e5}询\u{5185}"
fileprivate let noti_currentPath:String = "when cooperative deed败, Er"
fileprivate let mainArrowMsg:[Character] = ["r","o","r",":"]

/*: "IAP-purchased: :*/
fileprivate let showKingTermCenterStr:String = "injury resume disabled lapIAP-p"
fileprivate let showNothingName:[Character] = [":"]

/*: , 订单号: :*/
fileprivate let kFatherUniformUserStr:String = ", 订单result:"

/*: , 商品Id: :*/
fileprivate let app_anonKitId:String = ", 商品Id:birthday always queen"

/*: , 苹果transId: :*/
fileprivate let dataFaceFormat:String = ", 苹果tdevice whole acquisition forth"
fileprivate let app_profileData:String = "enjoy norransId:"

/*: "IAP-failed: :*/
fileprivate let noti_existKey:[Character] = ["I","A","P","-","f","a","i","l","e"]
fileprivate let noti_policyName:[Character] = ["d",":"]

/*: "IAP-deferred: :*/
fileprivate let dataHorizonBackMsg:String = "IAP-title force remark extra"
fileprivate let appOperativeAngleMsg:String = "attach interaction border seekred:"

/*: "IAP未知交易类型: :*/
fileprivate let noti_deviceMessage:[Character] = ["I","A","P","未","知","交","\u{6613}"]
fileprivate let main_collectValue:[Character] = ["类","型",":"]

/*: " 未知的交易类型" :*/
fileprivate let dataBladeUrl:String = "relation"
fileprivate let mainAreaId:[Character] = [" ","未","知","的","交","易","\u{7c7b}","型"]

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let const_plantName:String = "IAP\u{83b7}"
fileprivate let app_motionId:[Character] = [","," ","t","r","a","n","s","a","c","t","i","o","n","I","d",":"]

/*: , orderId: :*/
fileprivate let userAdditionalData:String = ", ordecolumn back pattern"

/*: , payType: :*/
fileprivate let show_fleetAliveUrl:[Character] = [","," ","p","a","y","T","y","p","e",":"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FalseFruitLiteralLandAgentReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let const_captureName = NSNotification.Name(rawValue: String(bytes: appBalanceFormat.map{exZone(shrink: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let show_thirdTitle = NSNotification.Name(rawValue: String(bytes: noti_characterAgencyContent.map{hereCookie(manage: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let show_hundredValue = 9

/// 支付类型
//: enum ApplePayType {
enum MultiType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum StatusGraphConvertible: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (StatusGraphConvertible, Double, MultiType) -> Void

//: class AppleIAPManager: NSObject {
class FalseFruitLiteralLandAgentReactiveCompatible: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: MultiType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = FalseFruitLiteralLandAgentReactiveCompatible()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(mediaBox),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func mediaBox() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension FalseFruitLiteralLandAgentReactiveCompatible {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func be(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(dataPullPossibleMessage.suffix(5)) + noti_mailId.lowercased() + "e/cr" + k_blockSayStr.replacingOccurrences(of: "way", with: "ea") + String(user_stageCaptureName.prefix(4)) + "ePay")
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(mainSafetyValue.replacingOccurrences(of: "about", with: "r") + String(app_publishSubUrl))] = productId
        //: dict["source"] = source
        dict[(dataPossibleTitle.replacingOccurrences(of: "hello", with: "o"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                GraphicCanvasThen.mnemonics(msg: (user_calculateId + String(user_lawMsg)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(constPanelUrl.prefix(8)))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func mol(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(dataRouteMedalId.suffix(4)) + "echa" + String(constBringValue.suffix(4)) + "applePay" + String(mainLiteMessage.prefix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    GraphicCanvasThen.mnemonics(msg: (String(notiFemaleStr)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.settle(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.incur()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: const_captureName, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(String(kAddedToId) + dataBroadName.replacingOccurrences(of: "hung", with: "ey"))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension FalseFruitLiteralLandAgentReactiveCompatible {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func reasoned(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(notiPlainData) + String(data_becomeEquallyKey))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(mainSafetyValue.replacingOccurrences(of: "about", with: "r") + String(app_publishSubUrl))] = productId
        //: dict["source"] = source
        dict[(dataPossibleTitle.replacingOccurrences(of: "hello", with: "o"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                GraphicCanvasThen.mnemonics(msg: (String(user_pressureMessage)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: data_alwaysName.map{$0^202}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func dispose(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(mainBackgroundFormat.prefix(4)) + "utoSub" + String(noti_devoteUrl.prefix(5)) + "ePaySucc" + userCordMessage.replacingOccurrences(of: "slow", with: "es"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    GraphicCanvasThen.mnemonics(msg: (String(constChartPath) + dataFlexibleValue.replacingOccurrences(of: "player", with: ":")) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.settle(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.firstborn()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: show_thirdTitle, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(String(kAddedToId) + dataBroadName.replacingOccurrences(of: "hung", with: "ey"))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension FalseFruitLiteralLandAgentReactiveCompatible {
    /// 初始化数据
    //: private func iap_initData() {
    private func drawData() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = sessionGesture(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = sessionGesture(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func sessionGesture(payType: MultiType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = incur()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = firstborn()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func incur() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(mainForthValue.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = notiExploreKey.string(forKey: mainMethodData) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(kTalkUrl) + String(kDocumentMsg)) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func firstborn() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(mainForthValue.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = notiExploreKey.string(forKey: mainMethodData) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: show_centerValue.reversed(), encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func demurData(_ transactionId: String, _ payType: MultiType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: notiTotalValue.map{taskGiving(domain: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: notiTotalValue.map{taskGiving(domain: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension FalseFruitLiteralLandAgentReactiveCompatible {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func umber() {
        //: iap_initData()
        drawData()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            homeboy(dict[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            homeboy(dict[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func homeboy(_ transactionId: String?, _ payType: MultiType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        settle(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension FalseFruitLiteralLandAgentReactiveCompatible {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func strikeOut(productId: String, payType: MultiType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        drawData()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            be(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.golfer(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            reasoned(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.golfer(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func golfer(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            GraphicCanvasThen.mnemonics(msg: (String(main_foreLaterData.suffix(6)) + main_oppositionText + constLengthGoingPath.replacingOccurrences(of: "capture", with: "c") + "tId:") + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.listing()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func listing() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension FalseFruitLiteralLandAgentReactiveCompatible: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            GraphicCanvasThen.mnemonics(msg: (String(constRecoverKey.suffix(5)) + "\u{5185}\u{8d2d}商品" + String(kRequestFormat.prefix(6)) + show_possessionKey.replacingOccurrences(of: "member", with: "o") + "s.coun" + userCapableName))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        GraphicCanvasThen.mnemonics(msg: (notiMiniCreateerValue + "购商品失" + String(noti_currentPath.suffix(5)) + String(mainArrowMsg)) + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension FalseFruitLiteralLandAgentReactiveCompatible: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    GraphicCanvasThen.mnemonics(msg: (String(showKingTermCenterStr.suffix(5)) + "urchased" + String(showNothingName)) + "\(currentPayType)" + (kFatherUniformUserStr.replacingOccurrences(of: "result", with: "号")) + "\(String(describing: createOrderId))" + (String(app_anonKitId.prefix(7))) + "\(transaction.payment.productIdentifier)" + (String(dataFaceFormat.prefix(5)) + String(app_profileData.suffix(7))) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    settle(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                GraphicCanvasThen.mnemonics(msg: (String(noti_existKey) + String(noti_policyName)) + "\(currentPayType)" + (kFatherUniformUserStr.replacingOccurrences(of: "result", with: "号")) + "\(String(describing: createOrderId))" + (String(app_anonKitId.prefix(7))) + "\(transaction.payment.productIdentifier)" + (String(dataFaceFormat.prefix(5)) + String(app_profileData.suffix(7))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: let vc = TalkingContactServiceView.init()
                let vc = ImpressionView()
                //: vc.show()
                vc.indexWindow()
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                GraphicCanvasThen.mnemonics(msg: (String(dataHorizonBackMsg.prefix(4)) + "defer" + String(appOperativeAngleMsg.suffix(4))) + "\(currentPayType)" + (kFatherUniformUserStr.replacingOccurrences(of: "result", with: "号")) + "\(String(describing: createOrderId))" + (String(app_anonKitId.prefix(7))) + "\(transaction.payment.productIdentifier)" + (String(dataFaceFormat.prefix(5)) + String(app_profileData.suffix(7))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                GraphicCanvasThen.mnemonics(msg: (String(noti_deviceMessage) + String(main_collectValue)) + "\(currentPayType)" + (kFatherUniformUserStr.replacingOccurrences(of: "result", with: "号")) + "\(String(describing: createOrderId))" + (String(app_anonKitId.prefix(7))) + "\(transaction.payment.productIdentifier)" + (String(dataFaceFormat.prefix(5)) + String(app_profileData.suffix(7))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((dataBladeUrl.replacingOccurrences(of: "relation", with: "") + String(mainAreaId)))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func settle(_ transactionId: String, _ payType: MultiType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = demurData(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            GraphicCanvasThen.mnemonics(msg: (const_plantName + "取本地收\u{636e}\u{6570}\u{636e}失败" + String(app_motionId)) + "\(transactionId)" + (String(userAdditionalData.prefix(6)) + "rId:") + "\(String(describing: createOrderId))" + (String(show_fleetAliveUrl)) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))] == transactionId || $0[String(bytes: data_alwaysName.map{$0^202}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: data_alwaysName.map{$0^202}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: notiTotalValue.map{taskGiving(domain: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.incur()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))] == transactionId || $0[String(bytes: data_alwaysName.map{$0^202}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: data_alwaysName.map{$0^202}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: notiTotalValue.map{taskGiving(domain: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.firstborn()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > show_hundredValue {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            mol(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(appClickValueMessage.replacingOccurrences(of: "unless", with: "ct") + String(app_birdName.suffix(4)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            dispose(transactionId, params: paramsArr.first!)
        }
    }
}
