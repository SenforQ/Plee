
//: Declare String Begin

/*: "fee" :*/
fileprivate let data_bitName:String = "fsay"

/*: "VIPFee" :*/
fileprivate let k_broadText:String = "accept thanks disabledVIPFee"

/*: "chattingNum" :*/
fileprivate let dataNorMsg:String = "cninetti"
fileprivate let app_assignRootPeriodName:[Character] = ["m"]

/*: "You're already in her live room" :*/
fileprivate let notiSoundName:[UInt8] = [0x6d,0x6f,0x6f,0x72,0x20,0x65,0x76,0x69,0x6c,0x20,0x72,0x65,0x68,0x20,0x6e,0x69,0x20,0x79,0x64,0x61,0x65,0x72,0x6c,0x61,0x20,0x65,0x72,0x27,0x75,0x6f,0x59]

/*: "toUid" :*/
fileprivate let notiIndependenceStr:[UInt8] = [0x81,0x9a,0xa0,0x9c,0x91]

private func storyWelcome(transform num: UInt8) -> UInt8 {
    return num ^ 245
}

/*: "momentId" :*/
fileprivate let k_deliveryUrl:[UInt8] = [0x4c,0x4e,0x4c,0x44,0x4d,0x53,0x28,0x43]

fileprivate func flowContext(attractive num: UInt8) -> UInt8 {
    let value = Int(num) - 223
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "extra" :*/
fileprivate let k_norName:[UInt8] = [0x37,0x4a,0x46,0x44,0x33]

fileprivate func eyeContactLow(enough num: UInt8) -> UInt8 {
    let value = Int(num) - 210
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "uid" :*/
fileprivate let k_requestName:[UInt8] = [0x64,0x69,0x75]

/*: "onlineStatus" :*/
fileprivate let k_achievementPath:[UInt8] = [0x73,0x75,0x74,0x61,0x74,0x53,0x65,0x6e,0x69,0x6c,0x6e,0x6f]

/*: "isNewUser" :*/
fileprivate let dataCounterestedId:[UInt8] = [0xd3,0xc9,0xf4,0xdf,0xcd,0xef,0xc9,0xdf,0xc8]

/*: "userStatus" :*/
fileprivate let appPlantMessage:[UInt8] = [0x9d,0x9b,0x8d,0x9a,0x7b,0x9c,0x89,0x9c,0x9d,0x9b]

fileprivate func userMagnitude(context num: UInt8) -> UInt8 {
    let value = Int(num) - 40
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Account is restricted！" :*/
fileprivate let appHighlightUrl:String = "Accouspread total since scale"
fileprivate let noti_noseStr:String = "terms"
fileprivate let mainBeautyData:String = "stricted！conversion behind"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let show_rearYesterdayMsg:[UInt8] = [0xb6,0x91,0x99,0x9c,0x95,0x94,0xd0,0x84,0x9f,0xd0,0x95,0x9e,0x84,0x95,0x82,0xd0,0x84,0x98,0x95,0xd0,0x93,0x98,0x91,0x84,0xd0,0x82,0x9f,0x9f,0x9d,0xde,0xd0,0xa0,0x9c,0x95,0x91,0x83,0x95,0xd0,0x84,0x82,0x89,0xd0,0x91,0x97,0x91,0x99,0x9e,0xd0,0x9c,0x91,0x84,0x95,0x82]

private func constraintInsert(limit num: UInt8) -> UInt8 {
    return num ^ 240
}

/*: "url" :*/
fileprivate let appLevelStr:String = "URL"

/*: "userDetail" :*/
fileprivate let noti_giveName:[Character] = ["u","s","e","r","D","e","t","a","i","l"]

/*: "&type=6" :*/
fileprivate let data_assetMessage:String = "&type=6medication rid father work drawing"

/*: "&type=7" :*/
fileprivate let constEventText:String = "&type=7roll luck percept direct theme"

/*: "amount" :*/
fileprivate let noti_moreForePath:[UInt8] = [0xda,0xd6,0xd4,0xce,0xd5,0xcf]

private func behindHe(firm num: UInt8) -> UInt8 {
    return num ^ 187
}

/*: "https://" :*/
fileprivate let const_termFormat:String = "indicatort"
fileprivate let app_tipMessage:[Character] = ["p","s",":","/","/"]

/*: "http://" :*/
fileprivate let const_consentMessage:String = "left appear neverhttp://"

/*: "transparency" :*/
fileprivate let mainCommendationManFormat:String = "trthenns"
fileprivate let notiPlanTitle:String = "parencparty"

/*: "version= :*/
fileprivate let dataWantPath:[Character] = ["v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let noti_yourselfKey:String = "&paccookie beneath together retain"
fileprivate let k_nineWriteUrl:[Character] = ["="]

/*: &bundleId= :*/
fileprivate let main_pendingVoiceName:[Character] = ["&","b","u","n","d","l","e","I"]
fileprivate let const_noId:String = "organize cover one tap yad="

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BrowReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class BrowReactiveCompatible: NSObject {
public class BrowReactiveCompatible: NSObject {
    //: @objc static public let share = BrowReactiveCompatible()
    @objc public static let share = BrowReactiveCompatible()
    //: private override init() {}
    override private init() {}
}

//: extension BrowReactiveCompatible {
extension BrowReactiveCompatible {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func laterRand(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        dataFilterFormat.psychologicalFeatureTarget(eventID: k_nameText)
        //: guard TalkingSocketManager.shared.isFisher == false else {
        guard ManagerCommentThen.shared.isFisher == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.bringHome(showMsg: app_nextKey)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        GravityReactiveCompatible.nearStatusMicrophone { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            BrowThen.untilForm { succeed, result, _ in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = PossibilityMatchDelegate()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(data_bitName.replacingOccurrences(of: "say", with: "ee"))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(k_broadText.suffix(6)))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(dataNorMsg.replacingOccurrences(of: "nine", with: "ha") + "ngNu" + String(app_assignRootPeriodName))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.currentPositionController()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 完善资料

    //: func func__pushToFillUserInfoVC() {
    func pushBackSmall() {
        //: RealmReactiveCompatible.share.userFillInfoMode = UserFillInfoModel.init()
        RealmReactiveCompatible.share.userFillInfoMode = CommentInfoModel()
        //: let VC = TalkingLoginGenderVC()
        let VC = StanceGenderVc()
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = currentnessSuccessfully()
        //: if currentVC != nil {
        if currentVC != nil {
            //: currentVC?.navigationController?.pushViewController(VC, animated: true)
            currentVC?.navigationController?.pushViewController(VC, animated: true)
        }
    }

    // MARK: - 绑定邮箱

    /// - Parameters:
    ///   - type: 类型
    ///   - isShowBack: 是否显示返回按钮
    //: func func__pushToLockUserEmailVC(isShowBack: Bool = true) {
    func adminRender(isShowBack: Bool = true) {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = currentnessSuccessfully()
        //: if currentVC != nil {
        if currentVC != nil {
            //: if currentVC!.isKind(of: TalkingLoginBindEmailVC.self) ||
            if currentVC!.isKind(of: RuleViewController.self) ||
                //: currentVC!.isKind(of: TalkingLoginMainViewController.self) {
                currentVC!.isKind(of: BiochemicalMechanismThen.self)
            {
                //: return
                return
            }
            //: let vc = TalkingLoginBindEmailVC()
            let vc = RuleViewController()
            //: vc.isBack = isShowBack
            vc.isBack = isShowBack
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func builderUid(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.currentPositionController() else { return }
        //: if let liveVC = currentVC as? TalkingLiveBeautifyViewController, TalkingLiveManager.shared().isLive {
        if let liveVC = currentVC as? ExcitementViewController, SoundCombinedReactiveCompatible.buildShared().isLive {
            // 从主播页跳转个人资料时，切小屏模式
            //: liveVC.topView.miniButtonClick()
            liveVC.topView.waiter()

            //: } else if let profileVC = currentVC as? TalkingUserDetailViewController {
        } else if let profileVC = currentVC as? ManufacturerThen {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.makeup()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = ManufacturerThen(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        duringNote()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func funcInsideTypeMale(uid: String, enterType: OperateEnterType) {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard GravityReactiveCompatible.supposedBubble() == false else { return }

        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(SoundCombinedReactiveCompatible.buildShared().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.bringHome(showMsg: String(bytes: notiSoundName.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        AudienceManager().toAGreaterExtentCompletion(streamerUid: uid) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = FinsherHandyJSON.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.currentnessSuccessfully()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: CarrierViewController.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! CarrierViewController).yearAnimated()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            SoundCombinedReactiveCompatible.buildShared().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = CarrierViewController()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                dataFilterFormat.psychologicalFeatureTarget(eventID: show_cornerMessage, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                dataFilterFormat.psychologicalFeatureTarget(eventID: app_playTitle, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func pushMonth(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = MethodViewDelegate(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = currentnessSuccessfully()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func minCompletion(chatID: String, isFrom: CoverFromEnum = .Normal, completion: ((_ vc: ProgramBannerDelegate) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != CurrencyDefineReactiveCompatible.getXiaoMiID() {
        if chatID != CurrencyDefineReactiveCompatible.metricLinearUnit() { // 系统消息
            //: ProgressHUD.show()
            LawReactiveCompatible.frontEndShow()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: notiIndependenceStr.map{storyWelcome(transform: $0)}, encoding: .utf8)!: chatID, String(bytes: k_deliveryUrl.map{flowContext(attractive: $0)}, encoding: .utf8)!: "0", String(bytes: k_norName.map{eyeContactLow(enough: $0)}, encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            ChatThen.parent(param: param) { [weak self] succeed, result, _ in
                //: ProgressHUD.dismiss()
                LawReactiveCompatible.minify()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = CurrencyModelType.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = CurrencyModelType.sirenSong(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                DotInfoManager.unidentified(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: k_requestName.reversed(), encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: k_achievementPath.reversed(), encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: dataCounterestedId.map{$0^186}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: appPlantMessage.map{userMagnitude(context: $0)}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: userMaxWithoutData,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.bringHome(showMsg: (String(appHighlightUrl.prefix(5)) + "nt is r" + noti_noseStr.replacingOccurrences(of: "terms", with: "e") + String(mainBeautyData.prefix(9))).localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.utiliser(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: CurrencyModelType())
                let chatVC = self.utiliser(chatID: chatID, isFrom: isFrom, userWrap: CurrencyModelType())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: CurrencyModelType ) -> TalkingPrivateChatController {
    func utiliser(chatID: String, isFrom: CoverFromEnum = .Normal, userWrap: CurrencyModelType) -> ProgramBannerDelegate {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = currentnessSuccessfully()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: ProgramBannerDelegate.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! ProgramBannerDelegate
                    //: if vc.targetId == chatID {
                    if vc.targetId == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = ProgramBannerDelegate(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func incomeAvailable(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        LawReactiveCompatible.frontEndShow()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        ChatThen.organize(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            LawReactiveCompatible.minify()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.bringHome(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if CurrencyDefineReactiveCompatible.isGroupChat(groupId) {
            if CurrencyDefineReactiveCompatible.finding(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.localId(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = TelevisionReactiveCompatible(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.currentnessSuccessfully()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, _ in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.localId(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = TelevisionReactiveCompatible(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.currentnessSuccessfully()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.bringHome(showMsg: String(bytes: show_rearYesterdayMsg.map{constraintInsert(limit: $0)}, encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func localId(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.currentnessSuccessfully()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: TelevisionReactiveCompatible.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! TelevisionReactiveCompatible
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 真人认证

    //: func func__pushUserVerifyController(toast: String?) {
    func carryToast(toast _: String?) {
        //: let tpAuth = TPUserAuth(rawValue: RealmReactiveCompatible.share.loginUserMode.isTPAuth)
        let tpAuth = BrowSendable(rawValue: RealmReactiveCompatible.share.loginUserMode.isTPAuth)
        //: switch(tpAuth) {
        switch tpAuth {
        //: case .unknown, .isRefused:
        case .unknown, .isRefused:
            //: let vc = TalkingStatementEquityVC()
            let vc = NegationViewController()
//            vc.toastStr = toast ?? ""
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = currentnessSuccessfully()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: case .isOnGoing:
        case .isOnGoing:
            //: let vc = TalkingEditProfilesVC()
            let vc = ChangeViewController()
            //: let currentVC = func__getCurrentActivityVC()
            let currentVC = currentnessSuccessfully()
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)

        //: default:
        default:
            //: break
            break
        }
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func putVia(uid _: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func funcSimultaneously() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = currentnessSuccessfully()
        //: let vc = TalkingFeedbackVC.init()
        let vc = LevelViewController()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func lipModel(jumpModel: LawMeasurable) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (appLevelStr.lowercased()) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            blade(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (String(noti_giveName)) {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func nearAim() {
        //: BrowReactiveCompatible.share.func__pushToWebVC(webViewType: .SubscribePage)
        BrowReactiveCompatible.share.pathNext(webViewType: .SubscribePage)
    }

    // MARK: - 设置密码页面

    /// 跳转设置密码页面
    /// - Parameters:
    ///   - type: 手机号/邮箱
    ///   - content: 展示内容
    ///   - isNewPwd: 是否新设置密码（修改需要验证码）
    //: func func__pushToSetNewPasswordVC(type: LoginType, content: String, isNewPwd: Bool) {
    func commentPwd(type: GreatSubqueryConvertible, content: String, isNewPwd: Bool) {
        //: let vc = TalkingSetNewPasswordVC()
        let vc = StanceViewController()
        //: vc.type = type
        vc.type = type
        //: vc.content = content
        vc.content = content
        //: vc.isNewPwd = isNewPwd
        vc.isNewPwd = isNewPwd
        //: func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
        currentnessSuccessfully()?.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - 跳转H5

//: extension BrowReactiveCompatible {
extension BrowReactiveCompatible {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func pathNext(webViewType: ObjectDefaultsSerializable) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        fitAwake(webViewType: webViewType, webConfig: BarbConfig())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func playfulness(popupCallIndex: Int, appendParams: String = (String(data_assetMessage.prefix(7)))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = WrongView()
        //: view.show()
        view.statement()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.progressEvent(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                anyPressure(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func beContext(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(constEventText.prefix(7)))) {
        //: let payWinType = RealmReactiveCompatible.share.appUserConfigMode.payWinType
        let payWinType = RealmReactiveCompatible.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = WrongView()
        //: view.show()
        view.statement()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (RealmReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (RealmReactiveCompatible.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.progressEvent(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                anyPressure(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func anyPressure(appendParams: String = (String(constEventText.prefix(7)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = LimitedManager.ethmoidalArteryMobile(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: BrowReactiveCompatible.share.func__pushToWebVC(urlStr: urlStr)
        BrowReactiveCompatible.share.blade(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        dataFilterFormat.supposed(eventID: user_toMessage, parameterStr: [String(bytes: noti_moreForePath.map{behindHe(firm: $0)}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func progressEvent(webViewType: ObjectDefaultsSerializable, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = BarbConfig()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        fitAwake(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            data_nameAssetValue = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                dataFilterFormat.supposed(eventID: clickEvent, parameterStr: [String(bytes: noti_moreForePath.map{behindHe(firm: $0)}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func setView() {
        //: guard !RealmReactiveCompatible.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !RealmReactiveCompatible.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = RealmReactiveCompatible.share.appConfigMode.urlH5Domain + RealmReactiveCompatible.share.appUserConfigMode.discountUrl
        let httpsUrl = RealmReactiveCompatible.share.appConfigMode.urlH5Domain + RealmReactiveCompatible.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (const_termFormat.replacingOccurrences(of: "indicator", with: "ht") + String(app_tipMessage)), with: (String(const_consentMessage.suffix(7))))
        //: BrowReactiveCompatible.share.func__pushToWebVC(urlStr: httpUrl)
        BrowReactiveCompatible.share.blade(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func fitAwake(webViewType: ObjectDefaultsSerializable, webConfig: BarbConfig) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = LimitedManager.ethmoidalArteryMobile(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        cannibalize(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func blade(urlStr: String?, webConfig: BarbConfig? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? BarbConfig() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        cannibalize(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func cannibalize(urlStr: String?, webViewType: ObjectDefaultsSerializable?, webConfig: BarbConfig) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(mainCommendationManFormat.replacingOccurrences(of: "then", with: "a") + notiPlanTitle.replacingOccurrences(of: "party", with: "y"))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (String(dataWantPath)) + "\(data_malePath)" + (String(noti_yourselfKey.prefix(4)) + "kageId" + String(k_nineWriteUrl)) + "\(user_workName)" + (String(main_pendingVoiceName) + String(const_noId.suffix(2))) + "\(main_informationId)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = TransitionViewController(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = currentnessSuccessfully() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: TransitionViewController.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! TransitionViewController).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.lifeStyle(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataModeBlockStr, height: dataModeBlockStr / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension BrowReactiveCompatible {
public extension BrowReactiveCompatible {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func currentnessSuccessfully() -> UIViewController? {
        //: return currentViewController()
        return currentPositionController()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct LawMeasurable: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
