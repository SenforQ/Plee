
//: Declare String Begin

/*: "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$" :*/
fileprivate let kNothingName:[UInt8] = [0x8c,0xfa,0x89,0xb3,0xff,0xa8,0x93,0xff,0x88,0xe2,0xff,0xeb,0x8d,0x8e,0xfc,0xff,0x8f,0xf9,0xfb,0x92,0xfa,0x89,0x8e,0xb6,0xb3,0xff,0xa8,0x8e,0xfc,0xff,0x8f,0xf9,0xfb,0x8e,0xfc,0xfa,0x89,0xb3,0xff,0xa8,0x8e,0xfc,0x8f,0xa9,0xe0,0xfe,0xe4,0xaf,0xfb,0xf6]

private func rapArea(theme num: UInt8) -> UInt8 {
    return num ^ 210
}

/*: "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$" :*/
fileprivate let notiReasonAppealId:[UInt8] = [0x6d,0x1b,0x1b,0x2,0x68,0x0,0x7,0x6,0x4,0xb,0x6e,0x68,0x3,0x1e,0xa,0x6e,0x48,0xa,0x4e,0x1a,0x4f,0x1b,0x1b,0x3,0x6f,0x57,0x48,0x1,0x4e,0x1e,0x6f,0x57,0x48,0xb,0x4e,0x1a,0x4f,0x1b,0x3,0x6f,0x57,0x48,0x0,0x4e,0x1e,0x6f,0x57,0x48,0x4,0x1f,0xb,0x4e,0x1a,0x4f,0x1b,0x3,0x6f,0x57,0x48,0x2,0x3,0x1f,0x2,0x2,0x4e,0x1a,0x1a,0x17]

private func accuracyCoordinator(err num: UInt8) -> UInt8 {
    return num ^ 51
}

/*: "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)" :*/
fileprivate let userTarValue:[UInt8] = [0xa4,0xd2,0xa1,0xa6,0x8f,0xce,0xbf,0xca,0xca,0xd7,0xa6,0x8f,0xc3,0xbc,0xbb,0xcf,0xbb,0xd7,0xa0,0x9b,0xd7,0x80,0xca,0xd7,0xc3,0xa5,0xa7,0x81,0xc8,0xd6,0xcb,0xca,0x87,0xde,0xd3]

private func consciousnessTwo(scheme num: UInt8) -> UInt8 {
    return num ^ 250
}

/*: "^[0-8]\\d{5}(?!\\d)$" :*/
fileprivate let user_siblingMsg:String = "^[0-8]"
fileprivate let appMeasurementFloorStr:[Character] = ["\\","d","{","5","}"]
fileprivate let kSFollowFormat:[Character] = ["(","?","!","\\","d",")","$"]

/*: "^[a-zA-Z0-9]{6,20}+$" :*/
fileprivate let show_netUrl:[Character] = ["^","[","a","-","z","A","-","Z","0","-","9"]
fileprivate let data_sufficientText:String = "prove"
fileprivate let k_audienceUrl:String = "{her,2"

/*: "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$" :*/
fileprivate let main_normalUrl:[UInt8] = [0x83,0x4d,0x8d,0x99,0x99,0x95,0x98,0x64,0x5f,0x81,0x54,0x81,0x54,0x4e,0x64,0x4d,0x80,0x81,0x89,0x86,0x52,0x9f,0x81,0x53,0x52,0x82,0x50,0x4e,0x81,0x53,0x4d,0x80,0x86,0x52,0x9f,0x81,0x53,0x82,0xa0,0x57,0x51,0x5b,0xa2,0x4e,0x4d,0x80,0x81,0x54,0x81,0x9c,0x45,0x81,0x53,0x52,0x82,0x4f,0x4e,0x4f,0x81,0x54,0x64,0x49]

fileprivate func toAGreaterExtent(fee num: UInt8) -> UInt8 {
    let value = Int(num) + 219
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" :*/
fileprivate let constExecuteAdviceToolStr:[UInt8] = [0x7c,0xa,0x1d,0x18,0xa,0x1d,0x18,0x10,0x17,0x79,0x12,0xf,0x17,0x7f,0x5e,0x10,0x79,0x12,0xf,0x16,0x7f,0x79,0x12,0xf,0x1b,0x7f,0x5e,0x79,0x12,0x13,0x7f,0x1d,0x79,0x12,0xf,0x1b,0x7f,0x79,0x12,0xf,0x1b,0x7f,0x1d,0xb,0x7e,0xc,0xb,0x2,0x59,0x11,0x5f,0xa,0x1d,0x18,0x10,0x17,0x79,0x12,0xf,0x17,0x7f,0x5e,0x10,0x79,0x12,0xf,0x16,0x7f,0x79,0x12,0xf,0x1b,0x7f,0x5e,0x79,0x12,0x13,0x7f,0x1d,0x79,0x12,0xf,0x1b,0x7f,0x79,0x12,0xf,0x1b,0x7f,0x1d,0xb,0x6]

private func refuseAfter(mini num: UInt8) -> UInt8 {
    return num ^ 34
}

/*: "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)" :*/
fileprivate let k_suitCosAlterTitle:[UInt8] = [0xc1,0xf7,0xf4,0xc9,0xc6,0xd2,0xf6,0x14,0xca,0xce,0x16,0xbd,0xc2,0x15,0xc1,0xf4,0xc9,0xc6,0xd2,0xf6,0x14,0xca,0xd0,0x16,0xc1,0xf4,0xc9,0xc6,0xd2,0xf6,0x15,0xf4,0x11,0xf1,0xf6,0xc2,0xbd,0xc2]

fileprivate func spacewalkPresentation(premium num: UInt8) -> UInt8 {
    let value = Int(num) - 153
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "SELF MATCHES %@" :*/
fileprivate let dataLiteralEyeMessage:[Character] = ["S","E","L","F"," ","M","A","T","C","H","E","S"]
fileprivate let app_homeUrl:String = " %@"

/*: "Feedback" :*/
fileprivate let appDateId:[Character] = ["F","e","e","d","b","a"]
fileprivate let userAreaStr:[Character] = ["c","k"]

/*: "Enter your feedback…" :*/
fileprivate let const_chooseName:String = "bean ok usEnter"
fileprivate let user_yearMessage:[Character] = [" "]
fileprivate let mainAmpleName:String = "fcordcorddb"

/*: "#999999" :*/
fileprivate let mainRefuseUrl:String = "#9999"
fileprivate let mainYearLogMsg:[Character] = ["9","9"]

/*: "Your email (Required) " :*/
fileprivate let userMobileValue:String = "Your equipment yield discount"
fileprivate let constNearbyDismissPropertyId:[Character] = ["e"]
fileprivate let dataFatalName:String = "connect planmail "
fileprivate let user_loseUrl:String = "ired) plant output beneath"

/*: "icon_me_feelback_star" :*/
fileprivate let show_snapMessage:String = "icon_mmp maker translate accounting tick"
fileprivate let show_effectMsg:String = "ship"
fileprivate let appAdvancedFormat:String = "_feeplatform number thanks"
fileprivate let mainNumberName:String = "_stargoing harassment"

/*: "#CCCCCC" :*/
fileprivate let userDiscountFormat:String = "#CCCCCcost exclusive food prompt identify"
fileprivate let constBranchResponsibilityTitle:String = "span"

/*: "Send" :*/
fileprivate let main_successfulFormat:String = "Sendaware equally yet"

/*: "#D0D0D0" :*/
fileprivate let app_analysisValue:String = "bold remove#D0D0"
fileprivate let show_violationTillMessage:[Character] = ["D","0"]

/*: "Please fill in the content" :*/
fileprivate let k_wouldText:[Character] = ["P","l","e","a","s","e"]
fileprivate let showIonId:String = "last add paper business video fil"
fileprivate let constLatStr:String = "blind basic chart here admin the c"

/*: "Please enter the correct email address" :*/
fileprivate let constOppositeContent:[UInt8] = [0xaf,0x93,0x9a,0x9e,0x8c,0x9a,0xdf,0x9a,0x91,0x8b,0x9a,0x8d,0xdf,0x8b,0x97,0x9a,0xdf,0x9c,0x90,0x8d,0x8d,0x9a,0x9c,0x8b,0xdf,0x9a,0x92,0x9e,0x96,0x93,0xdf,0x9e,0x9b,0x9b,0x8d,0x9a,0x8c,0x8c]

private func gravityColor(maker num: UInt8) -> UInt8 {
    return num ^ 255
}

/*: "content" :*/
fileprivate let appRelatedText:String = "camera"
fileprivate let main_darknessStr:String = "oproviderteprovidert"

/*: "contactWay" :*/
fileprivate let userLatFormat:String = "cgreennt"
fileprivate let notiPressureMomentComfortablePath:[Character] = ["a","y"]

/*: "platform" :*/
fileprivate let kSearchName:String = "plwith"

/*: "iphone" :*/
fileprivate let userEvaluateName:[Character] = ["i","p","h","o","n","e"]

/*: "version" :*/
fileprivate let const_threeMsg:String = "submitersion"

/*: "type" :*/
fileprivate let constHideSelectName:String = "TYPE"

/*: "Operation succeeded" :*/
fileprivate let constSchemeStr:[Character] = ["O","p","e","r","a","t","i","o","n"," ","s","u"]
fileprivate let user_projectUrl:[Character] = ["c","c","e","e","d","e","d"]

/*: "Problem statements" :*/
fileprivate let showTransformationName:String = "and possibility cover comfortable userProble"
fileprivate let main_directionCircleValue:String = "m stajust complaint mail straight"

/*: "Feature advice" :*/
fileprivate let k_mmDrawingStr:String = "till guide capacity tot whereFeatu"
fileprivate let main_monthRecoverFormat:String = "horizon"

/*: "Operation questions" :*/
fileprivate let show_retainMessage:[Character] = ["O","p","e","r","a","t","i","o","n"," ","q","u","e","s","t"]
fileprivate let main_remJawName:String = "ioscore"

/*: "Others" :*/
fileprivate let showChangeData:[Character] = ["O","t","h","e","r"]
fileprivate let constTalkMZonePath:[Character] = ["s"]

/*: "#EFEDFF" :*/
fileprivate let k_checkIgnoreText:[Character] = ["#","E","F","E","D","F","F"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LevelViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/15.
//

//: import UIKit
import UIKit

//: enum Validate {
enum GraphicLostValidate {
    //: case email(_: String)
    case email(_: String) //  邮箱
    //: case phoneNumber(_: String)
    case phoneNumber(_: String) //  手机号
    //: case userName(_: String)
    case userName(_: String) //  用户名
    //: case password(_: String)
    case password(_: String) //  密码
    //: case nickName(_: String)
    case nickName(_: String) //  昵称
    //: case postalCode(_: String)
    case postalCode(_: String) //  邮编
    //: case URL(_: String)
    case URL(_: String) //  URL
    //: case IP(_: String)
    case IP(_: String) //  IP
    //: case isChinese(_: String)
    case isChinese(_: String) //  是否全是中文字符
    //: case idNumber(_: String)
    case idNumber(_: String) //  身份证号

    //: var isRight: Bool {
    var isRight: Bool {
        //: var predicateStr: String!
        var predicateStr: String!
        //: var currentObject: String!
        var currentObject: String!
        //: switch self {
        switch self {
        //: case let .email(str):
        case let .email(str):
            //: predicateStr = "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$"
            predicateStr = String(bytes: kNothingName.map{rapArea(theme: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .phoneNumber(str):
        case let .phoneNumber(str):
            //: predicateStr = "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$"
            predicateStr = String(bytes: notiReasonAppealId.map{accuracyCoordinator(err: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .userName(str):
        case let .userName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: userTarValue.map{consciousnessTwo(scheme: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .postalCode(str):
        case let .postalCode(str):
            //: predicateStr = "^[0-8]\\d{5}(?!\\d)$"
            predicateStr = (user_siblingMsg.capitalized + String(appMeasurementFloorStr) + String(kSFollowFormat))
            //: currentObject = str
            currentObject = str
        //: case let .password(str):
        case let .password(str):
            //: predicateStr = "^[a-zA-Z0-9]{6,20}+$"
            predicateStr = (String(show_netUrl) + data_sufficientText.replacingOccurrences(of: "prove", with: "]") + k_audienceUrl.replacingOccurrences(of: "her", with: "6") + "0}+$")
            //: currentObject = str
            currentObject = str
        //: case let .nickName(str):
        case let .nickName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: userTarValue.map{consciousnessTwo(scheme: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .URL(str):
        case let .URL(str):
            //: predicateStr = "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$"
            predicateStr = String(bytes: main_normalUrl.map{toAGreaterExtent(fee: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .IP(str):
        case let .IP(str):
            //: predicateStr = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
            predicateStr = String(bytes: constExecuteAdviceToolStr.map{refuseAfter(mini: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let . isChinese(str):
        case let .isChinese(str):
            //: predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            //: currentObject = str
            currentObject = str
        //: case let . idNumber(str):
        case let .idNumber(str):
            //: predicateStr = "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)"
            predicateStr = String(bytes: k_suitCosAlterTitle.map{spacewalkPresentation(premium: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        }
        //: let predicate =  NSPredicate(format: "SELF MATCHES %@", predicateStr)
        let predicate = NSPredicate(format: (String(dataLiteralEyeMessage) + app_homeUrl.capitalized), predicateStr)
        //: return predicate.evaluate(with: currentObject)
        return predicate.evaluate(with: currentObject)
    }
}

//: class TalkingFeedbackVC: TalkingBaseViewController {
class LevelViewController: OperateReactiveCompatible {
    //: let limitCount = 300
    let limitCount = 300
    //: let EmialCount = 50
    let EmialCount = 50
    //: var seleteBtn: UIButton?
    var seleteBtn: UIButton?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()

        //: self.title = "Feedback".localized
        self.title = (String(appDateId) + String(userAreaStr)).localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: designView()
        burnView()
        //: addTapGestureRecognizer()
        asSet()
        //: addKeyboardNotification()
        visualEnrich()
    }

    // MARK: - Lazy load

    //: lazy var backView: UIScrollView = {
    lazy var backView: UIScrollView = {
        //: let view = UIScrollView.init()
        let view = UIScrollView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.showsHorizontalScrollIndicator = false
        view.showsHorizontalScrollIndicator = false
        //: view.showsVerticalScrollIndicator = false
        view.showsVerticalScrollIndicator = false
        //: return view
        return view
        //: }()
    }()

    //: lazy var contView: UIView = {
    lazy var contView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var inputTView: UIView = {
    lazy var inputTView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: view.backgroundColor = .appBgColor()
        view.backgroundColor = .smallColor()
        //: return view
        return view
        //: }()
    }()

    //: lazy var textView: TalkingTextView = {
    lazy var textView: GravityContentView = {
        //: let textView = TalkingTextView.init()
        let textView = GravityContentView()
        //: textView.placeholder = "Enter your feedback…".localized
        textView.placeholder = (String(const_chooseName.suffix(5)) + " your" + String(user_yearMessage) + mainAmpleName.replacingOccurrences(of: "cord", with: "e") + "ack…").localized
        //: textView.placeholderColor = UIColor.init(hex: "#999999")!
        textView.placeholderColor = UIColor(hex: (mainRefuseUrl.capitalized + String(mainYearLogMsg)))!
        //: textView.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        textView.font = UIFont.shrinkSize(type: .Regular, fontSize: 15)
        //: textView.textColor = UIColor.appTitleColor()
        textView.textColor = UIColor.askNameColor()
        //: textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)
        textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)

        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textContainer.lineBreakMode = .byTruncatingTail
        textView.textContainer.lineBreakMode = .byTruncatingTail
        //: textView.delegate = self
        textView.delegate = self
        //: textView.returnKeyType = UIReturnKeyType.done
        textView.returnKeyType = UIReturnKeyType.done
        //: textView.bounces = false
        textView.bounces = false
        //: textView.backgroundColor = .appBgColor()
        textView.backgroundColor = .smallColor()
        //: numberLB.text =  "0/\(limitCount)"
        numberLB.text = "0/\(limitCount)"
        //: return textView
        return textView
        //: }()
    }()

    //: lazy var numberLB: UILabel = {
    lazy var numberLB: UILabel = {
        //: let LB = UILabel.init()
        let LB = UILabel()
        //: LB.textColor = UIColor.init(hex: "#999999")
        LB.textColor = UIColor(hex: (mainRefuseUrl.capitalized + String(mainYearLogMsg)))
        //: LB.font = UIFont.pingfangFont(type: .Regular, fontSize: 14)
        LB.font = UIFont.shrinkSize(type: .Regular, fontSize: 14)
        //: LB.text = "0/\(limitCount)"
        LB.text = "0/\(limitCount)"
        //: LB.textAlignment = .right
        LB.textAlignment = .right
        //: return LB
        return LB
        //: }()
    }()

    //: lazy var emailTF: UITextField = {
    lazy var emailTF: UITextField = {
        //: let tf = UITextField.init()
        let tf = UITextField()
        //: tf.backgroundColor = .appBgColor()
        tf.backgroundColor = .smallColor()
        //: tf.textColor = UIColor.appTitleColor()
        tf.textColor = UIColor.askNameColor()
        //: tf.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        tf.font = UIFont.shrinkSize(type: .Regular, fontSize: 16)
        //: tf.delegate = self
        tf.delegate = self
        //: tf.returnKeyType = UIReturnKeyType.done
        tf.returnKeyType = UIReturnKeyType.done
        //: tf.attributedPlaceholder = NSAttributedString.init(string: "Your email (Required) ".localized, attributes: [
        tf.attributedPlaceholder = NSAttributedString(string: (String(userMobileValue.prefix(5)) + String(constNearbyDismissPropertyId) + String(dataFatalName.suffix(5)) + "(Requ" + String(user_loseUrl.prefix(6))).localized, attributes: [
            //: NSAttributedString.Key.foregroundColor: UIColor.init(hex: "#999999")!])
            NSAttributedString.Key.foregroundColor: UIColor(hex: (mainRefuseUrl.capitalized + String(mainYearLogMsg)))!])
        //: tf.clipsToBounds = true
        tf.clipsToBounds = true
        //: tf.layer.cornerRadius = 8
        tf.layer.cornerRadius = 8
        //: if LanguageManager.shared.direction == .rightToLeft {
        if RepresentLanguageManager.shared.direction == .rightToLeft {
            //: tf.textAlignment = .right
            tf.textAlignment = .right
        }
        //: let leftView = UIView()
        let leftView = UIView()
        //: leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))
        leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))

        //: let leftImgView = UIImageView.init()
        let leftImgView = UIImageView()
        //: leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        //: leftImgView.image = UIImage.BundleImageNamed(name: "icon_me_feelback_star")
        leftImgView.image = UIImage.adjust(name: (String(show_snapMessage.prefix(6)) + show_effectMsg.replacingOccurrences(of: "ship", with: "e") + String(appAdvancedFormat.prefix(4)) + "lback" + String(mainNumberName.prefix(5))))
        //: leftView.addSubview(leftImgView)
        leftView.addSubview(leftImgView)
        //: tf.leftView = leftView
        tf.leftView = leftView
        //: tf.leftViewMode = .always
        tf.leftViewMode = .always
        //: return tf
        return tf
        //: }()
    }()

    //: lazy var doneBtn: UIButton = {
    lazy var doneBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#CCCCCC")!, forState: .disabled)
        btn.heritageSubmit(color: UIColor(hex: (String(userDiscountFormat.prefix(6)) + constBranchResponsibilityTitle.replacingOccurrences(of: "span", with: "C")))!, forState: .disabled)
        //: btn.setBackgroundColor(color: UIColor.appThemeColor(), forState: .normal)
        btn.heritageSubmit(color: UIColor.labIndicator(), forState: .normal)
        //: btn.setTitle("Send".localized, for: .normal)
        btn.setTitle((String(main_successfulFormat.prefix(4))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.shrinkSize(type: .Medium, fontSize: 17)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(rowCancel), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFeedbackVC {
extension LevelViewController {
    //: @objc func TagBtnClick(sender: UIButton) {
    @objc func stay(sender: UIButton) {
        //: seleteBtn?.isSelected = false
        seleteBtn?.isSelected = false
        //: seleteBtn?.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
        seleteBtn?.layer.borderColor = UIColor(hex: (String(app_analysisValue.suffix(5)) + String(show_violationTillMessage)))?.cgColor
        //: sender.isSelected = !sender.isSelected
        sender.isSelected = !sender.isSelected
        //: seleteBtn = sender
        seleteBtn = sender
        //: seleteBtn!.layer.borderColor = UIColor.clear.cgColor
        seleteBtn!.layer.borderColor = UIColor.clear.cgColor
    }

    //: @objc func doneBtnClick() {
    @objc func rowCancel() {
        //: if seleteBtn == nil || textView.text.count<=0 || emailTF.text!.count<=0 {
        if seleteBtn == nil || textView.text.count <= 0 || emailTF.text!.count <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please fill in the content".localized)
            self.bringHome(showMsg: (String(k_wouldText) + String(showIonId.suffix(4)) + "l in" + String(constLatStr.suffix(6)) + "ontent").localized)
            //: return
            return
        }

        //: if LanguageManager.shared.direction == .leftToRight {
        if RepresentLanguageManager.shared.direction == .leftToRight {
            //: if !Validate.email(emailTF.text!).isRight {
            if !GraphicLostValidate.email(emailTF.text!).isRight {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please enter the correct email address".localized)
                self.bringHome(showMsg: String(bytes: constOppositeContent.map{gravityColor(maker: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["content"] = textView.text
        dict[(appRelatedText.replacingOccurrences(of: "camera", with: "c") + main_darknessStr.replacingOccurrences(of: "provider", with: "n"))] = textView.text
        //: dict["contactWay"] = emailTF.text
        dict[(userLatFormat.replacingOccurrences(of: "green", with: "o") + "actW" + String(notiPressureMomentComfortablePath))] = emailTF.text
        //: dict["platform"] = "iphone"
        dict[(kSearchName.replacingOccurrences(of: "with", with: "at") + "form")] = (String(userEvaluateName))
        //: dict["version"] = AppVersion
        dict[(const_threeMsg.replacingOccurrences(of: "submit", with: "v"))] = app_matchMessage
        //: dict["type"] = String((seleteBtn?.tag ?? 100)-100)
        dict[(constHideSelectName.lowercased())] = String((seleteBtn?.tag ?? 100) - 100)

        //: ProgressHUD.show()
        LawReactiveCompatible.frontEndShow()

        //: TalkingMeRequestTool.req_Feedback(params: dict) { succeed, result, errorModel in
        CombinedThen.anAction(params: dict) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            LawReactiveCompatible.minify()
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Operation succeeded".localized)
                self.readingDownMsg(showMsg: (String(constSchemeStr) + String(user_projectUrl)).localized)
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    //: func addKeyboardNotification() {
    func visualEnrich() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillShow(notification:)),
                                               selector: #selector(tuneUp(notification:)),
                                               //: name: UIResponder.keyboardWillShowNotification,
                                               name: UIResponder.keyboardWillShowNotification,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillHide(notification:)),
                                               selector: #selector(notice(notification:)),
                                               //: name: UIResponder.keyboardWillHideNotification,
                                               name: UIResponder.keyboardWillHideNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func keyboardWillShow(notification: Notification) {
    @objc func tuneUp(notification: Notification) {
        //: guard emailTF.isFirstResponder else { return }
        guard emailTF.isFirstResponder else { return }
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        //: let frame = value?.cgRectValue ?? .zero
        let frame = value?.cgRectValue ?? .zero
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        //: guard (emailTF.bottom + StatusBarNavigationBarHeight) > frame.minY else { return }
        guard (emailTF.bottom + mainEnterStr) > frame.minY else { return }

        //: let y = (emailTF.bottom + StatusBarNavigationBarHeight) - frame.minY
        let y = (emailTF.bottom + mainEnterStr) - frame.minY
        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
            self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
        }
    }

    //: @objc func keyboardWillHide(notification: Notification) {
    @objc func notice(notification: Notification) {
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25

        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = .identity
            self.backView.transform = .identity
        }
    }
}

// MARK: - UITextViewDelegate, UITextFieldDelegate

//: extension TalkingFeedbackVC: UITextViewDelegate, UITextFieldDelegate {
extension LevelViewController: UITextViewDelegate, UITextFieldDelegate {
    //: public func textViewDidChange(_ textView: UITextView) {
    public func textViewDidChange(_ textView: UITextView) {
        //: if textView.text.count > limitCount {
        if textView.text.count > limitCount {
            // 获得已输出字数与正输入字母数
            //: let selectRange = textView.markedTextRange
            let selectRange = textView.markedTextRange

            // 获取高亮部分 － 如果有联想词则解包成功
            //: if let selectRange = selectRange {
            if let selectRange = selectRange {
                //: let position =  textView.position(from: (selectRange.start), offset: 0)
                let position = textView.position(from: selectRange.start, offset: 0)
                //: if (position != nil) {
                if position != nil {
                    //: return
                    return
                }
            }

            //: let textContent = textView.text ?? ""
            let textContent = textView.text ?? ""
            //: let textNum = textContent.count
            let textNum = textContent.count

            // 截取
            //: if textNum > limitCount && limitCount > 0 {
            if textNum > limitCount && limitCount > 0 {
                //: textView.text = string_prefix(index: limitCount, text: textContent)
                textView.text = descriptionUser(index: limitCount, text: textContent)
            }
        }
        //: numberLB.text =  "\(textView.text.count)/\(limitCount)"
        numberLB.text = "\(textView.text.count)/\(limitCount)"
    }

    //: public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
    public func textViewShouldBeginEditing(_: UITextView) -> Bool {
        //: numberLB.isHidden = false
        numberLB.isHidden = false
        //: return true
        return true
    }

    //: public func textViewDidEndEditing(_ textView: UITextView) {
    public func textViewDidEndEditing(_ textView: UITextView) {
        //: if textView.text.isEmpty {
        if textView.text.isEmpty {
            //: numberLB.isHidden = false
            numberLB.isHidden = false
            //: } else {
        } else {
            //: numberLB.isHidden = true
            numberLB.isHidden = true
        }
    }

    //: public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    public func textView(_ textView: UITextView, shouldChangeTextIn _: NSRange, replacementText text: String) -> Bool {
        //: if textView.returnKeyType == .done {
        if textView.returnKeyType == .done {
            //: if text == "\n" {
            if text == "\n" {
                //: textView.resignFirstResponder()
                textView.resignFirstResponder()
            }
            //: return true
            return true
        }
        //: return true
        return true
    }

    // 字符串的截取 从头截取到指定index
    //: private func string_prefix(index: Int,text:String) -> String {
    private func descriptionUser(index: Int, text: String) -> String {
        //: if text.count <= index {
        if text.count <= index {
            //: return text
            return text
            //: } else {
        } else {
            //: let index = text.index(text.startIndex, offsetBy: index)
            let index = text.index(text.startIndex, offsetBy: index)
            //: let str = text.prefix(upTo: index)
            let str = text.prefix(upTo: index)
            //: return String(str)
            return String(str)
        }
    }

    //: func textFieldShouldReturn(_ textField: UITextField) -> Bool {
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        //: textField.resignFirstResponder()
        textField.resignFirstResponder()

        //: return true
        return true
    }

    //: func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
    func textField(_ textField: UITextField, shouldChangeCharactersIn _: NSRange, replacementString string: String) -> Bool {
        //: if textField.text!.count+string.count>EmialCount || textField.text!.count+string.count <= 0 {
        if textField.text!.count + string.count > EmialCount || textField.text!.count + string.count <= 0 {
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - UI

//: extension TalkingFeedbackVC {
extension LevelViewController {
    //: func designView() {
    func burnView() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: backView.addSubview(contView)
        backView.addSubview(contView)
        //: contView.snp.makeConstraints { make in
        contView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(backView)
            make.leading.trailing.top.bottom.equalTo(backView)
        }

        //: contView.addSubview(inputTView)
        contView.addSubview(inputTView)
        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(contView).offset(128)
            make.top.equalTo(contView).offset(128)
            //: make.height.equalTo(210)
            make.height.equalTo(210)
        }

        //: inputTView.addSubview(textView)
        inputTView.addSubview(textView)
        //: inputTView.addSubview(numberLB)
        inputTView.addSubview(numberLB)
        //: textView.snp.makeConstraints { make in
        textView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(inputTView)
            make.leading.trailing.top.equalTo(inputTView)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
        }
        //: numberLB.snp.makeConstraints { make in
        numberLB.snp.makeConstraints { make in
            //: make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: contView.addSubview(emailTF)
        contView.addSubview(emailTF)
        //: emailTF.snp.makeConstraints { make in
        emailTF.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(inputTView.snp.bottom).offset(16)
            make.top.equalTo(inputTView.snp.bottom).offset(16)
            //: make.height.equalTo(49)
            make.height.equalTo(49)
        }

        //: contView.addSubview(doneBtn)
        contView.addSubview(doneBtn)
        //: doneBtn.snp.makeConstraints { make in
        doneBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(54)
            make.leading.equalTo(contView).offset(54)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-54)
            make.trailing.equalTo(contView.snp.trailing).offset(-54)
            //: make.top.equalTo(emailTF.snp.bottom).offset(111)
            make.top.equalTo(emailTF.snp.bottom).offset(111)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: let arr = ["Problem statements".localized,
        let arr = [(String(showTransformationName.suffix(6)) + String(main_directionCircleValue.prefix(5)) + "tements").localized,
                   //: "Feature advice".localized,
                   (String(k_mmDrawingStr.suffix(5)) + "re advic" + main_monthRecoverFormat.replacingOccurrences(of: "horizon", with: "e")).localized,
                   //: "Operation questions".localized,
                   (String(show_retainMessage) + main_remJawName.replacingOccurrences(of: "score", with: "ns")).localized,
                   //: "Others".localized]
                   (String(showChangeData) + String(constTalkMZonePath)).localized]

        //: for i in 0..<arr.count {
        for i in 0 ..< arr.count {
            //: let xz = 15
            let xz = 15
            //: let yz = 16
            let yz = 16
            //: let Width = (Int(ScreenWidth)-xz*3)/2
            let Width = (Int(dataModeBlockStr) - xz * 3) / 2
            //: let height = 38
            let height = 38
            //: let X = xz+(xz + Width)*(i%2)
            let X = xz + (xz + Width) * (i % 2)
            //: let Y = yz+(yz+height)*(i/2)
            let Y = yz + (yz + height) * (i / 2)

            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.frame =  CGRect.init(x: X, y: Y , width: Width, height: height)
            btn.frame = CGRect(x: X, y: Y, width: Width, height: height)
            //: btn.setTitle(arr[i], for: .normal)
            btn.setTitle(arr[i], for: .normal)
            //: btn.setTitleColor(.black, for: .normal)
            btn.setTitleColor(.black, for: .normal)
            //: btn.setTitleColor(.appThemeColor(), for: .selected)
            btn.setTitleColor(.labIndicator(), for: .selected)

            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
            btn.titleLabel?.font = UIFont.shrinkSize(type: .Regular, fontSize: 15)
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            if CoverAddrTool.share.interfaceLang == CurrencyTableConvertible.es.rawValue || CoverAddrTool.share.interfaceLang == CurrencyTableConvertible.pt.rawValue {
                //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 13)
                btn.titleLabel?.font = UIFont.shrinkSize(type: .Regular, fontSize: 13)
            }
            //: btn.layer.cornerRadius = 19
            btn.layer.cornerRadius = 19
            //: btn.layer.borderWidth = 1
            btn.layer.borderWidth = 1
            //: btn.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
            btn.layer.borderColor = UIColor(hex: (String(app_analysisValue.suffix(5)) + String(show_violationTillMessage)))?.cgColor
            //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .selected)
            btn.heritageSubmit(color: UIColor(hex: (String(k_checkIgnoreText)))!, forState: .selected)
            //: btn.setBackgroundColor(color: .white, forState: .normal)
            btn.heritageSubmit(color: .white, forState: .normal)
            //: btn.tag = 100+i
            btn.tag = 100 + i
            //: btn.addTarget( self, action: #selector(TagBtnClick(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(stay(sender:)), for: .touchUpInside)
            //: contView.addSubview(btn)
            contView.addSubview(btn)
        }

        //: contView.snp.remakeConstraints { make in
        contView.snp.remakeConstraints { make in
            //: make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            //: make.width.equalTo(backView)
            make.width.equalTo(backView)
        }
    }
}
