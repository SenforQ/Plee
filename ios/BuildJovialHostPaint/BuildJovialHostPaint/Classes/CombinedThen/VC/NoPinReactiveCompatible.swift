
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let showBulletDogMsg:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Text greeting" :*/
fileprivate let main_reasonMsg:[Character] = ["T","e","x","t"," ","g","r","e","e","t","i"]
fileprivate let k_curText:String = "nnotice"

/*: "Add" :*/
fileprivate let userActiveWaterUrl:[Character] = ["A","d","d"]

/*: "type" :*/
fileprivate let appHistoryConvertData:[Character] = ["t","y","p","e"]

/*: "addStatus" :*/
fileprivate let show_aboutTitle:String = "huge turn execute line minaddSt"
fileprivate let noti_nineToneMsg:[Character] = ["a","t","u","s"]

/*: "list" :*/
fileprivate let appWhetherMessage:String = "lpairst"

/*: "Are you sure to delete this message？" :*/
fileprivate let kSomeoneUrl:[UInt8] = [0x74,0x47,0x50,0x15,0x4c,0x5a,0x40,0x15,0x46,0x40,0x47,0x50,0x15,0x41,0x5a,0x15,0x51,0x50,0x59,0x50,0x41,0x50,0x15,0x41,0x5d,0x5c,0x46,0x15,0x58,0x50,0x46,0x46,0x54,0x52,0x50,0xda,0x89,0xaa]

/*: "Cancel" :*/
fileprivate let constAssistPath:String = "status micCancel"

/*: "Delete" :*/
fileprivate let appNetworkKey:[Character] = ["D","e","l","e","t","e"]

/*: "The upper limit has been reached" :*/
fileprivate let notiEarlyId:[UInt8] = [0xb4,0x88,0x85,0xc0,0x95,0x90,0x90,0x85,0x92,0xc0,0x8c,0x89,0x8d,0x89,0x94,0xc0,0x88,0x81,0x93,0xc0,0x82,0x85,0x85,0x8e,0xc0,0x92,0x85,0x81,0x83,0x88,0x85,0x84]

private func pictureTab(comply num: UInt8) -> UInt8 {
    return num ^ 224
}

/*: "text" :*/
fileprivate let notiSlideMsg:[Character] = ["t","e","x","t"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let k_textureSplitKey:[UInt8] = [0xe5,0xc3,0xd5,0xd5,0xd3,0xc5,0xc5,0xd0,0xc3,0xda,0xda,0xcf,0x96,0xd7,0xd2,0xd2,0xd3,0xd2,0x9a,0x96,0xc6,0xda,0xd3,0xd7,0xc5,0xd3,0x96,0xc1,0xd7,0xdf,0xc2,0x96,0xd0,0xd9,0xc4,0x96,0xd7,0xc6,0xc6,0xc4,0xd9,0xc0,0xd7,0xda]

private func brightWriting(must num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "Any-Hex/Java" :*/
fileprivate let mainServeUrl:String = "keep replaceAny-"
fileprivate let appUtilizeMsg:[Character] = ["a"]

/*: "#FF506D" :*/
fileprivate let dataHerFormat:[Character] = ["#","F","F","5","0","6","D"]

/*: "btn_message_delete_nor" :*/
fileprivate let constBassLearnSourceMsg:[Character] = ["b","t","n","_","m","e","s"]
fileprivate let data_shouldName:[Character] = ["s","a","g","e","_","d","e","l","e","t","e","_","n","o","r"]

/*: "Click the button below to add\ncontent" :*/
fileprivate let user_futurePath:[UInt8] = [0x54,0x7d,0x7a,0x74,0x7c,0x31,0x85,0x79,0x76,0x31,0x73,0x86,0x85,0x85,0x80,0x7f,0x31,0x73,0x76,0x7d,0x80,0x88,0x31,0x85,0x80,0x31,0x72,0x75,0x75,0x1b,0x74,0x80,0x7f,0x85,0x76,0x7f,0x85]

fileprivate func muttShake(administrative num: UInt8) -> UInt8 {
    let value = Int(num) + 239
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "When adding multiple text message, the syst em will send them by turns;slide to the message to delete" :*/
fileprivate let show_duringPath:[UInt8] = [0x89,0x9a,0x97,0xa0,0x52,0x93,0x96,0x96,0x9b,0xa0,0x99,0x52,0x9f,0xa7,0x9e,0xa6,0x9b,0xa2,0x9e,0x97,0x52,0xa6,0x97,0xaa,0xa6,0x52,0x9f,0x97,0xa5,0xa5,0x93,0x99,0x97,0x5e,0x52,0xa6,0x9a,0x97,0x52,0xa5,0xab,0xa5,0xa6,0x52,0x97,0x9f,0x52,0xa9,0x9b,0x9e,0x9e,0x52,0xa5,0x97,0xa0,0x96,0x52,0xa6,0x9a,0x97,0x9f,0x52,0x94,0xab,0x52,0xa6,0xa7,0xa4,0xa0,0xa5,0x6d,0xa5,0x9e,0x9b,0x96,0x97,0x52,0xa6,0xa1,0x52,0xa6,0x9a,0x97,0x52,0x9f,0x97,0xa5,0xa5,0x93,0x99,0x97,0x52,0xa6,0xa1,0x52,0x96,0x97,0x9e,0x97,0xa6,0x97]

fileprivate func resourceHair(its num: UInt8) -> UInt8 {
    let value = Int(num) - 50
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  NoPinReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/15.
//

//: import UIKit
import UIKit

//: class TalkingGreetTextVC: TalkingBaseViewController {
class NoPinReactiveCompatible: OperateReactiveCompatible {
    //: var addStatus = -1
    var addStatus = -1
    //: var recordView: TalkingGreetEditTextView?
    var recordView: NoTitleView?

    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: showBulletDogMsg.reversed(), encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Text greeting".localized
        self.title = (String(main_reasonMsg) + k_curText.replacingOccurrences(of: "notice", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.smallColor()
        //: designView()
        panoramicView()
        //: getAudioListData()
        remain()
    }

    //: deinit {
    deinit {}

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [StanceHandyJSON] = {
        //: var array = [TalkingGreetModel]()
        var array = [StanceHandyJSON]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(userActiveWaterUrl)).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.shrinkSize(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.thumbNumbererest(colors: UIColor.ratingConfirm(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(request), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetTextVC {
extension NoPinReactiveCompatible {
    //: func getAudioListData() {
    func remain() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 2
        dict[(String(appHistoryConvertData))] = 2
        //: ProgressHUD.show()
        LawReactiveCompatible.frontEndShow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        CombinedThen.duringEnter(params: dict) { [weak self] succeed, result, _ in
            //: ProgressHUD.dismiss()
            LawReactiveCompatible.minify()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.afterMiss()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(show_aboutTitle.suffix(5)) + String(noti_nineToneMsg))] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(appWhetherMessage.replacingOccurrences(of: "pair", with: "i"))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [StanceHandyJSON] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<StanceHandyJSON>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func cleanBy(index: Int) {
        //: let config = ShowAlertConfig()
        let config = CharacterizationAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.shrinkSize(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        StuffReactiveCompatible.restore(message: String(bytes: kSomeoneUrl.map{$0^53}, encoding: .utf8)!.localized, leftBtnTitle: (String(constAssistPath.suffix(6))).localized, rightBtnTitle: (String(appNetworkKey)).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            StuffReactiveCompatible.outFeedback()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            StuffReactiveCompatible.outFeedback()
            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: StanceHandyJSON = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: ProgressHUD.show()
            LawReactiveCompatible.frontEndShow()
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            CombinedThen.removeParamsSplitEmpty(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                LawReactiveCompatible.minify()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.remain()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetTextVC {
extension NoPinReactiveCompatible {
    //: @objc func addButtonClick() {
    @objc func request() {
        //: if (self.addStatus == 4) {
        if self.addStatus == 4 {
            //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
            self.bringHome(showMsg: String(bytes: notiEarlyId.map{pictureTab(comply: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.recordView = TalkingGreetEditTextView.init(frame: self.view.frame)
        self.recordView = NoTitleView(frame: self.view.frame)
        //: self.recordView?.show()
        self.recordView?.happening()
        //: self.recordView?.textUpload = {[weak self] str in
        self.recordView?.textUpload = { [weak self] str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let tempStr = self.unicodeString(str)
            let tempStr = self.unicode(str)
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["type"] = 2
            dict[(String(appHistoryConvertData))] = 2
            //: dict["text"] = tempStr
            dict[(String(notiSlideMsg))] = tempStr
            //: ProgressHUD.show()
            LawReactiveCompatible.frontEndShow()
            //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) { succeed, result, errorModel in
            CombinedThen.ok(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                LawReactiveCompatible.minify()
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.readingDownMsg(showMsg: String(bytes: k_textureSplitKey.map{brightWriting(must: $0)}, encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.remain()
                //: self.recordView?.dismiss()
                self.recordView?.uSGovernment()
            }
        }
    }

    //: func unicodeString(_ str: String) -> String {
    func unicode(_ str: String) -> String {
        //: let mutableStr = NSMutableString(string: str) as CFMutableString
        let mutableStr = NSMutableString(string: str) as CFMutableString
        //: CFStringTransform(mutableStr, nil, "Any-Hex/Java" as CFString, true)
        CFStringTransform(mutableStr, nil, (String(mainServeUrl.suffix(4)) + "Hex/Jav" + String(appUtilizeMsg)) as CFString, true)
        //: return mutableStr as String
        return mutableStr as String
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetTextVC: UITableViewDelegate, UITableViewDataSource {
extension NoPinReactiveCompatible: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.section]
        let model = self.dataSourceArr[indexPath.section]
        //: var height: CGFloat = 18+22
        var height: CGFloat = 18 + 22
        //: let rect = model.content.boundingRect(with: CGSize(width: ScreenWidth-60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.pingfangFont(type: .Medium, fontSize: 16)], context: nil)
        let rect = model.content.boundingRect(with: CGSize(width: dataModeBlockStr - 60, height: 0), options: [NSStringDrawingOptions.usesLineFragmentOrigin, NSStringDrawingOptions.usesFontLeading, NSStringDrawingOptions.usesDeviceMetrics], attributes: [NSAttributedString.Key.font: UIFont.shrinkSize(type: .Medium, fontSize: 16)], context: nil)
        //: height += rect.height
        height += rect.height
        //: if model.status == 0 {
        if model.status == 0 {
            //: height += 16+12
            height += 16 + 12
        }
        //: return height
        return height
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetTextCell.className()
        let identifier = AnyoneViewCell.className()
        //: let cell: TalkingGreetTextCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetTextCell
        let cell: AnyoneViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! AnyoneViewCell

        //: cell.setGreetTextCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.graphicalLock(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)
        //: return cell
        return cell
    }

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(appNetworkKey)).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.cleanBy(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(dataHerFormat)))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.cleanBy(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.adjust(name: (String(constBassLearnSourceMsg) + String(data_shouldName))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = RunningImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func tableView(_ tableView: UITableView, heightForFooterInSection section: Int) -> CGFloat {
    func tableView(_: UITableView, heightForFooterInSection _: Int) -> CGFloat {
        //: return 0.01
        return 0.01
    }
}

// MARK: - Layout

//: extension TalkingGreetTextVC {
extension NoPinReactiveCompatible {
    //: private func setupEmptyDataView() {
    private func afterMiss() {
        //: var style = EmptyStyle()
        var style = FreshEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: user_futurePath.map{muttShake(administrative: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .smallColor()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func panoramicView() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }

        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetTextCell.self, forCellReuseIdentifier: TalkingGreetTextCell.className())
        MainTable.register(AnyoneViewCell.self, forCellReuseIdentifier: AnyoneViewCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 100))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: 100))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.resumeName()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.ditMention(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "When adding multiple text message, the syst em will send them by turns;slide to the message to delete".localized
        let string = String(bytes: show_duringPath.map{resourceHair(its: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
