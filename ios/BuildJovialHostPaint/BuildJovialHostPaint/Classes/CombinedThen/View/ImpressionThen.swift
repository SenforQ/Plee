
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constGoRenderFormat:[UInt8] = [0xc3,0xc4,0xc3,0xde,0x82,0xc9,0xc5,0xce,0xcf,0xd8,0x90,0x83,0x8a,0xc2,0xcb,0xd9,0x8a,0xc4,0xc5,0xde,0x8a,0xc8,0xcf,0xcf,0xc4,0x8a,0xc3,0xc7,0xda,0xc6,0xcf,0xc7,0xcf,0xc4,0xde,0xcf,0xce]

private func limitFar(stop num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "Cancel" :*/
fileprivate let notiAlterAppValue:String = "Cancelhuman passage stand require write"

/*: "Submit" :*/
fileprivate let data_transitionDeviceName:String = "cloud normally visible eventSubmit"

/*: "Please tell us your problems                   " :*/
fileprivate let const_clickMessage:[UInt8] = [0xe1,0xfd,0xf6,0xf2,0x4,0xf6,0xb1,0x5,0xf6,0xfd,0xfd,0xb1,0x6,0x4,0xb1,0xa,0x0,0x6,0x3,0xb1,0x1,0x3,0x0,0xf3,0xfd,0xf6,0xfe,0x4,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1,0xb1]

fileprivate func soonInmateForward(nap num: UInt8) -> UInt8 {
    let value = Int(num) - 145
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "You need to choose at least one." :*/
fileprivate let constSystemUrl:[UInt8] = [0xef,0xd9,0xc3,0x96,0xd8,0xd3,0xd3,0xd2,0x96,0xc2,0xd9,0x96,0xd5,0xde,0xd9,0xd9,0xc5,0xd3,0x96,0xd7,0xc2,0x96,0xda,0xd3,0xd7,0xc5,0xc2,0x96,0xd9,0xd8,0xd3,0x98]

private func manageCon(too num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "Please tell us your problems." :*/
fileprivate let dataLearnPath:[Character] = ["P","l","e","a","s","e"," ","t","e","l","l"," ","u"]
fileprivate let userCycleLovelyFormat:String = "stop sense maker itss you"
fileprivate let appLaterTitle:[Character] = ["b","l","e","m","s","."]

/*: "app/userReport" :*/
fileprivate let user_currentKey:String = "launch the addition interestedapp/"
fileprivate let kWrittenSufficientData:String = "reward happy be stackReport"

/*: "reason" :*/
fileprivate let main_stepName:String = "reasnown"

/*: "desc" :*/
fileprivate let show_thatStr:[UInt8] = [0xc,0xd,0x1b,0xb]

private func earlyAngle(square num: UInt8) -> UInt8 {
    return num ^ 104
}

/*: "mf/moment/report" :*/
fileprivate let k_featureMsg:String = "acquire ample fastmf/m"
fileprivate let kHornStr:String = "privacy food/repo"
fileprivate let userShareName:String = "half"

/*: "momentId" :*/
fileprivate let noti_policyBedId:[UInt8] = [0x73,0x75,0x73,0x6b,0x74,0x7a,0x4f,0x6a]

fileprivate func familyBorder(executive num: UInt8) -> UInt8 {
    let value = Int(num) - 6
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/momentReply/report" :*/
fileprivate let appDisagreeKey:String = "last collectmf/mom"
fileprivate let mainHarvestUrl:String = "eply/rturn warning network"
fileprivate let show_positionKey:String = "chance"

/*: "app/videoReport" :*/
fileprivate let app_somebodyStr:String = "app/vface compute device"
fileprivate let app_jawFormat:String = "better law transformationReport"

/*: "logId" :*/
fileprivate let data_tooThinFormat:[UInt8] = [0x93,0x90,0x98,0xb6,0x9b]

private func refVery(ply num: UInt8) -> UInt8 {
    return num ^ 255
}

/*: "Submitted successfully" :*/
fileprivate let showYieldValue:String = "simultaneously license motivation province entitySubm"
fileprivate let k_outcomeStr:String = "succreserve"
fileprivate let noti_methodMsg:String = "sfutapy"

/*: "Nudity" :*/
fileprivate let data_succeedAidMessage:[Character] = ["N","u","d","i","t","y"]

/*: "Violence" :*/
fileprivate let userMatterMsg:[Character] = ["V","i","o","l","e","n","c"]
fileprivate let showLawPath:String = "plain"

/*: "Harassment" :*/
fileprivate let user_fastText:String = "closely people stat portrait plainHarass"
fileprivate let showMapName:[Character] = ["m","e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let k_additionalData:[Character] = ["S","u","i","c","i","d","e"," ","o"]
fileprivate let show_judgmentAliveMsg:[Character] = ["r"," ","S","e","l","f","-","i","n","j","u","r","y"]

/*: "False Information" :*/
fileprivate let showCharacterData:String = "event collection gumFalse"
fileprivate let main_withoutKey:String = "equipment combine between reason what Inf"

/*: "Spam" :*/
fileprivate let kRecognizeMsg:String = "Spamfore difference street"

/*: "Hate Speech" :*/
fileprivate let userMatchName:String = "decide runHate Sp"
fileprivate let data_lowDestroyValue:String = "translationtranslationch"

/*: "Terrorism" :*/
fileprivate let show_smoothFileData:String = "down detailed example richTerrori"
fileprivate let constCapableMsg:String = "huge"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let kAgainIssueKey:[UInt8] = [0xdb,0xe9,0xe4,0xed,0xa8,0xe7,0xee,0xa8,0xc1,0xe4,0xe4,0xed,0xef,0xe9,0xe4,0xa8,0xe7,0xfa,0xa8,0xda,0xed,0xef,0xfd,0xe4,0xe9,0xfc,0xed,0xec,0xa8,0xcf,0xe7,0xe7,0xec,0xfb]

private func rangeStrong(drama num: UInt8) -> UInt8 {
    return num ^ 136
}

/*: "Sending disgusting messages" :*/
fileprivate let main_mirrorKey:[Character] = ["S","e","n","d","i","n","g"," ","d","i","s"]
fileprivate let kNearbyPicKey:String = "advancedst"
fileprivate let appOutsideKey:[Character] = ["e","s","s","a","g","e","s"]

/*: "Using photos of nudity or drugs" :*/
fileprivate let data_digStr:[UInt8] = [0x73,0x67,0x75,0x72,0x64,0x20,0x72,0x6f,0x20,0x79,0x74,0x69,0x64,0x75,0x6e,0x20,0x66,0x6f,0x20,0x73,0x6f,0x74,0x6f,0x68,0x70,0x20,0x67,0x6e,0x69,0x73,0x55]

/*: "Harassing or sending ads" :*/
fileprivate let app_aircraftName:[Character] = ["H","a","r","a","s","s","i","n","g"," ","o","r"," ","s","e","n","d","i","n"]
fileprivate let user_attractiveFormat:[Character] = ["g"," ","a","d","s"]

/*: "Defraud or using fake info" :*/
fileprivate let main_exclusiveTitle:String = "Defrlay column permission"
fileprivate let kVerticalData:String = "r usincute arc equipment"
fileprivate let mainSearchedName:String = "master render letter jaw mentionke in"
fileprivate let show_safetyTitle:String = "flanguage"

/*: "Others" :*/
fileprivate let user_whatData:[Character] = ["O","t","h","e","r"]
fileprivate let notiSmallPath:String = "S"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ImpressionThen.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum CarrierCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class ImpressionThen: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: CarrierCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [FisherThen] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = FisherThen()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: CarrierCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        lay()
        //: setupSubViewsConstraint()
        characterConstraint()
        //: setupReasons()
        fit()
        //: bindInteraction()
        refreshState()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constGoRenderFormat.map{limitFar(stop: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(notiAlterAppValue.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.labIndicator(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.shrinkSize(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.labIndicator().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(rawAction), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(data_transitionDeviceName.suffix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.shrinkSize(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.thumbNumbererest(colors: UIColor.ratingConfirm(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(consecrate), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: dataModeBlockStr - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.restate(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = CanvasView(placeholder: String(bytes: const_clickMessage.map{soonInmateForward(nap: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.resumeName(), frame: CGRect(x: 10, y: 10, width: dataModeBlockStr - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .shrinkSize(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.askNameColor()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .shrinkSize(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.restate(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(VanguardViewCell.self, forCellReuseIdentifier: VanguardViewCell.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension ImpressionThen {
    //: @objc func func__TapGestureRecognizer() {
    @objc func model() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func become(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = CurrencyDefineReactiveCompatible.getWindow()
            let window = CurrencyDefineReactiveCompatible.stripElement()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func cutSufficient(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func rawAction() {
        //: self.dismissReportView(succeed: false)
        self.cutSufficient(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func consecrate() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.bringHome(showMsg: String(bytes: constSystemUrl.map{manageCon(too: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.bringHome(showMsg: (String(dataLearnPath) + String(userCycleLovelyFormat.suffix(5)) + "r pro" + String(appLaterTitle)).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(user_currentKey.suffix(4)) + "user" + String(kWrittenSufficientData.suffix(6)))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (main_stepName.replacingOccurrences(of: "now", with: "o")): selectedModel.type, String(bytes: show_thatStr.map{earlyAngle(square: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(k_featureMsg.suffix(4)) + "oment" + String(kHornStr.suffix(5)) + userShareName.replacingOccurrences(of: "half", with: "rt"))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: noti_policyBedId.map{familyBorder(executive: $0)}, encoding: .utf8)!: tempId, (main_stepName.replacingOccurrences(of: "now", with: "o")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(appDisagreeKey.suffix(6)) + "entR" + String(mainHarvestUrl.prefix(6)) + "epor" + show_positionKey.replacingOccurrences(of: "chance", with: "t"))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (main_stepName.replacingOccurrences(of: "now", with: "o")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(app_somebodyStr.prefix(5)) + "ideo" + String(app_jawFormat.suffix(6)))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: data_tooThinFormat.map{refVery(ply: $0)}, encoding: .utf8)!: tempId, (main_stepName.replacingOccurrences(of: "now", with: "o")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        FisherOutsideManagerThen.than(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.readingDownMsg(showMsg: (String(showYieldValue.suffix(4)) + "itted " + k_outcomeStr.replacingOccurrences(of: "reserve", with: "es") + noti_methodMsg.replacingOccurrences(of: "tap", with: "ll")).localized)
                //: self.dismissReportView(succeed: true)
                self.cutSufficient(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension ImpressionThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: VanguardViewCell.className()) as! VanguardViewCell
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.describe(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension ImpressionThen {
    /// 添加视图
    //: private func setupSubviews() {
    private func lay() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func characterConstraint() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func fit() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(data_succeedAidMessage)).localized,
                           //: "Violence".localized,
                           (String(userMatterMsg) + showLawPath.replacingOccurrences(of: "plain", with: "e")).localized,
                           //: "Harassment".localized,
                           (String(user_fastText.suffix(6)) + String(showMapName)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(k_additionalData) + String(show_judgmentAliveMsg)).localized,
                           //: "False Information".localized,
                           (String(showCharacterData.suffix(5)) + String(main_withoutKey.suffix(4)) + "ormation").localized,
                           //: "Spam".localized,
                           (String(kRecognizeMsg.prefix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(userMatchName.suffix(7)) + data_lowDestroyValue.replacingOccurrences(of: "translation", with: "e")).localized,
                           //: "Terrorism".localized,
                           (String(show_smoothFileData.suffix(7)) + constCapableMsg.replacingOccurrences(of: "huge", with: "sm")).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: kAgainIssueKey.map{rangeStrong(drama: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(main_mirrorKey) + kNearbyPicKey.replacingOccurrences(of: "advanced", with: "gu") + "ing m" + String(appOutsideKey)).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: data_digStr.reversed(), encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(app_aircraftName) + String(user_attractiveFormat)).localized,
                           //: "Defraud or using fake info".localized,
                           (String(main_exclusiveTitle.prefix(4)) + "aud o" + String(kVerticalData.prefix(6)) + "g fa" + String(mainSearchedName.suffix(5)) + show_safetyTitle.replacingOccurrences(of: "language", with: "o")).localized,
                           //: "Others".localized]
                           (String(user_whatData) + notiSmallPath.lowercased()).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = RealmReactiveCompatible.share.appConfigMode.videoReport["reason"] {
            if let reason = RealmReactiveCompatible.share.appConfigMode.videoReport[(main_stepName.replacingOccurrences(of: "now", with: "o"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = FisherThen()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func refreshState() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
