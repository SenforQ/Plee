
//: Declare String Begin

/*: "cmd" :*/
fileprivate let userCalledId:[UInt8] = [0xf7,0xf9,0xf0]

/*: "cancelCall" :*/
fileprivate let noti_bagForceUrl:String = "ban"
fileprivate let mainEquallyMsg:[Character] = ["a","n","c","e","l","C","a","l","l"]

/*: "data" :*/
fileprivate let constOuterKey:[UInt8] = [0x7c,0x79,0x6c,0x79]

private func mergeEarly(creation num: UInt8) -> UInt8 {
    return num ^ 24
}

/*: "logId" :*/
fileprivate let show_platValue:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let mainPrettyKey:[Character] = ["#","3","2","2","8"]
fileprivate let constBurnFormat:String = "7unable"

/*: "Tap to start video chatting" :*/
fileprivate let appWithUrl:[Character] = ["T","a","p"," ","t","o"," ","s","t","a"]
fileprivate let showPrivacyKey:String = "rt vwhat wing focus cover"
fileprivate let data_nearbyPath:String = "chattiannouncementg"

/*: "icon_close_nor" :*/
fileprivate let show_hiPadUrl:[Character] = ["i","c","o","n","_","c","l","o","s","e"]
fileprivate let data_componentId:String = "numb inmate refuse_nor"

/*: "icon_views_number" :*/
fileprivate let k_liteStr:String = "ianalyze"
fileprivate let kSurgeryUrl:String = "currency communicationon_vi"
fileprivate let appStartMessage:String = "umchecker"

/*: "%@ chatting" :*/
fileprivate let kBetweenId:[Character] = ["%","@"," ","c","h","a"]
fileprivate let appReplaceUrl:[Character] = ["t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let dataRearMessage:[Character] = ["i","c","o","n","_","l","o","u"]
fileprivate let mainShouldId:[Character] = ["n","g","e"]

/*: "icon_rank_coin" :*/
fileprivate let noti_holdId:[Character] = ["i","c","o","n","_","r","a","n","k","_","c","o","i"]
fileprivate let const_nearKey:String = "model"

/*: "vip_btn_top" :*/
fileprivate let showThemeId:String = "media represent string plantvip_"

/*: "btn_nearby_settings_nor" :*/
fileprivate let user_earnText:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t","i","n"]
fileprivate let show_checkPath:String = "gs_nortext back detailed resign"

/*: "%@ Coins/min" :*/
fileprivate let userUnderData:String = "%@ "
fileprivate let constTingUrl:String = "enterprise consciousness weCoins/min"

/*: "get json error" :*/
fileprivate let dataBigRichStr:String = "eh purchase falseget json "
fileprivate let const_restrictionSurgeryMsg:String = "errfactorr"

/*: "matchId" :*/
fileprivate let k_signalUrl:[Character] = ["m","a"]
fileprivate let user_subjectData:[Character] = ["t","c","h","I","d"]

/*: "errno" :*/
fileprivate let app_loopKey:[UInt8] = [0x6f,0x6e,0x72,0x72,0x65]

/*: "VIPFee" :*/
fileprivate let constHideId:String = "VIPFeewoman flat discount"

/*: "Matching now" :*/
fileprivate let dataBrakeBigLightPath:String = "Matchmeeting tape"

/*: "userTouch" :*/
fileprivate let showFairPath:String = "mobile weight fill formatuserT"
fileprivate let mainIconTradeFormat:[Character] = ["o","u","c","h"]

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let appConcentrationPadText:String = "selectionf"
fileprivate let constGlassValue:String = "/freeneed issue dig procedure"
fileprivate let main_holdName:String = "life manageetVide"

/*: "fromType" :*/
fileprivate let main_costData:[UInt8] = [0x87,0x93,0x8e,0x8c,0xb5,0x98,0x91,0x84]

private func askMasterFine(listener num: UInt8) -> UInt8 {
    return num ^ 225
}

/*: "You've been barred from receiving calls" :*/
fileprivate let noti_guestValue:[UInt8] = [0x73,0x6c,0x6c,0x61,0x63,0x20,0x67,0x6e,0x69,0x76,0x69,0x65,0x63,0x65,0x72,0x20,0x6d,0x6f,0x72,0x66,0x20,0x64,0x65,0x72,0x72,0x61,0x62,0x20,0x6e,0x65,0x65,0x62,0x20,0x65,0x76,0x27,0x75,0x6f,0x59]

/*: "source" :*/
fileprivate let showLayerSnapSimultaneouslyValue:[UInt8] = [0xb9,0xb5,0xbb,0xb8,0xa9,0xab]

fileprivate func becomeFace(concentration num: UInt8) -> UInt8 {
    let value = Int(num) + 186
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "type" :*/
fileprivate let showAnnouncementStr:[UInt8] = [0xf0,0xfd,0xf4,0xe1]

private func finiteRid(aware num: UInt8) -> UInt8 {
    return num ^ 132
}

/*: "uid" :*/
fileprivate let k_crushKey:[UInt8] = [0x4b,0x57,0x5a]

private func randomSufficient(gravity num: UInt8) -> UInt8 {
    return num ^ 62
}

/*: "fromFreeCall" :*/
fileprivate let noti_neverData:String = "ftempom"

/*: "requestCall" :*/
fileprivate let mainInvitePath:String = "reqrelation"
fileprivate let const_duringYesName:String = "visible"

/*: "onVideoMatch" :*/
fileprivate let notiCommitMsg:[Character] = ["o","n","V","i","d","e"]
fileprivate let app_alterText:String = "device rice prepare opportunity robotoMatch"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let notiDemonstrateStr:[UInt8] = [0x67,0x6e,0x69,0x6d,0x6f,0x63,0x6e,0x69,0x20,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x76,0x69,0x65,0x63,0x65,0x52,0x20,0x5d,0x6f,0x65,0x64,0x69,0x76,0x20,0x63,0x69,0x74,0x61,0x6d,0x6f,0x74,0x75,0x41,0x5b]

/*: "refuseCall" :*/
fileprivate let user_wayMethodText:String = "refuseCowner middle"
fileprivate let kMyValue:String = "endl"

/*: "onCancelCall" :*/
fileprivate let kAgoText:[Character] = ["o","n","C","a","n","c","e"]
fileprivate let show_dropPath:String = "lCalloutput matter ear"

/*: "retryAfter" :*/
fileprivate let constBassKey:String = "retryAban computer modify double"

/*: "onRequestCall" :*/
fileprivate let app_figureValue:String = "onRequesurgery extension comfortable"
fileprivate let app_hereSpecialFormat:[Character] = ["s","t","C","a","l","l"]

/*: "failType" :*/
fileprivate let showDialogContent:String = "failTypeus certain ear reach shadow"

/*: "callWaitingTime" :*/
fileprivate let mainFormName:[Character] = ["c","a","l","l","W","a","i","t","i","n","g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let user_successfullyMessage:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let data_representationFormat:String = "Freebase theme what golf"
fileprivate let showExpectedFormat:String = " 1 mincap lap she"

/*: "%@/min" :*/
fileprivate let constUntilId:String = "punish circuit record odd%@/min"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PossibilityMatchDelegate.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class PossibilityMatchDelegate: OperateReactiveCompatible {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.disableIn()
            }
        }
        //: self.resetCamera()
        self.backCamera()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        mutual()
        //: setupSubViewsConstraint()
        performConstraint()
        //: bindInteraction()
        utilize()
        //: videoManager.startPreview(isMatch: true)
        videoManager.tetrasodiumPyrophosphate(isMatch: true)
        //: setPlayer(isRandon: false)
        nett(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            pureMe()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: userCalledId.map{$0^148}, encoding: .utf8)!: (noti_bagForceUrl.replacingOccurrences(of: "ban", with: "c") + String(mainEquallyMsg)), String(bytes: constOuterKey.map{mergeEarly(creation: $0)}, encoding: .utf8)!: [(String(show_platValue)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        device()
        //: if TalkingSocketManager.shared.isFisher == false {
        if ManagerCommentThen.shared.isFisher == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ManagerCommentThen.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ManagerCommentThen.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ManagerCommentThen.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ManagerCommentThen.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: PortraitExcitementReactiveCompatible = {
        //: let m = TalkingVideoChatManager()
        let m = PortraitExcitementReactiveCompatible()
        //: let info = TalkingVideoChatModel()
        let info = CanvasChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .askNameColor()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(pureMe), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: dataModeBlockStr + 1, height: constCornerDetailUrl + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (String(mainPrettyKey) + constBurnFormat.replacingOccurrences(of: "unable", with: "0")))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(appWithUrl) + String(showPrivacyKey.prefix(4)) + "ideo " + data_nearbyPath.replacingOccurrences(of: "announcement", with: "n")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.adjust(name: (String(show_hiPadUrl) + String(data_componentId.suffix(4)))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(query), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: PostureTalkingButton = {
        //: let btn = TalkingButton()
        let btn = PostureTalkingButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.adjust(name: (k_liteStr.replacingOccurrences(of: "analyze", with: "c") + String(kSurgeryUrl.suffix(5)) + "ews_n" + appStartMessage.replacingOccurrences(of: "check", with: "b"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.adjust(name: (k_liteStr.replacingOccurrences(of: "analyze", with: "c") + String(kSurgeryUrl.suffix(5)) + "ews_n" + appStartMessage.replacingOccurrences(of: "check", with: "b"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.shrinkSize(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(kBetweenId) + String(appReplaceUrl)).restateArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.adjust(name: (String(dataRearMessage) + String(mainShouldId)))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.adjust(name: (String(noti_holdId) + const_nearKey.replacingOccurrences(of: "model", with: "n")))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.shrinkSize(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.adjust(name: (String(showThemeId.suffix(4)) + "btn_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: PostureTalkingButton = {
        //: let btn = TalkingButton()
        let btn = PostureTalkingButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(inmateBy), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.adjust(name: (String(user_earnText) + String(show_checkPath.prefix(6)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.adjust(name: (String(noti_holdId) + const_nearKey.replacingOccurrences(of: "model", with: "n")))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .originalColor()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (userUnderData.capitalized + String(constTingUrl.suffix(9))).restateArguments(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.shrinkSize(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: ViewThen = //: return ViewThen(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension PossibilityMatchDelegate {
    //: private func setPlayer(isRandon: Bool) {
    private func nett(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = PortraitJackEffectTool.default.responsibility(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(dataBigRichStr.suffix(9)) + const_restrictionSurgeryMsg.replacingOccurrences(of: "factor", with: "o")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func intervalerval(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(k_signalUrl) + String(user_subjectData)): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: userCalledId.map{$0^148}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: app_loopKey.reversed(), encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        BrowThen.totalo(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.videoArray()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(k_signalUrl) + String(user_subjectData))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func ambush() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        BrowThen.untilForm { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(constHideId.prefix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.disableIn()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension PossibilityMatchDelegate {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func inmateBy() {
        //: BrowReactiveCompatible.share.func__pushToSubscribeAlert()
        BrowReactiveCompatible.share.anyPressure()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func query() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        demonstrateSecondTool()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        dataFilterFormat.psychologicalFeatureTarget(eventID: dataBackAddPath)

        //: if RealmReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, RealmReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
        if RealmReactiveCompatible.share.appConfigMode.displayMatchV4PopUp, RealmReactiveCompatible.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.allShow()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        currentPositionController()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func pureMe() {
        //: guard TalkingSocketManager.shared.isFisher == false else {
        guard ManagerCommentThen.shared.isFisher == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.bringHome(showMsg: app_nextKey)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        tarOfHide()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(dataBrakeBigLightPath.prefix(5)) + "ing now").localized
        //: setPlayer(isRandon: true)
        nett(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if RealmReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
        if RealmReactiveCompatible.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            dataFilterFormat.psychologicalFeatureTarget(eventID: user_qualityUrl)
            //: req_getVideoInfo()
            overPopular()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            dataFilterFormat.psychologicalFeatureTarget(eventID: show_allPath)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            ManagerCommentThen.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            ManagerCommentThen.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            ManagerCommentThen.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            ManagerCommentThen.shared.errorDelegate = self
            //: restart_randomVideo()
            numbererpretation()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func numbererpretation(cmd: String = (String(showFairPath.suffix(5)) + String(mainIconTradeFormat)), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        device()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        intervalerval(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func videoArray() {
        // 停止定时器
        //: destroyWaitingTimer()
        device()
        //: showCloseBtn()
        demonstrateSecondTool()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(appWithUrl) + String(showPrivacyKey.prefix(4)) + "ideo " + data_nearbyPath.replacingOccurrences(of: "announcement", with: "n")).localized
        //: setPlayer(isRandon: false)
        nett(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isFisher == false {
        if ManagerCommentThen.shared.isFisher == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            ManagerCommentThen.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            ManagerCommentThen.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            ManagerCommentThen.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            ManagerCommentThen.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        disableIn()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func device() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func overPopular() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (appConcentrationPadText.replacingOccurrences(of: "selection", with: "m") + String(constGlassValue.prefix(5)) + "Call/g" + String(main_holdName.suffix(6)) + "oInfo")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: main_costData.map{askMasterFine(listener: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.videoArray()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: RealmReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    RealmReactiveCompatible.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.pureMe()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.bringHome(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = CompositionModelType.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = AmplePlayListener()
                //: player.setMute(bEnable: true)
                player.appShould(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.midnight(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                PortionThen.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                GravityReactiveCompatible.nearStatusMicrophone { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.insideThrough()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func insideThrough() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        dataFilterFormat.psychologicalFeatureTarget(eventID: mainCurrencyMeKey)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard PortionThen.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.bringHome(showMsg: String(bytes: noti_guestValue.reversed(), encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            PortionThen.shared.remarkCompletion(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.videoArray()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = ThanksPlayerDelegate()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.currentPositionController()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.videoArray()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func tarOfHide() {
        //: if RealmReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if RealmReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(RealmReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(RealmReactiveCompatible.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func demonstrateSecondTool() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func backCamera() {
        //: guard TalkingSocketManager.shared.isFisher == false else {
        guard ManagerCommentThen.shared.isFisher == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.tetrasodiumPyrophosphate()
        }
    }
}

// MARK: - CurrencyMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension PossibilityMatchDelegate: CurrencyMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func doingerFill(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = SeekGreatResultModel.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(k_signalUrl) + String(user_subjectData)): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: showLayerSnapSimultaneouslyValue.map{becomeFace(concentration: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: showAnnouncementStr.map{finiteRid(aware: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: k_crushKey.map{randomSufficient(gravity: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = DotSubscriptType.randow
        //: if RealmReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && RealmReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if RealmReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && RealmReactiveCompatible.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (noti_neverData.replacingOccurrences(of: "temp", with: "r") + "FreeCall"))
            //: TalkingSocketManager.shared.isFreeCall = true
            ManagerCommentThen.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: userCalledId.map{$0^148}, encoding: .utf8)!: (mainInvitePath.replacingOccurrences(of: "relation", with: "u") + "estCal" + const_duringYesName.replacingOccurrences(of: "visible", with: "l")), String(bytes: constOuterKey.map{mergeEarly(creation: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            ManagerCommentThen.shared.telephoneReceiverPhoneMessage(info: [String(bytes: userCalledId.map{$0^148}, encoding: .utf8)!: (noti_bagForceUrl.replacingOccurrences(of: "ban", with: "c") + String(mainEquallyMsg)), String(bytes: constOuterKey.map{mergeEarly(creation: $0)}, encoding: .utf8)!: [(String(show_platValue)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.numbererpretation(cmd: (String(notiCommitMsg) + String(app_alterText.suffix(6))))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func linkData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(show_platValue))].intValue
    }
}

// MARK: - ExistingObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension PossibilityMatchDelegate: ExistingObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func labelToday(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func doHighlight(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func drownData(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func beautyEndLibrary(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            TranslateThen.attribute(body: String(bytes: notiDemonstrateStr.reversed(), encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = SeekGreatResultModel.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            upendFromTo(toUid: String(uid))
        }
        //: stop_randomVideo()
        videoArray()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.alongGift()
        //: RealmReactiveCompatible.share.start1v1TalkCall(info: data, situation: .Calling)
        RealmReactiveCompatible.share.boxSituation(info: data, situation: .Calling)
    }
}

// MARK: - AnyoneMaterialPartyDelegate【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension PossibilityMatchDelegate: AnyoneMaterialPartyDelegate {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func bitCollection(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        numbererpretation(cmd: (String(user_wayMethodText.prefix(7)) + kMyValue.replacingOccurrences(of: "end", with: "al")))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func acceptObserve(data _: [String: Any]) {}
}

// MARK: - BetiseObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension PossibilityMatchDelegate: BetiseObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func beforeRelationHolder(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(kAgoText) + String(show_dropPath.prefix(5))) { // 取消通话异常
            //: stop_randomVideo()
            videoArray()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(notiCommitMsg) + String(app_alterText.suffix(6))), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            videoArray()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(k_signalUrl) + String(user_subjectData))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.bringHome(showMsg: msg)
            //: uploadFailTime(type: 2)
            offce(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(notiCommitMsg) + String(app_alterText.suffix(6))), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(constBassKey.prefix(6)) + "fter")].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(k_signalUrl) + String(user_subjectData))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.numbererpretation(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(app_figureValue.prefix(7)) + String(app_hereSpecialFormat)), errorNo == GraphicExcitementStrideable.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            videoArray()
            //: guard RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue else { return }
            //: BrowReactiveCompatible.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            BrowReactiveCompatible.share.beContext(clickEvent: constMaxPath, sufficient: false)
            //: uploadFailTime(type: 1)
            offce(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        numbererpretation(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension PossibilityMatchDelegate {
    //: func uploadFailTime(type: Int) {
    func offce(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(showDialogContent.prefix(8)))] = type
        //: parm["callWaitingTime"] = time
        parm[(String(mainFormName))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        dataFilterFormat.supposed(eventID: constPathData, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func upendFromTo(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(mainFormName))] = time
        //: parm["toUid"] = toUid
        parm[(String(user_successfullyMessage))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        dataFilterFormat.supposed(eventID: noti_guideTitle, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension PossibilityMatchDelegate {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func disableIn() {
        //: guard RealmReactiveCompatible.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard RealmReactiveCompatible.share.appStatus == GraphHashableRepresentation.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !RealmReactiveCompatible.share.loginUserMode.loungePlus
        vipIcon.isHidden = !RealmReactiveCompatible.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = DotSubscriptType.randow
        //: if RealmReactiveCompatible.share.loginUserMode.freeCallTimes > 0 && RealmReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if RealmReactiveCompatible.share.loginUserMode.freeCallTimes > 0, RealmReactiveCompatible.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if RealmReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
            if RealmReactiveCompatible.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(data_representationFormat.prefix(4)) + String(showExpectedFormat.prefix(6))).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = RealmReactiveCompatible.share.loginUserMode.loungePlus ? vipFee:fee
            let price = RealmReactiveCompatible.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(constUntilId.suffix(6))).restateArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(constUntilId.suffix(6))).restateArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.telescopic(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = RealmReactiveCompatible.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = RealmReactiveCompatible.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = RealmReactiveCompatible.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = RealmReactiveCompatible.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !RealmReactiveCompatible.share.loginUserMode.loungePlus
            priceStackView.isHidden = !RealmReactiveCompatible.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func mutual() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func performConstraint() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(constSoundTabStr + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func utilize() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(disableIn),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: const_matchName,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(ambush),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: k_messageContent,
                                               //: object: nil)
                                               object: nil)
    }
}
