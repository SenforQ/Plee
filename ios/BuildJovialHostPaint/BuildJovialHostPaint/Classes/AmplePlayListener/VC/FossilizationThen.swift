
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let notiEhDisagreeFormat:[UInt8] = [0x35,0x32,0x35,0x28,0x74,0x3f,0x33,0x38,0x39,0x2e,0x66,0x75,0x7c,0x34,0x3d,0x2f,0x7c,0x32,0x33,0x28,0x7c,0x3e,0x39,0x39,0x32,0x7c,0x35,0x31,0x2c,0x30,0x39,0x31,0x39,0x32,0x28,0x39,0x38]

private func findingStance(running num: UInt8) -> UInt8 {
    return num ^ 92
}

/*: "There's no posts yet." :*/
fileprivate let userSimultaneouslyDirectData:String = "rank blade street transactionThere"
fileprivate let data_nightFastValue:String = "\'s no"
fileprivate let user_situationDiskUrl:[Character] = ["t","s"," ","y","e","t","."]

/*: "uid" :*/
fileprivate let userDropOwnUrl:String = "uiversion"

/*: "page" :*/
fileprivate let notiNowData:[Character] = ["p","a","g","e"]

/*: "list" :*/
fileprivate let show_jumpValue:[Character] = ["l","i","s","t"]

/*: "nickname" :*/
fileprivate let constBothText:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "age" :*/
fileprivate let dataRandomFormat:[Character] = ["a","g","e"]

/*: "sex" :*/
fileprivate let mainBetweenFormat:String = "seestimated"

/*: "isTPAuth" :*/
fileprivate let noti_anonFormat:[Character] = ["i","s","T","P","A","u","t","h"]

/*: "headPic" :*/
fileprivate let appFunValue:String = "manage"
fileprivate let mainPanelStr:String = "clothes go ieadPic"

/*: "pinCount" :*/
fileprivate let dataLineMsg:String = "pinCountreport builder"

/*: "model" :*/
fileprivate let noti_todayContent:String = "MODEL"

/*: "Unpin from profile" :*/
fileprivate let userFieldText:[Character] = ["U","n","p","i","n"," ","f","r","o","m"," "]
fileprivate let noti_hugeStr:[Character] = ["p","r","o","f","i","l","e"]

/*: "Delete Post" :*/
fileprivate let dataSwitchdPath:String = "creation characterize about charmDele"
fileprivate let const_schemeKey:String = "both"

/*: "Pin to profile" :*/
fileprivate let noti_voiceMessage:[Character] = ["P","i","n"," ","t","o"," ","p","r","o","f","i"]
fileprivate let noti_limitationTotalroMessage:String = "lscript"

/*: "momentId" :*/
fileprivate let notiThinkDeedName:[Character] = ["m","o","m","e","n"]
fileprivate let user_straightText:String = "effect certaintId"

/*: "status" :*/
fileprivate let data_formalPath:String = "STATUS"

/*: "Your post has been pinned" :*/
fileprivate let kDocStr:String = "ply official busy platformYour"
fileprivate let main_numbValue:String = "commercial inform has "
fileprivate let notiWriterMsg:String = " pinnedtell don bridge solar kind"

/*: "Your post has been Unpinned" :*/
fileprivate let data_progressStr:[Character] = ["Y","o","u","r"," ","p"]
fileprivate let dataPlatStr:[Character] = ["o","s","t"," ","h","a","s"," ","b","e","e","n"," "]
fileprivate let constUserNumberervalData:String = "Unpinnedlat trade"

/*: "Posts" :*/
fileprivate let data_driverUrl:[Character] = ["P","o","s","t","s"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FossilizationThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/22.
//

//: import UIKit
import UIKit

// MARK: - 属性声明 & 生命周期方法

//: class TalkingUserMomentVC: TalkingBaseViewController {
class FossilizationThen: OperateReactiveCompatible {
    //: var uid = ""
    var uid = ""
    //: var pageIndex = 0
    var pageIndex = 0
    //: var disposeBag = DisposeBag()
    var disposeBag = DisposeBag()
    //: var dataSourceArr: [TalkingMomentModel] = []
    var dataSourceArr: [WalkModelType] = []

    //: init(uid: String) {
    init(uid: String) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.uid = uid
        self.uid = uid
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: notiEhDisagreeFormat.map{findingStance(running: $0)}, encoding: .utf8)!)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: setupSubviews()
        audience()
        //: setupSubViewsConstraint()
        personDoing()
        //: bindInteraction()
        file()
        //: reqData()
        turnConnection()
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: lazy var mainTableView: UITableView = {
    lazy var mainTableView: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: table.contentInsetAdjustmentBehavior = .never
            table.contentInsetAdjustmentBehavior = .never
            //: } else {
        } else {
            //: self.automaticallyAdjustsScrollViewInsets = false
            self.automaticallyAdjustsScrollViewInsets = false
        }
        //: table.showsVerticalScrollIndicator = true
        table.showsVerticalScrollIndicator = true
        //: table.estimatedRowHeight = 0
        table.estimatedRowHeight = 0
        //: table.estimatedSectionFooterHeight = 0
        table.estimatedSectionFooterHeight = 0
        //: table.estimatedSectionHeaderHeight = 0
        table.estimatedSectionHeaderHeight = 0
        //: table.separatorStyle = .none
        table.separatorStyle = .none
        //: table.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        table.autoresizingMask = [.flexibleWidth, .flexibleHeight]
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: var style = EmptyStyle()
        var style = FreshEmptyStyle()
        //: style.TipsTitle = "There's no posts yet."
        style.TipsTitle = (String(userSimultaneouslyDirectData.suffix(5)) + data_nightFastValue + " pos" + String(user_situationDiskUrl))
        //: let view = EmptyView.init(frame: self.view.frame, style: style)
        let view = EmptyView(frame: self.view.frame, style: style)
        //: return view
        return view
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingUserMomentVC {
extension FossilizationThen {
    //: func reqData() {
    func turnConnection() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["uid"] = uid
        dict[(userDropOwnUrl.replacingOccurrences(of: "version", with: "d"))] = uid
        //: dict["page"] = pageIndex
        dict[(String(notiNowData))] = pageIndex
        //: TalkingMomentRequestTool.req_UserMomentList(params: dict) { succeed, result, errorModel in
        MomentRequestTool.bubble(params: dict) { succeed, result, errorModel in

            //: self.mainTableView.endRefresh()
            self.mainTableView.globalFor()
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: if self.pageIndex > 0 {
                    if self.pageIndex > 0 {
                        //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                        self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    }
                    //: return
                    return
                }

                //: guard let array = dict["list"] as? Array<Any> else {
                guard let array = dict[(String(show_jumpValue))] as? [Any] else {
                    //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    //: return
                    return
                }
                //: var dataArr: [TalkingMomentModel] = []
                var dataArr: [WalkModelType] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if var model = JSONDeserializer<TalkingMomentModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if var model = JSONDeserializer<WalkModelType>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: model.nickname = dict["nickname"] as? String
                        model.nickname = dict[(String(constBothText))] as? String
                        //: model.age = dict["age"] as? Int
                        model.age = dict[(String(dataRandomFormat))] as? Int
                        //: model.sex = dict["sex"] as? String
                        model.sex = dict[(mainBetweenFormat.replacingOccurrences(of: "estimated", with: "x"))] as? String
                        //: model.uid = dict["uid"] as? String
                        model.uid = dict[(userDropOwnUrl.replacingOccurrences(of: "version", with: "d"))] as? String
                        //: model.isTPAuth = dict["isTPAuth"] as? Bool ?? false
                        model.isTPAuth = dict[(String(noti_anonFormat))] as? Bool ?? false
                        //: model.headPic = dict["headPic"] as? String
                        model.headPic = dict[(appFunValue.replacingOccurrences(of: "manage", with: "h") + String(mainPanelStr.suffix(6)))] as? String
                        //: model.pinCount = dict["pinCount"] as! Int
                        model.pinCount = dict[(String(dataLineMsg.prefix(8)))] as! Int
                        //: model.caculateItemHeight()
                        model.modelTransfer()
                        //: if model.uid == RealmReactiveCompatible.share.loginUserMode.userID {
                        if model.uid == RealmReactiveCompatible.share.loginUserMode.userID {
                            //: model.isUserDetail = true
                            model.isUserDetail = true
                        }
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }

                //: if dataArr.count < 20 {
                if dataArr.count < 20 { // 不足一页，隐藏更多
                    //: self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                    self.mainTableView.mj_footer?.endRefreshingWithNoMoreData()
                }

                //: if self.pageIndex == 0 {
                if self.pageIndex == 0 {
                    //: self.dataSourceArr = dataArr
                    self.dataSourceArr = dataArr
                    //: if dataArr.count>0 {
                    if dataArr.count > 0 {
                        //: self.emptyView.removeFromSuperview()
                        self.emptyView.removeFromSuperview()
                    }
                    //: } else {
                } else {
                    //: self.dataSourceArr.append(contentsOf: dataArr)
                    self.dataSourceArr.append(contentsOf: dataArr)
                }

                //: self.mainTableView.reloadData()
                self.mainTableView.reloadData()
                //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg )
                self.bringHome(showMsg: errorModel!.errorMsg)
            }
        }
    }
}

// MARK: - 事件方法

//: extension TalkingUserMomentVC {
extension FossilizationThen {
    //: func headerRefresh() {
    func betweenWriterFlush() {
        //: pageIndex = 0
        pageIndex = 0
        //: mainTableView.mj_header?.beginRefreshing()
        mainTableView.mj_header?.beginRefreshing()
        //: reqData()
        turnConnection()
    }

    //: func footerRefresh() {
    func oppositeAgain() {
        //: pageIndex += 1
        pageIndex += 1
        //: reqData()
        turnConnection()
    }

    //: @objc func updateLikeNumber(notification: NSNotification) -> Void {
    @objc func position(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: var model: TalkingMomentModel = userinfo["model"] as! TalkingMomentModel
        var model: WalkModelType = userinfo[(noti_todayContent.lowercased())] as! WalkModelType
        //: if model.uid == RealmReactiveCompatible.share.loginUserMode.userID {
        if model.uid == RealmReactiveCompatible.share.loginUserMode.userID {
            //: model.isUserDetail = true
            model.isUserDetail = true
        }
        //: for i in 0..<self.dataSourceArr.count {
        for i in 0 ..< self.dataSourceArr.count {
            //: let tmodel = self.dataSourceArr[i]
            let tmodel = self.dataSourceArr[i]
            //: if model.mid == tmodel.mid {
            if model.mid == tmodel.mid {
                //: self.dataSourceArr[i] = model
                self.dataSourceArr[i] = model
                //: self.mainTableView.reloadRows(at: [IndexPath.init(row: i, section: 0)], with: .none)
                self.mainTableView.reloadRows(at: [IndexPath(row: i, section: 0)], with: .none)
                //: return
                return
            }
        }
    }

    //: func deleteCell(model: TalkingMomentModel, index: Int) {
    func disable(model: WalkModelType, index: Int) {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ExpectationPopView(frame: self.view.frame)

        //: if model.pinCount<3 {
        if model.pinCount < 3 {
            //: if model.pinStatus {
            if model.pinStatus {
                //: vc.initwithList(cellTitleList: ["Unpin from profile".localized, "Delete Post".localized])
                vc.likeList(cellTitleList: [(String(userFieldText) + String(noti_hugeStr)).localized, (String(dataSwitchdPath.suffix(4)) + "te Pos" + const_schemeKey.replacingOccurrences(of: "both", with: "t")).localized])
                //: } else {
            } else {
                //: vc.initwithList(cellTitleList: ["Pin to profile".localized, "Delete Post".localized])
                vc.likeList(cellTitleList: [(String(noti_voiceMessage) + noti_limitationTotalroMessage.replacingOccurrences(of: "script", with: "e")).localized, (String(dataSwitchdPath.suffix(4)) + "te Pos" + const_schemeKey.replacingOccurrences(of: "both", with: "t")).localized])
            }
            //: } else {
        } else {
            //: if model.pinStatus {
            if model.pinStatus {
                //: vc.initwithList(cellTitleList: ["Unpin from profile".localized, "Delete Post".localized])
                vc.likeList(cellTitleList: [(String(userFieldText) + String(noti_hugeStr)).localized, (String(dataSwitchdPath.suffix(4)) + "te Pos" + const_schemeKey.replacingOccurrences(of: "both", with: "t")).localized])
                //: } else {
            } else {
                //: vc.initwithList(cellTitleList: ["Delete Post".localized])
                vc.likeList(cellTitleList: [(String(dataSwitchdPath.suffix(4)) + "te Pos" + const_schemeKey.replacingOccurrences(of: "both", with: "t")).localized])
            }
        }

        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: if str == "Unpin from profile".localized {
            if str == (String(userFieldText) + String(noti_hugeStr)).localized {
                //: self.topMoment(isTop: 0, model: model)
                self.visible(isTop: 0, model: model)
                //: } else if str == "Pin to profile".localized {
            } else if str == (String(noti_voiceMessage) + noti_limitationTotalroMessage.replacingOccurrences(of: "script", with: "e")).localized {
                //: self.topMoment(isTop: 1, model: model)
                self.visible(isTop: 1, model: model)
                //: } else if str == "Delete Post".localized {
            } else if str == (String(dataSwitchdPath.suffix(4)) + "te Pos" + const_schemeKey.replacingOccurrences(of: "both", with: "t")).localized {
                //: ProgressHUD.show()
                LawReactiveCompatible.frontEndShow()
                //: var dict = Dictionary<String, Any>()
                var dict = [String: Any]()
                //: dict["id"] = model.mid
                dict["id"] = model.mid
                //: TalkingMomentRequestTool.req_DeleteMoment(params: dict) { succeed, result, errorModel in
                MomentRequestTool.sumerrupt(params: dict) { succeed, _, errorModel in
                    //: ProgressHUD.dismiss()
                    LawReactiveCompatible.minify()
                    //: if succeed {
                    if succeed {
                        //: if index<self.dataSourceArr.count {
                        if index < self.dataSourceArr.count {
                            //: self.disposeBag = DisposeBag()
                            self.disposeBag = DisposeBag()
                            //: self.mainTableView.mj_header?.beginRefreshing()
                            self.mainTableView.mj_header?.beginRefreshing()
                            //: } else if self.dataSourceArr.count == 1 {
                        } else if self.dataSourceArr.count == 1 {
                            //: self.dataSourceArr.removeAll()
                            self.dataSourceArr.removeAll()
                            //: self.disposeBag = DisposeBag()
                            self.disposeBag = DisposeBag()
                            //: self.view.addSubview(self.emptyView)
                            self.view.addSubview(self.emptyView)
                        }
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.bringHome(showMsg: errorModel!.errorMsg)
                    }
                }
            }
        }
    }

    //: func topMoment(isTop: Int, model: TalkingMomentModel) {
    func visible(isTop: Int, model: WalkModelType) {
        //: ProgressHUD.show()
        LawReactiveCompatible.frontEndShow()
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["momentId"] = model.mid
        dict[(String(notiThinkDeedName) + String(user_straightText.suffix(3)))] = model.mid
        //: dict["status"] = isTop
        dict[(data_formalPath.lowercased())] = isTop
        //: TalkingMomentRequestTool.req_TopMoment(params: dict) { succeed, result, errorModel in
        MomentRequestTool.sinceLibrary(params: dict) { succeed, _, errorModel in
            //: ProgressHUD.dismiss()
            LawReactiveCompatible.minify()
            //: if succeed {
            if succeed {
                //: let str = isTop==1 ? "Your post has been pinned".localized : "Your post has been Unpinned".localized
                let str = self.showTop(isTop)
                //: self.func__showStatusBarSuccessMsg(showMsg: str)
                self.readingDownMsg(showMsg: str)
                //: self.disposeBag = DisposeBag()
                self.disposeBag = DisposeBag()
                //: self.mainTableView.mj_header?.beginRefreshing()
                self.mainTableView.mj_header?.beginRefreshing()
                //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                self.bringHome(showMsg: errorModel!.errorMsg)
            }
        }
    }
    
    private func showTop(_ top: Int) -> String {
        let str = top == 1 ? (String(kDocStr.suffix(4)) + " post" + String(main_numbValue.suffix(5)) + "been" + String(notiWriterMsg.prefix(7))).localized : (String(data_progressStr) + String(dataPlatStr) + String(constUserNumberervalData.prefix(8))).localized

        return str
    }
}

// MARK: - UITableViewDelegate,UITableViewdataSourceArr

//: extension TalkingUserMomentVC: UITableViewDelegate, UITableViewDataSource {
extension FossilizationThen: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return self.dataSourceArr.count
        return self.dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: let model = self.dataSourceArr[indexPath.row]
        let model = self.dataSourceArr[indexPath.row]
        //: return model.itemHeight ?? 56
        return model.itemHeight ?? 56
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = TalkingMomentItemCell(style: .default, reuseIdentifier: TalkingMomentItemCell.className(), isMyHost: true)
        let cell = RepresentMomentConstituentCompartmentReactiveCompatible(style: .default, reuseIdentifier: RepresentMomentConstituentCompartmentReactiveCompatible.className(), isMyHost: true)
        //: let model: TalkingMomentModel = self.dataSourceArr[indexPath.row]
        let model: WalkModelType = self.dataSourceArr[indexPath.row]
        //: cell.configCell(model: model)
        cell.area(model: model)
        //: cell.userInfoView.deleteBtn.rx.tap.subscribe(onNext: { [weak self] in
        cell.userInfoView.deleteBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.deleteCell(model: model, index: indexPath.row)
            self.disable(model: model, index: indexPath.row)
            //: }).disposed(by: cell.cellDisposeBag )
        }).disposed(by: cell.cellDisposeBag)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, willDisplay cell: UITableViewCell, forRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, willDisplay _: UITableViewCell, forRowAt _: IndexPath) {}

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: let model = self.dataSourceArr[indexPath.row]
        let model = self.dataSourceArr[indexPath.row]
        //: let vc =  TalkingMomentDetailVC.init(model: model)
        let vc = StuffVariableRecognizerDelegate(model: model)
        //: vc.delegete = { [weak self] in
        vc.delegete = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if indexPath.row<self.dataSourceArr.count {
            if indexPath.row < self.dataSourceArr.count {
                //: self.dataSourceArr.remove(at: indexPath.row)
                self.dataSourceArr.remove(at: indexPath.row)
                //: self.mainTableView.reloadData()
                self.mainTableView.reloadData()
                //: self.disposeBag = DisposeBag()
                self.disposeBag = DisposeBag()
            }
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }
}

// MARK: - Layout

//: extension TalkingUserMomentVC {
extension FossilizationThen {
    // 添加视图
    //: private func setupSubviews() {
    private func audience() {
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.smallColor()
        //: self.title = "Posts".localized
        self.title = (String(data_driverUrl)).localized
        //: self.view.addSubview(emptyView)
        self.view.addSubview(emptyView)

        //: self.view.addSubview(mainTableView)
        self.view.addSubview(mainTableView)
        //: mainTableView.register(TalkingMomentItemCell.self, forCellReuseIdentifier: TalkingMomentItemCell.className())
        mainTableView.register(RepresentMomentConstituentCompartmentReactiveCompatible.self, forCellReuseIdentifier: RepresentMomentConstituentCompartmentReactiveCompatible.className())
        //: mainTableView.dataSource = self
        mainTableView.dataSource = self
        //: mainTableView.delegate = self
        mainTableView.delegate = self
        //: self.view.bringSubviewToFront(emptyView)
        self.view.bringSubviewToFront(emptyView)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func personDoing() {
        //: mainTableView.snp.makeConstraints { make in
        mainTableView.snp.makeConstraints { make in
            //: make.top.equalTo(self.view).offset(12)
            make.top.equalTo(self.view).offset(12)
            //: make.bottom.equalTo(self.view.snp.bottom).offset(-12)
            make.bottom.equalTo(self.view.snp.bottom).offset(-12)
            //: make.leading.trailing.equalTo(self.view)
            make.leading.trailing.equalTo(self.view)
        }
    }

    // 添加事件
    //: private func bindInteraction() {
    private func file() {
        //: mainTableView.addHeaderRefresh { [weak self] in
        mainTableView.mixInEraseComplectionTrack { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.headerRefresh()
            self.betweenWriterFlush()
        }
        //: mainTableView.addFooterRefresh { [weak self] in
        mainTableView.talkExtra { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.footerRefresh()
            self.oppositeAgain()
        }

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.mainTableView.mj_header?.beginRefreshing()
            self.mainTableView.mj_header?.beginRefreshing()
        }
        //: NotificationCenter.default.addObserver(self, selector: #selector(updateLikeNumber(notification:)), name: UPDATE_LIKE_NUMBER_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(position(notification:)), name: user_managerId, object: nil)
    }
}
