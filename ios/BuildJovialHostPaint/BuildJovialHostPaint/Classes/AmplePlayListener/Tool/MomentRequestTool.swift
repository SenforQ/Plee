
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let noti_thinkDailyKey:String = "year black exist process cleanmf/momen"
fileprivate let notiInsideKey:String = "tool doc going draft enterpriset/list"

/*: "mf/moment/moments" :*/
fileprivate let constNorthwestData:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let show_thinkName:[Character] = ["t","/","m"]
fileprivate let kBigData:String = "OMENTS"

/*: "mf/moment/like" :*/
fileprivate let dataChangeMsg:String = "mf/momentreader why pop"
fileprivate let data_easeId:String = "pop recommend correct produce with/like"

/*: "mf/crush/send" :*/
fileprivate let k_certainData:String = "all increase mouth missingmf/c"
fileprivate let main_willUrl:String = "brake"

/*: "mf/moment/view" :*/
fileprivate let constAddressUrl:String = "master please not secure tarmf/mome"
fileprivate let showGiveData:String = "tab recentnt/view"

/*: "mf/momentReply/list" :*/
fileprivate let userBridgeTitle:[Character] = ["m","f"]
fileprivate let dataItemFormat:String = "increase visible/mom"
fileprivate let showSurgeryId:String = "achievement letter pi forward coreply/list"

/*: "mf/momentReply/save" :*/
fileprivate let user_rewardText:String = "mf/momin hi lack gray bridge"
fileprivate let showFlatPath:String = "stopply"

/*: "mf/moment/videoSign" :*/
fileprivate let appCleanNextStr:[Character] = ["m","f","/","m","o","m","e","n","t","/","v","i","d"]
fileprivate let mainFollowRangePath:String = "installation memory peereoSign"

/*: "dynamicPic.zip" :*/
fileprivate let kBorrowUrl:[Character] = ["d","y","n","a","m","i","c","P","i","c"]
fileprivate let showOperatePath:String = ".zipsecure consent"

/*: "pic" :*/
fileprivate let showHypothesisStr:[UInt8] = [0x63,0x69,0x70]

/*: "content" :*/
fileprivate let notiSystemGoldData:[Character] = ["c","o","n","t","e","n","t"]

/*: "mf/moment/save" :*/
fileprivate let const_pickId:[Character] = ["m","f","/","m","o","m","e","n"]
fileprivate let user_closeMessage:[Character] = ["t"]
fileprivate let dataCooperativeTitle:String = "assert production devil drown/save"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let show_messageFormat:[UInt8] = [0x6d,0x48,0x5f,0x51,0x54,0x4e,0x55,0x1d,0x6e,0x48,0x5e,0x5e,0x58,0x4e,0x4e,0x5b,0x48,0x51,0x11,0x1d,0x7a,0x52,0x1d,0x49,0x52,0x1d,0x70,0x44,0x1d,0x79,0x44,0x53,0x5c,0x50,0x54,0x5e,0x4e,0x1d,0x49,0x52,0x1d,0x4b,0x54,0x58,0x4a]

private func yieldBuild(comply num: UInt8) -> UInt8 {
    return num ^ 61
}

/*: "mf/moment/saveVideo" :*/
fileprivate let main_systemTaUrl:String = "mf/mmini day choose always"
fileprivate let dataBusinessStr:String = "section cycle priority recognize alivesaveVi"
fileprivate let app_allDescribeStr:String = "owneo"

/*: .png" :*/
fileprivate let user_addressAnyoneValue:String = ".pngfood pair execute environment para"

/*: "dynamicPic" :*/
fileprivate let app_readyName:[Character] = ["d","y"]
fileprivate let constFieldStr:String = "nacorei"

/*: "freeImage" :*/
fileprivate let kUnknownMustPath:[Character] = ["f","r","e","e"]
fileprivate let appSucceedTitle:String = "size required tent aspectImage"

/*: "mf/momentReply/replylist" :*/
fileprivate let const_absoluteMessage:String = "finite optimistic extentmf/momen"
fileprivate let k_valueYourKey:[Character] = ["t","R","e","p","l","y","/","r","e","p","l","y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let show_complyEarlyUnableId:String = "mf/mcook condition don anima short"
fileprivate let user_masterExclusiveStr:[Character] = ["l","e","t","e"]

/*: "mf/momentReply/delete" :*/
fileprivate let showElectPath:String = "mf/molag move"
fileprivate let kDragName:String = "choose above psychological together retly/de"

/*: "mf/moment/pin" :*/
fileprivate let const_yellowMessage:String = "secret receive toomf/mo"
fileprivate let const_pleaseFormat:String = "shared"
fileprivate let userHerFormat:String = "nt/pinhand print yourself should smart"

/*: "mf/moment/remindNum" :*/
fileprivate let app_effectMessage:[Character] = ["m","f","/","m","o","m","e","n","t","/","r","e","m","i","n","d","N","u","m"]

/*: "mf/moment/remindList" :*/
fileprivate let k_lifeValue:String = "mf/mcountry grade"
fileprivate let mainEarnValue:String = "OMEN"
fileprivate let app_rangeUrl:String = "to raw relatedmindList"

/*: "page" :*/
fileprivate let user_columnConnectionName:[UInt8] = [0x7d,0x6c,0x6a,0x68]

private func blankEarn(transaction num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "cos/getCredential" :*/
fileprivate let constAcceptMsg:[Character] = ["c","o","s","/","g","e","t","C","r","e"]
fileprivate let showBoyOneId:String = "dentiaabs"

/*: "type" :*/
fileprivate let noti_seaStr:String = "tyanone"

/*: "user/reportUserInfo" :*/
fileprivate let appLaterMessage:[Character] = ["u","s","e","r","/","r","e","p","o","r","t","U","s","e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let main_rollThreeName:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let appLowerRiceMessage:[Character] = ["d","e","v","i","c"]
fileprivate let constFarData:[Character] = ["e","I","d"]

/*: "deviceName" :*/
fileprivate let main_displayValue:String = "denow"
fileprivate let notiCompleteText:[Character] = ["i","c","e","N","a","m","e"]

/*: "iphone" :*/
fileprivate let mainFileData:[Character] = ["i"]
fileprivate let show_goldFlexibleTitle:String = "phmergene"

/*: "fileUrl" :*/
fileprivate let showProcessingKey:[Character] = ["f","i","l","e","U","r"]
fileprivate let main_tarKey:String = "attitude"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  MomentRequestTool.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class MomentRequestTool: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func bring(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(noti_thinkDailyKey.suffix(8)) + String(notiInsideKey.suffix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func bubble(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(constNorthwestData) + String(show_thinkName) + kBigData.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func collectionCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(dataChangeMsg.prefix(9)) + String(data_easeId.suffix(5)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func offAccount(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(k_certainData.suffix(4)) + "rush/sen" + main_willUrl.replacingOccurrences(of: "brake", with: "d"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func discount(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(constAddressUrl.suffix(7)) + String(showGiveData.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func substance(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(userBridgeTitle) + String(dataItemFormat.suffix(4)) + "entRe" + String(showSurgeryId.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func plan(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(user_rewardText.prefix(5)) + "mentR" + showFlatPath.replacingOccurrences(of: "stop", with: "e") + "/save")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func start(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(appCleanNextStr) + String(mainFollowRangePath.suffix(6)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func networkAwake(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        observance()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            incident(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((String(kBorrowUrl) + String(showOperatePath.prefix(4))))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: showHypothesisStr.reversed(), encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(notiSystemGoldData))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = ManhoodRequestModel()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(const_pickId) + String(user_closeMessage) + String(dataCooperativeTitle.suffix(5)))
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.readingDownMsg(showMsg: String(bytes: show_messageFormat.map{yieldBuild(comply: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.observance()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func inmateCompletion(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(main_systemTaUrl.prefix(4)) + "oment/" + String(dataBusinessStr.suffix(6)) + app_allDescribeStr.replacingOccurrences(of: "own", with: "d"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func incident(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.media()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(user_addressAnyoneValue.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((String(app_readyName) + constFieldStr.replacingOccurrences(of: "core", with: "m") + "cPic"))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func observance() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(kUnknownMustPath) + String(appSucceedTitle.suffix(5))))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func stumble(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(const_absoluteMessage.suffix(8)) + String(k_valueYourKey))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func sumerrupt(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(show_complyEarlyUnableId.prefix(4)) + "oment/de" + String(user_masterExclusiveStr))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func diaphragm(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(showElectPath.prefix(5)) + "mentRep" + String(kDragName.suffix(5)) + "lete")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func sinceLibrary(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(const_yellowMessage.suffix(5)) + const_pleaseFormat.replacingOccurrences(of: "shared", with: "me") + String(userHerFormat.prefix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func part(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(app_effectMessage))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func seekDraft(page: Int, completion: @escaping (_ data: [LimitationHandyJSON]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(k_lifeValue.prefix(4)) + mainEarnValue.lowercased() + "t/re" + String(app_rangeUrl.suffix(8)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: user_columnConnectionName.map{blankEarn(transaction: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { _, result, _ in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [LimitationHandyJSON] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<LimitationHandyJSON>.deserialize(from: arr as? Array) as? [LimitationHandyJSON] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func gradePoint(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(constAcceptMsg) + showBoyOneId.replacingOccurrences(of: "abs", with: "l"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(noti_seaStr.replacingOccurrences(of: "anon", with: "p")): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func leaveTotalerrupt(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = ManhoodRequestModel()
        //: reqModel.requestServer = RealmReactiveCompatible.share.appConfigMode.reportDomain
        reqModel.requestServer = RealmReactiveCompatible.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(appLaterMessage))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = RealmReactiveCompatible.share.loginUserMode.userID
        params[(String(main_rollThreeName))] = RealmReactiveCompatible.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(String(appLowerRiceMessage) + String(constFarData))] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(main_displayValue.replacingOccurrences(of: "now", with: "v") + String(notiCompleteText))] = (String(mainFileData) + show_goldFlexibleTitle.replacingOccurrences(of: "merge", with: "o"))
        //: params["type"] = 1
        params[(noti_seaStr.replacingOccurrences(of: "anon", with: "p"))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(showProcessingKey) + main_tarKey.replacingOccurrences(of: "attitude", with: "l"))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        userTextMessage.theoreticalAccount(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
