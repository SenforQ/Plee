
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_bringSaveUrl:[UInt8] = [0x58,0x5f,0x58,0x45,0x19,0x52,0x5e,0x55,0x54,0x43,0xb,0x18,0x11,0x59,0x50,0x42,0x11,0x5f,0x5e,0x45,0x11,0x53,0x54,0x54,0x5f,0x11,0x58,0x5c,0x41,0x5d,0x54,0x5c,0x54,0x5f,0x45,0x54,0x55]

private func freshPass(error num: UInt8) -> UInt8 {
    return num ^ 49
}

/*: "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation." :*/
fileprivate let data_localTitle:[UInt8] = [0x96,0xb7,0xac,0xb5,0x67,0xbb,0xaf,0xac,0xb5,0x67,0x6c,0x87,0x67,0xbe,0xb0,0xb3,0xb3,0x67,0xb9,0xac,0xaa,0xb6,0xb4,0xb4,0xac,0xb5,0xab,0x67,0xbc,0xba,0xac,0xb9,0xba,0x67,0xbb,0xb6,0x67,0xc0,0xb6,0xbc,0x75,0x67,0x88,0x67,0xb5,0xac,0xbe,0x67,0xaa,0xaf,0xa8,0xbb,0x67,0xbe,0xb0,0xb3,0xb3,0x67,0xa9,0xac,0x67,0xb4,0xa8,0xab,0xac,0x67,0xa8,0xad,0xbb,0xac,0xb9,0x67,0xa8,0x67,0xba,0xbc,0xaa,0xaa,0xac,0xba,0xba,0xad,0xbc,0xb3,0x67,0xb9,0xac,0xaa,0xb6,0xb4,0xb4,0xac,0xb5,0xab,0xa8,0xbb,0xb0,0xb6,0xb5,0x75]

fileprivate func wedList(reward num: UInt8) -> UInt8 {
    let value = Int(num) + 185
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Your avatar and nickname will not be displayed in the ranking." :*/
fileprivate let data_morningPath:[UInt8] = [0x36,0x4c,0x52,0x4f,0xfd,0x3e,0x53,0x3e,0x51,0x3e,0x4f,0xfd,0x3e,0x4b,0x41,0xfd,0x4b,0x46,0x40,0x48,0x4b,0x3e,0x4a,0x42,0xfd,0x54,0x46,0x49,0x49,0xfd,0x4b,0x4c,0x51,0xfd,0x3f,0x42,0xfd,0x41,0x46,0x50,0x4d,0x49,0x3e,0x56,0x42,0x41,0xfd,0x46,0x4b,0xfd,0x51,0x45,0x42,0xfd,0x4f,0x3e,0x4b,0x48,0x46,0x4b,0x44,0xb]

fileprivate func rightFinding(highlight num: UInt8) -> UInt8 {
    let value = Int(num) + 35
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Open then Applyname will recommend users to you. Use your identity to greet each other." :*/
fileprivate let data_resumeMessage:[UInt8] = [0x1d,0x22,0x37,0x3c,0x72,0x26,0x3a,0x37,0x3c,0x72,0x13,0x22,0x22,0x3e,0x2b,0x3c,0x33,0x3f,0x37,0x72,0x25,0x3b,0x3e,0x3e,0x72,0x20,0x37,0x31,0x3d,0x3f,0x3f,0x37,0x3c,0x36,0x72,0x27,0x21,0x37,0x20,0x21,0x72,0x26,0x3d,0x72,0x2b,0x3d,0x27,0x7c,0x72,0x7,0x21,0x37,0x72,0x2b,0x3d,0x27,0x20,0x72,0x3b,0x36,0x37,0x3c,0x26,0x3b,0x26,0x2b,0x72,0x26,0x3d,0x72,0x35,0x20,0x37,0x37,0x26,0x72,0x37,0x33,0x31,0x3a,0x72,0x3d,0x26,0x3a,0x37,0x20,0x7c]

private func fundamentalPitch(due num: UInt8) -> UInt8 {
    return num ^ 82
}

/*: "Open then %@ will recommend users to you. Use your identity to greet each other." :*/
fileprivate let user_tickMsg:[UInt8] = [0x48,0x77,0x62,0x69,0x27,0x73,0x6f,0x62,0x69,0x27,0x22,0x47,0x27,0x70,0x6e,0x6b,0x6b,0x27,0x75,0x62,0x64,0x68,0x6a,0x6a,0x62,0x69,0x63,0x27,0x72,0x74,0x62,0x75,0x74,0x27,0x73,0x68,0x27,0x7e,0x68,0x72,0x29,0x27,0x52,0x74,0x62,0x27,0x7e,0x68,0x72,0x75,0x27,0x6e,0x63,0x62,0x69,0x73,0x6e,0x73,0x7e,0x27,0x73,0x68,0x27,0x60,0x75,0x62,0x62,0x73,0x27,0x62,0x66,0x64,0x6f,0x27,0x68,0x73,0x6f,0x62,0x75,0x29]

/*: "#FF2348" :*/
fileprivate let noti_operativeFormat:String = "privacy which ample enjoy#FF234"
fileprivate let showSignatureMsg:[Character] = ["8"]

/*: "To set up a password, you need to verify the account, choose your verification method" :*/
fileprivate let user_thenId:[UInt8] = [0x6e,0x89,0x3a,0x8d,0x7f,0x8e,0x3a,0x8f,0x8a,0x3a,0x7b,0x3a,0x8a,0x7b,0x8d,0x8d,0x91,0x89,0x8c,0x7e,0x46,0x3a,0x93,0x89,0x8f,0x3a,0x88,0x7f,0x7f,0x7e,0x3a,0x8e,0x89,0x3a,0x90,0x7f,0x8c,0x83,0x80,0x93,0x3a,0x8e,0x82,0x7f,0x3a,0x7b,0x7d,0x7d,0x89,0x8f,0x88,0x8e,0x46,0x3a,0x7d,0x82,0x89,0x89,0x8d,0x7f,0x3a,0x93,0x89,0x8f,0x8c,0x3a,0x90,0x7f,0x8c,0x83,0x80,0x83,0x7d,0x7b,0x8e,0x83,0x89,0x88,0x3a,0x87,0x7f,0x8e,0x82,0x89,0x7e]

fileprivate func missingDrop(option num: UInt8) -> UInt8 {
    let value = Int(num) - 26
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Verified by Phone" :*/
fileprivate let mainRiverUrl:String = "be conversion smallVeri"
fileprivate let mainWaterData:[Character] = ["f","i","e","d"," ","b","y"," ","P","h","o","n","e"]

/*: "Verified by Email" :*/
fileprivate let mainDealTitle:[Character] = ["V","e","r","i","f"]
fileprivate let const_yetBitText:[Character] = ["i","e","d"," ","b","y"," ","E","m","a","i","l"]

/*: "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? " :*/
fileprivate let mainUniformTermMsg:[UInt8] = []

fileprivate func bearPressure(universal num: UInt8) -> UInt8 {
    let value = Int(num) - 32
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "UNRECOVERABLE" :*/
fileprivate let app_constraintUrl:String = "UNRbladeCOVbladeR"
fileprivate let appTextName:[Character] = ["A","B","L","E"]

/*: "Delete Account" :*/
fileprivate let k_makeupName:[Character] = ["D","e","l","e","t","e"," ","A","c","c","o","u","n","t"]

/*: "Cancel" :*/
fileprivate let mainTurnRawUnderName:String = "con eff make generalCancel"

/*: "Delete" :*/
fileprivate let mainCrushTitle:[Character] = ["D"]
fileprivate let kStripName:String = "communicationlcommunicationtcommunication"

/*: "Logout succeeded!" :*/
fileprivate let k_donData:[Character] = ["L","o","g","o","u","t"," ","s","u","c","c","e","e","d","e","d","!"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ThighRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/4/26.
//

//: import UIKit
import UIKit

//: enum SettingsUniversal: String {
enum ScoreSettingsConvertible: String {
    //: case Contact_information          = "Contact information"
    case Contact_information = "Contact information"
    //: case Notifications                = "Notifications"
    case Notifications
    //: case Social_Preferences           = "Social Preferences"
    case Social_Preferences = "Social Preferences"
}

//: enum SettingsUniversalType: String {
enum NoKeyRepresentable: String {
    //: case Phone_Number          = "Phone Number"
    case Phone_Number = "Phone Number"
    //: case Email_Address         = "Email Address"
    case Email_Address = "Email Address"
    //: case Turnon_Notif          = "Turn on Notification"
    case Turnon_Notif = "Turn on Notification"
    //: case Same_gendered         = "Same-gendered messaging"
    case Same_gendered = "Same-gendered messaging"
    //: case Cupid                 = "Cupid messaging"
    case Cupid = "Cupid messaging"
    //: case Ranking               = "Ranking anonymous"
    case Ranking = "Ranking anonymous"
    //: case Automatic             = "Automatic message"
    case Automatic = "Automatic message"
    //: case SetPassword           = "Set New Password"
    case SetPassword = "Set New Password"
    //: case DeleteA               = "Delete Account"
    case DeleteA = "Delete Account"
}

//: class TalkingSettingsUniversalVC: TalkingBaseViewController {
class ThighRecognizerDelegate: OperateReactiveCompatible {
    //: fileprivate lazy var bag = DisposeBag()
    fileprivate lazy var bag = DisposeBag()
    //: var settingModel = TalkingSettingModel()
    var settingModel = HumanModelType()

    //: var  type = SettingsUniversal.Contact_information
    var type = ScoreSettingsConvertible.Contact_information

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_bringSaveUrl.map{freshPass(error: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.smallColor()
        //: designView()
        weltanschauung()
        //: getSettingData()
        outSituation()
    }

    // MARK: - Lazy load

    //: private lazy var DataSource: [SettingsUniversalType] = {
    private lazy var DataSource: [NoKeyRepresentable] = {
        //: var array = [SettingsUniversalType]()
        var array = [NoKeyRepresentable]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-TabBarViewHeight), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: dataModeBlockStr, height: constCornerDetailUrl - const_hundredId), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: private lazy var DetailSource: [String] = {
    private lazy var DetailSource: [String] = {
        //: var array = ["", "Open then %@ will recommend users to you. A new chat will be made after a successful recommendation.".localizedArguments(AppName), "Your avatar and nickname will not be displayed in the ranking.".localized, "Open then Applyname will recommend users to you. Use your identity to greet each other.".localized]
        var array = ["", String(bytes: data_localTitle.map{wedList(reward: $0)}, encoding: .utf8)!.restateArguments(show_lessKey), String(bytes: data_morningPath.map{rightFinding(highlight: $0)}, encoding: .utf8)!.localized, String(bytes: data_resumeMessage.map{fundamentalPitch(due: $0)}, encoding: .utf8)!.localized]
        //: if RealmReactiveCompatible.share.loginUserMode.sex == "2" {
        if RealmReactiveCompatible.share.loginUserMode.sex == "2" {
            //: array[1] = "Open then %@ will recommend users to you. Use your identity to greet each other.".localizedArguments(AppName)
            array[1] = String(bytes: user_tickMsg.map{$0^7}, encoding: .utf8)!.restateArguments(show_lessKey)
        }
        //: return array
        return array
        //: }()
    }()
}

// MARK: - Load data

//: extension TalkingSettingsUniversalVC {
extension ThighRecognizerDelegate {
    //: func getSettingData() {
    func outSituation() {
        //: let dict = Dictionary<String, Any>()
        let dict = [String: Any]()
        //: TalkingMeRequestTool.req_GetSetting(params: dict) { succeed, result, errorModel in
        CombinedThen.except(params: dict) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: self.settingModel = JSONDeserializer<TalkingSettingModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil)!
                self.settingModel = JSONDeserializer<HumanModelType>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil)!
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func setUnicersalView(type: SettingsUniversal) {
    func remainType(type: ScoreSettingsConvertible) {
        //: self.type = type
        self.type = type
        //: self.title = type.rawValue.localized
        self.title = type.rawValue.localized
        //: switch type {
        switch type {
        //: case .Contact_information:
        case .Contact_information:
            //: if RealmReactiveCompatible.share.appConfigMode.enableSmsLogin {
            if RealmReactiveCompatible.share.appConfigMode.enableSmsLogin {
                //: DataSource.append(.Phone_Number)
                DataSource.append(.Phone_Number)
            }
            //: DataSource.append(.Email_Address)
            DataSource.append(.Email_Address)
            //: if (RealmReactiveCompatible.share.appConfigMode.enableSmsLogin &&
            if (RealmReactiveCompatible.share.appConfigMode.enableSmsLogin &&
                //: RealmReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0) ||
                RealmReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0) ||
                //: RealmReactiveCompatible.share.loginUserMode.email.count > 0 {
                RealmReactiveCompatible.share.loginUserMode.email.count > 0
            {
                //: DataSource.append(.SetPassword)
                DataSource.append(.SetPassword)
            }
            //: DataSource.append(.DeleteA)
            DataSource.append(.DeleteA)
        //: break
        //: case .Notifications:
        case .Notifications:
            //: DataSource.append(.Turnon_Notif)
            DataSource.append(.Turnon_Notif)
        //: break
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: DataSource.append(.Same_gendered)
            DataSource.append(.Same_gendered)
            //: DataSource.append(.Cupid)
            DataSource.append(.Cupid)
            //: DataSource.append(.Ranking)
            DataSource.append(.Ranking)
            //: DataSource.append(.Automatic)
            DataSource.append(.Automatic)
            //: break
        }
        //: self.MainTable.reloadData()
        self.MainTable.reloadData()
    }

    /// 获取修改【手机号】密码验证码
    //: private func req_sendModifyPwdSms() {
    private func miniCurrent() {
        //: ProgressHUD.show()
        LawReactiveCompatible.frontEndShow()
        //: TalkingLoginRequestTool.req_sendModifyPwdSms { succeed, result, errorModel in
        MiddleReactiveCompatible.who { succeed, _, _ in
            //: ProgressHUD.dismiss()
            LawReactiveCompatible.minify()
            //: guard succeed else { return }
            guard succeed else { return }
            //: BrowReactiveCompatible.share.func__pushToSetNewPasswordVC(type: .PhoneLogin, content: RealmReactiveCompatible.share.loginUserMode.mobile ?? "", isNewPwd: false)
            BrowReactiveCompatible.share.commentPwd(type: .PhoneLogin, content: RealmReactiveCompatible.share.loginUserMode.mobile ?? "", isNewPwd: false)
        }
    }

    /// 获取修改【邮箱】密码验证码
    //: private func req_sendModifyPwdEmail() {
    private func noseEmail() {
        //: ProgressHUD.show()
        LawReactiveCompatible.frontEndShow()
        //: TalkingLoginRequestTool.req_sendModifyPwdEmail { succeed, result, errorModel in
        MiddleReactiveCompatible.towardCustom { succeed, _, _ in
            //: ProgressHUD.dismiss()
            LawReactiveCompatible.minify()
            //: guard succeed else { return }
            guard succeed else { return }
            //: BrowReactiveCompatible.share.func__pushToSetNewPasswordVC(type: .EmailLogin, content: RealmReactiveCompatible.share.loginUserMode.email, isNewPwd: false)
            BrowReactiveCompatible.share.commentPwd(type: .EmailLogin, content: RealmReactiveCompatible.share.loginUserMode.email, isNewPwd: false)
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingSettingsUniversalVC: UITableViewDelegate, UITableViewDataSource {
extension ThighRecognizerDelegate: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return DataSource.count
        return DataSource.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: switch type {
        switch type {
        //: case .Contact_information, .Notifications:
        case .Contact_information, .Notifications:
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        //: case .Social_Preferences:
        case .Social_Preferences:
            //: if DataSource[indexPath.row] == .Same_gendered {
            if DataSource[indexPath.row] == .Same_gendered {
                //: return actualHeight(h: 50)
                return actualHeight(h: 50)
                //: }else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: if RealmReactiveCompatible.share.loginUserMode.sex == "2" {
                if RealmReactiveCompatible.share.loginUserMode.sex == "2" {
                    //: return actualHeight(h: 89)
                    return actualHeight(h: 89)
                    //: } else {
                } else {
                    //: return actualHeight(h: 109)
                    return actualHeight(h: 109)
                }
                //: }  else if (DataSource[indexPath.row] == .Ranking ||  DataSource[indexPath.row] == .Automatic) {
            } else if DataSource[indexPath.row] == .Ranking || DataSource[indexPath.row] == .Automatic {
                //: return actualHeight(h: 92)
                return actualHeight(h: 92)
            }
            //: return actualHeight(h: 50)
            return actualHeight(h: 50)
        }
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: switch self.type {
        switch self.type {
        //: case .Contact_information:
        case .Contact_information:
            //: let cell: TalkingSettingCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingCell.className(), for: indexPath) as! TalkingSettingCell
            let cell: PossibilitySettingCell = tableView.dequeueReusableCell(withIdentifier: PossibilitySettingCell.className(), for: indexPath) as! PossibilitySettingCell

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.priority(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: cell.titleBLB.textColor = UIColor.appTitleColor()
            cell.titleBLB.textColor = UIColor.askNameColor()

            //: if DataSource[indexPath.row] == SettingsUniversalType.Phone_Number {
            if DataSource[indexPath.row] == NoKeyRepresentable.Phone_Number {
                //: if RealmReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0 {
                if RealmReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0 {
                    //: cell.setCellDetails(detail: RealmReactiveCompatible.share.loginUserMode.mobile ?? "")
                    cell.detail(detail: RealmReactiveCompatible.share.loginUserMode.mobile ?? "")
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: cell.titleBLB.textColor = .appTitleColor()
                cell.titleBLB.textColor = .askNameColor()
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.Email_Address {
            } else if DataSource[indexPath.row] == NoKeyRepresentable.Email_Address {
                //: if RealmReactiveCompatible.share.loginUserMode.email.count > 0 {
                if RealmReactiveCompatible.share.loginUserMode.email.count > 0 {
                    //: cell.setCellDetails(detail: RealmReactiveCompatible.share.loginUserMode.email )
                    cell.detail(detail: RealmReactiveCompatible.share.loginUserMode.email)
                    //: } else {
                } else {
                    //: cell.editImag.isHidden = false
                    cell.editImag.isHidden = false
                }
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.SetPassword {
            } else if DataSource[indexPath.row] == NoKeyRepresentable.SetPassword {
                //: cell.editImag.isHidden = false
                cell.editImag.isHidden = false
                //: } else if DataSource[indexPath.row] == SettingsUniversalType.DeleteA {
            } else if DataSource[indexPath.row] == NoKeyRepresentable.DeleteA {
                //: cell.editImag.isHidden = true
                cell.editImag.isHidden = true
                //: cell.titleBLB.textColor = UIColor.init(hex: "#FF2348")
                cell.titleBLB.textColor = UIColor(hex: (String(noti_operativeFormat.suffix(6)) + String(showSignatureMsg)))
            }

            //: return cell
            return cell

        //: case .Notifications, .Social_Preferences:
        case .Notifications, .Social_Preferences:
            //: let cell: TalkingSettingNotifCell = tableView.dequeueReusableCell(withIdentifier: TalkingSettingNotifCell.className(), for: indexPath) as! TalkingSettingNotifCell
            let cell: DesignReactiveCompatible = tableView.dequeueReusableCell(withIdentifier: DesignReactiveCompatible.className(), for: indexPath) as! DesignReactiveCompatible

            //: cell.setCellMessage(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count-1 )
            cell.resignLast(titile: DataSource[indexPath.row].rawValue, row: indexPath.row, isLast: indexPath.row == DataSource.count - 1)
            //: if self.type == .Social_Preferences {
            if self.type == .Social_Preferences {
                //: cell.setCellDetails(detail: DetailSource[indexPath.row])
                cell.details(detail: DetailSource[indexPath.row])
            }
            //: if DataSource[indexPath.row] == .Turnon_Notif {
            if DataSource[indexPath.row] == .Turnon_Notif {
                //: UNUserNotificationCenter.current().getNotificationSettings { settings in
                UNUserNotificationCenter.current().getNotificationSettings { settings in
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: let result = settings.authorizationStatus == .authorized
                        let result = settings.authorizationStatus == .authorized
                        //: cell.setCellNotif(isShow: result ? 1 : -1)
                        cell.bass(isShow: result ? 1 : -1)
                    }
                }
                //: } else if DataSource[indexPath.row] == .Cupid {
            } else if DataSource[indexPath.row] == .Cupid {
                //: cell.setCellNotif(isShow: self.settingModel.isCupid ?? 1)
                cell.bass(isShow: self.settingModel.isCupid ?? 1)
                //: } else if DataSource[indexPath.row] == .Ranking {
            } else if DataSource[indexPath.row] == .Ranking {
                //: cell.setCellNotif(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                cell.bass(isShow: self.settingModel.isLeaderboardAnonymous ?? -1)
                //: } else if DataSource[indexPath.row] == .Same_gendered {
            } else if DataSource[indexPath.row] == .Same_gendered {
                //: cell.setCellNotif(isShow: self.settingModel.isSameGendered ?? -1)
                cell.bass(isShow: self.settingModel.isSameGendered ?? -1)
                //: } else if DataSource[indexPath.row] == .Automatic {
            } else if DataSource[indexPath.row] == .Automatic {
                //: cell.setCellNotif(isShow: self.settingModel.autoGreetAuth ?? -1)
                cell.bass(isShow: self.settingModel.autoGreetAuth ?? -1)
            }
            //: return cell
            return cell
        }
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: if self.type == .Contact_information {
        if self.type == .Contact_information {
            //: if DataSource[indexPath.row].rawValue == SettingsUniversalType.Phone_Number.rawValue {
            if DataSource[indexPath.row].rawValue == NoKeyRepresentable.Phone_Number.rawValue {
                //: let mobile = RealmReactiveCompatible.share.loginUserMode.mobile ?? ""
                let mobile = RealmReactiveCompatible.share.loginUserMode.mobile ?? ""
                //: if mobile.isEmptyString {
                if mobile.isEmptyString {
                    //: let vc = TalkingBindPhoneVC()
                    let vc = InconsistentViewController()
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.Email_Address.rawValue {
            } else if DataSource[indexPath.row].rawValue == NoKeyRepresentable.Email_Address.rawValue {
                //: if RealmReactiveCompatible.share.loginUserMode.email.count <= 0 {
                if RealmReactiveCompatible.share.loginUserMode.email.count <= 0 {
                    //: let vc = TalkingLoginBindEmailVC()
                    let vc = RuleViewController()
                    //: vc.isBack = true
                    vc.isBack = true
                    //: navigationController?.pushViewController(vc, animated: true)
                    navigationController?.pushViewController(vc, animated: true)
                }
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.DeleteA.rawValue {
            } else if DataSource[indexPath.row].rawValue == NoKeyRepresentable.DeleteA.rawValue {
                //: writeOff()
                withoutDoing()
                //: } else if DataSource[indexPath.row].rawValue == SettingsUniversalType.SetPassword.rawValue {
            } else if DataSource[indexPath.row].rawValue == NoKeyRepresentable.SetPassword.rawValue {
                //: let haveMobile = RealmReactiveCompatible.share.appConfigMode.enableSmsLogin && (RealmReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0)
                let haveMobile = RealmReactiveCompatible.share.appConfigMode.enableSmsLogin && (RealmReactiveCompatible.share.loginUserMode.mobile?.count ?? 0 > 0)
                //: let haveEmail = RealmReactiveCompatible.share.loginUserMode.email.count > 0
                let haveEmail = RealmReactiveCompatible.share.loginUserMode.email.count > 0
                //: if haveMobile, haveEmail {
                if haveMobile, haveEmail { // 手机号和邮箱都存在
                    //: let vc = TalkingMunuPopView(frame: self.view.frame)
                    let vc = ExpectationPopView(frame: self.view.frame)
                    //: vc.title = "To set up a password, you need to verify the account, choose your verification method".localized
                    vc.title = String(bytes: user_thenId.map{missingDrop(option: $0)}, encoding: .utf8)!.localized
                    //: vc.titleFont = UIFont.pingfangRugularFont(fontSize: 12)
                    vc.titleFont = UIFont.ditMention(fontSize: 12)
                    //: vc.titleColor = UIColor.appValueDetailColor()
                    vc.titleColor = UIColor.resumeName()
                    //: vc.cellTitleFont = UIFont.pingfangMediumFont(fontSize: 16)
                    vc.cellTitleFont = UIFont.telescopic(fontSize: 16)
                    //: vc.hasCancel = false
                    vc.hasCancel = false
                    //: vc.initwithList(cellTitleList: ["Verified by Phone".localized, "Verified by Email".localized])
                    vc.likeList(cellTitleList: [(String(mainRiverUrl.suffix(4)) + String(mainWaterData)).localized, (String(mainDealTitle) + String(const_yetBitText)).localized])
                    //: vc.munuBlock = { [weak self] index, str in
                    vc.munuBlock = { [weak self] index, _ in
                        //: guard let self = self else { return }
                        guard let self = self else { return }
                        //: if index == 0 {
                        if index == 0 {
                            //: self.req_sendModifyPwdSms()
                            self.miniCurrent()
                            //: } else if index == 1 {
                        } else if index == 1 {
                            //: self.req_sendModifyPwdEmail()
                            self.noseEmail()
                        }
                    }

                    //: } else if haveMobile {
                } else if haveMobile {
                    //: req_sendModifyPwdSms()
                    miniCurrent()

                    //: } else if haveEmail {
                } else if haveEmail {
                    //: req_sendModifyPwdEmail()
                    noseEmail()
                }
            }
        }
    }
}

//: extension TalkingSettingsUniversalVC {
extension ThighRecognizerDelegate {
    /// 注销
    //: private func writeOff() {
    private func withoutDoing() {
        //: guard TalkingPermissionTool.isLiveOrPartyActive() == false else { return }
        guard GravityReactiveCompatible.supposedBubble() == false else { return }
        //: guard TalkingSocketManager.shared.isFisher == false else {
        guard ManagerCommentThen.shared.isFisher == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.bringHome(showMsg: app_nextKey)
            //: return
            return
        }

        //: var seconds = 15
        var seconds = 15
        //: let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        let timer: DispatchSourceTimer = DispatchSource.makeTimerSource(flags: [], queue: DispatchQueue.main)
        //: timer.schedule(deadline: .now(), repeating: 1.0)
        timer.schedule(deadline: .now(), repeating: 1.0)

        //: let str = "You're going to delete your profile,messages, photos and matched friends PERMANENTLY and your account will be UNRECOVERABLE.Continue to delete your account? ".localized
        let str = String(bytes: mainUniformTermMsg.map{bearPressure(universal: $0)}, encoding: .utf8)!.localized

        //: let range =  str.exMatchStrRange("UNRECOVERABLE")
        let range = str.containRange((app_constraintUrl.replacingOccurrences(of: "blade", with: "E") + String(appTextName)))

        //: let string = NSMutableAttributedString.init(string: str )
        let string = NSMutableAttributedString(string: str)
        //: if range.count > 0 {
        if range.count > 0 {
            //: string.addAttribute(.foregroundColor, value: UIColor.msgTipsColor(), range: range.first!)
            string.addAttribute(.foregroundColor, value: UIColor.foursquare(), range: range.first!)
        }
        //: TalkingAlertShow.attribAlert(title: "Delete Account".localized, attributedMessage: string, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized) {
        StuffReactiveCompatible.grimoire(title: (String(k_makeupName)).localized, attributedMessage: string, leftBtnTitle: (String(mainTurnRawUnderName.suffix(6))).localized, rightBtnTitle: (String(mainCrushTitle) + kStripName.replacingOccurrences(of: "communication", with: "e")).localized) {
            //: TalkingAlertShow.hideAlert()
            StuffReactiveCompatible.outFeedback()
            //: timer.cancel()
            timer.cancel()
            //: } rightBlock: {
        } rightBlock: {
            //: TalkingAlertShow.hideAlert()
            StuffReactiveCompatible.outFeedback()

            //: self.LockAccount()
            self.columnToAccount()
        }

        //: timer.setEventHandler {
        timer.setEventHandler {
            //: seconds -= 1
            seconds -= 1
            //: if seconds <= 0 {
            if seconds <= 0 {
                //: timer.cancel()
                timer.cancel()
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized, isTouch: true)
                StuffReactiveCompatible.stance(title: (String(mainCrushTitle) + kStripName.replacingOccurrences(of: "communication", with: "e")).localized, isTouch: true)
                //: } else {
            } else {
                //: TalkingAlertShow.changeRightBtn(title: "Delete".localized + " (\(seconds)s)", isTouch: false)
                StuffReactiveCompatible.stance(title: (String(mainCrushTitle) + kStripName.replacingOccurrences(of: "communication", with: "e")).localized + " (\(seconds)s)", isTouch: false)
            }
        }
        //: timer.resume()
        timer.resume()
    }

    //: private func LockAccount() {
    private func columnToAccount() {
        //: ProgressHUD.show()
        LawReactiveCompatible.frontEndShow()
        //: TalkingMeRequestTool.req_LockAccount(params: [:]) { succeed, result, errorModel in
        CombinedThen.noteCompletion(params: [:]) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            LawReactiveCompatible.minify()
            //: if succeed {
            if succeed {
                //: self.logoutTool()
                self.ditTool()
                //: self.func__showStatusBarSuccessMsg(showMsg: "Logout succeeded!".localized)
                self.readingDownMsg(showMsg: (String(k_donData)).localized)
            }
        }
    }

    //: private func logoutTool() {
    private func ditTool() {
        //: guard TalkingSocketManager.shared.isFisher == false else {
        guard ManagerCommentThen.shared.isFisher == false else { // 音视频通话中
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.bringHome(showMsg: app_nextKey)
            //: return
            return
        }

        //: ProgressHUD.show()
        LawReactiveCompatible.frontEndShow()
        //: TalkingLoginRequestTool.req_loginOut { t in
        MiddleReactiveCompatible.soundCompletion { t in
            //: ProgressHUD.dismiss()
            LawReactiveCompatible.minify()
            //: if t {
            if t {
                //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: user_beginMsg, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - UI

//: extension TalkingSettingsUniversalVC {
extension ThighRecognizerDelegate {
    //: private func designView() {
    private func weltanschauung() {
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingSettingCell.self, forCellReuseIdentifier: TalkingSettingCell.className())
        MainTable.register(PossibilitySettingCell.self, forCellReuseIdentifier: PossibilitySettingCell.className())
        //: MainTable.register(TalkingSettingNotifCell.self, forCellReuseIdentifier: TalkingSettingNotifCell.className())
        MainTable.register(DesignReactiveCompatible.self, forCellReuseIdentifier: DesignReactiveCompatible.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.top.equalTo(20)
            make.top.equalTo(20)
            //: make.leading.trailing.bottom.equalTo(self.view)
            make.leading.trailing.bottom.equalTo(self.view)
        }
        //: MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
        MainTable.contentInset = UIEdgeInsets(top: 0, left: 0, bottom: 36, right: 0)
    }
}
